import { B as BitcoinNetwork, P as PageInfo, Q as SspClient, R as SparkUserRequestType, X as SparkUserRequestStatus, J as SparkWalletUserToUserRequestsConnection } from '../../client-Dzwi2Hqk.cjs';
export { Z as ClaimStaticDeposit, C as ClaimStaticDepositOutput, _ as ClaimStaticDepositStatus, $ as CompleteCoopExitInput, a0 as CompleteLeavesSwapInput, a1 as CoopExitFeeEstimate, a2 as CoopExitFeeEstimatesInput, a3 as CoopExitFeeEstimatesOutput, F as CoopExitFeeQuote, a5 as CoopExitFeeQuoteInput, E as CoopExitRequest, a7 as CurrencyAmount, a8 as CurrencyUnit, N as ExitSpeed, a9 as GetChallengeOutput, aa as Invoice, ab as Leaf, u as LeavesSwapFeeEstimateOutput, ad as LeavesSwapRequest, L as LightningReceiveRequest, af as LightningReceiveRequestStatus, z as LightningSendFeeEstimateInput, ag as LightningSendFeeEstimateOutput, y as LightningSendRequest, ai as LightningSendRequestStatus, aj as RequestCoopExitInput, ak as RequestLeavesSwapInput, al as RequestLightningReceiveInput, am as RequestLightningSendInput, an as SparkCoopExitRequestStatus, ao as SparkLeavesSwapRequestStatus, ap as SparkTransferToLeavesConnection, aq as StaticDepositQuoteInput, w as StaticDepositQuoteOutput, ar as SwapLeaf, as as Transfer, at as UserLeafInput, av as UserRequest, aw as VerifyChallengeOutput, Y as getClaimStaticDepositQuery, a4 as getCoopExitFeeQuoteQuery, a6 as getCoopExitRequestQuery, ac as getLeavesSwapRequestQuery, ae as getLightningReceiveRequestQuery, ah as getLightningSendRequestQuery, au as getUserRequestQuery } from '../../client-Dzwi2Hqk.cjs';
import { Query } from '@lightsparkdev/core';
import '../../spark-t7DfDw0r.cjs';
import '@scure/btc-signer';
import '@scure/bip32';
import '../../types-B3hMoTYO.cjs';
import '@bufbuild/protobuf/wire';
import 'nice-grpc-common';

declare enum ClaimStaticDepositRequestType {
    /**
     * This is an enum value that represents values that could be added in the future.
     * Clients should support unknown values as more of them could be added without notice.
     */
    FUTURE_VALUE = "FUTURE_VALUE",
    FIXED_AMOUNT = "FIXED_AMOUNT",
    MAX_FEE = "MAX_FEE"
}

interface ClaimStaticDepositInput {
    /** The transaction id of the deposit. **/
    transactionId: string;
    /** The output index of the deposit. **/
    outputIndex: number;
    /** The bitcoin network of the deposit. **/
    network: BitcoinNetwork;
    /** The type of the claim request. **/
    requestType: ClaimStaticDepositRequestType;
    /** The deposit key of the user. **/
    depositSecretKey: string;
    /** The signature of the claim provided by the user. **/
    signature: string;
    /** The signature of the quote provided by the SSP. **/
    quoteSignature: string;
    /** The amount of sats to claim for FIXED_AMOUNT quote. **/
    creditAmountSats?: number | undefined;
    /** The amount of sats to deduct from the UTXO value for MAX_FEE quote. **/
    maxFeeSats?: number | undefined;
}

interface CompleteCoopExitOutput {
    requestId: string;
}

interface CompleteLeavesSwapOutput {
    requestId: string;
}

interface CompleteSeedReleaseInput {
    phoneNumber: string;
    code: string;
}

interface CompleteSeedReleaseOutput {
    seed: string;
}

interface Connection {
    /**
 * The total count of objects in this connection, using the current filters. It is different from the
 * number of objects returned in the current page (in the `entities` field).
**/
    count: number;
    /** An object that holds pagination information about the objects in this connection. **/
    pageInfo: PageInfo;
    /** The typename of the object **/
    typename: string;
}

interface CoopExitFeeQuoteOutput {
    quoteId: string;
}

/** This interface is used by all the entities in the Lightspark system. It defines a few core fields that are available everywhere. Any object that implements this interface can be queried using the `entity` query and its ID. **/
interface Entity {
    /**
 * The unique identifier of this entity across all Lightspark systems. Should be treated as an opaque
 * string.
**/
    id: string;
    /** The date and time when the entity was first created. **/
    createdAt: string;
    /** The date and time when the entity was last updated. **/
    updatedAt: string;
    /** The typename of the object **/
    typename: string;
}

interface GetChallengeInput {
    publicKey: string;
}

interface LeavesSwapFeeEstimateInput {
    totalAmountSats: number;
}

interface NotifyReceiverTransferInput {
    phoneNumber: string;
    amountSats: number;
}

interface Provider {
    accountId: string;
    jwt: string;
}

interface RequestCoopExitOutput {
    requestId: string;
}

interface RequestLeavesSwapOutput {
    requestId: string;
}

interface RequestLightningReceiveOutput {
    requestId: string;
}

interface RequestLightningSendOutput {
    requestId: string;
}

declare class SparkWalletUser implements Entity {
    /**
     * The unique identifier of this entity across all Lightspark systems. Should be treated as an opaque
     * string.
     **/
    readonly id: string;
    /** The date and time when the entity was first created. **/
    readonly createdAt: string;
    /** The date and time when the entity was last updated. **/
    readonly updatedAt: string;
    /** The identity public key of the user. **/
    readonly identityPublicKey: string;
    /** The typename of the object **/
    readonly typename: string;
    constructor(
    /**
     * The unique identifier of this entity across all Lightspark systems. Should be treated as an opaque
     * string.
     **/
    id: string, 
    /** The date and time when the entity was first created. **/
    createdAt: string, 
    /** The date and time when the entity was last updated. **/
    updatedAt: string, 
    /** The identity public key of the user. **/
    identityPublicKey: string, 
    /** The typename of the object **/
    typename: string);
    getUserRequests(client: SspClient, first?: number | undefined, after?: string | undefined, types?: SparkUserRequestType[] | undefined, statuses?: SparkUserRequestStatus[] | undefined, networks?: BitcoinNetwork[] | undefined): Promise<SparkWalletUserToUserRequestsConnection>;
    static getSparkWalletUserQuery(id: string): Query<SparkWalletUser>;
    toJson(): {
        __typename: string;
        spark_wallet_user_id: string;
        spark_wallet_user_created_at: string;
        spark_wallet_user_updated_at: string;
        spark_wallet_user_identity_public_key: string;
    };
}

interface StartSeedReleaseInput {
    phoneNumber: string;
}

interface VerifyChallengeInput {
    protectedChallenge: string;
    signature: string;
    identityPublicKey: string;
    provider?: Provider | undefined;
}

interface WalletUserIdentityPublicKeyInput {
    phoneNumber: string;
}

interface WalletUserIdentityPublicKeyOutput {
    identityPublicKey: string;
}

export { BitcoinNetwork, type ClaimStaticDepositInput, ClaimStaticDepositRequestType, type CompleteCoopExitOutput, type CompleteLeavesSwapOutput, type CompleteSeedReleaseInput, type CompleteSeedReleaseOutput, type Connection, type CoopExitFeeQuoteOutput, type Entity, type GetChallengeInput, type LeavesSwapFeeEstimateInput, type NotifyReceiverTransferInput, PageInfo, type Provider, type RequestCoopExitOutput, type RequestLeavesSwapOutput, type RequestLightningReceiveOutput, type RequestLightningSendOutput, SparkUserRequestStatus, SparkUserRequestType, SparkWalletUser, SparkWalletUserToUserRequestsConnection, type StartSeedReleaseInput, type VerifyChallengeInput, type WalletUserIdentityPublicKeyInput, type WalletUserIdentityPublicKeyOutput };
