"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/lrc/utils/index.ts
var utils_exports = {};
__export(utils_exports, {
  BLINDING_FACTOR_SIZE: () => BLINDING_FACTOR_SIZE,
  DUST_AMOUNT: () => DUST_AMOUNT,
  ELECTRS_URL: () => ELECTRS_URL,
  EMPTY_TOKEN_PUBKEY: () => EMPTY_TOKEN_PUBKEY,
  Enumerable: () => Enumerable,
  EnumerableMethod: () => EnumerableMethod,
  G: () => G,
  JSONParse: () => JSONParse,
  JSONStringify: () => JSONStringify,
  JSONStringifyBodyDown: () => JSONStringifyBodyDown,
  LRC_NODE_URL: () => LRC_NODE_URL,
  MIN_DUST_AMOUNT: () => MIN_DUST_AMOUNT,
  PARITY: () => PARITY,
  TOKEN_AMOUNT_SIZE: () => TOKEN_AMOUNT_SIZE,
  TokenLeaf: () => TokenLeaf,
  TokenTransaction: () => TokenTransaction,
  createMethodDecorator: () => createMethodDecorator,
  filterUniqueUtxo: () => filterUniqueUtxo,
  findNotFirstUsingFind: () => findNotFirstUsingFind,
  network: () => network,
  reverseBuffer: () => reverseBuffer,
  toEvenParity: () => toEvenParity,
  toXOnly: () => toXOnly,
  tokenLeafHash: () => tokenLeafHash,
  tokenTransactionHash: () => tokenTransactionHash
});
module.exports = __toCommonJS(utils_exports);

// buffer.js
var import_buffer = require("buffer");
if (typeof globalThis.Buffer === "undefined") {
  globalThis.Buffer = import_buffer.Buffer;
}
if (typeof window !== "undefined") {
  if (typeof window.global === "undefined") {
    window.global = window;
  }
  if (typeof window.globalThis === "undefined") {
    window.globalThis = window;
  }
}

// src/lrc/utils/array.ts
function findNotFirstUsingFind(arr) {
  return arr.find((item, index) => index !== 0 && import_buffer.Buffer.compare(item, arr[0]));
}

// src/lrc/utils/bitcoin.ts
function filterUniqueUtxo(oldUtxos, newUtxos) {
  return newUtxos.filter((newUtxo) => {
    return oldUtxos.findIndex((oldUtxo) => oldUtxo.txid == newUtxo.txid && oldUtxo.vout == newUtxo.vout) == -1;
  });
}

// src/lrc/utils/constants.ts
var import_bitcoinjs_lib = require("bitcoinjs-lib");
var import_buffer6 = require("buffer");

// src/network/index.ts
var bitcoin = __toESM(require("bitcoinjs-lib"), 1);

// src/lrc/utils/constants.ts
var G = import_buffer6.Buffer.from("0279be667ef9dcbbac55a06295ce870b07029bfcdb2dce28d959f2815b16f81798", "hex");
var network = import_bitcoinjs_lib.networks.regtest;
var TOKEN_AMOUNT_SIZE = 32;
var BLINDING_FACTOR_SIZE = 16;
var MIN_DUST_AMOUNT = 1e3;
var DUST_AMOUNT = 354;
var PARITY = import_buffer6.Buffer.from([2]);
var EMPTY_TOKEN_PUBKEY = import_buffer6.Buffer.from(Array(33).fill(2));
var ELECTRS_URL = Object.freeze({
  [0 /* MAINNET */]: "https://mempool.space/api",
  [3 /* REGTEST */]: "https://regtest-mempool.us-west-2.sparkinfra.net/api",
  [1 /* TESTNET */]: "https://electrs.mutiny.18.215.149.26.sslip.io",
  [2 /* DEVNET */]: "https://electrs.stage.18.215.149.26.sslip.io",
  [4 /* LOCAL */]: "http://127.0.0.1:30000",
  default: "http://127.0.0.1:30000"
});
var LRC_NODE_URL = Object.freeze({
  [0 /* MAINNET */]: "https://mainnet.lrc20.us-west-2.sparkinfra.net",
  [3 /* REGTEST */]: "https://regtest.lrc20.us-west-2.sparkinfra.net",
  [1 /* TESTNET */]: "https://rpc.lrc20d.mutiny.18.215.149.26.sslip.io",
  [2 /* DEVNET */]: "https://rpc.lrc20.stage.18.215.149.26.sslip.io",
  [4 /* LOCAL */]: "http://127.0.0.1:18332",
  default: "http://127.0.0.1:18332"
});

// src/lrc/utils/buffer.ts
function reverseBuffer(buffer) {
  if (buffer.length < 1) return buffer;
  let j = buffer.length - 1;
  let tmp = 0;
  let reversed = import_buffer.Buffer.from(buffer);
  for (let i = 0; i < reversed.length / 2; i++) {
    tmp = reversed[i];
    reversed[i] = reversed[j];
    reversed[j] = tmp;
    j--;
  }
  return reversed;
}
var toXOnly = (pubKey) => pubKey.length === 32 ? pubKey : pubKey.slice(1, 33);
var toEvenParity = (pubKey) => {
  return import_buffer.Buffer.concat([PARITY, toXOnly(pubKey)]);
};

// src/lrc/utils/decorators.ts
function createMethodDecorator(fn) {
  return fn;
}
function Enumerable(value) {
  return (target, key) => {
    Reflect.defineProperty(target, key, {
      enumerable: value,
      set(val) {
        Reflect.defineProperty(this, key, {
          configurable: true,
          enumerable: value,
          value: val,
          writable: true
        });
      }
    });
  };
}
function EnumerableMethod(value) {
  return createMethodDecorator((_target, _propertyKey, descriptor) => {
    descriptor.enumerable = value;
  });
}

// src/lrc/utils/json.ts
var JSONStringify = (data) => {
  const bigInts = /([\[:])?"(-?\d+)n"([,\}\]])/g;
  const preliminaryJSON = JSON.stringify(
    data,
    (_, value) => typeof value === "bigint" ? value.toString() + "n" : value
  );
  const finalJSON = preliminaryJSON.replace(bigInts, "$1$2$3");
  return finalJSON;
};
function JSONStringifyBodyDown(data) {
  return JSON.stringify(data, (_, v) => typeof v === "bigint" ? Number(v) : v, 2);
}
var JSONParse = (json) => {
  const numbersBiggerThanMaxInt = /(?<=[^\\]":[\[]?|[^\\]":\[.*[^\.\d*])(-?\d{17,}|-?(?:[9](?:[1-9]07199254740991|0[1-9]7199254740991|00[8-9]199254740991|007[2-9]99254740991|007199[3-9]54740991|0071992[6-9]4740991|00719925[5-9]740991|007199254[8-9]40991|0071992547[5-9]0991|00719925474[1-9]991|00719925474099[2-9])))(?=,|\}[^"]|\][^"])/g;
  const serializedData = json.replace(numbersBiggerThanMaxInt, `"$1n"`);
  return JSON.parse(serializedData, (_, value) => {
    const isCustomFormatBigInt = typeof value === "string" && Boolean(value.match(/^-?\d+n$/));
    if (isCustomFormatBigInt) return BigInt(value.substring(0, value.length - 1));
    return value;
  });
};

// src/lrc/utils/spark.ts
var bitcoin2 = __toESM(require("bitcoinjs-lib"), 1);
var TokenLeaf = class {
  id;
  verifyingPublicKey;
  ownerIdentityPublicKey;
  revocationPublicKey;
  withdrawalFeeRateVb;
  withdrawalBondSats;
  withdrawalLocktime;
  tokenId;
  tokenAmount;
};
var TokenTransaction = class {
  leavesToSpend;
  leavesToCreate;
};
function tokenLeafHash(tokenLeaf) {
  const withdrawalFeeRateVbBuffer = import_buffer.Buffer.alloc(8);
  const withdrawalBondSatsBuffer = import_buffer.Buffer.alloc(8);
  const withdrawalLocktimeBuffer = import_buffer.Buffer.alloc(8);
  const tokenAmountBuffer = import_buffer.Buffer.alloc(8);
  withdrawalFeeRateVbBuffer.writeBigInt64BE(BigInt(tokenLeaf.withdrawalFeeRateVb));
  withdrawalBondSatsBuffer.writeBigInt64BE(BigInt(tokenLeaf.withdrawalBondSats));
  withdrawalLocktimeBuffer.writeBigInt64BE(BigInt(tokenLeaf.withdrawalLocktime));
  tokenAmountBuffer.writeBigInt64BE(BigInt(tokenLeaf.tokenAmount));
  const hashData = import_buffer.Buffer.concat([
    bitcoin2.crypto.sha256(tokenLeaf.verifyingPublicKey),
    bitcoin2.crypto.sha256(tokenLeaf.ownerIdentityPublicKey),
    bitcoin2.crypto.sha256(tokenLeaf.revocationPublicKey),
    bitcoin2.crypto.sha256(tokenLeaf.verifyingPublicKey),
    bitcoin2.crypto.sha256(withdrawalFeeRateVbBuffer),
    bitcoin2.crypto.sha256(withdrawalBondSatsBuffer),
    bitcoin2.crypto.sha256(withdrawalLocktimeBuffer),
    bitcoin2.crypto.sha256(tokenLeaf.tokenId),
    bitcoin2.crypto.sha256(tokenAmountBuffer)
  ]);
  return bitcoin2.crypto.sha256(hashData);
}
function tokenTransactionHash(tokenTransaction) {
  let hashes = [];
  const leavesToSpend = tokenTransaction.leavesToSpend;
  const leavesToCreate = tokenTransaction.leavesToCreate;
  for (let i = 0; i < leavesToSpend.length; i++) {
    hashes.push(tokenLeafHash(leavesToSpend[i]));
  }
  for (let i = 0; i < leavesToCreate.length; i++) {
    hashes.push(tokenLeafHash(leavesToCreate[i]));
  }
  let hashData = import_buffer.Buffer.concat(hashes);
  return bitcoin2.crypto.sha256(hashData);
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  BLINDING_FACTOR_SIZE,
  DUST_AMOUNT,
  ELECTRS_URL,
  EMPTY_TOKEN_PUBKEY,
  Enumerable,
  EnumerableMethod,
  G,
  JSONParse,
  JSONStringify,
  JSONStringifyBodyDown,
  LRC_NODE_URL,
  MIN_DUST_AMOUNT,
  PARITY,
  TOKEN_AMOUNT_SIZE,
  TokenLeaf,
  TokenTransaction,
  createMethodDecorator,
  filterUniqueUtxo,
  findNotFirstUsingFind,
  network,
  reverseBuffer,
  toEvenParity,
  toXOnly,
  tokenLeafHash,
  tokenTransactionHash
});
