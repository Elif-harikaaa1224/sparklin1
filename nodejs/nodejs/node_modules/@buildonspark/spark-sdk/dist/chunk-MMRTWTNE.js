import {
  __export
} from "./chunk-MVRQ5US7.js";

// src/proto/spark.ts
var spark_exports = {};
__export(spark_exports, {
  AdaptorPublicKeyPackage: () => AdaptorPublicKeyPackage,
  Address: () => Address,
  AddressNode: () => AddressNode,
  AddressRequestNode: () => AddressRequestNode,
  BitcoinTransactionOutput: () => BitcoinTransactionOutput,
  CancelTransferRequest: () => CancelTransferRequest,
  CancelTransferResponse: () => CancelTransferResponse,
  ClaimLeafKeyTweak: () => ClaimLeafKeyTweak,
  ClaimLeafKeyTweak_PubkeySharesTweakEntry: () => ClaimLeafKeyTweak_PubkeySharesTweakEntry,
  ClaimTransferSignRefundsRequest: () => ClaimTransferSignRefundsRequest,
  ClaimTransferSignRefundsResponse: () => ClaimTransferSignRefundsResponse,
  ClaimTransferTweakKeysRequest: () => ClaimTransferTweakKeysRequest,
  ConnectedEvent: () => ConnectedEvent,
  CooperativeExitRequest: () => CooperativeExitRequest,
  CooperativeExitResponse: () => CooperativeExitResponse,
  CounterLeafSwapRequest: () => CounterLeafSwapRequest,
  CounterLeafSwapResponse: () => CounterLeafSwapResponse,
  CreateTreeRequest: () => CreateTreeRequest,
  CreateTreeResponse: () => CreateTreeResponse,
  CreationNode: () => CreationNode,
  CreationResponseNode: () => CreationResponseNode,
  DepositAddressProof: () => DepositAddressProof,
  DepositAddressProof_AddressSignaturesEntry: () => DepositAddressProof_AddressSignaturesEntry,
  DepositAddressQueryResult: () => DepositAddressQueryResult,
  DepositEvent: () => DepositEvent,
  Direction: () => Direction,
  ExitSingleNodeTreeSigningResult: () => ExitSingleNodeTreeSigningResult,
  ExitSingleNodeTreesRequest: () => ExitSingleNodeTreesRequest,
  ExitSingleNodeTreesResponse: () => ExitSingleNodeTreesResponse,
  ExitingTree: () => ExitingTree,
  ExtendLeafRequest: () => ExtendLeafRequest,
  ExtendLeafResponse: () => ExtendLeafResponse,
  ExtendLeafSigningResult: () => ExtendLeafSigningResult,
  FinalizeNodeSignaturesRequest: () => FinalizeNodeSignaturesRequest,
  FinalizeNodeSignaturesResponse: () => FinalizeNodeSignaturesResponse,
  FinalizeTokenTransactionRequest: () => FinalizeTokenTransactionRequest,
  FinalizeTransferRequest: () => FinalizeTransferRequest,
  FinalizeTransferResponse: () => FinalizeTransferResponse,
  FinalizeTransferWithTransferPackageRequest: () => FinalizeTransferWithTransferPackageRequest,
  FreezeTokensPayload: () => FreezeTokensPayload,
  FreezeTokensRequest: () => FreezeTokensRequest,
  FreezeTokensResponse: () => FreezeTokensResponse,
  GenerateDepositAddressRequest: () => GenerateDepositAddressRequest,
  GenerateDepositAddressResponse: () => GenerateDepositAddressResponse,
  GenerateStaticDepositAddressRequest: () => GenerateStaticDepositAddressRequest,
  GenerateStaticDepositAddressResponse: () => GenerateStaticDepositAddressResponse,
  GetSigningCommitmentsRequest: () => GetSigningCommitmentsRequest,
  GetSigningCommitmentsResponse: () => GetSigningCommitmentsResponse,
  GetSigningOperatorListResponse: () => GetSigningOperatorListResponse,
  GetSigningOperatorListResponse_SigningOperatorsEntry: () => GetSigningOperatorListResponse_SigningOperatorsEntry,
  GetUtxosForAddressRequest: () => GetUtxosForAddressRequest,
  GetUtxosForAddressResponse: () => GetUtxosForAddressResponse,
  InitiatePreimageSwapRequest: () => InitiatePreimageSwapRequest,
  InitiatePreimageSwapRequest_Reason: () => InitiatePreimageSwapRequest_Reason,
  InitiatePreimageSwapResponse: () => InitiatePreimageSwapResponse,
  InitiateStaticDepositUtxoRefundRequest: () => InitiateStaticDepositUtxoRefundRequest,
  InitiateStaticDepositUtxoRefundResponse: () => InitiateStaticDepositUtxoRefundResponse,
  InitiateSwapPrimaryTransferRequest: () => InitiateSwapPrimaryTransferRequest,
  InitiateSwapPrimaryTransferResponse: () => InitiateSwapPrimaryTransferResponse,
  InitiateUtxoSwapRequest: () => InitiateUtxoSwapRequest,
  InitiateUtxoSwapResponse: () => InitiateUtxoSwapResponse,
  InvoiceAmount: () => InvoiceAmount,
  InvoiceAmountProof: () => InvoiceAmountProof,
  InvoiceResponse: () => InvoiceResponse,
  InvoiceStatus: () => InvoiceStatus,
  KeyshareWithIndex: () => KeyshareWithIndex,
  LeafRefundTxSigningJob: () => LeafRefundTxSigningJob,
  LeafRefundTxSigningResult: () => LeafRefundTxSigningResult,
  Network: () => Network,
  NodeOutput: () => NodeOutput,
  NodeSignatureShares: () => NodeSignatureShares,
  NodeSignatures: () => NodeSignatures,
  OperatorSpecificOwnerSignature: () => OperatorSpecificOwnerSignature,
  OperatorSpecificTokenTransactionSignablePayload: () => OperatorSpecificTokenTransactionSignablePayload,
  Order: () => Order,
  OutPoint: () => OutPoint,
  OutputWithPreviousTransactionData: () => OutputWithPreviousTransactionData,
  PageRequest: () => PageRequest,
  PageResponse: () => PageResponse,
  PreimageRequestStatus: () => PreimageRequestStatus,
  PreimageRequestWithTransfer: () => PreimageRequestWithTransfer,
  PrepareTreeAddressRequest: () => PrepareTreeAddressRequest,
  PrepareTreeAddressResponse: () => PrepareTreeAddressResponse,
  ProvidePreimageRequest: () => ProvidePreimageRequest,
  ProvidePreimageResponse: () => ProvidePreimageResponse,
  QueryBalanceRequest: () => QueryBalanceRequest,
  QueryBalanceResponse: () => QueryBalanceResponse,
  QueryBalanceResponse_NodeBalancesEntry: () => QueryBalanceResponse_NodeBalancesEntry,
  QueryHtlcRequest: () => QueryHtlcRequest,
  QueryHtlcResponse: () => QueryHtlcResponse,
  QueryNodesByValueRequest: () => QueryNodesByValueRequest,
  QueryNodesByValueResponse: () => QueryNodesByValueResponse,
  QueryNodesByValueResponse_NodesEntry: () => QueryNodesByValueResponse_NodesEntry,
  QueryNodesDistributionRequest: () => QueryNodesDistributionRequest,
  QueryNodesDistributionResponse: () => QueryNodesDistributionResponse,
  QueryNodesDistributionResponse_NodeDistributionEntry: () => QueryNodesDistributionResponse_NodeDistributionEntry,
  QueryNodesRequest: () => QueryNodesRequest,
  QueryNodesResponse: () => QueryNodesResponse,
  QueryNodesResponse_NodesEntry: () => QueryNodesResponse_NodesEntry,
  QuerySparkInvoicesRequest: () => QuerySparkInvoicesRequest,
  QuerySparkInvoicesResponse: () => QuerySparkInvoicesResponse,
  QueryStaticDepositAddressesRequest: () => QueryStaticDepositAddressesRequest,
  QueryStaticDepositAddressesResponse: () => QueryStaticDepositAddressesResponse,
  QueryTokenOutputsRequest: () => QueryTokenOutputsRequest,
  QueryTokenOutputsResponse: () => QueryTokenOutputsResponse,
  QueryTokenTransactionsRequest: () => QueryTokenTransactionsRequest,
  QueryTokenTransactionsResponse: () => QueryTokenTransactionsResponse,
  QueryTransfersResponse: () => QueryTransfersResponse,
  QueryUnusedDepositAddressesRequest: () => QueryUnusedDepositAddressesRequest,
  QueryUnusedDepositAddressesResponse: () => QueryUnusedDepositAddressesResponse,
  QueryUserSignedRefundsRequest: () => QueryUserSignedRefundsRequest,
  QueryUserSignedRefundsResponse: () => QueryUserSignedRefundsResponse,
  QueryWalletSettingRequest: () => QueryWalletSettingRequest,
  QueryWalletSettingResponse: () => QueryWalletSettingResponse,
  RefreshTimelockRequest: () => RefreshTimelockRequest,
  RefreshTimelockResponse: () => RefreshTimelockResponse,
  RefreshTimelockSigningResult: () => RefreshTimelockSigningResult,
  RenewLeafRequest: () => RenewLeafRequest,
  RenewLeafResponse: () => RenewLeafResponse,
  RenewNodeTimelockResult: () => RenewNodeTimelockResult,
  RenewNodeTimelockSigningJob: () => RenewNodeTimelockSigningJob,
  RenewNodeZeroTimelockResult: () => RenewNodeZeroTimelockResult,
  RenewNodeZeroTimelockSigningJob: () => RenewNodeZeroTimelockSigningJob,
  RenewRefundTimelockResult: () => RenewRefundTimelockResult,
  RenewRefundTimelockSigningJob: () => RenewRefundTimelockSigningJob,
  RequestedSigningCommitments: () => RequestedSigningCommitments,
  RequestedSigningCommitments_SigningNonceCommitmentsEntry: () => RequestedSigningCommitments_SigningNonceCommitmentsEntry,
  ReturnLightningPaymentRequest: () => ReturnLightningPaymentRequest,
  RevocationSecretWithIndex: () => RevocationSecretWithIndex,
  SatsPayment: () => SatsPayment,
  SatsTransfer: () => SatsTransfer,
  SecretProof: () => SecretProof,
  SecretShare: () => SecretShare,
  SendLeafKeyTweak: () => SendLeafKeyTweak,
  SendLeafKeyTweak_PubkeySharesTweakEntry: () => SendLeafKeyTweak_PubkeySharesTweakEntry,
  SendLeafKeyTweaks: () => SendLeafKeyTweaks,
  SignTokenTransactionRequest: () => SignTokenTransactionRequest,
  SignTokenTransactionResponse: () => SignTokenTransactionResponse,
  SignatureWithIndex: () => SignatureWithIndex,
  SigningCommitments: () => SigningCommitments,
  SigningCommitments_SigningCommitmentsEntry: () => SigningCommitments_SigningCommitmentsEntry,
  SigningJob: () => SigningJob,
  SigningKeyshare: () => SigningKeyshare,
  SigningKeyshare_PublicSharesEntry: () => SigningKeyshare_PublicSharesEntry,
  SigningOperatorInfo: () => SigningOperatorInfo,
  SigningResult: () => SigningResult2,
  SigningResult_PublicKeysEntry: () => SigningResult_PublicKeysEntry,
  SigningResult_SignatureSharesEntry: () => SigningResult_SignatureSharesEntry,
  SigningResult_SigningNonceCommitmentsEntry: () => SigningResult_SigningNonceCommitmentsEntry,
  SparkAddress: () => SparkAddress,
  SparkInvoiceFields: () => SparkInvoiceFields,
  SparkServiceDefinition: () => SparkServiceDefinition,
  SpentTokenOutputMetadata: () => SpentTokenOutputMetadata,
  StartDepositTreeCreationRequest: () => StartDepositTreeCreationRequest,
  StartDepositTreeCreationResponse: () => StartDepositTreeCreationResponse,
  StartTokenTransactionRequest: () => StartTokenTransactionRequest,
  StartTokenTransactionResponse: () => StartTokenTransactionResponse,
  StartTransferRequest: () => StartTransferRequest,
  StartTransferResponse: () => StartTransferResponse,
  StartTreeCreationRequest: () => StartTreeCreationRequest,
  StartTreeCreationResponse: () => StartTreeCreationResponse,
  StartUserSignedTransferRequest: () => StartUserSignedTransferRequest,
  StorePreimageShareRequest: () => StorePreimageShareRequest,
  SubscribeToEventsRequest: () => SubscribeToEventsRequest,
  SubscribeToEventsResponse: () => SubscribeToEventsResponse,
  TokenCreateInput: () => TokenCreateInput,
  TokenMintInput: () => TokenMintInput,
  TokenOutput: () => TokenOutput,
  TokenOutputToSpend: () => TokenOutputToSpend,
  TokenTransaction: () => TokenTransaction,
  TokenTransactionConfirmationMetadata: () => TokenTransactionConfirmationMetadata,
  TokenTransactionSignatures: () => TokenTransactionSignatures,
  TokenTransactionStatus: () => TokenTransactionStatus,
  TokenTransactionWithStatus: () => TokenTransactionWithStatus,
  TokenTransfer: () => TokenTransfer,
  TokenTransferInput: () => TokenTransferInput,
  TokensPayment: () => TokensPayment,
  Transfer: () => Transfer,
  TransferEvent: () => TransferEvent,
  TransferFilter: () => TransferFilter,
  TransferLeaf: () => TransferLeaf,
  TransferPackage: () => TransferPackage,
  TransferPackage_KeyTweakPackageEntry: () => TransferPackage_KeyTweakPackageEntry,
  TransferStatus: () => TransferStatus,
  TransferType: () => TransferType,
  TreeNode: () => TreeNode,
  TreeNodeIds: () => TreeNodeIds,
  TreeNodeStatus: () => TreeNodeStatus,
  UTXO: () => UTXO,
  UpdateWalletSettingRequest: () => UpdateWalletSettingRequest,
  UpdateWalletSettingResponse: () => UpdateWalletSettingResponse,
  UserSignedRefund: () => UserSignedRefund,
  UserSignedTxSigningJob: () => UserSignedTxSigningJob,
  UtxoSwapRequestType: () => UtxoSwapRequestType,
  WalletSetting: () => WalletSetting,
  directionFromJSON: () => directionFromJSON,
  directionToJSON: () => directionToJSON,
  initiatePreimageSwapRequest_ReasonFromJSON: () => initiatePreimageSwapRequest_ReasonFromJSON,
  initiatePreimageSwapRequest_ReasonToJSON: () => initiatePreimageSwapRequest_ReasonToJSON,
  invoiceStatusFromJSON: () => invoiceStatusFromJSON,
  invoiceStatusToJSON: () => invoiceStatusToJSON,
  networkFromJSON: () => networkFromJSON,
  networkToJSON: () => networkToJSON,
  orderFromJSON: () => orderFromJSON,
  orderToJSON: () => orderToJSON,
  preimageRequestStatusFromJSON: () => preimageRequestStatusFromJSON,
  preimageRequestStatusToJSON: () => preimageRequestStatusToJSON,
  protobufPackage: () => protobufPackage2,
  tokenTransactionStatusFromJSON: () => tokenTransactionStatusFromJSON,
  tokenTransactionStatusToJSON: () => tokenTransactionStatusToJSON,
  transferStatusFromJSON: () => transferStatusFromJSON,
  transferStatusToJSON: () => transferStatusToJSON,
  transferTypeFromJSON: () => transferTypeFromJSON,
  transferTypeToJSON: () => transferTypeToJSON,
  treeNodeStatusFromJSON: () => treeNodeStatusFromJSON,
  treeNodeStatusToJSON: () => treeNodeStatusToJSON,
  utxoSwapRequestTypeFromJSON: () => utxoSwapRequestTypeFromJSON,
  utxoSwapRequestTypeToJSON: () => utxoSwapRequestTypeToJSON
});
import { BinaryReader as BinaryReader4, BinaryWriter as BinaryWriter4 } from "@bufbuild/protobuf/wire";

// src/proto/common.ts
var common_exports = {};
__export(common_exports, {
  PackageMap: () => PackageMap,
  PackageMap_PackagesEntry: () => PackageMap_PackagesEntry,
  SignatureIntent: () => SignatureIntent,
  SigningCommitment: () => SigningCommitment,
  SigningResult: () => SigningResult,
  protobufPackage: () => protobufPackage,
  signatureIntentFromJSON: () => signatureIntentFromJSON,
  signatureIntentToJSON: () => signatureIntentToJSON
});
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
var protobufPackage = "common";
var SignatureIntent = /* @__PURE__ */ ((SignatureIntent3) => {
  SignatureIntent3[SignatureIntent3["CREATION"] = 0] = "CREATION";
  SignatureIntent3[SignatureIntent3["TRANSFER"] = 1] = "TRANSFER";
  SignatureIntent3[SignatureIntent3["AGGREGATE"] = 2] = "AGGREGATE";
  SignatureIntent3[SignatureIntent3["REFRESH"] = 3] = "REFRESH";
  SignatureIntent3[SignatureIntent3["EXTEND"] = 4] = "EXTEND";
  SignatureIntent3[SignatureIntent3["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
  return SignatureIntent3;
})(SignatureIntent || {});
function signatureIntentFromJSON(object) {
  switch (object) {
    case 0:
    case "CREATION":
      return 0 /* CREATION */;
    case 1:
    case "TRANSFER":
      return 1 /* TRANSFER */;
    case 2:
    case "AGGREGATE":
      return 2 /* AGGREGATE */;
    case 3:
    case "REFRESH":
      return 3 /* REFRESH */;
    case 4:
    case "EXTEND":
      return 4 /* EXTEND */;
    case -1:
    case "UNRECOGNIZED":
    default:
      return -1 /* UNRECOGNIZED */;
  }
}
function signatureIntentToJSON(object) {
  switch (object) {
    case 0 /* CREATION */:
      return "CREATION";
    case 1 /* TRANSFER */:
      return "TRANSFER";
    case 2 /* AGGREGATE */:
      return "AGGREGATE";
    case 3 /* REFRESH */:
      return "REFRESH";
    case 4 /* EXTEND */:
      return "EXTEND";
    case -1 /* UNRECOGNIZED */:
    default:
      return "UNRECOGNIZED";
  }
}
function createBasePackageMap() {
  return { packages: {} };
}
var PackageMap = {
  encode(message, writer = new BinaryWriter()) {
    Object.entries(message.packages).forEach(([key, value]) => {
      PackageMap_PackagesEntry.encode({ key, value }, writer.uint32(10).fork()).join();
    });
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePackageMap();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          const entry1 = PackageMap_PackagesEntry.decode(reader, reader.uint32());
          if (entry1.value !== void 0) {
            message.packages[entry1.key] = entry1.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      packages: isObject(object.packages) ? Object.entries(object.packages).reduce((acc, [key, value]) => {
        acc[key] = bytesFromBase64(value);
        return acc;
      }, {}) : {}
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.packages) {
      const entries = Object.entries(message.packages);
      if (entries.length > 0) {
        obj.packages = {};
        entries.forEach(([k, v]) => {
          obj.packages[k] = base64FromBytes(v);
        });
      }
    }
    return obj;
  },
  create(base) {
    return PackageMap.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBasePackageMap();
    message.packages = Object.entries(object.packages ?? {}).reduce(
      (acc, [key, value]) => {
        if (value !== void 0) {
          acc[key] = value;
        }
        return acc;
      },
      {}
    );
    return message;
  }
};
function createBasePackageMap_PackagesEntry() {
  return { key: "", value: new Uint8Array(0) };
}
var PackageMap_PackagesEntry = {
  encode(message, writer = new BinaryWriter()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value.length !== 0) {
      writer.uint32(18).bytes(message.value);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePackageMap_PackagesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.value = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? bytesFromBase64(object.value) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value.length !== 0) {
      obj.value = base64FromBytes(message.value);
    }
    return obj;
  },
  create(base) {
    return PackageMap_PackagesEntry.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBasePackageMap_PackagesEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? new Uint8Array(0);
    return message;
  }
};
function createBaseSigningCommitment() {
  return { hiding: new Uint8Array(0), binding: new Uint8Array(0) };
}
var SigningCommitment = {
  encode(message, writer = new BinaryWriter()) {
    if (message.hiding.length !== 0) {
      writer.uint32(10).bytes(message.hiding);
    }
    if (message.binding.length !== 0) {
      writer.uint32(18).bytes(message.binding);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSigningCommitment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.hiding = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.binding = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      hiding: isSet(object.hiding) ? bytesFromBase64(object.hiding) : new Uint8Array(0),
      binding: isSet(object.binding) ? bytesFromBase64(object.binding) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.hiding.length !== 0) {
      obj.hiding = base64FromBytes(message.hiding);
    }
    if (message.binding.length !== 0) {
      obj.binding = base64FromBytes(message.binding);
    }
    return obj;
  },
  create(base) {
    return SigningCommitment.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSigningCommitment();
    message.hiding = object.hiding ?? new Uint8Array(0);
    message.binding = object.binding ?? new Uint8Array(0);
    return message;
  }
};
function createBaseSigningResult() {
  return { signatureShare: new Uint8Array(0) };
}
var SigningResult = {
  encode(message, writer = new BinaryWriter()) {
    if (message.signatureShare.length !== 0) {
      writer.uint32(10).bytes(message.signatureShare);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSigningResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.signatureShare = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      signatureShare: isSet(object.signatureShare) ? bytesFromBase64(object.signatureShare) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.signatureShare.length !== 0) {
      obj.signatureShare = base64FromBytes(message.signatureShare);
    }
    return obj;
  },
  create(base) {
    return SigningResult.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSigningResult();
    message.signatureShare = object.signatureShare ?? new Uint8Array(0);
    return message;
  }
};
function bytesFromBase64(b64) {
  if (globalThis.Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes(arr) {
  if (globalThis.Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}
function isObject(value) {
  return typeof value === "object" && value !== null;
}
function isSet(value) {
  return value !== null && value !== void 0;
}

// src/proto/google/protobuf/empty.ts
import { BinaryReader as BinaryReader2, BinaryWriter as BinaryWriter2 } from "@bufbuild/protobuf/wire";
function createBaseEmpty() {
  return {};
}
var Empty = {
  encode(_, writer = new BinaryWriter2()) {
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader2 ? input : new BinaryReader2(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseEmpty();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(_) {
    return {};
  },
  toJSON(_) {
    const obj = {};
    return obj;
  },
  create(base) {
    return Empty.fromPartial(base ?? {});
  },
  fromPartial(_) {
    const message = createBaseEmpty();
    return message;
  }
};

// src/proto/google/protobuf/timestamp.ts
import { BinaryReader as BinaryReader3, BinaryWriter as BinaryWriter3 } from "@bufbuild/protobuf/wire";
function createBaseTimestamp() {
  return { seconds: 0, nanos: 0 };
}
var Timestamp = {
  encode(message, writer = new BinaryWriter3()) {
    if (message.seconds !== 0) {
      writer.uint32(8).int64(message.seconds);
    }
    if (message.nanos !== 0) {
      writer.uint32(16).int32(message.nanos);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader3 ? input : new BinaryReader3(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTimestamp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.seconds = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.nanos = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      seconds: isSet2(object.seconds) ? globalThis.Number(object.seconds) : 0,
      nanos: isSet2(object.nanos) ? globalThis.Number(object.nanos) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.seconds !== 0) {
      obj.seconds = Math.round(message.seconds);
    }
    if (message.nanos !== 0) {
      obj.nanos = Math.round(message.nanos);
    }
    return obj;
  },
  create(base) {
    return Timestamp.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseTimestamp();
    message.seconds = object.seconds ?? 0;
    message.nanos = object.nanos ?? 0;
    return message;
  }
};
function longToNumber(int64) {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}
function isSet2(value) {
  return value !== null && value !== void 0;
}

// src/proto/spark.ts
var protobufPackage2 = "spark";
var Network = /* @__PURE__ */ ((Network2) => {
  Network2[Network2["UNSPECIFIED"] = 0] = "UNSPECIFIED";
  Network2[Network2["MAINNET"] = 1] = "MAINNET";
  Network2[Network2["REGTEST"] = 2] = "REGTEST";
  Network2[Network2["TESTNET"] = 3] = "TESTNET";
  Network2[Network2["SIGNET"] = 4] = "SIGNET";
  Network2[Network2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
  return Network2;
})(Network || {});
function networkFromJSON(object) {
  switch (object) {
    case 0:
    case "UNSPECIFIED":
      return 0 /* UNSPECIFIED */;
    case 1:
    case "MAINNET":
      return 1 /* MAINNET */;
    case 2:
    case "REGTEST":
      return 2 /* REGTEST */;
    case 3:
    case "TESTNET":
      return 3 /* TESTNET */;
    case 4:
    case "SIGNET":
      return 4 /* SIGNET */;
    case -1:
    case "UNRECOGNIZED":
    default:
      return -1 /* UNRECOGNIZED */;
  }
}
function networkToJSON(object) {
  switch (object) {
    case 0 /* UNSPECIFIED */:
      return "UNSPECIFIED";
    case 1 /* MAINNET */:
      return "MAINNET";
    case 2 /* REGTEST */:
      return "REGTEST";
    case 3 /* TESTNET */:
      return "TESTNET";
    case 4 /* SIGNET */:
      return "SIGNET";
    case -1 /* UNRECOGNIZED */:
    default:
      return "UNRECOGNIZED";
  }
}
var Direction = /* @__PURE__ */ ((Direction2) => {
  Direction2[Direction2["NEXT"] = 0] = "NEXT";
  Direction2[Direction2["PREVIOUS"] = 1] = "PREVIOUS";
  Direction2[Direction2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
  return Direction2;
})(Direction || {});
function directionFromJSON(object) {
  switch (object) {
    case 0:
    case "NEXT":
      return 0 /* NEXT */;
    case 1:
    case "PREVIOUS":
      return 1 /* PREVIOUS */;
    case -1:
    case "UNRECOGNIZED":
    default:
      return -1 /* UNRECOGNIZED */;
  }
}
function directionToJSON(object) {
  switch (object) {
    case 0 /* NEXT */:
      return "NEXT";
    case 1 /* PREVIOUS */:
      return "PREVIOUS";
    case -1 /* UNRECOGNIZED */:
    default:
      return "UNRECOGNIZED";
  }
}
var TokenTransactionStatus = /* @__PURE__ */ ((TokenTransactionStatus2) => {
  TokenTransactionStatus2[TokenTransactionStatus2["TOKEN_TRANSACTION_STARTED"] = 0] = "TOKEN_TRANSACTION_STARTED";
  TokenTransactionStatus2[TokenTransactionStatus2["TOKEN_TRANSACTION_SIGNED"] = 1] = "TOKEN_TRANSACTION_SIGNED";
  TokenTransactionStatus2[TokenTransactionStatus2["TOKEN_TRANSACTION_REVEALED"] = 5] = "TOKEN_TRANSACTION_REVEALED";
  TokenTransactionStatus2[TokenTransactionStatus2["TOKEN_TRANSACTION_FINALIZED"] = 2] = "TOKEN_TRANSACTION_FINALIZED";
  TokenTransactionStatus2[TokenTransactionStatus2["TOKEN_TRANSACTION_STARTED_CANCELLED"] = 3] = "TOKEN_TRANSACTION_STARTED_CANCELLED";
  TokenTransactionStatus2[TokenTransactionStatus2["TOKEN_TRANSACTION_SIGNED_CANCELLED"] = 4] = "TOKEN_TRANSACTION_SIGNED_CANCELLED";
  TokenTransactionStatus2[TokenTransactionStatus2["TOKEN_TRANSACTION_UNKNOWN"] = 10] = "TOKEN_TRANSACTION_UNKNOWN";
  TokenTransactionStatus2[TokenTransactionStatus2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
  return TokenTransactionStatus2;
})(TokenTransactionStatus || {});
function tokenTransactionStatusFromJSON(object) {
  switch (object) {
    case 0:
    case "TOKEN_TRANSACTION_STARTED":
      return 0 /* TOKEN_TRANSACTION_STARTED */;
    case 1:
    case "TOKEN_TRANSACTION_SIGNED":
      return 1 /* TOKEN_TRANSACTION_SIGNED */;
    case 5:
    case "TOKEN_TRANSACTION_REVEALED":
      return 5 /* TOKEN_TRANSACTION_REVEALED */;
    case 2:
    case "TOKEN_TRANSACTION_FINALIZED":
      return 2 /* TOKEN_TRANSACTION_FINALIZED */;
    case 3:
    case "TOKEN_TRANSACTION_STARTED_CANCELLED":
      return 3 /* TOKEN_TRANSACTION_STARTED_CANCELLED */;
    case 4:
    case "TOKEN_TRANSACTION_SIGNED_CANCELLED":
      return 4 /* TOKEN_TRANSACTION_SIGNED_CANCELLED */;
    case 10:
    case "TOKEN_TRANSACTION_UNKNOWN":
      return 10 /* TOKEN_TRANSACTION_UNKNOWN */;
    case -1:
    case "UNRECOGNIZED":
    default:
      return -1 /* UNRECOGNIZED */;
  }
}
function tokenTransactionStatusToJSON(object) {
  switch (object) {
    case 0 /* TOKEN_TRANSACTION_STARTED */:
      return "TOKEN_TRANSACTION_STARTED";
    case 1 /* TOKEN_TRANSACTION_SIGNED */:
      return "TOKEN_TRANSACTION_SIGNED";
    case 5 /* TOKEN_TRANSACTION_REVEALED */:
      return "TOKEN_TRANSACTION_REVEALED";
    case 2 /* TOKEN_TRANSACTION_FINALIZED */:
      return "TOKEN_TRANSACTION_FINALIZED";
    case 3 /* TOKEN_TRANSACTION_STARTED_CANCELLED */:
      return "TOKEN_TRANSACTION_STARTED_CANCELLED";
    case 4 /* TOKEN_TRANSACTION_SIGNED_CANCELLED */:
      return "TOKEN_TRANSACTION_SIGNED_CANCELLED";
    case 10 /* TOKEN_TRANSACTION_UNKNOWN */:
      return "TOKEN_TRANSACTION_UNKNOWN";
    case -1 /* UNRECOGNIZED */:
    default:
      return "UNRECOGNIZED";
  }
}
var TransferStatus = /* @__PURE__ */ ((TransferStatus2) => {
  TransferStatus2[TransferStatus2["TRANSFER_STATUS_SENDER_INITIATED"] = 0] = "TRANSFER_STATUS_SENDER_INITIATED";
  TransferStatus2[TransferStatus2["TRANSFER_STATUS_SENDER_KEY_TWEAK_PENDING"] = 1] = "TRANSFER_STATUS_SENDER_KEY_TWEAK_PENDING";
  TransferStatus2[TransferStatus2["TRANSFER_STATUS_SENDER_KEY_TWEAKED"] = 2] = "TRANSFER_STATUS_SENDER_KEY_TWEAKED";
  TransferStatus2[TransferStatus2["TRANSFER_STATUS_RECEIVER_KEY_TWEAKED"] = 3] = "TRANSFER_STATUS_RECEIVER_KEY_TWEAKED";
  TransferStatus2[TransferStatus2["TRANSFER_STATUS_RECEIVER_REFUND_SIGNED"] = 4] = "TRANSFER_STATUS_RECEIVER_REFUND_SIGNED";
  TransferStatus2[TransferStatus2["TRANSFER_STATUS_COMPLETED"] = 5] = "TRANSFER_STATUS_COMPLETED";
  TransferStatus2[TransferStatus2["TRANSFER_STATUS_EXPIRED"] = 6] = "TRANSFER_STATUS_EXPIRED";
  TransferStatus2[TransferStatus2["TRANSFER_STATUS_RETURNED"] = 7] = "TRANSFER_STATUS_RETURNED";
  TransferStatus2[TransferStatus2["TRANSFER_STATUS_SENDER_INITIATED_COORDINATOR"] = 8] = "TRANSFER_STATUS_SENDER_INITIATED_COORDINATOR";
  TransferStatus2[TransferStatus2["TRANSFER_STATUS_RECEIVER_KEY_TWEAK_LOCKED"] = 9] = "TRANSFER_STATUS_RECEIVER_KEY_TWEAK_LOCKED";
  TransferStatus2[TransferStatus2["TRANSFER_STATUS_RECEIVER_KEY_TWEAK_APPLIED"] = 10] = "TRANSFER_STATUS_RECEIVER_KEY_TWEAK_APPLIED";
  TransferStatus2[TransferStatus2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
  return TransferStatus2;
})(TransferStatus || {});
function transferStatusFromJSON(object) {
  switch (object) {
    case 0:
    case "TRANSFER_STATUS_SENDER_INITIATED":
      return 0 /* TRANSFER_STATUS_SENDER_INITIATED */;
    case 1:
    case "TRANSFER_STATUS_SENDER_KEY_TWEAK_PENDING":
      return 1 /* TRANSFER_STATUS_SENDER_KEY_TWEAK_PENDING */;
    case 2:
    case "TRANSFER_STATUS_SENDER_KEY_TWEAKED":
      return 2 /* TRANSFER_STATUS_SENDER_KEY_TWEAKED */;
    case 3:
    case "TRANSFER_STATUS_RECEIVER_KEY_TWEAKED":
      return 3 /* TRANSFER_STATUS_RECEIVER_KEY_TWEAKED */;
    case 4:
    case "TRANSFER_STATUS_RECEIVER_REFUND_SIGNED":
      return 4 /* TRANSFER_STATUS_RECEIVER_REFUND_SIGNED */;
    case 5:
    case "TRANSFER_STATUS_COMPLETED":
      return 5 /* TRANSFER_STATUS_COMPLETED */;
    case 6:
    case "TRANSFER_STATUS_EXPIRED":
      return 6 /* TRANSFER_STATUS_EXPIRED */;
    case 7:
    case "TRANSFER_STATUS_RETURNED":
      return 7 /* TRANSFER_STATUS_RETURNED */;
    case 8:
    case "TRANSFER_STATUS_SENDER_INITIATED_COORDINATOR":
      return 8 /* TRANSFER_STATUS_SENDER_INITIATED_COORDINATOR */;
    case 9:
    case "TRANSFER_STATUS_RECEIVER_KEY_TWEAK_LOCKED":
      return 9 /* TRANSFER_STATUS_RECEIVER_KEY_TWEAK_LOCKED */;
    case 10:
    case "TRANSFER_STATUS_RECEIVER_KEY_TWEAK_APPLIED":
      return 10 /* TRANSFER_STATUS_RECEIVER_KEY_TWEAK_APPLIED */;
    case -1:
    case "UNRECOGNIZED":
    default:
      return -1 /* UNRECOGNIZED */;
  }
}
function transferStatusToJSON(object) {
  switch (object) {
    case 0 /* TRANSFER_STATUS_SENDER_INITIATED */:
      return "TRANSFER_STATUS_SENDER_INITIATED";
    case 1 /* TRANSFER_STATUS_SENDER_KEY_TWEAK_PENDING */:
      return "TRANSFER_STATUS_SENDER_KEY_TWEAK_PENDING";
    case 2 /* TRANSFER_STATUS_SENDER_KEY_TWEAKED */:
      return "TRANSFER_STATUS_SENDER_KEY_TWEAKED";
    case 3 /* TRANSFER_STATUS_RECEIVER_KEY_TWEAKED */:
      return "TRANSFER_STATUS_RECEIVER_KEY_TWEAKED";
    case 4 /* TRANSFER_STATUS_RECEIVER_REFUND_SIGNED */:
      return "TRANSFER_STATUS_RECEIVER_REFUND_SIGNED";
    case 5 /* TRANSFER_STATUS_COMPLETED */:
      return "TRANSFER_STATUS_COMPLETED";
    case 6 /* TRANSFER_STATUS_EXPIRED */:
      return "TRANSFER_STATUS_EXPIRED";
    case 7 /* TRANSFER_STATUS_RETURNED */:
      return "TRANSFER_STATUS_RETURNED";
    case 8 /* TRANSFER_STATUS_SENDER_INITIATED_COORDINATOR */:
      return "TRANSFER_STATUS_SENDER_INITIATED_COORDINATOR";
    case 9 /* TRANSFER_STATUS_RECEIVER_KEY_TWEAK_LOCKED */:
      return "TRANSFER_STATUS_RECEIVER_KEY_TWEAK_LOCKED";
    case 10 /* TRANSFER_STATUS_RECEIVER_KEY_TWEAK_APPLIED */:
      return "TRANSFER_STATUS_RECEIVER_KEY_TWEAK_APPLIED";
    case -1 /* UNRECOGNIZED */:
    default:
      return "UNRECOGNIZED";
  }
}
var TransferType = /* @__PURE__ */ ((TransferType2) => {
  TransferType2[TransferType2["PREIMAGE_SWAP"] = 0] = "PREIMAGE_SWAP";
  TransferType2[TransferType2["COOPERATIVE_EXIT"] = 1] = "COOPERATIVE_EXIT";
  TransferType2[TransferType2["TRANSFER"] = 2] = "TRANSFER";
  TransferType2[TransferType2["UTXO_SWAP"] = 3] = "UTXO_SWAP";
  TransferType2[TransferType2["SWAP"] = 30] = "SWAP";
  TransferType2[TransferType2["COUNTER_SWAP"] = 40] = "COUNTER_SWAP";
  TransferType2[TransferType2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
  return TransferType2;
})(TransferType || {});
function transferTypeFromJSON(object) {
  switch (object) {
    case 0:
    case "PREIMAGE_SWAP":
      return 0 /* PREIMAGE_SWAP */;
    case 1:
    case "COOPERATIVE_EXIT":
      return 1 /* COOPERATIVE_EXIT */;
    case 2:
    case "TRANSFER":
      return 2 /* TRANSFER */;
    case 3:
    case "UTXO_SWAP":
      return 3 /* UTXO_SWAP */;
    case 30:
    case "SWAP":
      return 30 /* SWAP */;
    case 40:
    case "COUNTER_SWAP":
      return 40 /* COUNTER_SWAP */;
    case -1:
    case "UNRECOGNIZED":
    default:
      return -1 /* UNRECOGNIZED */;
  }
}
function transferTypeToJSON(object) {
  switch (object) {
    case 0 /* PREIMAGE_SWAP */:
      return "PREIMAGE_SWAP";
    case 1 /* COOPERATIVE_EXIT */:
      return "COOPERATIVE_EXIT";
    case 2 /* TRANSFER */:
      return "TRANSFER";
    case 3 /* UTXO_SWAP */:
      return "UTXO_SWAP";
    case 30 /* SWAP */:
      return "SWAP";
    case 40 /* COUNTER_SWAP */:
      return "COUNTER_SWAP";
    case -1 /* UNRECOGNIZED */:
    default:
      return "UNRECOGNIZED";
  }
}
var Order = /* @__PURE__ */ ((Order2) => {
  Order2[Order2["DESCENDING"] = 0] = "DESCENDING";
  Order2[Order2["ASCENDING"] = 1] = "ASCENDING";
  Order2[Order2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
  return Order2;
})(Order || {});
function orderFromJSON(object) {
  switch (object) {
    case 0:
    case "DESCENDING":
      return 0 /* DESCENDING */;
    case 1:
    case "ASCENDING":
      return 1 /* ASCENDING */;
    case -1:
    case "UNRECOGNIZED":
    default:
      return -1 /* UNRECOGNIZED */;
  }
}
function orderToJSON(object) {
  switch (object) {
    case 0 /* DESCENDING */:
      return "DESCENDING";
    case 1 /* ASCENDING */:
      return "ASCENDING";
    case -1 /* UNRECOGNIZED */:
    default:
      return "UNRECOGNIZED";
  }
}
var PreimageRequestStatus = /* @__PURE__ */ ((PreimageRequestStatus2) => {
  PreimageRequestStatus2[PreimageRequestStatus2["PREIMAGE_REQUEST_STATUS_WAITING_FOR_PREIMAGE"] = 0] = "PREIMAGE_REQUEST_STATUS_WAITING_FOR_PREIMAGE";
  PreimageRequestStatus2[PreimageRequestStatus2["PREIMAGE_REQUEST_STATUS_PREIMAGE_SHARED"] = 1] = "PREIMAGE_REQUEST_STATUS_PREIMAGE_SHARED";
  PreimageRequestStatus2[PreimageRequestStatus2["PREIMAGE_REQUEST_STATUS_RETURNED"] = 2] = "PREIMAGE_REQUEST_STATUS_RETURNED";
  PreimageRequestStatus2[PreimageRequestStatus2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
  return PreimageRequestStatus2;
})(PreimageRequestStatus || {});
function preimageRequestStatusFromJSON(object) {
  switch (object) {
    case 0:
    case "PREIMAGE_REQUEST_STATUS_WAITING_FOR_PREIMAGE":
      return 0 /* PREIMAGE_REQUEST_STATUS_WAITING_FOR_PREIMAGE */;
    case 1:
    case "PREIMAGE_REQUEST_STATUS_PREIMAGE_SHARED":
      return 1 /* PREIMAGE_REQUEST_STATUS_PREIMAGE_SHARED */;
    case 2:
    case "PREIMAGE_REQUEST_STATUS_RETURNED":
      return 2 /* PREIMAGE_REQUEST_STATUS_RETURNED */;
    case -1:
    case "UNRECOGNIZED":
    default:
      return -1 /* UNRECOGNIZED */;
  }
}
function preimageRequestStatusToJSON(object) {
  switch (object) {
    case 0 /* PREIMAGE_REQUEST_STATUS_WAITING_FOR_PREIMAGE */:
      return "PREIMAGE_REQUEST_STATUS_WAITING_FOR_PREIMAGE";
    case 1 /* PREIMAGE_REQUEST_STATUS_PREIMAGE_SHARED */:
      return "PREIMAGE_REQUEST_STATUS_PREIMAGE_SHARED";
    case 2 /* PREIMAGE_REQUEST_STATUS_RETURNED */:
      return "PREIMAGE_REQUEST_STATUS_RETURNED";
    case -1 /* UNRECOGNIZED */:
    default:
      return "UNRECOGNIZED";
  }
}
var UtxoSwapRequestType = /* @__PURE__ */ ((UtxoSwapRequestType2) => {
  UtxoSwapRequestType2[UtxoSwapRequestType2["Fixed"] = 0] = "Fixed";
  UtxoSwapRequestType2[UtxoSwapRequestType2["MaxFee"] = 1] = "MaxFee";
  UtxoSwapRequestType2[UtxoSwapRequestType2["Refund"] = 2] = "Refund";
  UtxoSwapRequestType2[UtxoSwapRequestType2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
  return UtxoSwapRequestType2;
})(UtxoSwapRequestType || {});
function utxoSwapRequestTypeFromJSON(object) {
  switch (object) {
    case 0:
    case "Fixed":
      return 0 /* Fixed */;
    case 1:
    case "MaxFee":
      return 1 /* MaxFee */;
    case 2:
    case "Refund":
      return 2 /* Refund */;
    case -1:
    case "UNRECOGNIZED":
    default:
      return -1 /* UNRECOGNIZED */;
  }
}
function utxoSwapRequestTypeToJSON(object) {
  switch (object) {
    case 0 /* Fixed */:
      return "Fixed";
    case 1 /* MaxFee */:
      return "MaxFee";
    case 2 /* Refund */:
      return "Refund";
    case -1 /* UNRECOGNIZED */:
    default:
      return "UNRECOGNIZED";
  }
}
var InvoiceStatus = /* @__PURE__ */ ((InvoiceStatus2) => {
  InvoiceStatus2[InvoiceStatus2["NOT_FOUND"] = 0] = "NOT_FOUND";
  InvoiceStatus2[InvoiceStatus2["PENDING"] = 1] = "PENDING";
  InvoiceStatus2[InvoiceStatus2["FINALIZED"] = 2] = "FINALIZED";
  InvoiceStatus2[InvoiceStatus2["RETURNED"] = 4] = "RETURNED";
  InvoiceStatus2[InvoiceStatus2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
  return InvoiceStatus2;
})(InvoiceStatus || {});
function invoiceStatusFromJSON(object) {
  switch (object) {
    case 0:
    case "NOT_FOUND":
      return 0 /* NOT_FOUND */;
    case 1:
    case "PENDING":
      return 1 /* PENDING */;
    case 2:
    case "FINALIZED":
      return 2 /* FINALIZED */;
    case 4:
    case "RETURNED":
      return 4 /* RETURNED */;
    case -1:
    case "UNRECOGNIZED":
    default:
      return -1 /* UNRECOGNIZED */;
  }
}
function invoiceStatusToJSON(object) {
  switch (object) {
    case 0 /* NOT_FOUND */:
      return "NOT_FOUND";
    case 1 /* PENDING */:
      return "PENDING";
    case 2 /* FINALIZED */:
      return "FINALIZED";
    case 4 /* RETURNED */:
      return "RETURNED";
    case -1 /* UNRECOGNIZED */:
    default:
      return "UNRECOGNIZED";
  }
}
var TreeNodeStatus = /* @__PURE__ */ ((TreeNodeStatus2) => {
  TreeNodeStatus2[TreeNodeStatus2["TREE_NODE_STATUS_CREATING"] = 0] = "TREE_NODE_STATUS_CREATING";
  TreeNodeStatus2[TreeNodeStatus2["TREE_NODE_STATUS_AVAILABLE"] = 1] = "TREE_NODE_STATUS_AVAILABLE";
  TreeNodeStatus2[TreeNodeStatus2["TREE_NODE_STATUS_FROZEN_BY_ISSUER"] = 2] = "TREE_NODE_STATUS_FROZEN_BY_ISSUER";
  TreeNodeStatus2[TreeNodeStatus2["TREE_NODE_STATUS_TRANSFER_LOCKED"] = 3] = "TREE_NODE_STATUS_TRANSFER_LOCKED";
  TreeNodeStatus2[TreeNodeStatus2["TREE_NODE_STATUS_SPLIT_LOCKED"] = 4] = "TREE_NODE_STATUS_SPLIT_LOCKED";
  TreeNodeStatus2[TreeNodeStatus2["TREE_NODE_STATUS_SPLITTED"] = 5] = "TREE_NODE_STATUS_SPLITTED";
  TreeNodeStatus2[TreeNodeStatus2["TREE_NODE_STATUS_AGGREGATED"] = 6] = "TREE_NODE_STATUS_AGGREGATED";
  TreeNodeStatus2[TreeNodeStatus2["TREE_NODE_STATUS_ON_CHAIN"] = 7] = "TREE_NODE_STATUS_ON_CHAIN";
  TreeNodeStatus2[TreeNodeStatus2["TREE_NODE_STATUS_AGGREGATE_LOCK"] = 8] = "TREE_NODE_STATUS_AGGREGATE_LOCK";
  TreeNodeStatus2[TreeNodeStatus2["TREE_NODE_STATUS_EXITED"] = 9] = "TREE_NODE_STATUS_EXITED";
  TreeNodeStatus2[TreeNodeStatus2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
  return TreeNodeStatus2;
})(TreeNodeStatus || {});
function treeNodeStatusFromJSON(object) {
  switch (object) {
    case 0:
    case "TREE_NODE_STATUS_CREATING":
      return 0 /* TREE_NODE_STATUS_CREATING */;
    case 1:
    case "TREE_NODE_STATUS_AVAILABLE":
      return 1 /* TREE_NODE_STATUS_AVAILABLE */;
    case 2:
    case "TREE_NODE_STATUS_FROZEN_BY_ISSUER":
      return 2 /* TREE_NODE_STATUS_FROZEN_BY_ISSUER */;
    case 3:
    case "TREE_NODE_STATUS_TRANSFER_LOCKED":
      return 3 /* TREE_NODE_STATUS_TRANSFER_LOCKED */;
    case 4:
    case "TREE_NODE_STATUS_SPLIT_LOCKED":
      return 4 /* TREE_NODE_STATUS_SPLIT_LOCKED */;
    case 5:
    case "TREE_NODE_STATUS_SPLITTED":
      return 5 /* TREE_NODE_STATUS_SPLITTED */;
    case 6:
    case "TREE_NODE_STATUS_AGGREGATED":
      return 6 /* TREE_NODE_STATUS_AGGREGATED */;
    case 7:
    case "TREE_NODE_STATUS_ON_CHAIN":
      return 7 /* TREE_NODE_STATUS_ON_CHAIN */;
    case 8:
    case "TREE_NODE_STATUS_AGGREGATE_LOCK":
      return 8 /* TREE_NODE_STATUS_AGGREGATE_LOCK */;
    case 9:
    case "TREE_NODE_STATUS_EXITED":
      return 9 /* TREE_NODE_STATUS_EXITED */;
    case -1:
    case "UNRECOGNIZED":
    default:
      return -1 /* UNRECOGNIZED */;
  }
}
function treeNodeStatusToJSON(object) {
  switch (object) {
    case 0 /* TREE_NODE_STATUS_CREATING */:
      return "TREE_NODE_STATUS_CREATING";
    case 1 /* TREE_NODE_STATUS_AVAILABLE */:
      return "TREE_NODE_STATUS_AVAILABLE";
    case 2 /* TREE_NODE_STATUS_FROZEN_BY_ISSUER */:
      return "TREE_NODE_STATUS_FROZEN_BY_ISSUER";
    case 3 /* TREE_NODE_STATUS_TRANSFER_LOCKED */:
      return "TREE_NODE_STATUS_TRANSFER_LOCKED";
    case 4 /* TREE_NODE_STATUS_SPLIT_LOCKED */:
      return "TREE_NODE_STATUS_SPLIT_LOCKED";
    case 5 /* TREE_NODE_STATUS_SPLITTED */:
      return "TREE_NODE_STATUS_SPLITTED";
    case 6 /* TREE_NODE_STATUS_AGGREGATED */:
      return "TREE_NODE_STATUS_AGGREGATED";
    case 7 /* TREE_NODE_STATUS_ON_CHAIN */:
      return "TREE_NODE_STATUS_ON_CHAIN";
    case 8 /* TREE_NODE_STATUS_AGGREGATE_LOCK */:
      return "TREE_NODE_STATUS_AGGREGATE_LOCK";
    case 9 /* TREE_NODE_STATUS_EXITED */:
      return "TREE_NODE_STATUS_EXITED";
    case -1 /* UNRECOGNIZED */:
    default:
      return "UNRECOGNIZED";
  }
}
var InitiatePreimageSwapRequest_Reason = /* @__PURE__ */ ((InitiatePreimageSwapRequest_Reason2) => {
  InitiatePreimageSwapRequest_Reason2[InitiatePreimageSwapRequest_Reason2["REASON_SEND"] = 0] = "REASON_SEND";
  InitiatePreimageSwapRequest_Reason2[InitiatePreimageSwapRequest_Reason2["REASON_RECEIVE"] = 1] = "REASON_RECEIVE";
  InitiatePreimageSwapRequest_Reason2[InitiatePreimageSwapRequest_Reason2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
  return InitiatePreimageSwapRequest_Reason2;
})(InitiatePreimageSwapRequest_Reason || {});
function initiatePreimageSwapRequest_ReasonFromJSON(object) {
  switch (object) {
    case 0:
    case "REASON_SEND":
      return 0 /* REASON_SEND */;
    case 1:
    case "REASON_RECEIVE":
      return 1 /* REASON_RECEIVE */;
    case -1:
    case "UNRECOGNIZED":
    default:
      return -1 /* UNRECOGNIZED */;
  }
}
function initiatePreimageSwapRequest_ReasonToJSON(object) {
  switch (object) {
    case 0 /* REASON_SEND */:
      return "REASON_SEND";
    case 1 /* REASON_RECEIVE */:
      return "REASON_RECEIVE";
    case -1 /* UNRECOGNIZED */:
    default:
      return "UNRECOGNIZED";
  }
}
function createBaseSubscribeToEventsRequest() {
  return { identityPublicKey: new Uint8Array(0) };
}
var SubscribeToEventsRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.identityPublicKey.length !== 0) {
      writer.uint32(82).bytes(message.identityPublicKey);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSubscribeToEventsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.identityPublicKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      identityPublicKey: isSet3(object.identityPublicKey) ? bytesFromBase642(object.identityPublicKey) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.identityPublicKey.length !== 0) {
      obj.identityPublicKey = base64FromBytes2(message.identityPublicKey);
    }
    return obj;
  },
  create(base) {
    return SubscribeToEventsRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSubscribeToEventsRequest();
    message.identityPublicKey = object.identityPublicKey ?? new Uint8Array(0);
    return message;
  }
};
function createBaseSubscribeToEventsResponse() {
  return { event: void 0 };
}
var SubscribeToEventsResponse = {
  encode(message, writer = new BinaryWriter4()) {
    switch (message.event?.$case) {
      case "transfer":
        TransferEvent.encode(message.event.transfer, writer.uint32(10).fork()).join();
        break;
      case "deposit":
        DepositEvent.encode(message.event.deposit, writer.uint32(18).fork()).join();
        break;
      case "connected":
        ConnectedEvent.encode(message.event.connected, writer.uint32(26).fork()).join();
        break;
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSubscribeToEventsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.event = { $case: "transfer", transfer: TransferEvent.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.event = { $case: "deposit", deposit: DepositEvent.decode(reader, reader.uint32()) };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.event = { $case: "connected", connected: ConnectedEvent.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      event: isSet3(object.transfer) ? { $case: "transfer", transfer: TransferEvent.fromJSON(object.transfer) } : isSet3(object.deposit) ? { $case: "deposit", deposit: DepositEvent.fromJSON(object.deposit) } : isSet3(object.connected) ? { $case: "connected", connected: ConnectedEvent.fromJSON(object.connected) } : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.event?.$case === "transfer") {
      obj.transfer = TransferEvent.toJSON(message.event.transfer);
    } else if (message.event?.$case === "deposit") {
      obj.deposit = DepositEvent.toJSON(message.event.deposit);
    } else if (message.event?.$case === "connected") {
      obj.connected = ConnectedEvent.toJSON(message.event.connected);
    }
    return obj;
  },
  create(base) {
    return SubscribeToEventsResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSubscribeToEventsResponse();
    switch (object.event?.$case) {
      case "transfer": {
        if (object.event?.transfer !== void 0 && object.event?.transfer !== null) {
          message.event = { $case: "transfer", transfer: TransferEvent.fromPartial(object.event.transfer) };
        }
        break;
      }
      case "deposit": {
        if (object.event?.deposit !== void 0 && object.event?.deposit !== null) {
          message.event = { $case: "deposit", deposit: DepositEvent.fromPartial(object.event.deposit) };
        }
        break;
      }
      case "connected": {
        if (object.event?.connected !== void 0 && object.event?.connected !== null) {
          message.event = { $case: "connected", connected: ConnectedEvent.fromPartial(object.event.connected) };
        }
        break;
      }
    }
    return message;
  }
};
function createBaseConnectedEvent() {
  return {};
}
var ConnectedEvent = {
  encode(_, writer = new BinaryWriter4()) {
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseConnectedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(_) {
    return {};
  },
  toJSON(_) {
    const obj = {};
    return obj;
  },
  create(base) {
    return ConnectedEvent.fromPartial(base ?? {});
  },
  fromPartial(_) {
    const message = createBaseConnectedEvent();
    return message;
  }
};
function createBaseTransferEvent() {
  return { transfer: void 0 };
}
var TransferEvent = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.transfer !== void 0) {
      Transfer.encode(message.transfer, writer.uint32(82).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTransferEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.transfer = Transfer.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { transfer: isSet3(object.transfer) ? Transfer.fromJSON(object.transfer) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.transfer !== void 0) {
      obj.transfer = Transfer.toJSON(message.transfer);
    }
    return obj;
  },
  create(base) {
    return TransferEvent.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseTransferEvent();
    message.transfer = object.transfer !== void 0 && object.transfer !== null ? Transfer.fromPartial(object.transfer) : void 0;
    return message;
  }
};
function createBaseDepositEvent() {
  return { deposit: void 0 };
}
var DepositEvent = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.deposit !== void 0) {
      TreeNode.encode(message.deposit, writer.uint32(82).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseDepositEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.deposit = TreeNode.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { deposit: isSet3(object.deposit) ? TreeNode.fromJSON(object.deposit) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.deposit !== void 0) {
      obj.deposit = TreeNode.toJSON(message.deposit);
    }
    return obj;
  },
  create(base) {
    return DepositEvent.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseDepositEvent();
    message.deposit = object.deposit !== void 0 && object.deposit !== null ? TreeNode.fromPartial(object.deposit) : void 0;
    return message;
  }
};
function createBasePageRequest() {
  return { pageSize: 0, cursor: "", direction: 0 };
}
var PageRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.pageSize !== 0) {
      writer.uint32(8).int32(message.pageSize);
    }
    if (message.cursor !== "") {
      writer.uint32(18).string(message.cursor);
    }
    if (message.direction !== 0) {
      writer.uint32(24).int32(message.direction);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.pageSize = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.cursor = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.direction = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      pageSize: isSet3(object.pageSize) ? globalThis.Number(object.pageSize) : 0,
      cursor: isSet3(object.cursor) ? globalThis.String(object.cursor) : "",
      direction: isSet3(object.direction) ? directionFromJSON(object.direction) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.pageSize !== 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    if (message.cursor !== "") {
      obj.cursor = message.cursor;
    }
    if (message.direction !== 0) {
      obj.direction = directionToJSON(message.direction);
    }
    return obj;
  },
  create(base) {
    return PageRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBasePageRequest();
    message.pageSize = object.pageSize ?? 0;
    message.cursor = object.cursor ?? "";
    message.direction = object.direction ?? 0;
    return message;
  }
};
function createBasePageResponse() {
  return { hasNextPage: false, hasPreviousPage: false, nextCursor: "", previousCursor: "" };
}
var PageResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.hasNextPage !== false) {
      writer.uint32(8).bool(message.hasNextPage);
    }
    if (message.hasPreviousPage !== false) {
      writer.uint32(16).bool(message.hasPreviousPage);
    }
    if (message.nextCursor !== "") {
      writer.uint32(26).string(message.nextCursor);
    }
    if (message.previousCursor !== "") {
      writer.uint32(34).string(message.previousCursor);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.hasNextPage = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.hasPreviousPage = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.nextCursor = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.previousCursor = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      hasNextPage: isSet3(object.hasNextPage) ? globalThis.Boolean(object.hasNextPage) : false,
      hasPreviousPage: isSet3(object.hasPreviousPage) ? globalThis.Boolean(object.hasPreviousPage) : false,
      nextCursor: isSet3(object.nextCursor) ? globalThis.String(object.nextCursor) : "",
      previousCursor: isSet3(object.previousCursor) ? globalThis.String(object.previousCursor) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.hasNextPage !== false) {
      obj.hasNextPage = message.hasNextPage;
    }
    if (message.hasPreviousPage !== false) {
      obj.hasPreviousPage = message.hasPreviousPage;
    }
    if (message.nextCursor !== "") {
      obj.nextCursor = message.nextCursor;
    }
    if (message.previousCursor !== "") {
      obj.previousCursor = message.previousCursor;
    }
    return obj;
  },
  create(base) {
    return PageResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBasePageResponse();
    message.hasNextPage = object.hasNextPage ?? false;
    message.hasPreviousPage = object.hasPreviousPage ?? false;
    message.nextCursor = object.nextCursor ?? "";
    message.previousCursor = object.previousCursor ?? "";
    return message;
  }
};
function createBaseDepositAddressProof() {
  return { addressSignatures: {}, proofOfPossessionSignature: new Uint8Array(0) };
}
var DepositAddressProof = {
  encode(message, writer = new BinaryWriter4()) {
    Object.entries(message.addressSignatures).forEach(([key, value]) => {
      DepositAddressProof_AddressSignaturesEntry.encode({ key, value }, writer.uint32(10).fork()).join();
    });
    if (message.proofOfPossessionSignature.length !== 0) {
      writer.uint32(18).bytes(message.proofOfPossessionSignature);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseDepositAddressProof();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          const entry1 = DepositAddressProof_AddressSignaturesEntry.decode(reader, reader.uint32());
          if (entry1.value !== void 0) {
            message.addressSignatures[entry1.key] = entry1.value;
          }
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.proofOfPossessionSignature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      addressSignatures: isObject2(object.addressSignatures) ? Object.entries(object.addressSignatures).reduce((acc, [key, value]) => {
        acc[key] = bytesFromBase642(value);
        return acc;
      }, {}) : {},
      proofOfPossessionSignature: isSet3(object.proofOfPossessionSignature) ? bytesFromBase642(object.proofOfPossessionSignature) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.addressSignatures) {
      const entries = Object.entries(message.addressSignatures);
      if (entries.length > 0) {
        obj.addressSignatures = {};
        entries.forEach(([k, v]) => {
          obj.addressSignatures[k] = base64FromBytes2(v);
        });
      }
    }
    if (message.proofOfPossessionSignature.length !== 0) {
      obj.proofOfPossessionSignature = base64FromBytes2(message.proofOfPossessionSignature);
    }
    return obj;
  },
  create(base) {
    return DepositAddressProof.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseDepositAddressProof();
    message.addressSignatures = Object.entries(object.addressSignatures ?? {}).reduce(
      (acc, [key, value]) => {
        if (value !== void 0) {
          acc[key] = value;
        }
        return acc;
      },
      {}
    );
    message.proofOfPossessionSignature = object.proofOfPossessionSignature ?? new Uint8Array(0);
    return message;
  }
};
function createBaseDepositAddressProof_AddressSignaturesEntry() {
  return { key: "", value: new Uint8Array(0) };
}
var DepositAddressProof_AddressSignaturesEntry = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value.length !== 0) {
      writer.uint32(18).bytes(message.value);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseDepositAddressProof_AddressSignaturesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.value = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet3(object.key) ? globalThis.String(object.key) : "",
      value: isSet3(object.value) ? bytesFromBase642(object.value) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value.length !== 0) {
      obj.value = base64FromBytes2(message.value);
    }
    return obj;
  },
  create(base) {
    return DepositAddressProof_AddressSignaturesEntry.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseDepositAddressProof_AddressSignaturesEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? new Uint8Array(0);
    return message;
  }
};
function createBaseGenerateDepositAddressRequest() {
  return {
    signingPublicKey: new Uint8Array(0),
    identityPublicKey: new Uint8Array(0),
    network: 0,
    leafId: void 0,
    isStatic: void 0
  };
}
var GenerateDepositAddressRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.signingPublicKey.length !== 0) {
      writer.uint32(10).bytes(message.signingPublicKey);
    }
    if (message.identityPublicKey.length !== 0) {
      writer.uint32(18).bytes(message.identityPublicKey);
    }
    if (message.network !== 0) {
      writer.uint32(24).int32(message.network);
    }
    if (message.leafId !== void 0) {
      writer.uint32(34).string(message.leafId);
    }
    if (message.isStatic !== void 0) {
      writer.uint32(40).bool(message.isStatic);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGenerateDepositAddressRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.signingPublicKey = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.identityPublicKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.network = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.leafId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }
          message.isStatic = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      signingPublicKey: isSet3(object.signingPublicKey) ? bytesFromBase642(object.signingPublicKey) : new Uint8Array(0),
      identityPublicKey: isSet3(object.identityPublicKey) ? bytesFromBase642(object.identityPublicKey) : new Uint8Array(0),
      network: isSet3(object.network) ? networkFromJSON(object.network) : 0,
      leafId: isSet3(object.leafId) ? globalThis.String(object.leafId) : void 0,
      isStatic: isSet3(object.isStatic) ? globalThis.Boolean(object.isStatic) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.signingPublicKey.length !== 0) {
      obj.signingPublicKey = base64FromBytes2(message.signingPublicKey);
    }
    if (message.identityPublicKey.length !== 0) {
      obj.identityPublicKey = base64FromBytes2(message.identityPublicKey);
    }
    if (message.network !== 0) {
      obj.network = networkToJSON(message.network);
    }
    if (message.leafId !== void 0) {
      obj.leafId = message.leafId;
    }
    if (message.isStatic !== void 0) {
      obj.isStatic = message.isStatic;
    }
    return obj;
  },
  create(base) {
    return GenerateDepositAddressRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGenerateDepositAddressRequest();
    message.signingPublicKey = object.signingPublicKey ?? new Uint8Array(0);
    message.identityPublicKey = object.identityPublicKey ?? new Uint8Array(0);
    message.network = object.network ?? 0;
    message.leafId = object.leafId ?? void 0;
    message.isStatic = object.isStatic ?? void 0;
    return message;
  }
};
function createBaseAddress() {
  return { address: "", verifyingKey: new Uint8Array(0), depositAddressProof: void 0, isStatic: false };
}
var Address = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.verifyingKey.length !== 0) {
      writer.uint32(18).bytes(message.verifyingKey);
    }
    if (message.depositAddressProof !== void 0) {
      DepositAddressProof.encode(message.depositAddressProof, writer.uint32(26).fork()).join();
    }
    if (message.isStatic !== false) {
      writer.uint32(40).bool(message.isStatic);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseAddress();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.address = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.verifyingKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.depositAddressProof = DepositAddressProof.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }
          message.isStatic = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      address: isSet3(object.address) ? globalThis.String(object.address) : "",
      verifyingKey: isSet3(object.verifyingKey) ? bytesFromBase642(object.verifyingKey) : new Uint8Array(0),
      depositAddressProof: isSet3(object.depositAddressProof) ? DepositAddressProof.fromJSON(object.depositAddressProof) : void 0,
      isStatic: isSet3(object.isStatic) ? globalThis.Boolean(object.isStatic) : false
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.verifyingKey.length !== 0) {
      obj.verifyingKey = base64FromBytes2(message.verifyingKey);
    }
    if (message.depositAddressProof !== void 0) {
      obj.depositAddressProof = DepositAddressProof.toJSON(message.depositAddressProof);
    }
    if (message.isStatic !== false) {
      obj.isStatic = message.isStatic;
    }
    return obj;
  },
  create(base) {
    return Address.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseAddress();
    message.address = object.address ?? "";
    message.verifyingKey = object.verifyingKey ?? new Uint8Array(0);
    message.depositAddressProof = object.depositAddressProof !== void 0 && object.depositAddressProof !== null ? DepositAddressProof.fromPartial(object.depositAddressProof) : void 0;
    message.isStatic = object.isStatic ?? false;
    return message;
  }
};
function createBaseGenerateDepositAddressResponse() {
  return { depositAddress: void 0 };
}
var GenerateDepositAddressResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.depositAddress !== void 0) {
      Address.encode(message.depositAddress, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGenerateDepositAddressResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.depositAddress = Address.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { depositAddress: isSet3(object.depositAddress) ? Address.fromJSON(object.depositAddress) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.depositAddress !== void 0) {
      obj.depositAddress = Address.toJSON(message.depositAddress);
    }
    return obj;
  },
  create(base) {
    return GenerateDepositAddressResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGenerateDepositAddressResponse();
    message.depositAddress = object.depositAddress !== void 0 && object.depositAddress !== null ? Address.fromPartial(object.depositAddress) : void 0;
    return message;
  }
};
function createBaseGenerateStaticDepositAddressRequest() {
  return { signingPublicKey: new Uint8Array(0), identityPublicKey: new Uint8Array(0), network: 0 };
}
var GenerateStaticDepositAddressRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.signingPublicKey.length !== 0) {
      writer.uint32(10).bytes(message.signingPublicKey);
    }
    if (message.identityPublicKey.length !== 0) {
      writer.uint32(18).bytes(message.identityPublicKey);
    }
    if (message.network !== 0) {
      writer.uint32(24).int32(message.network);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGenerateStaticDepositAddressRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.signingPublicKey = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.identityPublicKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.network = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      signingPublicKey: isSet3(object.signingPublicKey) ? bytesFromBase642(object.signingPublicKey) : new Uint8Array(0),
      identityPublicKey: isSet3(object.identityPublicKey) ? bytesFromBase642(object.identityPublicKey) : new Uint8Array(0),
      network: isSet3(object.network) ? networkFromJSON(object.network) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.signingPublicKey.length !== 0) {
      obj.signingPublicKey = base64FromBytes2(message.signingPublicKey);
    }
    if (message.identityPublicKey.length !== 0) {
      obj.identityPublicKey = base64FromBytes2(message.identityPublicKey);
    }
    if (message.network !== 0) {
      obj.network = networkToJSON(message.network);
    }
    return obj;
  },
  create(base) {
    return GenerateStaticDepositAddressRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGenerateStaticDepositAddressRequest();
    message.signingPublicKey = object.signingPublicKey ?? new Uint8Array(0);
    message.identityPublicKey = object.identityPublicKey ?? new Uint8Array(0);
    message.network = object.network ?? 0;
    return message;
  }
};
function createBaseGenerateStaticDepositAddressResponse() {
  return { depositAddress: void 0 };
}
var GenerateStaticDepositAddressResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.depositAddress !== void 0) {
      Address.encode(message.depositAddress, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGenerateStaticDepositAddressResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.depositAddress = Address.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { depositAddress: isSet3(object.depositAddress) ? Address.fromJSON(object.depositAddress) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.depositAddress !== void 0) {
      obj.depositAddress = Address.toJSON(message.depositAddress);
    }
    return obj;
  },
  create(base) {
    return GenerateStaticDepositAddressResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGenerateStaticDepositAddressResponse();
    message.depositAddress = object.depositAddress !== void 0 && object.depositAddress !== null ? Address.fromPartial(object.depositAddress) : void 0;
    return message;
  }
};
function createBaseUTXO() {
  return { rawTx: new Uint8Array(0), vout: 0, network: 0, txid: new Uint8Array(0) };
}
var UTXO = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.rawTx.length !== 0) {
      writer.uint32(10).bytes(message.rawTx);
    }
    if (message.vout !== 0) {
      writer.uint32(16).uint32(message.vout);
    }
    if (message.network !== 0) {
      writer.uint32(24).int32(message.network);
    }
    if (message.txid.length !== 0) {
      writer.uint32(34).bytes(message.txid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUTXO();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.rawTx = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.vout = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.network = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.txid = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      rawTx: isSet3(object.rawTx) ? bytesFromBase642(object.rawTx) : new Uint8Array(0),
      vout: isSet3(object.vout) ? globalThis.Number(object.vout) : 0,
      network: isSet3(object.network) ? networkFromJSON(object.network) : 0,
      txid: isSet3(object.txid) ? bytesFromBase642(object.txid) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.rawTx.length !== 0) {
      obj.rawTx = base64FromBytes2(message.rawTx);
    }
    if (message.vout !== 0) {
      obj.vout = Math.round(message.vout);
    }
    if (message.network !== 0) {
      obj.network = networkToJSON(message.network);
    }
    if (message.txid.length !== 0) {
      obj.txid = base64FromBytes2(message.txid);
    }
    return obj;
  },
  create(base) {
    return UTXO.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseUTXO();
    message.rawTx = object.rawTx ?? new Uint8Array(0);
    message.vout = object.vout ?? 0;
    message.network = object.network ?? 0;
    message.txid = object.txid ?? new Uint8Array(0);
    return message;
  }
};
function createBaseNodeOutput() {
  return { nodeId: "", vout: 0 };
}
var NodeOutput = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.nodeId !== "") {
      writer.uint32(10).string(message.nodeId);
    }
    if (message.vout !== 0) {
      writer.uint32(16).uint32(message.vout);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseNodeOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.nodeId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.vout = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      nodeId: isSet3(object.nodeId) ? globalThis.String(object.nodeId) : "",
      vout: isSet3(object.vout) ? globalThis.Number(object.vout) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.nodeId !== "") {
      obj.nodeId = message.nodeId;
    }
    if (message.vout !== 0) {
      obj.vout = Math.round(message.vout);
    }
    return obj;
  },
  create(base) {
    return NodeOutput.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseNodeOutput();
    message.nodeId = object.nodeId ?? "";
    message.vout = object.vout ?? 0;
    return message;
  }
};
function createBaseSigningJob() {
  return { signingPublicKey: new Uint8Array(0), rawTx: new Uint8Array(0), signingNonceCommitment: void 0 };
}
var SigningJob = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.signingPublicKey.length !== 0) {
      writer.uint32(10).bytes(message.signingPublicKey);
    }
    if (message.rawTx.length !== 0) {
      writer.uint32(18).bytes(message.rawTx);
    }
    if (message.signingNonceCommitment !== void 0) {
      SigningCommitment.encode(message.signingNonceCommitment, writer.uint32(26).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSigningJob();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.signingPublicKey = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.rawTx = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.signingNonceCommitment = SigningCommitment.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      signingPublicKey: isSet3(object.signingPublicKey) ? bytesFromBase642(object.signingPublicKey) : new Uint8Array(0),
      rawTx: isSet3(object.rawTx) ? bytesFromBase642(object.rawTx) : new Uint8Array(0),
      signingNonceCommitment: isSet3(object.signingNonceCommitment) ? SigningCommitment.fromJSON(object.signingNonceCommitment) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.signingPublicKey.length !== 0) {
      obj.signingPublicKey = base64FromBytes2(message.signingPublicKey);
    }
    if (message.rawTx.length !== 0) {
      obj.rawTx = base64FromBytes2(message.rawTx);
    }
    if (message.signingNonceCommitment !== void 0) {
      obj.signingNonceCommitment = SigningCommitment.toJSON(message.signingNonceCommitment);
    }
    return obj;
  },
  create(base) {
    return SigningJob.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSigningJob();
    message.signingPublicKey = object.signingPublicKey ?? new Uint8Array(0);
    message.rawTx = object.rawTx ?? new Uint8Array(0);
    message.signingNonceCommitment = object.signingNonceCommitment !== void 0 && object.signingNonceCommitment !== null ? SigningCommitment.fromPartial(object.signingNonceCommitment) : void 0;
    return message;
  }
};
function createBaseSigningKeyshare() {
  return { ownerIdentifiers: [], threshold: 0, publicKey: new Uint8Array(0), publicShares: {}, updatedTime: void 0 };
}
var SigningKeyshare = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.ownerIdentifiers) {
      writer.uint32(10).string(v);
    }
    if (message.threshold !== 0) {
      writer.uint32(16).uint32(message.threshold);
    }
    if (message.publicKey.length !== 0) {
      writer.uint32(26).bytes(message.publicKey);
    }
    Object.entries(message.publicShares).forEach(([key, value]) => {
      SigningKeyshare_PublicSharesEntry.encode({ key, value }, writer.uint32(34).fork()).join();
    });
    if (message.updatedTime !== void 0) {
      Timestamp.encode(toTimestamp(message.updatedTime), writer.uint32(42).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSigningKeyshare();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.ownerIdentifiers.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.threshold = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.publicKey = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          const entry4 = SigningKeyshare_PublicSharesEntry.decode(reader, reader.uint32());
          if (entry4.value !== void 0) {
            message.publicShares[entry4.key] = entry4.value;
          }
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.updatedTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      ownerIdentifiers: globalThis.Array.isArray(object?.ownerIdentifiers) ? object.ownerIdentifiers.map((e) => globalThis.String(e)) : [],
      threshold: isSet3(object.threshold) ? globalThis.Number(object.threshold) : 0,
      publicKey: isSet3(object.publicKey) ? bytesFromBase642(object.publicKey) : new Uint8Array(0),
      publicShares: isObject2(object.publicShares) ? Object.entries(object.publicShares).reduce((acc, [key, value]) => {
        acc[key] = bytesFromBase642(value);
        return acc;
      }, {}) : {},
      updatedTime: isSet3(object.updatedTime) ? fromJsonTimestamp(object.updatedTime) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.ownerIdentifiers?.length) {
      obj.ownerIdentifiers = message.ownerIdentifiers;
    }
    if (message.threshold !== 0) {
      obj.threshold = Math.round(message.threshold);
    }
    if (message.publicKey.length !== 0) {
      obj.publicKey = base64FromBytes2(message.publicKey);
    }
    if (message.publicShares) {
      const entries = Object.entries(message.publicShares);
      if (entries.length > 0) {
        obj.publicShares = {};
        entries.forEach(([k, v]) => {
          obj.publicShares[k] = base64FromBytes2(v);
        });
      }
    }
    if (message.updatedTime !== void 0) {
      obj.updatedTime = message.updatedTime.toISOString();
    }
    return obj;
  },
  create(base) {
    return SigningKeyshare.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSigningKeyshare();
    message.ownerIdentifiers = object.ownerIdentifiers?.map((e) => e) || [];
    message.threshold = object.threshold ?? 0;
    message.publicKey = object.publicKey ?? new Uint8Array(0);
    message.publicShares = Object.entries(object.publicShares ?? {}).reduce(
      (acc, [key, value]) => {
        if (value !== void 0) {
          acc[key] = value;
        }
        return acc;
      },
      {}
    );
    message.updatedTime = object.updatedTime ?? void 0;
    return message;
  }
};
function createBaseSigningKeyshare_PublicSharesEntry() {
  return { key: "", value: new Uint8Array(0) };
}
var SigningKeyshare_PublicSharesEntry = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value.length !== 0) {
      writer.uint32(18).bytes(message.value);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSigningKeyshare_PublicSharesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.value = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet3(object.key) ? globalThis.String(object.key) : "",
      value: isSet3(object.value) ? bytesFromBase642(object.value) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value.length !== 0) {
      obj.value = base64FromBytes2(message.value);
    }
    return obj;
  },
  create(base) {
    return SigningKeyshare_PublicSharesEntry.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSigningKeyshare_PublicSharesEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? new Uint8Array(0);
    return message;
  }
};
function createBaseSigningResult2() {
  return { publicKeys: {}, signingNonceCommitments: {}, signatureShares: {}, signingKeyshare: void 0 };
}
var SigningResult2 = {
  encode(message, writer = new BinaryWriter4()) {
    Object.entries(message.publicKeys).forEach(([key, value]) => {
      SigningResult_PublicKeysEntry.encode({ key, value }, writer.uint32(10).fork()).join();
    });
    Object.entries(message.signingNonceCommitments).forEach(([key, value]) => {
      SigningResult_SigningNonceCommitmentsEntry.encode({ key, value }, writer.uint32(18).fork()).join();
    });
    Object.entries(message.signatureShares).forEach(([key, value]) => {
      SigningResult_SignatureSharesEntry.encode({ key, value }, writer.uint32(26).fork()).join();
    });
    if (message.signingKeyshare !== void 0) {
      SigningKeyshare.encode(message.signingKeyshare, writer.uint32(34).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSigningResult2();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          const entry1 = SigningResult_PublicKeysEntry.decode(reader, reader.uint32());
          if (entry1.value !== void 0) {
            message.publicKeys[entry1.key] = entry1.value;
          }
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          const entry2 = SigningResult_SigningNonceCommitmentsEntry.decode(reader, reader.uint32());
          if (entry2.value !== void 0) {
            message.signingNonceCommitments[entry2.key] = entry2.value;
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          const entry3 = SigningResult_SignatureSharesEntry.decode(reader, reader.uint32());
          if (entry3.value !== void 0) {
            message.signatureShares[entry3.key] = entry3.value;
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.signingKeyshare = SigningKeyshare.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      publicKeys: isObject2(object.publicKeys) ? Object.entries(object.publicKeys).reduce((acc, [key, value]) => {
        acc[key] = bytesFromBase642(value);
        return acc;
      }, {}) : {},
      signingNonceCommitments: isObject2(object.signingNonceCommitments) ? Object.entries(object.signingNonceCommitments).reduce(
        (acc, [key, value]) => {
          acc[key] = SigningCommitment.fromJSON(value);
          return acc;
        },
        {}
      ) : {},
      signatureShares: isObject2(object.signatureShares) ? Object.entries(object.signatureShares).reduce((acc, [key, value]) => {
        acc[key] = bytesFromBase642(value);
        return acc;
      }, {}) : {},
      signingKeyshare: isSet3(object.signingKeyshare) ? SigningKeyshare.fromJSON(object.signingKeyshare) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.publicKeys) {
      const entries = Object.entries(message.publicKeys);
      if (entries.length > 0) {
        obj.publicKeys = {};
        entries.forEach(([k, v]) => {
          obj.publicKeys[k] = base64FromBytes2(v);
        });
      }
    }
    if (message.signingNonceCommitments) {
      const entries = Object.entries(message.signingNonceCommitments);
      if (entries.length > 0) {
        obj.signingNonceCommitments = {};
        entries.forEach(([k, v]) => {
          obj.signingNonceCommitments[k] = SigningCommitment.toJSON(v);
        });
      }
    }
    if (message.signatureShares) {
      const entries = Object.entries(message.signatureShares);
      if (entries.length > 0) {
        obj.signatureShares = {};
        entries.forEach(([k, v]) => {
          obj.signatureShares[k] = base64FromBytes2(v);
        });
      }
    }
    if (message.signingKeyshare !== void 0) {
      obj.signingKeyshare = SigningKeyshare.toJSON(message.signingKeyshare);
    }
    return obj;
  },
  create(base) {
    return SigningResult2.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSigningResult2();
    message.publicKeys = Object.entries(object.publicKeys ?? {}).reduce(
      (acc, [key, value]) => {
        if (value !== void 0) {
          acc[key] = value;
        }
        return acc;
      },
      {}
    );
    message.signingNonceCommitments = Object.entries(object.signingNonceCommitments ?? {}).reduce((acc, [key, value]) => {
      if (value !== void 0) {
        acc[key] = SigningCommitment.fromPartial(value);
      }
      return acc;
    }, {});
    message.signatureShares = Object.entries(object.signatureShares ?? {}).reduce(
      (acc, [key, value]) => {
        if (value !== void 0) {
          acc[key] = value;
        }
        return acc;
      },
      {}
    );
    message.signingKeyshare = object.signingKeyshare !== void 0 && object.signingKeyshare !== null ? SigningKeyshare.fromPartial(object.signingKeyshare) : void 0;
    return message;
  }
};
function createBaseSigningResult_PublicKeysEntry() {
  return { key: "", value: new Uint8Array(0) };
}
var SigningResult_PublicKeysEntry = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value.length !== 0) {
      writer.uint32(18).bytes(message.value);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSigningResult_PublicKeysEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.value = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet3(object.key) ? globalThis.String(object.key) : "",
      value: isSet3(object.value) ? bytesFromBase642(object.value) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value.length !== 0) {
      obj.value = base64FromBytes2(message.value);
    }
    return obj;
  },
  create(base) {
    return SigningResult_PublicKeysEntry.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSigningResult_PublicKeysEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? new Uint8Array(0);
    return message;
  }
};
function createBaseSigningResult_SigningNonceCommitmentsEntry() {
  return { key: "", value: void 0 };
}
var SigningResult_SigningNonceCommitmentsEntry = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== void 0) {
      SigningCommitment.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSigningResult_SigningNonceCommitmentsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.value = SigningCommitment.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet3(object.key) ? globalThis.String(object.key) : "",
      value: isSet3(object.value) ? SigningCommitment.fromJSON(object.value) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== void 0) {
      obj.value = SigningCommitment.toJSON(message.value);
    }
    return obj;
  },
  create(base) {
    return SigningResult_SigningNonceCommitmentsEntry.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSigningResult_SigningNonceCommitmentsEntry();
    message.key = object.key ?? "";
    message.value = object.value !== void 0 && object.value !== null ? SigningCommitment.fromPartial(object.value) : void 0;
    return message;
  }
};
function createBaseSigningResult_SignatureSharesEntry() {
  return { key: "", value: new Uint8Array(0) };
}
var SigningResult_SignatureSharesEntry = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value.length !== 0) {
      writer.uint32(18).bytes(message.value);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSigningResult_SignatureSharesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.value = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet3(object.key) ? globalThis.String(object.key) : "",
      value: isSet3(object.value) ? bytesFromBase642(object.value) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value.length !== 0) {
      obj.value = base64FromBytes2(message.value);
    }
    return obj;
  },
  create(base) {
    return SigningResult_SignatureSharesEntry.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSigningResult_SignatureSharesEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? new Uint8Array(0);
    return message;
  }
};
function createBaseRenewLeafRequest() {
  return { leafId: "", signingJobs: void 0 };
}
var RenewLeafRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.leafId !== "") {
      writer.uint32(10).string(message.leafId);
    }
    switch (message.signingJobs?.$case) {
      case "renewNodeTimelockSigningJob":
        RenewNodeTimelockSigningJob.encode(message.signingJobs.renewNodeTimelockSigningJob, writer.uint32(18).fork()).join();
        break;
      case "renewRefundTimelockSigningJob":
        RenewRefundTimelockSigningJob.encode(
          message.signingJobs.renewRefundTimelockSigningJob,
          writer.uint32(26).fork()
        ).join();
        break;
      case "renewNodeZeroTimelockSigningJob":
        RenewNodeZeroTimelockSigningJob.encode(
          message.signingJobs.renewNodeZeroTimelockSigningJob,
          writer.uint32(34).fork()
        ).join();
        break;
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRenewLeafRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.leafId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.signingJobs = {
            $case: "renewNodeTimelockSigningJob",
            renewNodeTimelockSigningJob: RenewNodeTimelockSigningJob.decode(reader, reader.uint32())
          };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.signingJobs = {
            $case: "renewRefundTimelockSigningJob",
            renewRefundTimelockSigningJob: RenewRefundTimelockSigningJob.decode(reader, reader.uint32())
          };
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.signingJobs = {
            $case: "renewNodeZeroTimelockSigningJob",
            renewNodeZeroTimelockSigningJob: RenewNodeZeroTimelockSigningJob.decode(reader, reader.uint32())
          };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      leafId: isSet3(object.leafId) ? globalThis.String(object.leafId) : "",
      signingJobs: isSet3(object.renewNodeTimelockSigningJob) ? {
        $case: "renewNodeTimelockSigningJob",
        renewNodeTimelockSigningJob: RenewNodeTimelockSigningJob.fromJSON(object.renewNodeTimelockSigningJob)
      } : isSet3(object.renewRefundTimelockSigningJob) ? {
        $case: "renewRefundTimelockSigningJob",
        renewRefundTimelockSigningJob: RenewRefundTimelockSigningJob.fromJSON(object.renewRefundTimelockSigningJob)
      } : isSet3(object.renewNodeZeroTimelockSigningJob) ? {
        $case: "renewNodeZeroTimelockSigningJob",
        renewNodeZeroTimelockSigningJob: RenewNodeZeroTimelockSigningJob.fromJSON(
          object.renewNodeZeroTimelockSigningJob
        )
      } : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.leafId !== "") {
      obj.leafId = message.leafId;
    }
    if (message.signingJobs?.$case === "renewNodeTimelockSigningJob") {
      obj.renewNodeTimelockSigningJob = RenewNodeTimelockSigningJob.toJSON(
        message.signingJobs.renewNodeTimelockSigningJob
      );
    } else if (message.signingJobs?.$case === "renewRefundTimelockSigningJob") {
      obj.renewRefundTimelockSigningJob = RenewRefundTimelockSigningJob.toJSON(
        message.signingJobs.renewRefundTimelockSigningJob
      );
    } else if (message.signingJobs?.$case === "renewNodeZeroTimelockSigningJob") {
      obj.renewNodeZeroTimelockSigningJob = RenewNodeZeroTimelockSigningJob.toJSON(
        message.signingJobs.renewNodeZeroTimelockSigningJob
      );
    }
    return obj;
  },
  create(base) {
    return RenewLeafRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRenewLeafRequest();
    message.leafId = object.leafId ?? "";
    switch (object.signingJobs?.$case) {
      case "renewNodeTimelockSigningJob": {
        if (object.signingJobs?.renewNodeTimelockSigningJob !== void 0 && object.signingJobs?.renewNodeTimelockSigningJob !== null) {
          message.signingJobs = {
            $case: "renewNodeTimelockSigningJob",
            renewNodeTimelockSigningJob: RenewNodeTimelockSigningJob.fromPartial(
              object.signingJobs.renewNodeTimelockSigningJob
            )
          };
        }
        break;
      }
      case "renewRefundTimelockSigningJob": {
        if (object.signingJobs?.renewRefundTimelockSigningJob !== void 0 && object.signingJobs?.renewRefundTimelockSigningJob !== null) {
          message.signingJobs = {
            $case: "renewRefundTimelockSigningJob",
            renewRefundTimelockSigningJob: RenewRefundTimelockSigningJob.fromPartial(
              object.signingJobs.renewRefundTimelockSigningJob
            )
          };
        }
        break;
      }
      case "renewNodeZeroTimelockSigningJob": {
        if (object.signingJobs?.renewNodeZeroTimelockSigningJob !== void 0 && object.signingJobs?.renewNodeZeroTimelockSigningJob !== null) {
          message.signingJobs = {
            $case: "renewNodeZeroTimelockSigningJob",
            renewNodeZeroTimelockSigningJob: RenewNodeZeroTimelockSigningJob.fromPartial(
              object.signingJobs.renewNodeZeroTimelockSigningJob
            )
          };
        }
        break;
      }
    }
    return message;
  }
};
function createBaseRenewNodeTimelockSigningJob() {
  return {
    splitNodeTxSigningJob: void 0,
    splitNodeDirectTxSigningJob: void 0,
    nodeTxSigningJob: void 0,
    refundTxSigningJob: void 0,
    directNodeTxSigningJob: void 0,
    directRefundTxSigningJob: void 0,
    directFromCpfpRefundTxSigningJob: void 0
  };
}
var RenewNodeTimelockSigningJob = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.splitNodeTxSigningJob !== void 0) {
      UserSignedTxSigningJob.encode(message.splitNodeTxSigningJob, writer.uint32(10).fork()).join();
    }
    if (message.splitNodeDirectTxSigningJob !== void 0) {
      UserSignedTxSigningJob.encode(message.splitNodeDirectTxSigningJob, writer.uint32(18).fork()).join();
    }
    if (message.nodeTxSigningJob !== void 0) {
      UserSignedTxSigningJob.encode(message.nodeTxSigningJob, writer.uint32(26).fork()).join();
    }
    if (message.refundTxSigningJob !== void 0) {
      UserSignedTxSigningJob.encode(message.refundTxSigningJob, writer.uint32(34).fork()).join();
    }
    if (message.directNodeTxSigningJob !== void 0) {
      UserSignedTxSigningJob.encode(message.directNodeTxSigningJob, writer.uint32(42).fork()).join();
    }
    if (message.directRefundTxSigningJob !== void 0) {
      UserSignedTxSigningJob.encode(message.directRefundTxSigningJob, writer.uint32(50).fork()).join();
    }
    if (message.directFromCpfpRefundTxSigningJob !== void 0) {
      UserSignedTxSigningJob.encode(message.directFromCpfpRefundTxSigningJob, writer.uint32(58).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRenewNodeTimelockSigningJob();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.splitNodeTxSigningJob = UserSignedTxSigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.splitNodeDirectTxSigningJob = UserSignedTxSigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.nodeTxSigningJob = UserSignedTxSigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.refundTxSigningJob = UserSignedTxSigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.directNodeTxSigningJob = UserSignedTxSigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.directRefundTxSigningJob = UserSignedTxSigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.directFromCpfpRefundTxSigningJob = UserSignedTxSigningJob.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      splitNodeTxSigningJob: isSet3(object.splitNodeTxSigningJob) ? UserSignedTxSigningJob.fromJSON(object.splitNodeTxSigningJob) : void 0,
      splitNodeDirectTxSigningJob: isSet3(object.splitNodeDirectTxSigningJob) ? UserSignedTxSigningJob.fromJSON(object.splitNodeDirectTxSigningJob) : void 0,
      nodeTxSigningJob: isSet3(object.nodeTxSigningJob) ? UserSignedTxSigningJob.fromJSON(object.nodeTxSigningJob) : void 0,
      refundTxSigningJob: isSet3(object.refundTxSigningJob) ? UserSignedTxSigningJob.fromJSON(object.refundTxSigningJob) : void 0,
      directNodeTxSigningJob: isSet3(object.directNodeTxSigningJob) ? UserSignedTxSigningJob.fromJSON(object.directNodeTxSigningJob) : void 0,
      directRefundTxSigningJob: isSet3(object.directRefundTxSigningJob) ? UserSignedTxSigningJob.fromJSON(object.directRefundTxSigningJob) : void 0,
      directFromCpfpRefundTxSigningJob: isSet3(object.directFromCpfpRefundTxSigningJob) ? UserSignedTxSigningJob.fromJSON(object.directFromCpfpRefundTxSigningJob) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.splitNodeTxSigningJob !== void 0) {
      obj.splitNodeTxSigningJob = UserSignedTxSigningJob.toJSON(message.splitNodeTxSigningJob);
    }
    if (message.splitNodeDirectTxSigningJob !== void 0) {
      obj.splitNodeDirectTxSigningJob = UserSignedTxSigningJob.toJSON(message.splitNodeDirectTxSigningJob);
    }
    if (message.nodeTxSigningJob !== void 0) {
      obj.nodeTxSigningJob = UserSignedTxSigningJob.toJSON(message.nodeTxSigningJob);
    }
    if (message.refundTxSigningJob !== void 0) {
      obj.refundTxSigningJob = UserSignedTxSigningJob.toJSON(message.refundTxSigningJob);
    }
    if (message.directNodeTxSigningJob !== void 0) {
      obj.directNodeTxSigningJob = UserSignedTxSigningJob.toJSON(message.directNodeTxSigningJob);
    }
    if (message.directRefundTxSigningJob !== void 0) {
      obj.directRefundTxSigningJob = UserSignedTxSigningJob.toJSON(message.directRefundTxSigningJob);
    }
    if (message.directFromCpfpRefundTxSigningJob !== void 0) {
      obj.directFromCpfpRefundTxSigningJob = UserSignedTxSigningJob.toJSON(message.directFromCpfpRefundTxSigningJob);
    }
    return obj;
  },
  create(base) {
    return RenewNodeTimelockSigningJob.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRenewNodeTimelockSigningJob();
    message.splitNodeTxSigningJob = object.splitNodeTxSigningJob !== void 0 && object.splitNodeTxSigningJob !== null ? UserSignedTxSigningJob.fromPartial(object.splitNodeTxSigningJob) : void 0;
    message.splitNodeDirectTxSigningJob = object.splitNodeDirectTxSigningJob !== void 0 && object.splitNodeDirectTxSigningJob !== null ? UserSignedTxSigningJob.fromPartial(object.splitNodeDirectTxSigningJob) : void 0;
    message.nodeTxSigningJob = object.nodeTxSigningJob !== void 0 && object.nodeTxSigningJob !== null ? UserSignedTxSigningJob.fromPartial(object.nodeTxSigningJob) : void 0;
    message.refundTxSigningJob = object.refundTxSigningJob !== void 0 && object.refundTxSigningJob !== null ? UserSignedTxSigningJob.fromPartial(object.refundTxSigningJob) : void 0;
    message.directNodeTxSigningJob = object.directNodeTxSigningJob !== void 0 && object.directNodeTxSigningJob !== null ? UserSignedTxSigningJob.fromPartial(object.directNodeTxSigningJob) : void 0;
    message.directRefundTxSigningJob = object.directRefundTxSigningJob !== void 0 && object.directRefundTxSigningJob !== null ? UserSignedTxSigningJob.fromPartial(object.directRefundTxSigningJob) : void 0;
    message.directFromCpfpRefundTxSigningJob = object.directFromCpfpRefundTxSigningJob !== void 0 && object.directFromCpfpRefundTxSigningJob !== null ? UserSignedTxSigningJob.fromPartial(object.directFromCpfpRefundTxSigningJob) : void 0;
    return message;
  }
};
function createBaseRenewRefundTimelockSigningJob() {
  return {
    nodeTxSigningJob: void 0,
    refundTxSigningJob: void 0,
    directNodeTxSigningJob: void 0,
    directRefundTxSigningJob: void 0,
    directFromCpfpRefundTxSigningJob: void 0
  };
}
var RenewRefundTimelockSigningJob = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.nodeTxSigningJob !== void 0) {
      UserSignedTxSigningJob.encode(message.nodeTxSigningJob, writer.uint32(10).fork()).join();
    }
    if (message.refundTxSigningJob !== void 0) {
      UserSignedTxSigningJob.encode(message.refundTxSigningJob, writer.uint32(18).fork()).join();
    }
    if (message.directNodeTxSigningJob !== void 0) {
      UserSignedTxSigningJob.encode(message.directNodeTxSigningJob, writer.uint32(26).fork()).join();
    }
    if (message.directRefundTxSigningJob !== void 0) {
      UserSignedTxSigningJob.encode(message.directRefundTxSigningJob, writer.uint32(34).fork()).join();
    }
    if (message.directFromCpfpRefundTxSigningJob !== void 0) {
      UserSignedTxSigningJob.encode(message.directFromCpfpRefundTxSigningJob, writer.uint32(42).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRenewRefundTimelockSigningJob();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.nodeTxSigningJob = UserSignedTxSigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.refundTxSigningJob = UserSignedTxSigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.directNodeTxSigningJob = UserSignedTxSigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.directRefundTxSigningJob = UserSignedTxSigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.directFromCpfpRefundTxSigningJob = UserSignedTxSigningJob.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      nodeTxSigningJob: isSet3(object.nodeTxSigningJob) ? UserSignedTxSigningJob.fromJSON(object.nodeTxSigningJob) : void 0,
      refundTxSigningJob: isSet3(object.refundTxSigningJob) ? UserSignedTxSigningJob.fromJSON(object.refundTxSigningJob) : void 0,
      directNodeTxSigningJob: isSet3(object.directNodeTxSigningJob) ? UserSignedTxSigningJob.fromJSON(object.directNodeTxSigningJob) : void 0,
      directRefundTxSigningJob: isSet3(object.directRefundTxSigningJob) ? UserSignedTxSigningJob.fromJSON(object.directRefundTxSigningJob) : void 0,
      directFromCpfpRefundTxSigningJob: isSet3(object.directFromCpfpRefundTxSigningJob) ? UserSignedTxSigningJob.fromJSON(object.directFromCpfpRefundTxSigningJob) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.nodeTxSigningJob !== void 0) {
      obj.nodeTxSigningJob = UserSignedTxSigningJob.toJSON(message.nodeTxSigningJob);
    }
    if (message.refundTxSigningJob !== void 0) {
      obj.refundTxSigningJob = UserSignedTxSigningJob.toJSON(message.refundTxSigningJob);
    }
    if (message.directNodeTxSigningJob !== void 0) {
      obj.directNodeTxSigningJob = UserSignedTxSigningJob.toJSON(message.directNodeTxSigningJob);
    }
    if (message.directRefundTxSigningJob !== void 0) {
      obj.directRefundTxSigningJob = UserSignedTxSigningJob.toJSON(message.directRefundTxSigningJob);
    }
    if (message.directFromCpfpRefundTxSigningJob !== void 0) {
      obj.directFromCpfpRefundTxSigningJob = UserSignedTxSigningJob.toJSON(message.directFromCpfpRefundTxSigningJob);
    }
    return obj;
  },
  create(base) {
    return RenewRefundTimelockSigningJob.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRenewRefundTimelockSigningJob();
    message.nodeTxSigningJob = object.nodeTxSigningJob !== void 0 && object.nodeTxSigningJob !== null ? UserSignedTxSigningJob.fromPartial(object.nodeTxSigningJob) : void 0;
    message.refundTxSigningJob = object.refundTxSigningJob !== void 0 && object.refundTxSigningJob !== null ? UserSignedTxSigningJob.fromPartial(object.refundTxSigningJob) : void 0;
    message.directNodeTxSigningJob = object.directNodeTxSigningJob !== void 0 && object.directNodeTxSigningJob !== null ? UserSignedTxSigningJob.fromPartial(object.directNodeTxSigningJob) : void 0;
    message.directRefundTxSigningJob = object.directRefundTxSigningJob !== void 0 && object.directRefundTxSigningJob !== null ? UserSignedTxSigningJob.fromPartial(object.directRefundTxSigningJob) : void 0;
    message.directFromCpfpRefundTxSigningJob = object.directFromCpfpRefundTxSigningJob !== void 0 && object.directFromCpfpRefundTxSigningJob !== null ? UserSignedTxSigningJob.fromPartial(object.directFromCpfpRefundTxSigningJob) : void 0;
    return message;
  }
};
function createBaseRenewNodeZeroTimelockSigningJob() {
  return {
    nodeTxSigningJob: void 0,
    refundTxSigningJob: void 0,
    directNodeTxSigningJob: void 0,
    directFromCpfpRefundTxSigningJob: void 0
  };
}
var RenewNodeZeroTimelockSigningJob = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.nodeTxSigningJob !== void 0) {
      UserSignedTxSigningJob.encode(message.nodeTxSigningJob, writer.uint32(10).fork()).join();
    }
    if (message.refundTxSigningJob !== void 0) {
      UserSignedTxSigningJob.encode(message.refundTxSigningJob, writer.uint32(18).fork()).join();
    }
    if (message.directNodeTxSigningJob !== void 0) {
      UserSignedTxSigningJob.encode(message.directNodeTxSigningJob, writer.uint32(26).fork()).join();
    }
    if (message.directFromCpfpRefundTxSigningJob !== void 0) {
      UserSignedTxSigningJob.encode(message.directFromCpfpRefundTxSigningJob, writer.uint32(42).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRenewNodeZeroTimelockSigningJob();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.nodeTxSigningJob = UserSignedTxSigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.refundTxSigningJob = UserSignedTxSigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.directNodeTxSigningJob = UserSignedTxSigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.directFromCpfpRefundTxSigningJob = UserSignedTxSigningJob.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      nodeTxSigningJob: isSet3(object.nodeTxSigningJob) ? UserSignedTxSigningJob.fromJSON(object.nodeTxSigningJob) : void 0,
      refundTxSigningJob: isSet3(object.refundTxSigningJob) ? UserSignedTxSigningJob.fromJSON(object.refundTxSigningJob) : void 0,
      directNodeTxSigningJob: isSet3(object.directNodeTxSigningJob) ? UserSignedTxSigningJob.fromJSON(object.directNodeTxSigningJob) : void 0,
      directFromCpfpRefundTxSigningJob: isSet3(object.directFromCpfpRefundTxSigningJob) ? UserSignedTxSigningJob.fromJSON(object.directFromCpfpRefundTxSigningJob) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.nodeTxSigningJob !== void 0) {
      obj.nodeTxSigningJob = UserSignedTxSigningJob.toJSON(message.nodeTxSigningJob);
    }
    if (message.refundTxSigningJob !== void 0) {
      obj.refundTxSigningJob = UserSignedTxSigningJob.toJSON(message.refundTxSigningJob);
    }
    if (message.directNodeTxSigningJob !== void 0) {
      obj.directNodeTxSigningJob = UserSignedTxSigningJob.toJSON(message.directNodeTxSigningJob);
    }
    if (message.directFromCpfpRefundTxSigningJob !== void 0) {
      obj.directFromCpfpRefundTxSigningJob = UserSignedTxSigningJob.toJSON(message.directFromCpfpRefundTxSigningJob);
    }
    return obj;
  },
  create(base) {
    return RenewNodeZeroTimelockSigningJob.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRenewNodeZeroTimelockSigningJob();
    message.nodeTxSigningJob = object.nodeTxSigningJob !== void 0 && object.nodeTxSigningJob !== null ? UserSignedTxSigningJob.fromPartial(object.nodeTxSigningJob) : void 0;
    message.refundTxSigningJob = object.refundTxSigningJob !== void 0 && object.refundTxSigningJob !== null ? UserSignedTxSigningJob.fromPartial(object.refundTxSigningJob) : void 0;
    message.directNodeTxSigningJob = object.directNodeTxSigningJob !== void 0 && object.directNodeTxSigningJob !== null ? UserSignedTxSigningJob.fromPartial(object.directNodeTxSigningJob) : void 0;
    message.directFromCpfpRefundTxSigningJob = object.directFromCpfpRefundTxSigningJob !== void 0 && object.directFromCpfpRefundTxSigningJob !== null ? UserSignedTxSigningJob.fromPartial(object.directFromCpfpRefundTxSigningJob) : void 0;
    return message;
  }
};
function createBaseRenewLeafResponse() {
  return { renewResult: void 0 };
}
var RenewLeafResponse = {
  encode(message, writer = new BinaryWriter4()) {
    switch (message.renewResult?.$case) {
      case "renewNodeTimelockResult":
        RenewNodeTimelockResult.encode(message.renewResult.renewNodeTimelockResult, writer.uint32(10).fork()).join();
        break;
      case "renewRefundTimelockResult":
        RenewRefundTimelockResult.encode(message.renewResult.renewRefundTimelockResult, writer.uint32(18).fork()).join();
        break;
      case "renewNodeZeroTimelockResult":
        RenewNodeZeroTimelockResult.encode(message.renewResult.renewNodeZeroTimelockResult, writer.uint32(26).fork()).join();
        break;
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRenewLeafResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.renewResult = {
            $case: "renewNodeTimelockResult",
            renewNodeTimelockResult: RenewNodeTimelockResult.decode(reader, reader.uint32())
          };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.renewResult = {
            $case: "renewRefundTimelockResult",
            renewRefundTimelockResult: RenewRefundTimelockResult.decode(reader, reader.uint32())
          };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.renewResult = {
            $case: "renewNodeZeroTimelockResult",
            renewNodeZeroTimelockResult: RenewNodeZeroTimelockResult.decode(reader, reader.uint32())
          };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      renewResult: isSet3(object.renewNodeTimelockResult) ? {
        $case: "renewNodeTimelockResult",
        renewNodeTimelockResult: RenewNodeTimelockResult.fromJSON(object.renewNodeTimelockResult)
      } : isSet3(object.renewRefundTimelockResult) ? {
        $case: "renewRefundTimelockResult",
        renewRefundTimelockResult: RenewRefundTimelockResult.fromJSON(object.renewRefundTimelockResult)
      } : isSet3(object.renewNodeZeroTimelockResult) ? {
        $case: "renewNodeZeroTimelockResult",
        renewNodeZeroTimelockResult: RenewNodeZeroTimelockResult.fromJSON(object.renewNodeZeroTimelockResult)
      } : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.renewResult?.$case === "renewNodeTimelockResult") {
      obj.renewNodeTimelockResult = RenewNodeTimelockResult.toJSON(message.renewResult.renewNodeTimelockResult);
    } else if (message.renewResult?.$case === "renewRefundTimelockResult") {
      obj.renewRefundTimelockResult = RenewRefundTimelockResult.toJSON(message.renewResult.renewRefundTimelockResult);
    } else if (message.renewResult?.$case === "renewNodeZeroTimelockResult") {
      obj.renewNodeZeroTimelockResult = RenewNodeZeroTimelockResult.toJSON(
        message.renewResult.renewNodeZeroTimelockResult
      );
    }
    return obj;
  },
  create(base) {
    return RenewLeafResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRenewLeafResponse();
    switch (object.renewResult?.$case) {
      case "renewNodeTimelockResult": {
        if (object.renewResult?.renewNodeTimelockResult !== void 0 && object.renewResult?.renewNodeTimelockResult !== null) {
          message.renewResult = {
            $case: "renewNodeTimelockResult",
            renewNodeTimelockResult: RenewNodeTimelockResult.fromPartial(object.renewResult.renewNodeTimelockResult)
          };
        }
        break;
      }
      case "renewRefundTimelockResult": {
        if (object.renewResult?.renewRefundTimelockResult !== void 0 && object.renewResult?.renewRefundTimelockResult !== null) {
          message.renewResult = {
            $case: "renewRefundTimelockResult",
            renewRefundTimelockResult: RenewRefundTimelockResult.fromPartial(
              object.renewResult.renewRefundTimelockResult
            )
          };
        }
        break;
      }
      case "renewNodeZeroTimelockResult": {
        if (object.renewResult?.renewNodeZeroTimelockResult !== void 0 && object.renewResult?.renewNodeZeroTimelockResult !== null) {
          message.renewResult = {
            $case: "renewNodeZeroTimelockResult",
            renewNodeZeroTimelockResult: RenewNodeZeroTimelockResult.fromPartial(
              object.renewResult.renewNodeZeroTimelockResult
            )
          };
        }
        break;
      }
    }
    return message;
  }
};
function createBaseRenewNodeTimelockResult() {
  return { splitNode: void 0, node: void 0 };
}
var RenewNodeTimelockResult = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.splitNode !== void 0) {
      TreeNode.encode(message.splitNode, writer.uint32(10).fork()).join();
    }
    if (message.node !== void 0) {
      TreeNode.encode(message.node, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRenewNodeTimelockResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.splitNode = TreeNode.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.node = TreeNode.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      splitNode: isSet3(object.splitNode) ? TreeNode.fromJSON(object.splitNode) : void 0,
      node: isSet3(object.node) ? TreeNode.fromJSON(object.node) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.splitNode !== void 0) {
      obj.splitNode = TreeNode.toJSON(message.splitNode);
    }
    if (message.node !== void 0) {
      obj.node = TreeNode.toJSON(message.node);
    }
    return obj;
  },
  create(base) {
    return RenewNodeTimelockResult.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRenewNodeTimelockResult();
    message.splitNode = object.splitNode !== void 0 && object.splitNode !== null ? TreeNode.fromPartial(object.splitNode) : void 0;
    message.node = object.node !== void 0 && object.node !== null ? TreeNode.fromPartial(object.node) : void 0;
    return message;
  }
};
function createBaseRenewRefundTimelockResult() {
  return { node: void 0 };
}
var RenewRefundTimelockResult = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.node !== void 0) {
      TreeNode.encode(message.node, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRenewRefundTimelockResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.node = TreeNode.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { node: isSet3(object.node) ? TreeNode.fromJSON(object.node) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.node !== void 0) {
      obj.node = TreeNode.toJSON(message.node);
    }
    return obj;
  },
  create(base) {
    return RenewRefundTimelockResult.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRenewRefundTimelockResult();
    message.node = object.node !== void 0 && object.node !== null ? TreeNode.fromPartial(object.node) : void 0;
    return message;
  }
};
function createBaseRenewNodeZeroTimelockResult() {
  return { splitNode: void 0, node: void 0 };
}
var RenewNodeZeroTimelockResult = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.splitNode !== void 0) {
      TreeNode.encode(message.splitNode, writer.uint32(10).fork()).join();
    }
    if (message.node !== void 0) {
      TreeNode.encode(message.node, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRenewNodeZeroTimelockResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.splitNode = TreeNode.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.node = TreeNode.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      splitNode: isSet3(object.splitNode) ? TreeNode.fromJSON(object.splitNode) : void 0,
      node: isSet3(object.node) ? TreeNode.fromJSON(object.node) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.splitNode !== void 0) {
      obj.splitNode = TreeNode.toJSON(message.splitNode);
    }
    if (message.node !== void 0) {
      obj.node = TreeNode.toJSON(message.node);
    }
    return obj;
  },
  create(base) {
    return RenewNodeZeroTimelockResult.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRenewNodeZeroTimelockResult();
    message.splitNode = object.splitNode !== void 0 && object.splitNode !== null ? TreeNode.fromPartial(object.splitNode) : void 0;
    message.node = object.node !== void 0 && object.node !== null ? TreeNode.fromPartial(object.node) : void 0;
    return message;
  }
};
function createBaseNodeSignatureShares() {
  return {
    nodeId: "",
    nodeTxSigningResult: void 0,
    refundTxSigningResult: void 0,
    verifyingKey: new Uint8Array(0),
    directNodeTxSigningResult: void 0,
    directRefundTxSigningResult: void 0,
    directFromCpfpRefundTxSigningResult: void 0
  };
}
var NodeSignatureShares = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.nodeId !== "") {
      writer.uint32(10).string(message.nodeId);
    }
    if (message.nodeTxSigningResult !== void 0) {
      SigningResult2.encode(message.nodeTxSigningResult, writer.uint32(18).fork()).join();
    }
    if (message.refundTxSigningResult !== void 0) {
      SigningResult2.encode(message.refundTxSigningResult, writer.uint32(26).fork()).join();
    }
    if (message.verifyingKey.length !== 0) {
      writer.uint32(34).bytes(message.verifyingKey);
    }
    if (message.directNodeTxSigningResult !== void 0) {
      SigningResult2.encode(message.directNodeTxSigningResult, writer.uint32(42).fork()).join();
    }
    if (message.directRefundTxSigningResult !== void 0) {
      SigningResult2.encode(message.directRefundTxSigningResult, writer.uint32(50).fork()).join();
    }
    if (message.directFromCpfpRefundTxSigningResult !== void 0) {
      SigningResult2.encode(message.directFromCpfpRefundTxSigningResult, writer.uint32(58).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseNodeSignatureShares();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.nodeId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.nodeTxSigningResult = SigningResult2.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.refundTxSigningResult = SigningResult2.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.verifyingKey = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.directNodeTxSigningResult = SigningResult2.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.directRefundTxSigningResult = SigningResult2.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.directFromCpfpRefundTxSigningResult = SigningResult2.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      nodeId: isSet3(object.nodeId) ? globalThis.String(object.nodeId) : "",
      nodeTxSigningResult: isSet3(object.nodeTxSigningResult) ? SigningResult2.fromJSON(object.nodeTxSigningResult) : void 0,
      refundTxSigningResult: isSet3(object.refundTxSigningResult) ? SigningResult2.fromJSON(object.refundTxSigningResult) : void 0,
      verifyingKey: isSet3(object.verifyingKey) ? bytesFromBase642(object.verifyingKey) : new Uint8Array(0),
      directNodeTxSigningResult: isSet3(object.directNodeTxSigningResult) ? SigningResult2.fromJSON(object.directNodeTxSigningResult) : void 0,
      directRefundTxSigningResult: isSet3(object.directRefundTxSigningResult) ? SigningResult2.fromJSON(object.directRefundTxSigningResult) : void 0,
      directFromCpfpRefundTxSigningResult: isSet3(object.directFromCpfpRefundTxSigningResult) ? SigningResult2.fromJSON(object.directFromCpfpRefundTxSigningResult) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.nodeId !== "") {
      obj.nodeId = message.nodeId;
    }
    if (message.nodeTxSigningResult !== void 0) {
      obj.nodeTxSigningResult = SigningResult2.toJSON(message.nodeTxSigningResult);
    }
    if (message.refundTxSigningResult !== void 0) {
      obj.refundTxSigningResult = SigningResult2.toJSON(message.refundTxSigningResult);
    }
    if (message.verifyingKey.length !== 0) {
      obj.verifyingKey = base64FromBytes2(message.verifyingKey);
    }
    if (message.directNodeTxSigningResult !== void 0) {
      obj.directNodeTxSigningResult = SigningResult2.toJSON(message.directNodeTxSigningResult);
    }
    if (message.directRefundTxSigningResult !== void 0) {
      obj.directRefundTxSigningResult = SigningResult2.toJSON(message.directRefundTxSigningResult);
    }
    if (message.directFromCpfpRefundTxSigningResult !== void 0) {
      obj.directFromCpfpRefundTxSigningResult = SigningResult2.toJSON(message.directFromCpfpRefundTxSigningResult);
    }
    return obj;
  },
  create(base) {
    return NodeSignatureShares.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseNodeSignatureShares();
    message.nodeId = object.nodeId ?? "";
    message.nodeTxSigningResult = object.nodeTxSigningResult !== void 0 && object.nodeTxSigningResult !== null ? SigningResult2.fromPartial(object.nodeTxSigningResult) : void 0;
    message.refundTxSigningResult = object.refundTxSigningResult !== void 0 && object.refundTxSigningResult !== null ? SigningResult2.fromPartial(object.refundTxSigningResult) : void 0;
    message.verifyingKey = object.verifyingKey ?? new Uint8Array(0);
    message.directNodeTxSigningResult = object.directNodeTxSigningResult !== void 0 && object.directNodeTxSigningResult !== null ? SigningResult2.fromPartial(object.directNodeTxSigningResult) : void 0;
    message.directRefundTxSigningResult = object.directRefundTxSigningResult !== void 0 && object.directRefundTxSigningResult !== null ? SigningResult2.fromPartial(object.directRefundTxSigningResult) : void 0;
    message.directFromCpfpRefundTxSigningResult = object.directFromCpfpRefundTxSigningResult !== void 0 && object.directFromCpfpRefundTxSigningResult !== null ? SigningResult2.fromPartial(object.directFromCpfpRefundTxSigningResult) : void 0;
    return message;
  }
};
function createBaseNodeSignatures() {
  return {
    nodeId: "",
    nodeTxSignature: new Uint8Array(0),
    refundTxSignature: new Uint8Array(0),
    directNodeTxSignature: new Uint8Array(0),
    directRefundTxSignature: new Uint8Array(0),
    directFromCpfpRefundTxSignature: new Uint8Array(0)
  };
}
var NodeSignatures = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.nodeId !== "") {
      writer.uint32(10).string(message.nodeId);
    }
    if (message.nodeTxSignature.length !== 0) {
      writer.uint32(18).bytes(message.nodeTxSignature);
    }
    if (message.refundTxSignature.length !== 0) {
      writer.uint32(26).bytes(message.refundTxSignature);
    }
    if (message.directNodeTxSignature.length !== 0) {
      writer.uint32(34).bytes(message.directNodeTxSignature);
    }
    if (message.directRefundTxSignature.length !== 0) {
      writer.uint32(42).bytes(message.directRefundTxSignature);
    }
    if (message.directFromCpfpRefundTxSignature.length !== 0) {
      writer.uint32(50).bytes(message.directFromCpfpRefundTxSignature);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseNodeSignatures();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.nodeId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.nodeTxSignature = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.refundTxSignature = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.directNodeTxSignature = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.directRefundTxSignature = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.directFromCpfpRefundTxSignature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      nodeId: isSet3(object.nodeId) ? globalThis.String(object.nodeId) : "",
      nodeTxSignature: isSet3(object.nodeTxSignature) ? bytesFromBase642(object.nodeTxSignature) : new Uint8Array(0),
      refundTxSignature: isSet3(object.refundTxSignature) ? bytesFromBase642(object.refundTxSignature) : new Uint8Array(0),
      directNodeTxSignature: isSet3(object.directNodeTxSignature) ? bytesFromBase642(object.directNodeTxSignature) : new Uint8Array(0),
      directRefundTxSignature: isSet3(object.directRefundTxSignature) ? bytesFromBase642(object.directRefundTxSignature) : new Uint8Array(0),
      directFromCpfpRefundTxSignature: isSet3(object.directFromCpfpRefundTxSignature) ? bytesFromBase642(object.directFromCpfpRefundTxSignature) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.nodeId !== "") {
      obj.nodeId = message.nodeId;
    }
    if (message.nodeTxSignature.length !== 0) {
      obj.nodeTxSignature = base64FromBytes2(message.nodeTxSignature);
    }
    if (message.refundTxSignature.length !== 0) {
      obj.refundTxSignature = base64FromBytes2(message.refundTxSignature);
    }
    if (message.directNodeTxSignature.length !== 0) {
      obj.directNodeTxSignature = base64FromBytes2(message.directNodeTxSignature);
    }
    if (message.directRefundTxSignature.length !== 0) {
      obj.directRefundTxSignature = base64FromBytes2(message.directRefundTxSignature);
    }
    if (message.directFromCpfpRefundTxSignature.length !== 0) {
      obj.directFromCpfpRefundTxSignature = base64FromBytes2(message.directFromCpfpRefundTxSignature);
    }
    return obj;
  },
  create(base) {
    return NodeSignatures.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseNodeSignatures();
    message.nodeId = object.nodeId ?? "";
    message.nodeTxSignature = object.nodeTxSignature ?? new Uint8Array(0);
    message.refundTxSignature = object.refundTxSignature ?? new Uint8Array(0);
    message.directNodeTxSignature = object.directNodeTxSignature ?? new Uint8Array(0);
    message.directRefundTxSignature = object.directRefundTxSignature ?? new Uint8Array(0);
    message.directFromCpfpRefundTxSignature = object.directFromCpfpRefundTxSignature ?? new Uint8Array(0);
    return message;
  }
};
function createBaseStartTreeCreationRequest() {
  return {
    identityPublicKey: new Uint8Array(0),
    onChainUtxo: void 0,
    rootTxSigningJob: void 0,
    refundTxSigningJob: void 0,
    directRootTxSigningJob: void 0,
    directRefundTxSigningJob: void 0,
    directFromCpfpRefundTxSigningJob: void 0
  };
}
var StartTreeCreationRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.identityPublicKey.length !== 0) {
      writer.uint32(10).bytes(message.identityPublicKey);
    }
    if (message.onChainUtxo !== void 0) {
      UTXO.encode(message.onChainUtxo, writer.uint32(18).fork()).join();
    }
    if (message.rootTxSigningJob !== void 0) {
      SigningJob.encode(message.rootTxSigningJob, writer.uint32(26).fork()).join();
    }
    if (message.refundTxSigningJob !== void 0) {
      SigningJob.encode(message.refundTxSigningJob, writer.uint32(34).fork()).join();
    }
    if (message.directRootTxSigningJob !== void 0) {
      SigningJob.encode(message.directRootTxSigningJob, writer.uint32(42).fork()).join();
    }
    if (message.directRefundTxSigningJob !== void 0) {
      SigningJob.encode(message.directRefundTxSigningJob, writer.uint32(50).fork()).join();
    }
    if (message.directFromCpfpRefundTxSigningJob !== void 0) {
      SigningJob.encode(message.directFromCpfpRefundTxSigningJob, writer.uint32(58).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseStartTreeCreationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.identityPublicKey = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.onChainUtxo = UTXO.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.rootTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.refundTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.directRootTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.directRefundTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.directFromCpfpRefundTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      identityPublicKey: isSet3(object.identityPublicKey) ? bytesFromBase642(object.identityPublicKey) : new Uint8Array(0),
      onChainUtxo: isSet3(object.onChainUtxo) ? UTXO.fromJSON(object.onChainUtxo) : void 0,
      rootTxSigningJob: isSet3(object.rootTxSigningJob) ? SigningJob.fromJSON(object.rootTxSigningJob) : void 0,
      refundTxSigningJob: isSet3(object.refundTxSigningJob) ? SigningJob.fromJSON(object.refundTxSigningJob) : void 0,
      directRootTxSigningJob: isSet3(object.directRootTxSigningJob) ? SigningJob.fromJSON(object.directRootTxSigningJob) : void 0,
      directRefundTxSigningJob: isSet3(object.directRefundTxSigningJob) ? SigningJob.fromJSON(object.directRefundTxSigningJob) : void 0,
      directFromCpfpRefundTxSigningJob: isSet3(object.directFromCpfpRefundTxSigningJob) ? SigningJob.fromJSON(object.directFromCpfpRefundTxSigningJob) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.identityPublicKey.length !== 0) {
      obj.identityPublicKey = base64FromBytes2(message.identityPublicKey);
    }
    if (message.onChainUtxo !== void 0) {
      obj.onChainUtxo = UTXO.toJSON(message.onChainUtxo);
    }
    if (message.rootTxSigningJob !== void 0) {
      obj.rootTxSigningJob = SigningJob.toJSON(message.rootTxSigningJob);
    }
    if (message.refundTxSigningJob !== void 0) {
      obj.refundTxSigningJob = SigningJob.toJSON(message.refundTxSigningJob);
    }
    if (message.directRootTxSigningJob !== void 0) {
      obj.directRootTxSigningJob = SigningJob.toJSON(message.directRootTxSigningJob);
    }
    if (message.directRefundTxSigningJob !== void 0) {
      obj.directRefundTxSigningJob = SigningJob.toJSON(message.directRefundTxSigningJob);
    }
    if (message.directFromCpfpRefundTxSigningJob !== void 0) {
      obj.directFromCpfpRefundTxSigningJob = SigningJob.toJSON(message.directFromCpfpRefundTxSigningJob);
    }
    return obj;
  },
  create(base) {
    return StartTreeCreationRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseStartTreeCreationRequest();
    message.identityPublicKey = object.identityPublicKey ?? new Uint8Array(0);
    message.onChainUtxo = object.onChainUtxo !== void 0 && object.onChainUtxo !== null ? UTXO.fromPartial(object.onChainUtxo) : void 0;
    message.rootTxSigningJob = object.rootTxSigningJob !== void 0 && object.rootTxSigningJob !== null ? SigningJob.fromPartial(object.rootTxSigningJob) : void 0;
    message.refundTxSigningJob = object.refundTxSigningJob !== void 0 && object.refundTxSigningJob !== null ? SigningJob.fromPartial(object.refundTxSigningJob) : void 0;
    message.directRootTxSigningJob = object.directRootTxSigningJob !== void 0 && object.directRootTxSigningJob !== null ? SigningJob.fromPartial(object.directRootTxSigningJob) : void 0;
    message.directRefundTxSigningJob = object.directRefundTxSigningJob !== void 0 && object.directRefundTxSigningJob !== null ? SigningJob.fromPartial(object.directRefundTxSigningJob) : void 0;
    message.directFromCpfpRefundTxSigningJob = object.directFromCpfpRefundTxSigningJob !== void 0 && object.directFromCpfpRefundTxSigningJob !== null ? SigningJob.fromPartial(object.directFromCpfpRefundTxSigningJob) : void 0;
    return message;
  }
};
function createBaseStartTreeCreationResponse() {
  return { treeId: "", rootNodeSignatureShares: void 0 };
}
var StartTreeCreationResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.treeId !== "") {
      writer.uint32(10).string(message.treeId);
    }
    if (message.rootNodeSignatureShares !== void 0) {
      NodeSignatureShares.encode(message.rootNodeSignatureShares, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseStartTreeCreationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.treeId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.rootNodeSignatureShares = NodeSignatureShares.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      treeId: isSet3(object.treeId) ? globalThis.String(object.treeId) : "",
      rootNodeSignatureShares: isSet3(object.rootNodeSignatureShares) ? NodeSignatureShares.fromJSON(object.rootNodeSignatureShares) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.treeId !== "") {
      obj.treeId = message.treeId;
    }
    if (message.rootNodeSignatureShares !== void 0) {
      obj.rootNodeSignatureShares = NodeSignatureShares.toJSON(message.rootNodeSignatureShares);
    }
    return obj;
  },
  create(base) {
    return StartTreeCreationResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseStartTreeCreationResponse();
    message.treeId = object.treeId ?? "";
    message.rootNodeSignatureShares = object.rootNodeSignatureShares !== void 0 && object.rootNodeSignatureShares !== null ? NodeSignatureShares.fromPartial(object.rootNodeSignatureShares) : void 0;
    return message;
  }
};
function createBaseStartDepositTreeCreationRequest() {
  return {
    identityPublicKey: new Uint8Array(0),
    onChainUtxo: void 0,
    rootTxSigningJob: void 0,
    refundTxSigningJob: void 0,
    directRootTxSigningJob: void 0,
    directRefundTxSigningJob: void 0,
    directFromCpfpRefundTxSigningJob: void 0
  };
}
var StartDepositTreeCreationRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.identityPublicKey.length !== 0) {
      writer.uint32(10).bytes(message.identityPublicKey);
    }
    if (message.onChainUtxo !== void 0) {
      UTXO.encode(message.onChainUtxo, writer.uint32(18).fork()).join();
    }
    if (message.rootTxSigningJob !== void 0) {
      SigningJob.encode(message.rootTxSigningJob, writer.uint32(26).fork()).join();
    }
    if (message.refundTxSigningJob !== void 0) {
      SigningJob.encode(message.refundTxSigningJob, writer.uint32(34).fork()).join();
    }
    if (message.directRootTxSigningJob !== void 0) {
      SigningJob.encode(message.directRootTxSigningJob, writer.uint32(42).fork()).join();
    }
    if (message.directRefundTxSigningJob !== void 0) {
      SigningJob.encode(message.directRefundTxSigningJob, writer.uint32(50).fork()).join();
    }
    if (message.directFromCpfpRefundTxSigningJob !== void 0) {
      SigningJob.encode(message.directFromCpfpRefundTxSigningJob, writer.uint32(58).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseStartDepositTreeCreationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.identityPublicKey = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.onChainUtxo = UTXO.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.rootTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.refundTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.directRootTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.directRefundTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.directFromCpfpRefundTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      identityPublicKey: isSet3(object.identityPublicKey) ? bytesFromBase642(object.identityPublicKey) : new Uint8Array(0),
      onChainUtxo: isSet3(object.onChainUtxo) ? UTXO.fromJSON(object.onChainUtxo) : void 0,
      rootTxSigningJob: isSet3(object.rootTxSigningJob) ? SigningJob.fromJSON(object.rootTxSigningJob) : void 0,
      refundTxSigningJob: isSet3(object.refundTxSigningJob) ? SigningJob.fromJSON(object.refundTxSigningJob) : void 0,
      directRootTxSigningJob: isSet3(object.directRootTxSigningJob) ? SigningJob.fromJSON(object.directRootTxSigningJob) : void 0,
      directRefundTxSigningJob: isSet3(object.directRefundTxSigningJob) ? SigningJob.fromJSON(object.directRefundTxSigningJob) : void 0,
      directFromCpfpRefundTxSigningJob: isSet3(object.directFromCpfpRefundTxSigningJob) ? SigningJob.fromJSON(object.directFromCpfpRefundTxSigningJob) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.identityPublicKey.length !== 0) {
      obj.identityPublicKey = base64FromBytes2(message.identityPublicKey);
    }
    if (message.onChainUtxo !== void 0) {
      obj.onChainUtxo = UTXO.toJSON(message.onChainUtxo);
    }
    if (message.rootTxSigningJob !== void 0) {
      obj.rootTxSigningJob = SigningJob.toJSON(message.rootTxSigningJob);
    }
    if (message.refundTxSigningJob !== void 0) {
      obj.refundTxSigningJob = SigningJob.toJSON(message.refundTxSigningJob);
    }
    if (message.directRootTxSigningJob !== void 0) {
      obj.directRootTxSigningJob = SigningJob.toJSON(message.directRootTxSigningJob);
    }
    if (message.directRefundTxSigningJob !== void 0) {
      obj.directRefundTxSigningJob = SigningJob.toJSON(message.directRefundTxSigningJob);
    }
    if (message.directFromCpfpRefundTxSigningJob !== void 0) {
      obj.directFromCpfpRefundTxSigningJob = SigningJob.toJSON(message.directFromCpfpRefundTxSigningJob);
    }
    return obj;
  },
  create(base) {
    return StartDepositTreeCreationRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseStartDepositTreeCreationRequest();
    message.identityPublicKey = object.identityPublicKey ?? new Uint8Array(0);
    message.onChainUtxo = object.onChainUtxo !== void 0 && object.onChainUtxo !== null ? UTXO.fromPartial(object.onChainUtxo) : void 0;
    message.rootTxSigningJob = object.rootTxSigningJob !== void 0 && object.rootTxSigningJob !== null ? SigningJob.fromPartial(object.rootTxSigningJob) : void 0;
    message.refundTxSigningJob = object.refundTxSigningJob !== void 0 && object.refundTxSigningJob !== null ? SigningJob.fromPartial(object.refundTxSigningJob) : void 0;
    message.directRootTxSigningJob = object.directRootTxSigningJob !== void 0 && object.directRootTxSigningJob !== null ? SigningJob.fromPartial(object.directRootTxSigningJob) : void 0;
    message.directRefundTxSigningJob = object.directRefundTxSigningJob !== void 0 && object.directRefundTxSigningJob !== null ? SigningJob.fromPartial(object.directRefundTxSigningJob) : void 0;
    message.directFromCpfpRefundTxSigningJob = object.directFromCpfpRefundTxSigningJob !== void 0 && object.directFromCpfpRefundTxSigningJob !== null ? SigningJob.fromPartial(object.directFromCpfpRefundTxSigningJob) : void 0;
    return message;
  }
};
function createBaseStartDepositTreeCreationResponse() {
  return { treeId: "", rootNodeSignatureShares: void 0 };
}
var StartDepositTreeCreationResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.treeId !== "") {
      writer.uint32(10).string(message.treeId);
    }
    if (message.rootNodeSignatureShares !== void 0) {
      NodeSignatureShares.encode(message.rootNodeSignatureShares, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseStartDepositTreeCreationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.treeId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.rootNodeSignatureShares = NodeSignatureShares.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      treeId: isSet3(object.treeId) ? globalThis.String(object.treeId) : "",
      rootNodeSignatureShares: isSet3(object.rootNodeSignatureShares) ? NodeSignatureShares.fromJSON(object.rootNodeSignatureShares) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.treeId !== "") {
      obj.treeId = message.treeId;
    }
    if (message.rootNodeSignatureShares !== void 0) {
      obj.rootNodeSignatureShares = NodeSignatureShares.toJSON(message.rootNodeSignatureShares);
    }
    return obj;
  },
  create(base) {
    return StartDepositTreeCreationResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseStartDepositTreeCreationResponse();
    message.treeId = object.treeId ?? "";
    message.rootNodeSignatureShares = object.rootNodeSignatureShares !== void 0 && object.rootNodeSignatureShares !== null ? NodeSignatureShares.fromPartial(object.rootNodeSignatureShares) : void 0;
    return message;
  }
};
function createBaseTokenOutputToSpend() {
  return { prevTokenTransactionHash: new Uint8Array(0), prevTokenTransactionVout: 0 };
}
var TokenOutputToSpend = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.prevTokenTransactionHash.length !== 0) {
      writer.uint32(10).bytes(message.prevTokenTransactionHash);
    }
    if (message.prevTokenTransactionVout !== 0) {
      writer.uint32(16).uint32(message.prevTokenTransactionVout);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTokenOutputToSpend();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.prevTokenTransactionHash = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.prevTokenTransactionVout = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      prevTokenTransactionHash: isSet3(object.prevTokenTransactionHash) ? bytesFromBase642(object.prevTokenTransactionHash) : new Uint8Array(0),
      prevTokenTransactionVout: isSet3(object.prevTokenTransactionVout) ? globalThis.Number(object.prevTokenTransactionVout) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.prevTokenTransactionHash.length !== 0) {
      obj.prevTokenTransactionHash = base64FromBytes2(message.prevTokenTransactionHash);
    }
    if (message.prevTokenTransactionVout !== 0) {
      obj.prevTokenTransactionVout = Math.round(message.prevTokenTransactionVout);
    }
    return obj;
  },
  create(base) {
    return TokenOutputToSpend.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseTokenOutputToSpend();
    message.prevTokenTransactionHash = object.prevTokenTransactionHash ?? new Uint8Array(0);
    message.prevTokenTransactionVout = object.prevTokenTransactionVout ?? 0;
    return message;
  }
};
function createBaseTokenTransferInput() {
  return { outputsToSpend: [] };
}
var TokenTransferInput = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.outputsToSpend) {
      TokenOutputToSpend.encode(v, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTokenTransferInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.outputsToSpend.push(TokenOutputToSpend.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      outputsToSpend: globalThis.Array.isArray(object?.outputsToSpend) ? object.outputsToSpend.map((e) => TokenOutputToSpend.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.outputsToSpend?.length) {
      obj.outputsToSpend = message.outputsToSpend.map((e) => TokenOutputToSpend.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return TokenTransferInput.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseTokenTransferInput();
    message.outputsToSpend = object.outputsToSpend?.map((e) => TokenOutputToSpend.fromPartial(e)) || [];
    return message;
  }
};
function createBaseTokenMintInput() {
  return { issuerPublicKey: new Uint8Array(0), issuerProvidedTimestamp: 0, tokenIdentifier: void 0 };
}
var TokenMintInput = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.issuerPublicKey.length !== 0) {
      writer.uint32(10).bytes(message.issuerPublicKey);
    }
    if (message.issuerProvidedTimestamp !== 0) {
      writer.uint32(16).uint64(message.issuerProvidedTimestamp);
    }
    if (message.tokenIdentifier !== void 0) {
      writer.uint32(26).bytes(message.tokenIdentifier);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTokenMintInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.issuerPublicKey = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.issuerProvidedTimestamp = longToNumber2(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.tokenIdentifier = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      issuerPublicKey: isSet3(object.issuerPublicKey) ? bytesFromBase642(object.issuerPublicKey) : new Uint8Array(0),
      issuerProvidedTimestamp: isSet3(object.issuerProvidedTimestamp) ? globalThis.Number(object.issuerProvidedTimestamp) : 0,
      tokenIdentifier: isSet3(object.tokenIdentifier) ? bytesFromBase642(object.tokenIdentifier) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.issuerPublicKey.length !== 0) {
      obj.issuerPublicKey = base64FromBytes2(message.issuerPublicKey);
    }
    if (message.issuerProvidedTimestamp !== 0) {
      obj.issuerProvidedTimestamp = Math.round(message.issuerProvidedTimestamp);
    }
    if (message.tokenIdentifier !== void 0) {
      obj.tokenIdentifier = base64FromBytes2(message.tokenIdentifier);
    }
    return obj;
  },
  create(base) {
    return TokenMintInput.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseTokenMintInput();
    message.issuerPublicKey = object.issuerPublicKey ?? new Uint8Array(0);
    message.issuerProvidedTimestamp = object.issuerProvidedTimestamp ?? 0;
    message.tokenIdentifier = object.tokenIdentifier ?? void 0;
    return message;
  }
};
function createBaseTokenCreateInput() {
  return {
    issuerPublicKey: new Uint8Array(0),
    tokenName: "",
    tokenTicker: "",
    decimals: 0,
    maxSupply: new Uint8Array(0),
    isFreezable: false,
    creationEntityPublicKey: void 0
  };
}
var TokenCreateInput = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.issuerPublicKey.length !== 0) {
      writer.uint32(10).bytes(message.issuerPublicKey);
    }
    if (message.tokenName !== "") {
      writer.uint32(18).string(message.tokenName);
    }
    if (message.tokenTicker !== "") {
      writer.uint32(26).string(message.tokenTicker);
    }
    if (message.decimals !== 0) {
      writer.uint32(32).uint32(message.decimals);
    }
    if (message.maxSupply.length !== 0) {
      writer.uint32(42).bytes(message.maxSupply);
    }
    if (message.isFreezable !== false) {
      writer.uint32(48).bool(message.isFreezable);
    }
    if (message.creationEntityPublicKey !== void 0) {
      writer.uint32(58).bytes(message.creationEntityPublicKey);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTokenCreateInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.issuerPublicKey = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.tokenName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.tokenTicker = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }
          message.decimals = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.maxSupply = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }
          message.isFreezable = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.creationEntityPublicKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      issuerPublicKey: isSet3(object.issuerPublicKey) ? bytesFromBase642(object.issuerPublicKey) : new Uint8Array(0),
      tokenName: isSet3(object.tokenName) ? globalThis.String(object.tokenName) : "",
      tokenTicker: isSet3(object.tokenTicker) ? globalThis.String(object.tokenTicker) : "",
      decimals: isSet3(object.decimals) ? globalThis.Number(object.decimals) : 0,
      maxSupply: isSet3(object.maxSupply) ? bytesFromBase642(object.maxSupply) : new Uint8Array(0),
      isFreezable: isSet3(object.isFreezable) ? globalThis.Boolean(object.isFreezable) : false,
      creationEntityPublicKey: isSet3(object.creationEntityPublicKey) ? bytesFromBase642(object.creationEntityPublicKey) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.issuerPublicKey.length !== 0) {
      obj.issuerPublicKey = base64FromBytes2(message.issuerPublicKey);
    }
    if (message.tokenName !== "") {
      obj.tokenName = message.tokenName;
    }
    if (message.tokenTicker !== "") {
      obj.tokenTicker = message.tokenTicker;
    }
    if (message.decimals !== 0) {
      obj.decimals = Math.round(message.decimals);
    }
    if (message.maxSupply.length !== 0) {
      obj.maxSupply = base64FromBytes2(message.maxSupply);
    }
    if (message.isFreezable !== false) {
      obj.isFreezable = message.isFreezable;
    }
    if (message.creationEntityPublicKey !== void 0) {
      obj.creationEntityPublicKey = base64FromBytes2(message.creationEntityPublicKey);
    }
    return obj;
  },
  create(base) {
    return TokenCreateInput.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseTokenCreateInput();
    message.issuerPublicKey = object.issuerPublicKey ?? new Uint8Array(0);
    message.tokenName = object.tokenName ?? "";
    message.tokenTicker = object.tokenTicker ?? "";
    message.decimals = object.decimals ?? 0;
    message.maxSupply = object.maxSupply ?? new Uint8Array(0);
    message.isFreezable = object.isFreezable ?? false;
    message.creationEntityPublicKey = object.creationEntityPublicKey ?? void 0;
    return message;
  }
};
function createBaseTokenOutput() {
  return {
    id: void 0,
    ownerPublicKey: new Uint8Array(0),
    revocationCommitment: void 0,
    withdrawBondSats: void 0,
    withdrawRelativeBlockLocktime: void 0,
    tokenPublicKey: void 0,
    tokenIdentifier: void 0,
    tokenAmount: new Uint8Array(0)
  };
}
var TokenOutput = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.id !== void 0) {
      writer.uint32(10).string(message.id);
    }
    if (message.ownerPublicKey.length !== 0) {
      writer.uint32(18).bytes(message.ownerPublicKey);
    }
    if (message.revocationCommitment !== void 0) {
      writer.uint32(26).bytes(message.revocationCommitment);
    }
    if (message.withdrawBondSats !== void 0) {
      writer.uint32(32).uint64(message.withdrawBondSats);
    }
    if (message.withdrawRelativeBlockLocktime !== void 0) {
      writer.uint32(40).uint64(message.withdrawRelativeBlockLocktime);
    }
    if (message.tokenPublicKey !== void 0) {
      writer.uint32(50).bytes(message.tokenPublicKey);
    }
    if (message.tokenIdentifier !== void 0) {
      writer.uint32(66).bytes(message.tokenIdentifier);
    }
    if (message.tokenAmount.length !== 0) {
      writer.uint32(58).bytes(message.tokenAmount);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTokenOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.ownerPublicKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.revocationCommitment = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }
          message.withdrawBondSats = longToNumber2(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }
          message.withdrawRelativeBlockLocktime = longToNumber2(reader.uint64());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.tokenPublicKey = reader.bytes();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }
          message.tokenIdentifier = reader.bytes();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.tokenAmount = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      id: isSet3(object.id) ? globalThis.String(object.id) : void 0,
      ownerPublicKey: isSet3(object.ownerPublicKey) ? bytesFromBase642(object.ownerPublicKey) : new Uint8Array(0),
      revocationCommitment: isSet3(object.revocationCommitment) ? bytesFromBase642(object.revocationCommitment) : void 0,
      withdrawBondSats: isSet3(object.withdrawBondSats) ? globalThis.Number(object.withdrawBondSats) : void 0,
      withdrawRelativeBlockLocktime: isSet3(object.withdrawRelativeBlockLocktime) ? globalThis.Number(object.withdrawRelativeBlockLocktime) : void 0,
      tokenPublicKey: isSet3(object.tokenPublicKey) ? bytesFromBase642(object.tokenPublicKey) : void 0,
      tokenIdentifier: isSet3(object.tokenIdentifier) ? bytesFromBase642(object.tokenIdentifier) : void 0,
      tokenAmount: isSet3(object.tokenAmount) ? bytesFromBase642(object.tokenAmount) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.id !== void 0) {
      obj.id = message.id;
    }
    if (message.ownerPublicKey.length !== 0) {
      obj.ownerPublicKey = base64FromBytes2(message.ownerPublicKey);
    }
    if (message.revocationCommitment !== void 0) {
      obj.revocationCommitment = base64FromBytes2(message.revocationCommitment);
    }
    if (message.withdrawBondSats !== void 0) {
      obj.withdrawBondSats = Math.round(message.withdrawBondSats);
    }
    if (message.withdrawRelativeBlockLocktime !== void 0) {
      obj.withdrawRelativeBlockLocktime = Math.round(message.withdrawRelativeBlockLocktime);
    }
    if (message.tokenPublicKey !== void 0) {
      obj.tokenPublicKey = base64FromBytes2(message.tokenPublicKey);
    }
    if (message.tokenIdentifier !== void 0) {
      obj.tokenIdentifier = base64FromBytes2(message.tokenIdentifier);
    }
    if (message.tokenAmount.length !== 0) {
      obj.tokenAmount = base64FromBytes2(message.tokenAmount);
    }
    return obj;
  },
  create(base) {
    return TokenOutput.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseTokenOutput();
    message.id = object.id ?? void 0;
    message.ownerPublicKey = object.ownerPublicKey ?? new Uint8Array(0);
    message.revocationCommitment = object.revocationCommitment ?? void 0;
    message.withdrawBondSats = object.withdrawBondSats ?? void 0;
    message.withdrawRelativeBlockLocktime = object.withdrawRelativeBlockLocktime ?? void 0;
    message.tokenPublicKey = object.tokenPublicKey ?? void 0;
    message.tokenIdentifier = object.tokenIdentifier ?? void 0;
    message.tokenAmount = object.tokenAmount ?? new Uint8Array(0);
    return message;
  }
};
function createBaseTokenTransaction() {
  return { tokenInputs: void 0, tokenOutputs: [], sparkOperatorIdentityPublicKeys: [], network: 0 };
}
var TokenTransaction = {
  encode(message, writer = new BinaryWriter4()) {
    switch (message.tokenInputs?.$case) {
      case "mintInput":
        TokenMintInput.encode(message.tokenInputs.mintInput, writer.uint32(10).fork()).join();
        break;
      case "transferInput":
        TokenTransferInput.encode(message.tokenInputs.transferInput, writer.uint32(18).fork()).join();
        break;
      case "createInput":
        TokenCreateInput.encode(message.tokenInputs.createInput, writer.uint32(42).fork()).join();
        break;
    }
    for (const v of message.tokenOutputs) {
      TokenOutput.encode(v, writer.uint32(26).fork()).join();
    }
    for (const v of message.sparkOperatorIdentityPublicKeys) {
      writer.uint32(34).bytes(v);
    }
    if (message.network !== 0) {
      writer.uint32(80).int32(message.network);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTokenTransaction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.tokenInputs = { $case: "mintInput", mintInput: TokenMintInput.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.tokenInputs = {
            $case: "transferInput",
            transferInput: TokenTransferInput.decode(reader, reader.uint32())
          };
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.tokenInputs = { $case: "createInput", createInput: TokenCreateInput.decode(reader, reader.uint32()) };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.tokenOutputs.push(TokenOutput.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.sparkOperatorIdentityPublicKeys.push(reader.bytes());
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }
          message.network = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      tokenInputs: isSet3(object.mintInput) ? { $case: "mintInput", mintInput: TokenMintInput.fromJSON(object.mintInput) } : isSet3(object.transferInput) ? { $case: "transferInput", transferInput: TokenTransferInput.fromJSON(object.transferInput) } : isSet3(object.createInput) ? { $case: "createInput", createInput: TokenCreateInput.fromJSON(object.createInput) } : void 0,
      tokenOutputs: globalThis.Array.isArray(object?.tokenOutputs) ? object.tokenOutputs.map((e) => TokenOutput.fromJSON(e)) : [],
      sparkOperatorIdentityPublicKeys: globalThis.Array.isArray(object?.sparkOperatorIdentityPublicKeys) ? object.sparkOperatorIdentityPublicKeys.map((e) => bytesFromBase642(e)) : [],
      network: isSet3(object.network) ? networkFromJSON(object.network) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.tokenInputs?.$case === "mintInput") {
      obj.mintInput = TokenMintInput.toJSON(message.tokenInputs.mintInput);
    } else if (message.tokenInputs?.$case === "transferInput") {
      obj.transferInput = TokenTransferInput.toJSON(message.tokenInputs.transferInput);
    } else if (message.tokenInputs?.$case === "createInput") {
      obj.createInput = TokenCreateInput.toJSON(message.tokenInputs.createInput);
    }
    if (message.tokenOutputs?.length) {
      obj.tokenOutputs = message.tokenOutputs.map((e) => TokenOutput.toJSON(e));
    }
    if (message.sparkOperatorIdentityPublicKeys?.length) {
      obj.sparkOperatorIdentityPublicKeys = message.sparkOperatorIdentityPublicKeys.map((e) => base64FromBytes2(e));
    }
    if (message.network !== 0) {
      obj.network = networkToJSON(message.network);
    }
    return obj;
  },
  create(base) {
    return TokenTransaction.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseTokenTransaction();
    switch (object.tokenInputs?.$case) {
      case "mintInput": {
        if (object.tokenInputs?.mintInput !== void 0 && object.tokenInputs?.mintInput !== null) {
          message.tokenInputs = {
            $case: "mintInput",
            mintInput: TokenMintInput.fromPartial(object.tokenInputs.mintInput)
          };
        }
        break;
      }
      case "transferInput": {
        if (object.tokenInputs?.transferInput !== void 0 && object.tokenInputs?.transferInput !== null) {
          message.tokenInputs = {
            $case: "transferInput",
            transferInput: TokenTransferInput.fromPartial(object.tokenInputs.transferInput)
          };
        }
        break;
      }
      case "createInput": {
        if (object.tokenInputs?.createInput !== void 0 && object.tokenInputs?.createInput !== null) {
          message.tokenInputs = {
            $case: "createInput",
            createInput: TokenCreateInput.fromPartial(object.tokenInputs.createInput)
          };
        }
        break;
      }
    }
    message.tokenOutputs = object.tokenOutputs?.map((e) => TokenOutput.fromPartial(e)) || [];
    message.sparkOperatorIdentityPublicKeys = object.sparkOperatorIdentityPublicKeys?.map((e) => e) || [];
    message.network = object.network ?? 0;
    return message;
  }
};
function createBaseSpentTokenOutputMetadata() {
  return { outputId: "", revocationSecret: new Uint8Array(0) };
}
var SpentTokenOutputMetadata = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.outputId !== "") {
      writer.uint32(10).string(message.outputId);
    }
    if (message.revocationSecret.length !== 0) {
      writer.uint32(18).bytes(message.revocationSecret);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSpentTokenOutputMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.outputId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.revocationSecret = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      outputId: isSet3(object.outputId) ? globalThis.String(object.outputId) : "",
      revocationSecret: isSet3(object.revocationSecret) ? bytesFromBase642(object.revocationSecret) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.outputId !== "") {
      obj.outputId = message.outputId;
    }
    if (message.revocationSecret.length !== 0) {
      obj.revocationSecret = base64FromBytes2(message.revocationSecret);
    }
    return obj;
  },
  create(base) {
    return SpentTokenOutputMetadata.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSpentTokenOutputMetadata();
    message.outputId = object.outputId ?? "";
    message.revocationSecret = object.revocationSecret ?? new Uint8Array(0);
    return message;
  }
};
function createBaseTokenTransactionConfirmationMetadata() {
  return { spentTokenOutputsMetadata: [] };
}
var TokenTransactionConfirmationMetadata = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.spentTokenOutputsMetadata) {
      SpentTokenOutputMetadata.encode(v, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTokenTransactionConfirmationMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.spentTokenOutputsMetadata.push(SpentTokenOutputMetadata.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      spentTokenOutputsMetadata: globalThis.Array.isArray(object?.spentTokenOutputsMetadata) ? object.spentTokenOutputsMetadata.map((e) => SpentTokenOutputMetadata.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.spentTokenOutputsMetadata?.length) {
      obj.spentTokenOutputsMetadata = message.spentTokenOutputsMetadata.map((e) => SpentTokenOutputMetadata.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return TokenTransactionConfirmationMetadata.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseTokenTransactionConfirmationMetadata();
    message.spentTokenOutputsMetadata = object.spentTokenOutputsMetadata?.map((e) => SpentTokenOutputMetadata.fromPartial(e)) || [];
    return message;
  }
};
function createBaseTokenTransactionWithStatus() {
  return {
    tokenTransaction: void 0,
    status: 0,
    confirmationMetadata: void 0,
    tokenTransactionHash: new Uint8Array(0)
  };
}
var TokenTransactionWithStatus = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.tokenTransaction !== void 0) {
      TokenTransaction.encode(message.tokenTransaction, writer.uint32(10).fork()).join();
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.confirmationMetadata !== void 0) {
      TokenTransactionConfirmationMetadata.encode(message.confirmationMetadata, writer.uint32(26).fork()).join();
    }
    if (message.tokenTransactionHash.length !== 0) {
      writer.uint32(34).bytes(message.tokenTransactionHash);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTokenTransactionWithStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.tokenTransaction = TokenTransaction.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.status = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.confirmationMetadata = TokenTransactionConfirmationMetadata.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.tokenTransactionHash = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      tokenTransaction: isSet3(object.tokenTransaction) ? TokenTransaction.fromJSON(object.tokenTransaction) : void 0,
      status: isSet3(object.status) ? tokenTransactionStatusFromJSON(object.status) : 0,
      confirmationMetadata: isSet3(object.confirmationMetadata) ? TokenTransactionConfirmationMetadata.fromJSON(object.confirmationMetadata) : void 0,
      tokenTransactionHash: isSet3(object.tokenTransactionHash) ? bytesFromBase642(object.tokenTransactionHash) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.tokenTransaction !== void 0) {
      obj.tokenTransaction = TokenTransaction.toJSON(message.tokenTransaction);
    }
    if (message.status !== 0) {
      obj.status = tokenTransactionStatusToJSON(message.status);
    }
    if (message.confirmationMetadata !== void 0) {
      obj.confirmationMetadata = TokenTransactionConfirmationMetadata.toJSON(message.confirmationMetadata);
    }
    if (message.tokenTransactionHash.length !== 0) {
      obj.tokenTransactionHash = base64FromBytes2(message.tokenTransactionHash);
    }
    return obj;
  },
  create(base) {
    return TokenTransactionWithStatus.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseTokenTransactionWithStatus();
    message.tokenTransaction = object.tokenTransaction !== void 0 && object.tokenTransaction !== null ? TokenTransaction.fromPartial(object.tokenTransaction) : void 0;
    message.status = object.status ?? 0;
    message.confirmationMetadata = object.confirmationMetadata !== void 0 && object.confirmationMetadata !== null ? TokenTransactionConfirmationMetadata.fromPartial(object.confirmationMetadata) : void 0;
    message.tokenTransactionHash = object.tokenTransactionHash ?? new Uint8Array(0);
    return message;
  }
};
function createBaseSignatureWithIndex() {
  return { signature: new Uint8Array(0), inputIndex: 0 };
}
var SignatureWithIndex = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.signature.length !== 0) {
      writer.uint32(10).bytes(message.signature);
    }
    if (message.inputIndex !== 0) {
      writer.uint32(16).uint32(message.inputIndex);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSignatureWithIndex();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.signature = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.inputIndex = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      signature: isSet3(object.signature) ? bytesFromBase642(object.signature) : new Uint8Array(0),
      inputIndex: isSet3(object.inputIndex) ? globalThis.Number(object.inputIndex) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes2(message.signature);
    }
    if (message.inputIndex !== 0) {
      obj.inputIndex = Math.round(message.inputIndex);
    }
    return obj;
  },
  create(base) {
    return SignatureWithIndex.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSignatureWithIndex();
    message.signature = object.signature ?? new Uint8Array(0);
    message.inputIndex = object.inputIndex ?? 0;
    return message;
  }
};
function createBaseTokenTransactionSignatures() {
  return { ownerSignatures: [] };
}
var TokenTransactionSignatures = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.ownerSignatures) {
      SignatureWithIndex.encode(v, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTokenTransactionSignatures();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.ownerSignatures.push(SignatureWithIndex.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      ownerSignatures: globalThis.Array.isArray(object?.ownerSignatures) ? object.ownerSignatures.map((e) => SignatureWithIndex.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.ownerSignatures?.length) {
      obj.ownerSignatures = message.ownerSignatures.map((e) => SignatureWithIndex.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return TokenTransactionSignatures.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseTokenTransactionSignatures();
    message.ownerSignatures = object.ownerSignatures?.map((e) => SignatureWithIndex.fromPartial(e)) || [];
    return message;
  }
};
function createBaseStartTokenTransactionRequest() {
  return {
    identityPublicKey: new Uint8Array(0),
    partialTokenTransaction: void 0,
    tokenTransactionSignatures: void 0,
    sparkPaymentIntent: ""
  };
}
var StartTokenTransactionRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.identityPublicKey.length !== 0) {
      writer.uint32(10).bytes(message.identityPublicKey);
    }
    if (message.partialTokenTransaction !== void 0) {
      TokenTransaction.encode(message.partialTokenTransaction, writer.uint32(18).fork()).join();
    }
    if (message.tokenTransactionSignatures !== void 0) {
      TokenTransactionSignatures.encode(message.tokenTransactionSignatures, writer.uint32(26).fork()).join();
    }
    if (message.sparkPaymentIntent !== "") {
      writer.uint32(34).string(message.sparkPaymentIntent);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseStartTokenTransactionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.identityPublicKey = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.partialTokenTransaction = TokenTransaction.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.tokenTransactionSignatures = TokenTransactionSignatures.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.sparkPaymentIntent = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      identityPublicKey: isSet3(object.identityPublicKey) ? bytesFromBase642(object.identityPublicKey) : new Uint8Array(0),
      partialTokenTransaction: isSet3(object.partialTokenTransaction) ? TokenTransaction.fromJSON(object.partialTokenTransaction) : void 0,
      tokenTransactionSignatures: isSet3(object.tokenTransactionSignatures) ? TokenTransactionSignatures.fromJSON(object.tokenTransactionSignatures) : void 0,
      sparkPaymentIntent: isSet3(object.sparkPaymentIntent) ? globalThis.String(object.sparkPaymentIntent) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.identityPublicKey.length !== 0) {
      obj.identityPublicKey = base64FromBytes2(message.identityPublicKey);
    }
    if (message.partialTokenTransaction !== void 0) {
      obj.partialTokenTransaction = TokenTransaction.toJSON(message.partialTokenTransaction);
    }
    if (message.tokenTransactionSignatures !== void 0) {
      obj.tokenTransactionSignatures = TokenTransactionSignatures.toJSON(message.tokenTransactionSignatures);
    }
    if (message.sparkPaymentIntent !== "") {
      obj.sparkPaymentIntent = message.sparkPaymentIntent;
    }
    return obj;
  },
  create(base) {
    return StartTokenTransactionRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseStartTokenTransactionRequest();
    message.identityPublicKey = object.identityPublicKey ?? new Uint8Array(0);
    message.partialTokenTransaction = object.partialTokenTransaction !== void 0 && object.partialTokenTransaction !== null ? TokenTransaction.fromPartial(object.partialTokenTransaction) : void 0;
    message.tokenTransactionSignatures = object.tokenTransactionSignatures !== void 0 && object.tokenTransactionSignatures !== null ? TokenTransactionSignatures.fromPartial(object.tokenTransactionSignatures) : void 0;
    message.sparkPaymentIntent = object.sparkPaymentIntent ?? "";
    return message;
  }
};
function createBaseStartTokenTransactionResponse() {
  return { finalTokenTransaction: void 0, keyshareInfo: void 0 };
}
var StartTokenTransactionResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.finalTokenTransaction !== void 0) {
      TokenTransaction.encode(message.finalTokenTransaction, writer.uint32(10).fork()).join();
    }
    if (message.keyshareInfo !== void 0) {
      SigningKeyshare.encode(message.keyshareInfo, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseStartTokenTransactionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.finalTokenTransaction = TokenTransaction.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.keyshareInfo = SigningKeyshare.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      finalTokenTransaction: isSet3(object.finalTokenTransaction) ? TokenTransaction.fromJSON(object.finalTokenTransaction) : void 0,
      keyshareInfo: isSet3(object.keyshareInfo) ? SigningKeyshare.fromJSON(object.keyshareInfo) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.finalTokenTransaction !== void 0) {
      obj.finalTokenTransaction = TokenTransaction.toJSON(message.finalTokenTransaction);
    }
    if (message.keyshareInfo !== void 0) {
      obj.keyshareInfo = SigningKeyshare.toJSON(message.keyshareInfo);
    }
    return obj;
  },
  create(base) {
    return StartTokenTransactionResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseStartTokenTransactionResponse();
    message.finalTokenTransaction = object.finalTokenTransaction !== void 0 && object.finalTokenTransaction !== null ? TokenTransaction.fromPartial(object.finalTokenTransaction) : void 0;
    message.keyshareInfo = object.keyshareInfo !== void 0 && object.keyshareInfo !== null ? SigningKeyshare.fromPartial(object.keyshareInfo) : void 0;
    return message;
  }
};
function createBaseOperatorSpecificTokenTransactionSignablePayload() {
  return { finalTokenTransactionHash: new Uint8Array(0), operatorIdentityPublicKey: new Uint8Array(0) };
}
var OperatorSpecificTokenTransactionSignablePayload = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.finalTokenTransactionHash.length !== 0) {
      writer.uint32(10).bytes(message.finalTokenTransactionHash);
    }
    if (message.operatorIdentityPublicKey.length !== 0) {
      writer.uint32(18).bytes(message.operatorIdentityPublicKey);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseOperatorSpecificTokenTransactionSignablePayload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.finalTokenTransactionHash = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.operatorIdentityPublicKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      finalTokenTransactionHash: isSet3(object.finalTokenTransactionHash) ? bytesFromBase642(object.finalTokenTransactionHash) : new Uint8Array(0),
      operatorIdentityPublicKey: isSet3(object.operatorIdentityPublicKey) ? bytesFromBase642(object.operatorIdentityPublicKey) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.finalTokenTransactionHash.length !== 0) {
      obj.finalTokenTransactionHash = base64FromBytes2(message.finalTokenTransactionHash);
    }
    if (message.operatorIdentityPublicKey.length !== 0) {
      obj.operatorIdentityPublicKey = base64FromBytes2(message.operatorIdentityPublicKey);
    }
    return obj;
  },
  create(base) {
    return OperatorSpecificTokenTransactionSignablePayload.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseOperatorSpecificTokenTransactionSignablePayload();
    message.finalTokenTransactionHash = object.finalTokenTransactionHash ?? new Uint8Array(0);
    message.operatorIdentityPublicKey = object.operatorIdentityPublicKey ?? new Uint8Array(0);
    return message;
  }
};
function createBaseOperatorSpecificOwnerSignature() {
  return { ownerSignature: void 0, payload: void 0 };
}
var OperatorSpecificOwnerSignature = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.ownerSignature !== void 0) {
      SignatureWithIndex.encode(message.ownerSignature, writer.uint32(10).fork()).join();
    }
    if (message.payload !== void 0) {
      OperatorSpecificTokenTransactionSignablePayload.encode(message.payload, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseOperatorSpecificOwnerSignature();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.ownerSignature = SignatureWithIndex.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.payload = OperatorSpecificTokenTransactionSignablePayload.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      ownerSignature: isSet3(object.ownerSignature) ? SignatureWithIndex.fromJSON(object.ownerSignature) : void 0,
      payload: isSet3(object.payload) ? OperatorSpecificTokenTransactionSignablePayload.fromJSON(object.payload) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.ownerSignature !== void 0) {
      obj.ownerSignature = SignatureWithIndex.toJSON(message.ownerSignature);
    }
    if (message.payload !== void 0) {
      obj.payload = OperatorSpecificTokenTransactionSignablePayload.toJSON(message.payload);
    }
    return obj;
  },
  create(base) {
    return OperatorSpecificOwnerSignature.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseOperatorSpecificOwnerSignature();
    message.ownerSignature = object.ownerSignature !== void 0 && object.ownerSignature !== null ? SignatureWithIndex.fromPartial(object.ownerSignature) : void 0;
    message.payload = object.payload !== void 0 && object.payload !== null ? OperatorSpecificTokenTransactionSignablePayload.fromPartial(object.payload) : void 0;
    return message;
  }
};
function createBaseSignTokenTransactionRequest() {
  return { finalTokenTransaction: void 0, operatorSpecificSignatures: [], identityPublicKey: new Uint8Array(0) };
}
var SignTokenTransactionRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.finalTokenTransaction !== void 0) {
      TokenTransaction.encode(message.finalTokenTransaction, writer.uint32(10).fork()).join();
    }
    for (const v of message.operatorSpecificSignatures) {
      OperatorSpecificOwnerSignature.encode(v, writer.uint32(18).fork()).join();
    }
    if (message.identityPublicKey.length !== 0) {
      writer.uint32(26).bytes(message.identityPublicKey);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSignTokenTransactionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.finalTokenTransaction = TokenTransaction.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.operatorSpecificSignatures.push(OperatorSpecificOwnerSignature.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.identityPublicKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      finalTokenTransaction: isSet3(object.finalTokenTransaction) ? TokenTransaction.fromJSON(object.finalTokenTransaction) : void 0,
      operatorSpecificSignatures: globalThis.Array.isArray(object?.operatorSpecificSignatures) ? object.operatorSpecificSignatures.map((e) => OperatorSpecificOwnerSignature.fromJSON(e)) : [],
      identityPublicKey: isSet3(object.identityPublicKey) ? bytesFromBase642(object.identityPublicKey) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.finalTokenTransaction !== void 0) {
      obj.finalTokenTransaction = TokenTransaction.toJSON(message.finalTokenTransaction);
    }
    if (message.operatorSpecificSignatures?.length) {
      obj.operatorSpecificSignatures = message.operatorSpecificSignatures.map(
        (e) => OperatorSpecificOwnerSignature.toJSON(e)
      );
    }
    if (message.identityPublicKey.length !== 0) {
      obj.identityPublicKey = base64FromBytes2(message.identityPublicKey);
    }
    return obj;
  },
  create(base) {
    return SignTokenTransactionRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSignTokenTransactionRequest();
    message.finalTokenTransaction = object.finalTokenTransaction !== void 0 && object.finalTokenTransaction !== null ? TokenTransaction.fromPartial(object.finalTokenTransaction) : void 0;
    message.operatorSpecificSignatures = object.operatorSpecificSignatures?.map((e) => OperatorSpecificOwnerSignature.fromPartial(e)) || [];
    message.identityPublicKey = object.identityPublicKey ?? new Uint8Array(0);
    return message;
  }
};
function createBaseKeyshareWithIndex() {
  return { inputIndex: 0, keyshare: new Uint8Array(0) };
}
var KeyshareWithIndex = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.inputIndex !== 0) {
      writer.uint32(8).uint32(message.inputIndex);
    }
    if (message.keyshare.length !== 0) {
      writer.uint32(18).bytes(message.keyshare);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseKeyshareWithIndex();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.inputIndex = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.keyshare = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      inputIndex: isSet3(object.inputIndex) ? globalThis.Number(object.inputIndex) : 0,
      keyshare: isSet3(object.keyshare) ? bytesFromBase642(object.keyshare) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.inputIndex !== 0) {
      obj.inputIndex = Math.round(message.inputIndex);
    }
    if (message.keyshare.length !== 0) {
      obj.keyshare = base64FromBytes2(message.keyshare);
    }
    return obj;
  },
  create(base) {
    return KeyshareWithIndex.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseKeyshareWithIndex();
    message.inputIndex = object.inputIndex ?? 0;
    message.keyshare = object.keyshare ?? new Uint8Array(0);
    return message;
  }
};
function createBaseSignTokenTransactionResponse() {
  return { sparkOperatorSignature: new Uint8Array(0), revocationKeyshares: [] };
}
var SignTokenTransactionResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.sparkOperatorSignature.length !== 0) {
      writer.uint32(10).bytes(message.sparkOperatorSignature);
    }
    for (const v of message.revocationKeyshares) {
      KeyshareWithIndex.encode(v, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSignTokenTransactionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.sparkOperatorSignature = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.revocationKeyshares.push(KeyshareWithIndex.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      sparkOperatorSignature: isSet3(object.sparkOperatorSignature) ? bytesFromBase642(object.sparkOperatorSignature) : new Uint8Array(0),
      revocationKeyshares: globalThis.Array.isArray(object?.revocationKeyshares) ? object.revocationKeyshares.map((e) => KeyshareWithIndex.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.sparkOperatorSignature.length !== 0) {
      obj.sparkOperatorSignature = base64FromBytes2(message.sparkOperatorSignature);
    }
    if (message.revocationKeyshares?.length) {
      obj.revocationKeyshares = message.revocationKeyshares.map((e) => KeyshareWithIndex.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return SignTokenTransactionResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSignTokenTransactionResponse();
    message.sparkOperatorSignature = object.sparkOperatorSignature ?? new Uint8Array(0);
    message.revocationKeyshares = object.revocationKeyshares?.map((e) => KeyshareWithIndex.fromPartial(e)) || [];
    return message;
  }
};
function createBaseRevocationSecretWithIndex() {
  return { inputIndex: 0, revocationSecret: new Uint8Array(0) };
}
var RevocationSecretWithIndex = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.inputIndex !== 0) {
      writer.uint32(8).uint32(message.inputIndex);
    }
    if (message.revocationSecret.length !== 0) {
      writer.uint32(18).bytes(message.revocationSecret);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRevocationSecretWithIndex();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.inputIndex = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.revocationSecret = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      inputIndex: isSet3(object.inputIndex) ? globalThis.Number(object.inputIndex) : 0,
      revocationSecret: isSet3(object.revocationSecret) ? bytesFromBase642(object.revocationSecret) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.inputIndex !== 0) {
      obj.inputIndex = Math.round(message.inputIndex);
    }
    if (message.revocationSecret.length !== 0) {
      obj.revocationSecret = base64FromBytes2(message.revocationSecret);
    }
    return obj;
  },
  create(base) {
    return RevocationSecretWithIndex.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRevocationSecretWithIndex();
    message.inputIndex = object.inputIndex ?? 0;
    message.revocationSecret = object.revocationSecret ?? new Uint8Array(0);
    return message;
  }
};
function createBaseFinalizeTokenTransactionRequest() {
  return {
    finalTokenTransaction: void 0,
    revocationSecrets: [],
    identityPublicKey: new Uint8Array(0),
    sparkPaymentIntent: ""
  };
}
var FinalizeTokenTransactionRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.finalTokenTransaction !== void 0) {
      TokenTransaction.encode(message.finalTokenTransaction, writer.uint32(10).fork()).join();
    }
    for (const v of message.revocationSecrets) {
      RevocationSecretWithIndex.encode(v, writer.uint32(18).fork()).join();
    }
    if (message.identityPublicKey.length !== 0) {
      writer.uint32(26).bytes(message.identityPublicKey);
    }
    if (message.sparkPaymentIntent !== "") {
      writer.uint32(34).string(message.sparkPaymentIntent);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseFinalizeTokenTransactionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.finalTokenTransaction = TokenTransaction.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.revocationSecrets.push(RevocationSecretWithIndex.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.identityPublicKey = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.sparkPaymentIntent = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      finalTokenTransaction: isSet3(object.finalTokenTransaction) ? TokenTransaction.fromJSON(object.finalTokenTransaction) : void 0,
      revocationSecrets: globalThis.Array.isArray(object?.revocationSecrets) ? object.revocationSecrets.map((e) => RevocationSecretWithIndex.fromJSON(e)) : [],
      identityPublicKey: isSet3(object.identityPublicKey) ? bytesFromBase642(object.identityPublicKey) : new Uint8Array(0),
      sparkPaymentIntent: isSet3(object.sparkPaymentIntent) ? globalThis.String(object.sparkPaymentIntent) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.finalTokenTransaction !== void 0) {
      obj.finalTokenTransaction = TokenTransaction.toJSON(message.finalTokenTransaction);
    }
    if (message.revocationSecrets?.length) {
      obj.revocationSecrets = message.revocationSecrets.map((e) => RevocationSecretWithIndex.toJSON(e));
    }
    if (message.identityPublicKey.length !== 0) {
      obj.identityPublicKey = base64FromBytes2(message.identityPublicKey);
    }
    if (message.sparkPaymentIntent !== "") {
      obj.sparkPaymentIntent = message.sparkPaymentIntent;
    }
    return obj;
  },
  create(base) {
    return FinalizeTokenTransactionRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseFinalizeTokenTransactionRequest();
    message.finalTokenTransaction = object.finalTokenTransaction !== void 0 && object.finalTokenTransaction !== null ? TokenTransaction.fromPartial(object.finalTokenTransaction) : void 0;
    message.revocationSecrets = object.revocationSecrets?.map((e) => RevocationSecretWithIndex.fromPartial(e)) || [];
    message.identityPublicKey = object.identityPublicKey ?? new Uint8Array(0);
    message.sparkPaymentIntent = object.sparkPaymentIntent ?? "";
    return message;
  }
};
function createBaseFreezeTokensPayload() {
  return {
    ownerPublicKey: new Uint8Array(0),
    tokenPublicKey: new Uint8Array(0),
    issuerProvidedTimestamp: 0,
    operatorIdentityPublicKey: new Uint8Array(0),
    shouldUnfreeze: false,
    tokenIdentifier: void 0
  };
}
var FreezeTokensPayload = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.ownerPublicKey.length !== 0) {
      writer.uint32(10).bytes(message.ownerPublicKey);
    }
    if (message.tokenPublicKey.length !== 0) {
      writer.uint32(18).bytes(message.tokenPublicKey);
    }
    if (message.issuerProvidedTimestamp !== 0) {
      writer.uint32(24).uint64(message.issuerProvidedTimestamp);
    }
    if (message.operatorIdentityPublicKey.length !== 0) {
      writer.uint32(34).bytes(message.operatorIdentityPublicKey);
    }
    if (message.shouldUnfreeze !== false) {
      writer.uint32(40).bool(message.shouldUnfreeze);
    }
    if (message.tokenIdentifier !== void 0) {
      writer.uint32(50).bytes(message.tokenIdentifier);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseFreezeTokensPayload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.ownerPublicKey = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.tokenPublicKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.issuerProvidedTimestamp = longToNumber2(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.operatorIdentityPublicKey = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }
          message.shouldUnfreeze = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.tokenIdentifier = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      ownerPublicKey: isSet3(object.ownerPublicKey) ? bytesFromBase642(object.ownerPublicKey) : new Uint8Array(0),
      tokenPublicKey: isSet3(object.tokenPublicKey) ? bytesFromBase642(object.tokenPublicKey) : new Uint8Array(0),
      issuerProvidedTimestamp: isSet3(object.issuerProvidedTimestamp) ? globalThis.Number(object.issuerProvidedTimestamp) : 0,
      operatorIdentityPublicKey: isSet3(object.operatorIdentityPublicKey) ? bytesFromBase642(object.operatorIdentityPublicKey) : new Uint8Array(0),
      shouldUnfreeze: isSet3(object.shouldUnfreeze) ? globalThis.Boolean(object.shouldUnfreeze) : false,
      tokenIdentifier: isSet3(object.tokenIdentifier) ? bytesFromBase642(object.tokenIdentifier) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.ownerPublicKey.length !== 0) {
      obj.ownerPublicKey = base64FromBytes2(message.ownerPublicKey);
    }
    if (message.tokenPublicKey.length !== 0) {
      obj.tokenPublicKey = base64FromBytes2(message.tokenPublicKey);
    }
    if (message.issuerProvidedTimestamp !== 0) {
      obj.issuerProvidedTimestamp = Math.round(message.issuerProvidedTimestamp);
    }
    if (message.operatorIdentityPublicKey.length !== 0) {
      obj.operatorIdentityPublicKey = base64FromBytes2(message.operatorIdentityPublicKey);
    }
    if (message.shouldUnfreeze !== false) {
      obj.shouldUnfreeze = message.shouldUnfreeze;
    }
    if (message.tokenIdentifier !== void 0) {
      obj.tokenIdentifier = base64FromBytes2(message.tokenIdentifier);
    }
    return obj;
  },
  create(base) {
    return FreezeTokensPayload.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseFreezeTokensPayload();
    message.ownerPublicKey = object.ownerPublicKey ?? new Uint8Array(0);
    message.tokenPublicKey = object.tokenPublicKey ?? new Uint8Array(0);
    message.issuerProvidedTimestamp = object.issuerProvidedTimestamp ?? 0;
    message.operatorIdentityPublicKey = object.operatorIdentityPublicKey ?? new Uint8Array(0);
    message.shouldUnfreeze = object.shouldUnfreeze ?? false;
    message.tokenIdentifier = object.tokenIdentifier ?? void 0;
    return message;
  }
};
function createBaseFreezeTokensRequest() {
  return { freezeTokensPayload: void 0, issuerSignature: new Uint8Array(0) };
}
var FreezeTokensRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.freezeTokensPayload !== void 0) {
      FreezeTokensPayload.encode(message.freezeTokensPayload, writer.uint32(10).fork()).join();
    }
    if (message.issuerSignature.length !== 0) {
      writer.uint32(18).bytes(message.issuerSignature);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseFreezeTokensRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.freezeTokensPayload = FreezeTokensPayload.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.issuerSignature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      freezeTokensPayload: isSet3(object.freezeTokensPayload) ? FreezeTokensPayload.fromJSON(object.freezeTokensPayload) : void 0,
      issuerSignature: isSet3(object.issuerSignature) ? bytesFromBase642(object.issuerSignature) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.freezeTokensPayload !== void 0) {
      obj.freezeTokensPayload = FreezeTokensPayload.toJSON(message.freezeTokensPayload);
    }
    if (message.issuerSignature.length !== 0) {
      obj.issuerSignature = base64FromBytes2(message.issuerSignature);
    }
    return obj;
  },
  create(base) {
    return FreezeTokensRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseFreezeTokensRequest();
    message.freezeTokensPayload = object.freezeTokensPayload !== void 0 && object.freezeTokensPayload !== null ? FreezeTokensPayload.fromPartial(object.freezeTokensPayload) : void 0;
    message.issuerSignature = object.issuerSignature ?? new Uint8Array(0);
    return message;
  }
};
function createBaseFreezeTokensResponse() {
  return { impactedOutputIds: [], impactedTokenAmount: new Uint8Array(0) };
}
var FreezeTokensResponse = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.impactedOutputIds) {
      writer.uint32(10).string(v);
    }
    if (message.impactedTokenAmount.length !== 0) {
      writer.uint32(18).bytes(message.impactedTokenAmount);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseFreezeTokensResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.impactedOutputIds.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.impactedTokenAmount = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      impactedOutputIds: globalThis.Array.isArray(object?.impactedOutputIds) ? object.impactedOutputIds.map((e) => globalThis.String(e)) : [],
      impactedTokenAmount: isSet3(object.impactedTokenAmount) ? bytesFromBase642(object.impactedTokenAmount) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.impactedOutputIds?.length) {
      obj.impactedOutputIds = message.impactedOutputIds;
    }
    if (message.impactedTokenAmount.length !== 0) {
      obj.impactedTokenAmount = base64FromBytes2(message.impactedTokenAmount);
    }
    return obj;
  },
  create(base) {
    return FreezeTokensResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseFreezeTokensResponse();
    message.impactedOutputIds = object.impactedOutputIds?.map((e) => e) || [];
    message.impactedTokenAmount = object.impactedTokenAmount ?? new Uint8Array(0);
    return message;
  }
};
function createBaseQueryTokenOutputsRequest() {
  return { ownerPublicKeys: [], tokenPublicKeys: [], tokenIdentifiers: [], network: 0 };
}
var QueryTokenOutputsRequest = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.ownerPublicKeys) {
      writer.uint32(10).bytes(v);
    }
    for (const v of message.tokenPublicKeys) {
      writer.uint32(18).bytes(v);
    }
    for (const v of message.tokenIdentifiers) {
      writer.uint32(34).bytes(v);
    }
    if (message.network !== 0) {
      writer.uint32(24).int32(message.network);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryTokenOutputsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.ownerPublicKeys.push(reader.bytes());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.tokenPublicKeys.push(reader.bytes());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.tokenIdentifiers.push(reader.bytes());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.network = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      ownerPublicKeys: globalThis.Array.isArray(object?.ownerPublicKeys) ? object.ownerPublicKeys.map((e) => bytesFromBase642(e)) : [],
      tokenPublicKeys: globalThis.Array.isArray(object?.tokenPublicKeys) ? object.tokenPublicKeys.map((e) => bytesFromBase642(e)) : [],
      tokenIdentifiers: globalThis.Array.isArray(object?.tokenIdentifiers) ? object.tokenIdentifiers.map((e) => bytesFromBase642(e)) : [],
      network: isSet3(object.network) ? networkFromJSON(object.network) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.ownerPublicKeys?.length) {
      obj.ownerPublicKeys = message.ownerPublicKeys.map((e) => base64FromBytes2(e));
    }
    if (message.tokenPublicKeys?.length) {
      obj.tokenPublicKeys = message.tokenPublicKeys.map((e) => base64FromBytes2(e));
    }
    if (message.tokenIdentifiers?.length) {
      obj.tokenIdentifiers = message.tokenIdentifiers.map((e) => base64FromBytes2(e));
    }
    if (message.network !== 0) {
      obj.network = networkToJSON(message.network);
    }
    return obj;
  },
  create(base) {
    return QueryTokenOutputsRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryTokenOutputsRequest();
    message.ownerPublicKeys = object.ownerPublicKeys?.map((e) => e) || [];
    message.tokenPublicKeys = object.tokenPublicKeys?.map((e) => e) || [];
    message.tokenIdentifiers = object.tokenIdentifiers?.map((e) => e) || [];
    message.network = object.network ?? 0;
    return message;
  }
};
function createBaseQueryTokenTransactionsRequest() {
  return {
    outputIds: [],
    ownerPublicKeys: [],
    tokenPublicKeys: [],
    tokenIdentifiers: [],
    tokenTransactionHashes: [],
    limit: 0,
    offset: 0
  };
}
var QueryTokenTransactionsRequest = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.outputIds) {
      writer.uint32(10).string(v);
    }
    for (const v of message.ownerPublicKeys) {
      writer.uint32(18).bytes(v);
    }
    for (const v of message.tokenPublicKeys) {
      writer.uint32(26).bytes(v);
    }
    for (const v of message.tokenIdentifiers) {
      writer.uint32(58).bytes(v);
    }
    for (const v of message.tokenTransactionHashes) {
      writer.uint32(34).bytes(v);
    }
    if (message.limit !== 0) {
      writer.uint32(40).int64(message.limit);
    }
    if (message.offset !== 0) {
      writer.uint32(48).int64(message.offset);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryTokenTransactionsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.outputIds.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.ownerPublicKeys.push(reader.bytes());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.tokenPublicKeys.push(reader.bytes());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.tokenIdentifiers.push(reader.bytes());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.tokenTransactionHashes.push(reader.bytes());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }
          message.limit = longToNumber2(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }
          message.offset = longToNumber2(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      outputIds: globalThis.Array.isArray(object?.outputIds) ? object.outputIds.map((e) => globalThis.String(e)) : [],
      ownerPublicKeys: globalThis.Array.isArray(object?.ownerPublicKeys) ? object.ownerPublicKeys.map((e) => bytesFromBase642(e)) : [],
      tokenPublicKeys: globalThis.Array.isArray(object?.tokenPublicKeys) ? object.tokenPublicKeys.map((e) => bytesFromBase642(e)) : [],
      tokenIdentifiers: globalThis.Array.isArray(object?.tokenIdentifiers) ? object.tokenIdentifiers.map((e) => bytesFromBase642(e)) : [],
      tokenTransactionHashes: globalThis.Array.isArray(object?.tokenTransactionHashes) ? object.tokenTransactionHashes.map((e) => bytesFromBase642(e)) : [],
      limit: isSet3(object.limit) ? globalThis.Number(object.limit) : 0,
      offset: isSet3(object.offset) ? globalThis.Number(object.offset) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.outputIds?.length) {
      obj.outputIds = message.outputIds;
    }
    if (message.ownerPublicKeys?.length) {
      obj.ownerPublicKeys = message.ownerPublicKeys.map((e) => base64FromBytes2(e));
    }
    if (message.tokenPublicKeys?.length) {
      obj.tokenPublicKeys = message.tokenPublicKeys.map((e) => base64FromBytes2(e));
    }
    if (message.tokenIdentifiers?.length) {
      obj.tokenIdentifiers = message.tokenIdentifiers.map((e) => base64FromBytes2(e));
    }
    if (message.tokenTransactionHashes?.length) {
      obj.tokenTransactionHashes = message.tokenTransactionHashes.map((e) => base64FromBytes2(e));
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    return obj;
  },
  create(base) {
    return QueryTokenTransactionsRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryTokenTransactionsRequest();
    message.outputIds = object.outputIds?.map((e) => e) || [];
    message.ownerPublicKeys = object.ownerPublicKeys?.map((e) => e) || [];
    message.tokenPublicKeys = object.tokenPublicKeys?.map((e) => e) || [];
    message.tokenIdentifiers = object.tokenIdentifiers?.map((e) => e) || [];
    message.tokenTransactionHashes = object.tokenTransactionHashes?.map((e) => e) || [];
    message.limit = object.limit ?? 0;
    message.offset = object.offset ?? 0;
    return message;
  }
};
function createBaseQueryTokenTransactionsResponse() {
  return { tokenTransactionsWithStatus: [], offset: 0 };
}
var QueryTokenTransactionsResponse = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.tokenTransactionsWithStatus) {
      TokenTransactionWithStatus.encode(v, writer.uint32(10).fork()).join();
    }
    if (message.offset !== 0) {
      writer.uint32(16).int64(message.offset);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryTokenTransactionsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.tokenTransactionsWithStatus.push(TokenTransactionWithStatus.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.offset = longToNumber2(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      tokenTransactionsWithStatus: globalThis.Array.isArray(object?.tokenTransactionsWithStatus) ? object.tokenTransactionsWithStatus.map((e) => TokenTransactionWithStatus.fromJSON(e)) : [],
      offset: isSet3(object.offset) ? globalThis.Number(object.offset) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.tokenTransactionsWithStatus?.length) {
      obj.tokenTransactionsWithStatus = message.tokenTransactionsWithStatus.map(
        (e) => TokenTransactionWithStatus.toJSON(e)
      );
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    return obj;
  },
  create(base) {
    return QueryTokenTransactionsResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryTokenTransactionsResponse();
    message.tokenTransactionsWithStatus = object.tokenTransactionsWithStatus?.map((e) => TokenTransactionWithStatus.fromPartial(e)) || [];
    message.offset = object.offset ?? 0;
    return message;
  }
};
function createBaseOutputWithPreviousTransactionData() {
  return { output: void 0, previousTransactionHash: new Uint8Array(0), previousTransactionVout: 0 };
}
var OutputWithPreviousTransactionData = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.output !== void 0) {
      TokenOutput.encode(message.output, writer.uint32(10).fork()).join();
    }
    if (message.previousTransactionHash.length !== 0) {
      writer.uint32(18).bytes(message.previousTransactionHash);
    }
    if (message.previousTransactionVout !== 0) {
      writer.uint32(24).uint32(message.previousTransactionVout);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseOutputWithPreviousTransactionData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.output = TokenOutput.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.previousTransactionHash = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.previousTransactionVout = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      output: isSet3(object.output) ? TokenOutput.fromJSON(object.output) : void 0,
      previousTransactionHash: isSet3(object.previousTransactionHash) ? bytesFromBase642(object.previousTransactionHash) : new Uint8Array(0),
      previousTransactionVout: isSet3(object.previousTransactionVout) ? globalThis.Number(object.previousTransactionVout) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.output !== void 0) {
      obj.output = TokenOutput.toJSON(message.output);
    }
    if (message.previousTransactionHash.length !== 0) {
      obj.previousTransactionHash = base64FromBytes2(message.previousTransactionHash);
    }
    if (message.previousTransactionVout !== 0) {
      obj.previousTransactionVout = Math.round(message.previousTransactionVout);
    }
    return obj;
  },
  create(base) {
    return OutputWithPreviousTransactionData.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseOutputWithPreviousTransactionData();
    message.output = object.output !== void 0 && object.output !== null ? TokenOutput.fromPartial(object.output) : void 0;
    message.previousTransactionHash = object.previousTransactionHash ?? new Uint8Array(0);
    message.previousTransactionVout = object.previousTransactionVout ?? 0;
    return message;
  }
};
function createBaseQueryTokenOutputsResponse() {
  return { outputsWithPreviousTransactionData: [] };
}
var QueryTokenOutputsResponse = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.outputsWithPreviousTransactionData) {
      OutputWithPreviousTransactionData.encode(v, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryTokenOutputsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.outputsWithPreviousTransactionData.push(
            OutputWithPreviousTransactionData.decode(reader, reader.uint32())
          );
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      outputsWithPreviousTransactionData: globalThis.Array.isArray(object?.outputsWithPreviousTransactionData) ? object.outputsWithPreviousTransactionData.map((e) => OutputWithPreviousTransactionData.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.outputsWithPreviousTransactionData?.length) {
      obj.outputsWithPreviousTransactionData = message.outputsWithPreviousTransactionData.map(
        (e) => OutputWithPreviousTransactionData.toJSON(e)
      );
    }
    return obj;
  },
  create(base) {
    return QueryTokenOutputsResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryTokenOutputsResponse();
    message.outputsWithPreviousTransactionData = object.outputsWithPreviousTransactionData?.map((e) => OutputWithPreviousTransactionData.fromPartial(e)) || [];
    return message;
  }
};
function createBaseTreeNode() {
  return {
    id: "",
    treeId: "",
    value: 0,
    parentNodeId: void 0,
    nodeTx: new Uint8Array(0),
    refundTx: new Uint8Array(0),
    vout: 0,
    verifyingPublicKey: new Uint8Array(0),
    ownerIdentityPublicKey: new Uint8Array(0),
    signingKeyshare: void 0,
    status: "",
    network: 0,
    createdTime: void 0,
    updatedTime: void 0,
    ownerSigningPublicKey: new Uint8Array(0),
    directTx: new Uint8Array(0),
    directRefundTx: new Uint8Array(0),
    directFromCpfpRefundTx: new Uint8Array(0)
  };
}
var TreeNode = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.treeId !== "") {
      writer.uint32(18).string(message.treeId);
    }
    if (message.value !== 0) {
      writer.uint32(24).uint64(message.value);
    }
    if (message.parentNodeId !== void 0) {
      writer.uint32(34).string(message.parentNodeId);
    }
    if (message.nodeTx.length !== 0) {
      writer.uint32(42).bytes(message.nodeTx);
    }
    if (message.refundTx.length !== 0) {
      writer.uint32(50).bytes(message.refundTx);
    }
    if (message.vout !== 0) {
      writer.uint32(56).uint32(message.vout);
    }
    if (message.verifyingPublicKey.length !== 0) {
      writer.uint32(66).bytes(message.verifyingPublicKey);
    }
    if (message.ownerIdentityPublicKey.length !== 0) {
      writer.uint32(74).bytes(message.ownerIdentityPublicKey);
    }
    if (message.signingKeyshare !== void 0) {
      SigningKeyshare.encode(message.signingKeyshare, writer.uint32(82).fork()).join();
    }
    if (message.status !== "") {
      writer.uint32(90).string(message.status);
    }
    if (message.network !== 0) {
      writer.uint32(96).int32(message.network);
    }
    if (message.createdTime !== void 0) {
      Timestamp.encode(toTimestamp(message.createdTime), writer.uint32(106).fork()).join();
    }
    if (message.updatedTime !== void 0) {
      Timestamp.encode(toTimestamp(message.updatedTime), writer.uint32(114).fork()).join();
    }
    if (message.ownerSigningPublicKey.length !== 0) {
      writer.uint32(122).bytes(message.ownerSigningPublicKey);
    }
    if (message.directTx.length !== 0) {
      writer.uint32(130).bytes(message.directTx);
    }
    if (message.directRefundTx.length !== 0) {
      writer.uint32(138).bytes(message.directRefundTx);
    }
    if (message.directFromCpfpRefundTx.length !== 0) {
      writer.uint32(146).bytes(message.directFromCpfpRefundTx);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTreeNode();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.treeId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.value = longToNumber2(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.parentNodeId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.nodeTx = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.refundTx = reader.bytes();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }
          message.vout = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }
          message.verifyingPublicKey = reader.bytes();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }
          message.ownerIdentityPublicKey = reader.bytes();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.signingKeyshare = SigningKeyshare.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }
          message.status = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }
          message.network = reader.int32();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }
          message.createdTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }
          message.updatedTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }
          message.ownerSigningPublicKey = reader.bytes();
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }
          message.directTx = reader.bytes();
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }
          message.directRefundTx = reader.bytes();
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }
          message.directFromCpfpRefundTx = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      id: isSet3(object.id) ? globalThis.String(object.id) : "",
      treeId: isSet3(object.treeId) ? globalThis.String(object.treeId) : "",
      value: isSet3(object.value) ? globalThis.Number(object.value) : 0,
      parentNodeId: isSet3(object.parentNodeId) ? globalThis.String(object.parentNodeId) : void 0,
      nodeTx: isSet3(object.nodeTx) ? bytesFromBase642(object.nodeTx) : new Uint8Array(0),
      refundTx: isSet3(object.refundTx) ? bytesFromBase642(object.refundTx) : new Uint8Array(0),
      vout: isSet3(object.vout) ? globalThis.Number(object.vout) : 0,
      verifyingPublicKey: isSet3(object.verifyingPublicKey) ? bytesFromBase642(object.verifyingPublicKey) : new Uint8Array(0),
      ownerIdentityPublicKey: isSet3(object.ownerIdentityPublicKey) ? bytesFromBase642(object.ownerIdentityPublicKey) : new Uint8Array(0),
      signingKeyshare: isSet3(object.signingKeyshare) ? SigningKeyshare.fromJSON(object.signingKeyshare) : void 0,
      status: isSet3(object.status) ? globalThis.String(object.status) : "",
      network: isSet3(object.network) ? networkFromJSON(object.network) : 0,
      createdTime: isSet3(object.createdTime) ? fromJsonTimestamp(object.createdTime) : void 0,
      updatedTime: isSet3(object.updatedTime) ? fromJsonTimestamp(object.updatedTime) : void 0,
      ownerSigningPublicKey: isSet3(object.ownerSigningPublicKey) ? bytesFromBase642(object.ownerSigningPublicKey) : new Uint8Array(0),
      directTx: isSet3(object.directTx) ? bytesFromBase642(object.directTx) : new Uint8Array(0),
      directRefundTx: isSet3(object.directRefundTx) ? bytesFromBase642(object.directRefundTx) : new Uint8Array(0),
      directFromCpfpRefundTx: isSet3(object.directFromCpfpRefundTx) ? bytesFromBase642(object.directFromCpfpRefundTx) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.treeId !== "") {
      obj.treeId = message.treeId;
    }
    if (message.value !== 0) {
      obj.value = Math.round(message.value);
    }
    if (message.parentNodeId !== void 0) {
      obj.parentNodeId = message.parentNodeId;
    }
    if (message.nodeTx.length !== 0) {
      obj.nodeTx = base64FromBytes2(message.nodeTx);
    }
    if (message.refundTx.length !== 0) {
      obj.refundTx = base64FromBytes2(message.refundTx);
    }
    if (message.vout !== 0) {
      obj.vout = Math.round(message.vout);
    }
    if (message.verifyingPublicKey.length !== 0) {
      obj.verifyingPublicKey = base64FromBytes2(message.verifyingPublicKey);
    }
    if (message.ownerIdentityPublicKey.length !== 0) {
      obj.ownerIdentityPublicKey = base64FromBytes2(message.ownerIdentityPublicKey);
    }
    if (message.signingKeyshare !== void 0) {
      obj.signingKeyshare = SigningKeyshare.toJSON(message.signingKeyshare);
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.network !== 0) {
      obj.network = networkToJSON(message.network);
    }
    if (message.createdTime !== void 0) {
      obj.createdTime = message.createdTime.toISOString();
    }
    if (message.updatedTime !== void 0) {
      obj.updatedTime = message.updatedTime.toISOString();
    }
    if (message.ownerSigningPublicKey.length !== 0) {
      obj.ownerSigningPublicKey = base64FromBytes2(message.ownerSigningPublicKey);
    }
    if (message.directTx.length !== 0) {
      obj.directTx = base64FromBytes2(message.directTx);
    }
    if (message.directRefundTx.length !== 0) {
      obj.directRefundTx = base64FromBytes2(message.directRefundTx);
    }
    if (message.directFromCpfpRefundTx.length !== 0) {
      obj.directFromCpfpRefundTx = base64FromBytes2(message.directFromCpfpRefundTx);
    }
    return obj;
  },
  create(base) {
    return TreeNode.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseTreeNode();
    message.id = object.id ?? "";
    message.treeId = object.treeId ?? "";
    message.value = object.value ?? 0;
    message.parentNodeId = object.parentNodeId ?? void 0;
    message.nodeTx = object.nodeTx ?? new Uint8Array(0);
    message.refundTx = object.refundTx ?? new Uint8Array(0);
    message.vout = object.vout ?? 0;
    message.verifyingPublicKey = object.verifyingPublicKey ?? new Uint8Array(0);
    message.ownerIdentityPublicKey = object.ownerIdentityPublicKey ?? new Uint8Array(0);
    message.signingKeyshare = object.signingKeyshare !== void 0 && object.signingKeyshare !== null ? SigningKeyshare.fromPartial(object.signingKeyshare) : void 0;
    message.status = object.status ?? "";
    message.network = object.network ?? 0;
    message.createdTime = object.createdTime ?? void 0;
    message.updatedTime = object.updatedTime ?? void 0;
    message.ownerSigningPublicKey = object.ownerSigningPublicKey ?? new Uint8Array(0);
    message.directTx = object.directTx ?? new Uint8Array(0);
    message.directRefundTx = object.directRefundTx ?? new Uint8Array(0);
    message.directFromCpfpRefundTx = object.directFromCpfpRefundTx ?? new Uint8Array(0);
    return message;
  }
};
function createBaseFinalizeNodeSignaturesRequest() {
  return { intent: 0, nodeSignatures: [] };
}
var FinalizeNodeSignaturesRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.intent !== 0) {
      writer.uint32(8).int32(message.intent);
    }
    for (const v of message.nodeSignatures) {
      NodeSignatures.encode(v, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseFinalizeNodeSignaturesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.intent = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.nodeSignatures.push(NodeSignatures.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      intent: isSet3(object.intent) ? signatureIntentFromJSON(object.intent) : 0,
      nodeSignatures: globalThis.Array.isArray(object?.nodeSignatures) ? object.nodeSignatures.map((e) => NodeSignatures.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.intent !== 0) {
      obj.intent = signatureIntentToJSON(message.intent);
    }
    if (message.nodeSignatures?.length) {
      obj.nodeSignatures = message.nodeSignatures.map((e) => NodeSignatures.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return FinalizeNodeSignaturesRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseFinalizeNodeSignaturesRequest();
    message.intent = object.intent ?? 0;
    message.nodeSignatures = object.nodeSignatures?.map((e) => NodeSignatures.fromPartial(e)) || [];
    return message;
  }
};
function createBaseFinalizeNodeSignaturesResponse() {
  return { nodes: [] };
}
var FinalizeNodeSignaturesResponse = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.nodes) {
      TreeNode.encode(v, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseFinalizeNodeSignaturesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.nodes.push(TreeNode.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { nodes: globalThis.Array.isArray(object?.nodes) ? object.nodes.map((e) => TreeNode.fromJSON(e)) : [] };
  },
  toJSON(message) {
    const obj = {};
    if (message.nodes?.length) {
      obj.nodes = message.nodes.map((e) => TreeNode.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return FinalizeNodeSignaturesResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseFinalizeNodeSignaturesResponse();
    message.nodes = object.nodes?.map((e) => TreeNode.fromPartial(e)) || [];
    return message;
  }
};
function createBaseSecretShare() {
  return { secretShare: new Uint8Array(0), proofs: [] };
}
var SecretShare = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.secretShare.length !== 0) {
      writer.uint32(10).bytes(message.secretShare);
    }
    for (const v of message.proofs) {
      writer.uint32(18).bytes(v);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSecretShare();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.secretShare = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.proofs.push(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      secretShare: isSet3(object.secretShare) ? bytesFromBase642(object.secretShare) : new Uint8Array(0),
      proofs: globalThis.Array.isArray(object?.proofs) ? object.proofs.map((e) => bytesFromBase642(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.secretShare.length !== 0) {
      obj.secretShare = base64FromBytes2(message.secretShare);
    }
    if (message.proofs?.length) {
      obj.proofs = message.proofs.map((e) => base64FromBytes2(e));
    }
    return obj;
  },
  create(base) {
    return SecretShare.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSecretShare();
    message.secretShare = object.secretShare ?? new Uint8Array(0);
    message.proofs = object.proofs?.map((e) => e) || [];
    return message;
  }
};
function createBaseSecretProof() {
  return { proofs: [] };
}
var SecretProof = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.proofs) {
      writer.uint32(10).bytes(v);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSecretProof();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.proofs.push(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      proofs: globalThis.Array.isArray(object?.proofs) ? object.proofs.map((e) => bytesFromBase642(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.proofs?.length) {
      obj.proofs = message.proofs.map((e) => base64FromBytes2(e));
    }
    return obj;
  },
  create(base) {
    return SecretProof.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSecretProof();
    message.proofs = object.proofs?.map((e) => e) || [];
    return message;
  }
};
function createBaseLeafRefundTxSigningJob() {
  return {
    leafId: "",
    refundTxSigningJob: void 0,
    directRefundTxSigningJob: void 0,
    directFromCpfpRefundTxSigningJob: void 0
  };
}
var LeafRefundTxSigningJob = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.leafId !== "") {
      writer.uint32(10).string(message.leafId);
    }
    if (message.refundTxSigningJob !== void 0) {
      SigningJob.encode(message.refundTxSigningJob, writer.uint32(18).fork()).join();
    }
    if (message.directRefundTxSigningJob !== void 0) {
      SigningJob.encode(message.directRefundTxSigningJob, writer.uint32(26).fork()).join();
    }
    if (message.directFromCpfpRefundTxSigningJob !== void 0) {
      SigningJob.encode(message.directFromCpfpRefundTxSigningJob, writer.uint32(34).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseLeafRefundTxSigningJob();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.leafId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.refundTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.directRefundTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.directFromCpfpRefundTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      leafId: isSet3(object.leafId) ? globalThis.String(object.leafId) : "",
      refundTxSigningJob: isSet3(object.refundTxSigningJob) ? SigningJob.fromJSON(object.refundTxSigningJob) : void 0,
      directRefundTxSigningJob: isSet3(object.directRefundTxSigningJob) ? SigningJob.fromJSON(object.directRefundTxSigningJob) : void 0,
      directFromCpfpRefundTxSigningJob: isSet3(object.directFromCpfpRefundTxSigningJob) ? SigningJob.fromJSON(object.directFromCpfpRefundTxSigningJob) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.leafId !== "") {
      obj.leafId = message.leafId;
    }
    if (message.refundTxSigningJob !== void 0) {
      obj.refundTxSigningJob = SigningJob.toJSON(message.refundTxSigningJob);
    }
    if (message.directRefundTxSigningJob !== void 0) {
      obj.directRefundTxSigningJob = SigningJob.toJSON(message.directRefundTxSigningJob);
    }
    if (message.directFromCpfpRefundTxSigningJob !== void 0) {
      obj.directFromCpfpRefundTxSigningJob = SigningJob.toJSON(message.directFromCpfpRefundTxSigningJob);
    }
    return obj;
  },
  create(base) {
    return LeafRefundTxSigningJob.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseLeafRefundTxSigningJob();
    message.leafId = object.leafId ?? "";
    message.refundTxSigningJob = object.refundTxSigningJob !== void 0 && object.refundTxSigningJob !== null ? SigningJob.fromPartial(object.refundTxSigningJob) : void 0;
    message.directRefundTxSigningJob = object.directRefundTxSigningJob !== void 0 && object.directRefundTxSigningJob !== null ? SigningJob.fromPartial(object.directRefundTxSigningJob) : void 0;
    message.directFromCpfpRefundTxSigningJob = object.directFromCpfpRefundTxSigningJob !== void 0 && object.directFromCpfpRefundTxSigningJob !== null ? SigningJob.fromPartial(object.directFromCpfpRefundTxSigningJob) : void 0;
    return message;
  }
};
function createBaseUserSignedTxSigningJob() {
  return {
    leafId: "",
    signingPublicKey: new Uint8Array(0),
    rawTx: new Uint8Array(0),
    signingNonceCommitment: void 0,
    userSignature: new Uint8Array(0),
    signingCommitments: void 0
  };
}
var UserSignedTxSigningJob = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.leafId !== "") {
      writer.uint32(10).string(message.leafId);
    }
    if (message.signingPublicKey.length !== 0) {
      writer.uint32(18).bytes(message.signingPublicKey);
    }
    if (message.rawTx.length !== 0) {
      writer.uint32(26).bytes(message.rawTx);
    }
    if (message.signingNonceCommitment !== void 0) {
      SigningCommitment.encode(message.signingNonceCommitment, writer.uint32(34).fork()).join();
    }
    if (message.userSignature.length !== 0) {
      writer.uint32(42).bytes(message.userSignature);
    }
    if (message.signingCommitments !== void 0) {
      SigningCommitments.encode(message.signingCommitments, writer.uint32(50).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUserSignedTxSigningJob();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.leafId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.signingPublicKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.rawTx = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.signingNonceCommitment = SigningCommitment.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.userSignature = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.signingCommitments = SigningCommitments.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      leafId: isSet3(object.leafId) ? globalThis.String(object.leafId) : "",
      signingPublicKey: isSet3(object.signingPublicKey) ? bytesFromBase642(object.signingPublicKey) : new Uint8Array(0),
      rawTx: isSet3(object.rawTx) ? bytesFromBase642(object.rawTx) : new Uint8Array(0),
      signingNonceCommitment: isSet3(object.signingNonceCommitment) ? SigningCommitment.fromJSON(object.signingNonceCommitment) : void 0,
      userSignature: isSet3(object.userSignature) ? bytesFromBase642(object.userSignature) : new Uint8Array(0),
      signingCommitments: isSet3(object.signingCommitments) ? SigningCommitments.fromJSON(object.signingCommitments) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.leafId !== "") {
      obj.leafId = message.leafId;
    }
    if (message.signingPublicKey.length !== 0) {
      obj.signingPublicKey = base64FromBytes2(message.signingPublicKey);
    }
    if (message.rawTx.length !== 0) {
      obj.rawTx = base64FromBytes2(message.rawTx);
    }
    if (message.signingNonceCommitment !== void 0) {
      obj.signingNonceCommitment = SigningCommitment.toJSON(message.signingNonceCommitment);
    }
    if (message.userSignature.length !== 0) {
      obj.userSignature = base64FromBytes2(message.userSignature);
    }
    if (message.signingCommitments !== void 0) {
      obj.signingCommitments = SigningCommitments.toJSON(message.signingCommitments);
    }
    return obj;
  },
  create(base) {
    return UserSignedTxSigningJob.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseUserSignedTxSigningJob();
    message.leafId = object.leafId ?? "";
    message.signingPublicKey = object.signingPublicKey ?? new Uint8Array(0);
    message.rawTx = object.rawTx ?? new Uint8Array(0);
    message.signingNonceCommitment = object.signingNonceCommitment !== void 0 && object.signingNonceCommitment !== null ? SigningCommitment.fromPartial(object.signingNonceCommitment) : void 0;
    message.userSignature = object.userSignature ?? new Uint8Array(0);
    message.signingCommitments = object.signingCommitments !== void 0 && object.signingCommitments !== null ? SigningCommitments.fromPartial(object.signingCommitments) : void 0;
    return message;
  }
};
function createBaseLeafRefundTxSigningResult() {
  return {
    leafId: "",
    refundTxSigningResult: void 0,
    verifyingKey: new Uint8Array(0),
    directRefundTxSigningResult: void 0,
    directFromCpfpRefundTxSigningResult: void 0
  };
}
var LeafRefundTxSigningResult = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.leafId !== "") {
      writer.uint32(10).string(message.leafId);
    }
    if (message.refundTxSigningResult !== void 0) {
      SigningResult2.encode(message.refundTxSigningResult, writer.uint32(18).fork()).join();
    }
    if (message.verifyingKey.length !== 0) {
      writer.uint32(26).bytes(message.verifyingKey);
    }
    if (message.directRefundTxSigningResult !== void 0) {
      SigningResult2.encode(message.directRefundTxSigningResult, writer.uint32(34).fork()).join();
    }
    if (message.directFromCpfpRefundTxSigningResult !== void 0) {
      SigningResult2.encode(message.directFromCpfpRefundTxSigningResult, writer.uint32(42).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseLeafRefundTxSigningResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.leafId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.refundTxSigningResult = SigningResult2.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.verifyingKey = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.directRefundTxSigningResult = SigningResult2.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.directFromCpfpRefundTxSigningResult = SigningResult2.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      leafId: isSet3(object.leafId) ? globalThis.String(object.leafId) : "",
      refundTxSigningResult: isSet3(object.refundTxSigningResult) ? SigningResult2.fromJSON(object.refundTxSigningResult) : void 0,
      verifyingKey: isSet3(object.verifyingKey) ? bytesFromBase642(object.verifyingKey) : new Uint8Array(0),
      directRefundTxSigningResult: isSet3(object.directRefundTxSigningResult) ? SigningResult2.fromJSON(object.directRefundTxSigningResult) : void 0,
      directFromCpfpRefundTxSigningResult: isSet3(object.directFromCpfpRefundTxSigningResult) ? SigningResult2.fromJSON(object.directFromCpfpRefundTxSigningResult) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.leafId !== "") {
      obj.leafId = message.leafId;
    }
    if (message.refundTxSigningResult !== void 0) {
      obj.refundTxSigningResult = SigningResult2.toJSON(message.refundTxSigningResult);
    }
    if (message.verifyingKey.length !== 0) {
      obj.verifyingKey = base64FromBytes2(message.verifyingKey);
    }
    if (message.directRefundTxSigningResult !== void 0) {
      obj.directRefundTxSigningResult = SigningResult2.toJSON(message.directRefundTxSigningResult);
    }
    if (message.directFromCpfpRefundTxSigningResult !== void 0) {
      obj.directFromCpfpRefundTxSigningResult = SigningResult2.toJSON(message.directFromCpfpRefundTxSigningResult);
    }
    return obj;
  },
  create(base) {
    return LeafRefundTxSigningResult.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseLeafRefundTxSigningResult();
    message.leafId = object.leafId ?? "";
    message.refundTxSigningResult = object.refundTxSigningResult !== void 0 && object.refundTxSigningResult !== null ? SigningResult2.fromPartial(object.refundTxSigningResult) : void 0;
    message.verifyingKey = object.verifyingKey ?? new Uint8Array(0);
    message.directRefundTxSigningResult = object.directRefundTxSigningResult !== void 0 && object.directRefundTxSigningResult !== null ? SigningResult2.fromPartial(object.directRefundTxSigningResult) : void 0;
    message.directFromCpfpRefundTxSigningResult = object.directFromCpfpRefundTxSigningResult !== void 0 && object.directFromCpfpRefundTxSigningResult !== null ? SigningResult2.fromPartial(object.directFromCpfpRefundTxSigningResult) : void 0;
    return message;
  }
};
function createBaseStartUserSignedTransferRequest() {
  return {
    transferId: "",
    ownerIdentityPublicKey: new Uint8Array(0),
    leavesToSend: [],
    receiverIdentityPublicKey: new Uint8Array(0),
    expiryTime: void 0,
    directLeavesToSend: [],
    directFromCpfpLeavesToSend: []
  };
}
var StartUserSignedTransferRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.transferId !== "") {
      writer.uint32(10).string(message.transferId);
    }
    if (message.ownerIdentityPublicKey.length !== 0) {
      writer.uint32(18).bytes(message.ownerIdentityPublicKey);
    }
    for (const v of message.leavesToSend) {
      UserSignedTxSigningJob.encode(v, writer.uint32(26).fork()).join();
    }
    if (message.receiverIdentityPublicKey.length !== 0) {
      writer.uint32(34).bytes(message.receiverIdentityPublicKey);
    }
    if (message.expiryTime !== void 0) {
      Timestamp.encode(toTimestamp(message.expiryTime), writer.uint32(42).fork()).join();
    }
    for (const v of message.directLeavesToSend) {
      UserSignedTxSigningJob.encode(v, writer.uint32(50).fork()).join();
    }
    for (const v of message.directFromCpfpLeavesToSend) {
      UserSignedTxSigningJob.encode(v, writer.uint32(58).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseStartUserSignedTransferRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.transferId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.ownerIdentityPublicKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.leavesToSend.push(UserSignedTxSigningJob.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.receiverIdentityPublicKey = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.expiryTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.directLeavesToSend.push(UserSignedTxSigningJob.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.directFromCpfpLeavesToSend.push(UserSignedTxSigningJob.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      transferId: isSet3(object.transferId) ? globalThis.String(object.transferId) : "",
      ownerIdentityPublicKey: isSet3(object.ownerIdentityPublicKey) ? bytesFromBase642(object.ownerIdentityPublicKey) : new Uint8Array(0),
      leavesToSend: globalThis.Array.isArray(object?.leavesToSend) ? object.leavesToSend.map((e) => UserSignedTxSigningJob.fromJSON(e)) : [],
      receiverIdentityPublicKey: isSet3(object.receiverIdentityPublicKey) ? bytesFromBase642(object.receiverIdentityPublicKey) : new Uint8Array(0),
      expiryTime: isSet3(object.expiryTime) ? fromJsonTimestamp(object.expiryTime) : void 0,
      directLeavesToSend: globalThis.Array.isArray(object?.directLeavesToSend) ? object.directLeavesToSend.map((e) => UserSignedTxSigningJob.fromJSON(e)) : [],
      directFromCpfpLeavesToSend: globalThis.Array.isArray(object?.directFromCpfpLeavesToSend) ? object.directFromCpfpLeavesToSend.map((e) => UserSignedTxSigningJob.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.transferId !== "") {
      obj.transferId = message.transferId;
    }
    if (message.ownerIdentityPublicKey.length !== 0) {
      obj.ownerIdentityPublicKey = base64FromBytes2(message.ownerIdentityPublicKey);
    }
    if (message.leavesToSend?.length) {
      obj.leavesToSend = message.leavesToSend.map((e) => UserSignedTxSigningJob.toJSON(e));
    }
    if (message.receiverIdentityPublicKey.length !== 0) {
      obj.receiverIdentityPublicKey = base64FromBytes2(message.receiverIdentityPublicKey);
    }
    if (message.expiryTime !== void 0) {
      obj.expiryTime = message.expiryTime.toISOString();
    }
    if (message.directLeavesToSend?.length) {
      obj.directLeavesToSend = message.directLeavesToSend.map((e) => UserSignedTxSigningJob.toJSON(e));
    }
    if (message.directFromCpfpLeavesToSend?.length) {
      obj.directFromCpfpLeavesToSend = message.directFromCpfpLeavesToSend.map((e) => UserSignedTxSigningJob.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return StartUserSignedTransferRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseStartUserSignedTransferRequest();
    message.transferId = object.transferId ?? "";
    message.ownerIdentityPublicKey = object.ownerIdentityPublicKey ?? new Uint8Array(0);
    message.leavesToSend = object.leavesToSend?.map((e) => UserSignedTxSigningJob.fromPartial(e)) || [];
    message.receiverIdentityPublicKey = object.receiverIdentityPublicKey ?? new Uint8Array(0);
    message.expiryTime = object.expiryTime ?? void 0;
    message.directLeavesToSend = object.directLeavesToSend?.map((e) => UserSignedTxSigningJob.fromPartial(e)) || [];
    message.directFromCpfpLeavesToSend = object.directFromCpfpLeavesToSend?.map((e) => UserSignedTxSigningJob.fromPartial(e)) || [];
    return message;
  }
};
function createBaseStartTransferRequest() {
  return {
    transferId: "",
    ownerIdentityPublicKey: new Uint8Array(0),
    leavesToSend: [],
    receiverIdentityPublicKey: new Uint8Array(0),
    expiryTime: void 0,
    transferPackage: void 0,
    sparkInvoice: ""
  };
}
var StartTransferRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.transferId !== "") {
      writer.uint32(10).string(message.transferId);
    }
    if (message.ownerIdentityPublicKey.length !== 0) {
      writer.uint32(18).bytes(message.ownerIdentityPublicKey);
    }
    for (const v of message.leavesToSend) {
      LeafRefundTxSigningJob.encode(v, writer.uint32(26).fork()).join();
    }
    if (message.receiverIdentityPublicKey.length !== 0) {
      writer.uint32(34).bytes(message.receiverIdentityPublicKey);
    }
    if (message.expiryTime !== void 0) {
      Timestamp.encode(toTimestamp(message.expiryTime), writer.uint32(42).fork()).join();
    }
    if (message.transferPackage !== void 0) {
      TransferPackage.encode(message.transferPackage, writer.uint32(58).fork()).join();
    }
    if (message.sparkInvoice !== "") {
      writer.uint32(82).string(message.sparkInvoice);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseStartTransferRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.transferId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.ownerIdentityPublicKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.leavesToSend.push(LeafRefundTxSigningJob.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.receiverIdentityPublicKey = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.expiryTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.transferPackage = TransferPackage.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.sparkInvoice = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      transferId: isSet3(object.transferId) ? globalThis.String(object.transferId) : "",
      ownerIdentityPublicKey: isSet3(object.ownerIdentityPublicKey) ? bytesFromBase642(object.ownerIdentityPublicKey) : new Uint8Array(0),
      leavesToSend: globalThis.Array.isArray(object?.leavesToSend) ? object.leavesToSend.map((e) => LeafRefundTxSigningJob.fromJSON(e)) : [],
      receiverIdentityPublicKey: isSet3(object.receiverIdentityPublicKey) ? bytesFromBase642(object.receiverIdentityPublicKey) : new Uint8Array(0),
      expiryTime: isSet3(object.expiryTime) ? fromJsonTimestamp(object.expiryTime) : void 0,
      transferPackage: isSet3(object.transferPackage) ? TransferPackage.fromJSON(object.transferPackage) : void 0,
      sparkInvoice: isSet3(object.sparkInvoice) ? globalThis.String(object.sparkInvoice) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.transferId !== "") {
      obj.transferId = message.transferId;
    }
    if (message.ownerIdentityPublicKey.length !== 0) {
      obj.ownerIdentityPublicKey = base64FromBytes2(message.ownerIdentityPublicKey);
    }
    if (message.leavesToSend?.length) {
      obj.leavesToSend = message.leavesToSend.map((e) => LeafRefundTxSigningJob.toJSON(e));
    }
    if (message.receiverIdentityPublicKey.length !== 0) {
      obj.receiverIdentityPublicKey = base64FromBytes2(message.receiverIdentityPublicKey);
    }
    if (message.expiryTime !== void 0) {
      obj.expiryTime = message.expiryTime.toISOString();
    }
    if (message.transferPackage !== void 0) {
      obj.transferPackage = TransferPackage.toJSON(message.transferPackage);
    }
    if (message.sparkInvoice !== "") {
      obj.sparkInvoice = message.sparkInvoice;
    }
    return obj;
  },
  create(base) {
    return StartTransferRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseStartTransferRequest();
    message.transferId = object.transferId ?? "";
    message.ownerIdentityPublicKey = object.ownerIdentityPublicKey ?? new Uint8Array(0);
    message.leavesToSend = object.leavesToSend?.map((e) => LeafRefundTxSigningJob.fromPartial(e)) || [];
    message.receiverIdentityPublicKey = object.receiverIdentityPublicKey ?? new Uint8Array(0);
    message.expiryTime = object.expiryTime ?? void 0;
    message.transferPackage = object.transferPackage !== void 0 && object.transferPackage !== null ? TransferPackage.fromPartial(object.transferPackage) : void 0;
    message.sparkInvoice = object.sparkInvoice ?? "";
    return message;
  }
};
function createBaseStartTransferResponse() {
  return { transfer: void 0, signingResults: [] };
}
var StartTransferResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.transfer !== void 0) {
      Transfer.encode(message.transfer, writer.uint32(10).fork()).join();
    }
    for (const v of message.signingResults) {
      LeafRefundTxSigningResult.encode(v, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseStartTransferResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.transfer = Transfer.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.signingResults.push(LeafRefundTxSigningResult.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      transfer: isSet3(object.transfer) ? Transfer.fromJSON(object.transfer) : void 0,
      signingResults: globalThis.Array.isArray(object?.signingResults) ? object.signingResults.map((e) => LeafRefundTxSigningResult.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.transfer !== void 0) {
      obj.transfer = Transfer.toJSON(message.transfer);
    }
    if (message.signingResults?.length) {
      obj.signingResults = message.signingResults.map((e) => LeafRefundTxSigningResult.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return StartTransferResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseStartTransferResponse();
    message.transfer = object.transfer !== void 0 && object.transfer !== null ? Transfer.fromPartial(object.transfer) : void 0;
    message.signingResults = object.signingResults?.map((e) => LeafRefundTxSigningResult.fromPartial(e)) || [];
    return message;
  }
};
function createBaseTransferPackage() {
  return {
    leavesToSend: [],
    keyTweakPackage: {},
    userSignature: new Uint8Array(0),
    directLeavesToSend: [],
    directFromCpfpLeavesToSend: []
  };
}
var TransferPackage = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.leavesToSend) {
      UserSignedTxSigningJob.encode(v, writer.uint32(10).fork()).join();
    }
    Object.entries(message.keyTweakPackage).forEach(([key, value]) => {
      TransferPackage_KeyTweakPackageEntry.encode({ key, value }, writer.uint32(18).fork()).join();
    });
    if (message.userSignature.length !== 0) {
      writer.uint32(26).bytes(message.userSignature);
    }
    for (const v of message.directLeavesToSend) {
      UserSignedTxSigningJob.encode(v, writer.uint32(34).fork()).join();
    }
    for (const v of message.directFromCpfpLeavesToSend) {
      UserSignedTxSigningJob.encode(v, writer.uint32(42).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTransferPackage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.leavesToSend.push(UserSignedTxSigningJob.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          const entry2 = TransferPackage_KeyTweakPackageEntry.decode(reader, reader.uint32());
          if (entry2.value !== void 0) {
            message.keyTweakPackage[entry2.key] = entry2.value;
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.userSignature = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.directLeavesToSend.push(UserSignedTxSigningJob.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.directFromCpfpLeavesToSend.push(UserSignedTxSigningJob.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      leavesToSend: globalThis.Array.isArray(object?.leavesToSend) ? object.leavesToSend.map((e) => UserSignedTxSigningJob.fromJSON(e)) : [],
      keyTweakPackage: isObject2(object.keyTweakPackage) ? Object.entries(object.keyTweakPackage).reduce((acc, [key, value]) => {
        acc[key] = bytesFromBase642(value);
        return acc;
      }, {}) : {},
      userSignature: isSet3(object.userSignature) ? bytesFromBase642(object.userSignature) : new Uint8Array(0),
      directLeavesToSend: globalThis.Array.isArray(object?.directLeavesToSend) ? object.directLeavesToSend.map((e) => UserSignedTxSigningJob.fromJSON(e)) : [],
      directFromCpfpLeavesToSend: globalThis.Array.isArray(object?.directFromCpfpLeavesToSend) ? object.directFromCpfpLeavesToSend.map((e) => UserSignedTxSigningJob.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.leavesToSend?.length) {
      obj.leavesToSend = message.leavesToSend.map((e) => UserSignedTxSigningJob.toJSON(e));
    }
    if (message.keyTweakPackage) {
      const entries = Object.entries(message.keyTweakPackage);
      if (entries.length > 0) {
        obj.keyTweakPackage = {};
        entries.forEach(([k, v]) => {
          obj.keyTweakPackage[k] = base64FromBytes2(v);
        });
      }
    }
    if (message.userSignature.length !== 0) {
      obj.userSignature = base64FromBytes2(message.userSignature);
    }
    if (message.directLeavesToSend?.length) {
      obj.directLeavesToSend = message.directLeavesToSend.map((e) => UserSignedTxSigningJob.toJSON(e));
    }
    if (message.directFromCpfpLeavesToSend?.length) {
      obj.directFromCpfpLeavesToSend = message.directFromCpfpLeavesToSend.map((e) => UserSignedTxSigningJob.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return TransferPackage.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseTransferPackage();
    message.leavesToSend = object.leavesToSend?.map((e) => UserSignedTxSigningJob.fromPartial(e)) || [];
    message.keyTweakPackage = Object.entries(object.keyTweakPackage ?? {}).reduce(
      (acc, [key, value]) => {
        if (value !== void 0) {
          acc[key] = value;
        }
        return acc;
      },
      {}
    );
    message.userSignature = object.userSignature ?? new Uint8Array(0);
    message.directLeavesToSend = object.directLeavesToSend?.map((e) => UserSignedTxSigningJob.fromPartial(e)) || [];
    message.directFromCpfpLeavesToSend = object.directFromCpfpLeavesToSend?.map((e) => UserSignedTxSigningJob.fromPartial(e)) || [];
    return message;
  }
};
function createBaseTransferPackage_KeyTweakPackageEntry() {
  return { key: "", value: new Uint8Array(0) };
}
var TransferPackage_KeyTweakPackageEntry = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value.length !== 0) {
      writer.uint32(18).bytes(message.value);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTransferPackage_KeyTweakPackageEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.value = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet3(object.key) ? globalThis.String(object.key) : "",
      value: isSet3(object.value) ? bytesFromBase642(object.value) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value.length !== 0) {
      obj.value = base64FromBytes2(message.value);
    }
    return obj;
  },
  create(base) {
    return TransferPackage_KeyTweakPackageEntry.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseTransferPackage_KeyTweakPackageEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? new Uint8Array(0);
    return message;
  }
};
function createBaseSendLeafKeyTweaks() {
  return { leavesToSend: [] };
}
var SendLeafKeyTweaks = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.leavesToSend) {
      SendLeafKeyTweak.encode(v, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSendLeafKeyTweaks();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.leavesToSend.push(SendLeafKeyTweak.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      leavesToSend: globalThis.Array.isArray(object?.leavesToSend) ? object.leavesToSend.map((e) => SendLeafKeyTweak.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.leavesToSend?.length) {
      obj.leavesToSend = message.leavesToSend.map((e) => SendLeafKeyTweak.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return SendLeafKeyTweaks.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSendLeafKeyTweaks();
    message.leavesToSend = object.leavesToSend?.map((e) => SendLeafKeyTweak.fromPartial(e)) || [];
    return message;
  }
};
function createBaseSendLeafKeyTweak() {
  return {
    leafId: "",
    secretShareTweak: void 0,
    pubkeySharesTweak: {},
    secretCipher: new Uint8Array(0),
    signature: new Uint8Array(0),
    refundSignature: new Uint8Array(0),
    directRefundSignature: new Uint8Array(0),
    directFromCpfpRefundSignature: new Uint8Array(0)
  };
}
var SendLeafKeyTweak = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.leafId !== "") {
      writer.uint32(10).string(message.leafId);
    }
    if (message.secretShareTweak !== void 0) {
      SecretShare.encode(message.secretShareTweak, writer.uint32(18).fork()).join();
    }
    Object.entries(message.pubkeySharesTweak).forEach(([key, value]) => {
      SendLeafKeyTweak_PubkeySharesTweakEntry.encode({ key, value }, writer.uint32(26).fork()).join();
    });
    if (message.secretCipher.length !== 0) {
      writer.uint32(34).bytes(message.secretCipher);
    }
    if (message.signature.length !== 0) {
      writer.uint32(42).bytes(message.signature);
    }
    if (message.refundSignature.length !== 0) {
      writer.uint32(50).bytes(message.refundSignature);
    }
    if (message.directRefundSignature.length !== 0) {
      writer.uint32(58).bytes(message.directRefundSignature);
    }
    if (message.directFromCpfpRefundSignature.length !== 0) {
      writer.uint32(66).bytes(message.directFromCpfpRefundSignature);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSendLeafKeyTweak();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.leafId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.secretShareTweak = SecretShare.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          const entry3 = SendLeafKeyTweak_PubkeySharesTweakEntry.decode(reader, reader.uint32());
          if (entry3.value !== void 0) {
            message.pubkeySharesTweak[entry3.key] = entry3.value;
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.secretCipher = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.signature = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.refundSignature = reader.bytes();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.directRefundSignature = reader.bytes();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }
          message.directFromCpfpRefundSignature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      leafId: isSet3(object.leafId) ? globalThis.String(object.leafId) : "",
      secretShareTweak: isSet3(object.secretShareTweak) ? SecretShare.fromJSON(object.secretShareTweak) : void 0,
      pubkeySharesTweak: isObject2(object.pubkeySharesTweak) ? Object.entries(object.pubkeySharesTweak).reduce((acc, [key, value]) => {
        acc[key] = bytesFromBase642(value);
        return acc;
      }, {}) : {},
      secretCipher: isSet3(object.secretCipher) ? bytesFromBase642(object.secretCipher) : new Uint8Array(0),
      signature: isSet3(object.signature) ? bytesFromBase642(object.signature) : new Uint8Array(0),
      refundSignature: isSet3(object.refundSignature) ? bytesFromBase642(object.refundSignature) : new Uint8Array(0),
      directRefundSignature: isSet3(object.directRefundSignature) ? bytesFromBase642(object.directRefundSignature) : new Uint8Array(0),
      directFromCpfpRefundSignature: isSet3(object.directFromCpfpRefundSignature) ? bytesFromBase642(object.directFromCpfpRefundSignature) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.leafId !== "") {
      obj.leafId = message.leafId;
    }
    if (message.secretShareTweak !== void 0) {
      obj.secretShareTweak = SecretShare.toJSON(message.secretShareTweak);
    }
    if (message.pubkeySharesTweak) {
      const entries = Object.entries(message.pubkeySharesTweak);
      if (entries.length > 0) {
        obj.pubkeySharesTweak = {};
        entries.forEach(([k, v]) => {
          obj.pubkeySharesTweak[k] = base64FromBytes2(v);
        });
      }
    }
    if (message.secretCipher.length !== 0) {
      obj.secretCipher = base64FromBytes2(message.secretCipher);
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes2(message.signature);
    }
    if (message.refundSignature.length !== 0) {
      obj.refundSignature = base64FromBytes2(message.refundSignature);
    }
    if (message.directRefundSignature.length !== 0) {
      obj.directRefundSignature = base64FromBytes2(message.directRefundSignature);
    }
    if (message.directFromCpfpRefundSignature.length !== 0) {
      obj.directFromCpfpRefundSignature = base64FromBytes2(message.directFromCpfpRefundSignature);
    }
    return obj;
  },
  create(base) {
    return SendLeafKeyTweak.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSendLeafKeyTweak();
    message.leafId = object.leafId ?? "";
    message.secretShareTweak = object.secretShareTweak !== void 0 && object.secretShareTweak !== null ? SecretShare.fromPartial(object.secretShareTweak) : void 0;
    message.pubkeySharesTweak = Object.entries(object.pubkeySharesTweak ?? {}).reduce(
      (acc, [key, value]) => {
        if (value !== void 0) {
          acc[key] = value;
        }
        return acc;
      },
      {}
    );
    message.secretCipher = object.secretCipher ?? new Uint8Array(0);
    message.signature = object.signature ?? new Uint8Array(0);
    message.refundSignature = object.refundSignature ?? new Uint8Array(0);
    message.directRefundSignature = object.directRefundSignature ?? new Uint8Array(0);
    message.directFromCpfpRefundSignature = object.directFromCpfpRefundSignature ?? new Uint8Array(0);
    return message;
  }
};
function createBaseSendLeafKeyTweak_PubkeySharesTweakEntry() {
  return { key: "", value: new Uint8Array(0) };
}
var SendLeafKeyTweak_PubkeySharesTweakEntry = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value.length !== 0) {
      writer.uint32(18).bytes(message.value);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSendLeafKeyTweak_PubkeySharesTweakEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.value = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet3(object.key) ? globalThis.String(object.key) : "",
      value: isSet3(object.value) ? bytesFromBase642(object.value) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value.length !== 0) {
      obj.value = base64FromBytes2(message.value);
    }
    return obj;
  },
  create(base) {
    return SendLeafKeyTweak_PubkeySharesTweakEntry.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSendLeafKeyTweak_PubkeySharesTweakEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? new Uint8Array(0);
    return message;
  }
};
function createBaseFinalizeTransferRequest() {
  return { transferId: "", ownerIdentityPublicKey: new Uint8Array(0), leavesToSend: [], sparkPaymentIntent: "" };
}
var FinalizeTransferRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.transferId !== "") {
      writer.uint32(10).string(message.transferId);
    }
    if (message.ownerIdentityPublicKey.length !== 0) {
      writer.uint32(18).bytes(message.ownerIdentityPublicKey);
    }
    for (const v of message.leavesToSend) {
      SendLeafKeyTweak.encode(v, writer.uint32(26).fork()).join();
    }
    if (message.sparkPaymentIntent !== "") {
      writer.uint32(34).string(message.sparkPaymentIntent);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseFinalizeTransferRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.transferId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.ownerIdentityPublicKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.leavesToSend.push(SendLeafKeyTweak.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.sparkPaymentIntent = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      transferId: isSet3(object.transferId) ? globalThis.String(object.transferId) : "",
      ownerIdentityPublicKey: isSet3(object.ownerIdentityPublicKey) ? bytesFromBase642(object.ownerIdentityPublicKey) : new Uint8Array(0),
      leavesToSend: globalThis.Array.isArray(object?.leavesToSend) ? object.leavesToSend.map((e) => SendLeafKeyTweak.fromJSON(e)) : [],
      sparkPaymentIntent: isSet3(object.sparkPaymentIntent) ? globalThis.String(object.sparkPaymentIntent) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.transferId !== "") {
      obj.transferId = message.transferId;
    }
    if (message.ownerIdentityPublicKey.length !== 0) {
      obj.ownerIdentityPublicKey = base64FromBytes2(message.ownerIdentityPublicKey);
    }
    if (message.leavesToSend?.length) {
      obj.leavesToSend = message.leavesToSend.map((e) => SendLeafKeyTweak.toJSON(e));
    }
    if (message.sparkPaymentIntent !== "") {
      obj.sparkPaymentIntent = message.sparkPaymentIntent;
    }
    return obj;
  },
  create(base) {
    return FinalizeTransferRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseFinalizeTransferRequest();
    message.transferId = object.transferId ?? "";
    message.ownerIdentityPublicKey = object.ownerIdentityPublicKey ?? new Uint8Array(0);
    message.leavesToSend = object.leavesToSend?.map((e) => SendLeafKeyTweak.fromPartial(e)) || [];
    message.sparkPaymentIntent = object.sparkPaymentIntent ?? "";
    return message;
  }
};
function createBaseFinalizeTransferWithTransferPackageRequest() {
  return { transferId: "", ownerIdentityPublicKey: new Uint8Array(0), transferPackage: void 0 };
}
var FinalizeTransferWithTransferPackageRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.transferId !== "") {
      writer.uint32(10).string(message.transferId);
    }
    if (message.ownerIdentityPublicKey.length !== 0) {
      writer.uint32(18).bytes(message.ownerIdentityPublicKey);
    }
    if (message.transferPackage !== void 0) {
      TransferPackage.encode(message.transferPackage, writer.uint32(26).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseFinalizeTransferWithTransferPackageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.transferId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.ownerIdentityPublicKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.transferPackage = TransferPackage.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      transferId: isSet3(object.transferId) ? globalThis.String(object.transferId) : "",
      ownerIdentityPublicKey: isSet3(object.ownerIdentityPublicKey) ? bytesFromBase642(object.ownerIdentityPublicKey) : new Uint8Array(0),
      transferPackage: isSet3(object.transferPackage) ? TransferPackage.fromJSON(object.transferPackage) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.transferId !== "") {
      obj.transferId = message.transferId;
    }
    if (message.ownerIdentityPublicKey.length !== 0) {
      obj.ownerIdentityPublicKey = base64FromBytes2(message.ownerIdentityPublicKey);
    }
    if (message.transferPackage !== void 0) {
      obj.transferPackage = TransferPackage.toJSON(message.transferPackage);
    }
    return obj;
  },
  create(base) {
    return FinalizeTransferWithTransferPackageRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseFinalizeTransferWithTransferPackageRequest();
    message.transferId = object.transferId ?? "";
    message.ownerIdentityPublicKey = object.ownerIdentityPublicKey ?? new Uint8Array(0);
    message.transferPackage = object.transferPackage !== void 0 && object.transferPackage !== null ? TransferPackage.fromPartial(object.transferPackage) : void 0;
    return message;
  }
};
function createBaseFinalizeTransferResponse() {
  return { transfer: void 0 };
}
var FinalizeTransferResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.transfer !== void 0) {
      Transfer.encode(message.transfer, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseFinalizeTransferResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.transfer = Transfer.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { transfer: isSet3(object.transfer) ? Transfer.fromJSON(object.transfer) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.transfer !== void 0) {
      obj.transfer = Transfer.toJSON(message.transfer);
    }
    return obj;
  },
  create(base) {
    return FinalizeTransferResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseFinalizeTransferResponse();
    message.transfer = object.transfer !== void 0 && object.transfer !== null ? Transfer.fromPartial(object.transfer) : void 0;
    return message;
  }
};
function createBaseTransfer() {
  return {
    id: "",
    senderIdentityPublicKey: new Uint8Array(0),
    receiverIdentityPublicKey: new Uint8Array(0),
    status: 0,
    totalValue: 0,
    expiryTime: void 0,
    leaves: [],
    createdTime: void 0,
    updatedTime: void 0,
    type: 0,
    sparkInvoice: ""
  };
}
var Transfer = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.senderIdentityPublicKey.length !== 0) {
      writer.uint32(18).bytes(message.senderIdentityPublicKey);
    }
    if (message.receiverIdentityPublicKey.length !== 0) {
      writer.uint32(26).bytes(message.receiverIdentityPublicKey);
    }
    if (message.status !== 0) {
      writer.uint32(32).int32(message.status);
    }
    if (message.totalValue !== 0) {
      writer.uint32(40).uint64(message.totalValue);
    }
    if (message.expiryTime !== void 0) {
      Timestamp.encode(toTimestamp(message.expiryTime), writer.uint32(50).fork()).join();
    }
    for (const v of message.leaves) {
      TransferLeaf.encode(v, writer.uint32(58).fork()).join();
    }
    if (message.createdTime !== void 0) {
      Timestamp.encode(toTimestamp(message.createdTime), writer.uint32(66).fork()).join();
    }
    if (message.updatedTime !== void 0) {
      Timestamp.encode(toTimestamp(message.updatedTime), writer.uint32(74).fork()).join();
    }
    if (message.type !== 0) {
      writer.uint32(80).int32(message.type);
    }
    if (message.sparkInvoice !== "") {
      writer.uint32(90).string(message.sparkInvoice);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTransfer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.senderIdentityPublicKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.receiverIdentityPublicKey = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }
          message.status = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }
          message.totalValue = longToNumber2(reader.uint64());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.expiryTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.leaves.push(TransferLeaf.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }
          message.createdTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }
          message.updatedTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }
          message.type = reader.int32();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }
          message.sparkInvoice = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      id: isSet3(object.id) ? globalThis.String(object.id) : "",
      senderIdentityPublicKey: isSet3(object.senderIdentityPublicKey) ? bytesFromBase642(object.senderIdentityPublicKey) : new Uint8Array(0),
      receiverIdentityPublicKey: isSet3(object.receiverIdentityPublicKey) ? bytesFromBase642(object.receiverIdentityPublicKey) : new Uint8Array(0),
      status: isSet3(object.status) ? transferStatusFromJSON(object.status) : 0,
      totalValue: isSet3(object.totalValue) ? globalThis.Number(object.totalValue) : 0,
      expiryTime: isSet3(object.expiryTime) ? fromJsonTimestamp(object.expiryTime) : void 0,
      leaves: globalThis.Array.isArray(object?.leaves) ? object.leaves.map((e) => TransferLeaf.fromJSON(e)) : [],
      createdTime: isSet3(object.createdTime) ? fromJsonTimestamp(object.createdTime) : void 0,
      updatedTime: isSet3(object.updatedTime) ? fromJsonTimestamp(object.updatedTime) : void 0,
      type: isSet3(object.type) ? transferTypeFromJSON(object.type) : 0,
      sparkInvoice: isSet3(object.sparkInvoice) ? globalThis.String(object.sparkInvoice) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.senderIdentityPublicKey.length !== 0) {
      obj.senderIdentityPublicKey = base64FromBytes2(message.senderIdentityPublicKey);
    }
    if (message.receiverIdentityPublicKey.length !== 0) {
      obj.receiverIdentityPublicKey = base64FromBytes2(message.receiverIdentityPublicKey);
    }
    if (message.status !== 0) {
      obj.status = transferStatusToJSON(message.status);
    }
    if (message.totalValue !== 0) {
      obj.totalValue = Math.round(message.totalValue);
    }
    if (message.expiryTime !== void 0) {
      obj.expiryTime = message.expiryTime.toISOString();
    }
    if (message.leaves?.length) {
      obj.leaves = message.leaves.map((e) => TransferLeaf.toJSON(e));
    }
    if (message.createdTime !== void 0) {
      obj.createdTime = message.createdTime.toISOString();
    }
    if (message.updatedTime !== void 0) {
      obj.updatedTime = message.updatedTime.toISOString();
    }
    if (message.type !== 0) {
      obj.type = transferTypeToJSON(message.type);
    }
    if (message.sparkInvoice !== "") {
      obj.sparkInvoice = message.sparkInvoice;
    }
    return obj;
  },
  create(base) {
    return Transfer.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseTransfer();
    message.id = object.id ?? "";
    message.senderIdentityPublicKey = object.senderIdentityPublicKey ?? new Uint8Array(0);
    message.receiverIdentityPublicKey = object.receiverIdentityPublicKey ?? new Uint8Array(0);
    message.status = object.status ?? 0;
    message.totalValue = object.totalValue ?? 0;
    message.expiryTime = object.expiryTime ?? void 0;
    message.leaves = object.leaves?.map((e) => TransferLeaf.fromPartial(e)) || [];
    message.createdTime = object.createdTime ?? void 0;
    message.updatedTime = object.updatedTime ?? void 0;
    message.type = object.type ?? 0;
    message.sparkInvoice = object.sparkInvoice ?? "";
    return message;
  }
};
function createBaseTransferLeaf() {
  return {
    leaf: void 0,
    secretCipher: new Uint8Array(0),
    signature: new Uint8Array(0),
    intermediateRefundTx: new Uint8Array(0),
    intermediateDirectRefundTx: new Uint8Array(0),
    intermediateDirectFromCpfpRefundTx: new Uint8Array(0),
    pendingKeyTweakPublicKey: new Uint8Array(0)
  };
}
var TransferLeaf = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.leaf !== void 0) {
      TreeNode.encode(message.leaf, writer.uint32(10).fork()).join();
    }
    if (message.secretCipher.length !== 0) {
      writer.uint32(18).bytes(message.secretCipher);
    }
    if (message.signature.length !== 0) {
      writer.uint32(26).bytes(message.signature);
    }
    if (message.intermediateRefundTx.length !== 0) {
      writer.uint32(34).bytes(message.intermediateRefundTx);
    }
    if (message.intermediateDirectRefundTx.length !== 0) {
      writer.uint32(42).bytes(message.intermediateDirectRefundTx);
    }
    if (message.intermediateDirectFromCpfpRefundTx.length !== 0) {
      writer.uint32(50).bytes(message.intermediateDirectFromCpfpRefundTx);
    }
    if (message.pendingKeyTweakPublicKey.length !== 0) {
      writer.uint32(58).bytes(message.pendingKeyTweakPublicKey);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTransferLeaf();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.leaf = TreeNode.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.secretCipher = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.signature = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.intermediateRefundTx = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.intermediateDirectRefundTx = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.intermediateDirectFromCpfpRefundTx = reader.bytes();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.pendingKeyTweakPublicKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      leaf: isSet3(object.leaf) ? TreeNode.fromJSON(object.leaf) : void 0,
      secretCipher: isSet3(object.secretCipher) ? bytesFromBase642(object.secretCipher) : new Uint8Array(0),
      signature: isSet3(object.signature) ? bytesFromBase642(object.signature) : new Uint8Array(0),
      intermediateRefundTx: isSet3(object.intermediateRefundTx) ? bytesFromBase642(object.intermediateRefundTx) : new Uint8Array(0),
      intermediateDirectRefundTx: isSet3(object.intermediateDirectRefundTx) ? bytesFromBase642(object.intermediateDirectRefundTx) : new Uint8Array(0),
      intermediateDirectFromCpfpRefundTx: isSet3(object.intermediateDirectFromCpfpRefundTx) ? bytesFromBase642(object.intermediateDirectFromCpfpRefundTx) : new Uint8Array(0),
      pendingKeyTweakPublicKey: isSet3(object.pendingKeyTweakPublicKey) ? bytesFromBase642(object.pendingKeyTweakPublicKey) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.leaf !== void 0) {
      obj.leaf = TreeNode.toJSON(message.leaf);
    }
    if (message.secretCipher.length !== 0) {
      obj.secretCipher = base64FromBytes2(message.secretCipher);
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes2(message.signature);
    }
    if (message.intermediateRefundTx.length !== 0) {
      obj.intermediateRefundTx = base64FromBytes2(message.intermediateRefundTx);
    }
    if (message.intermediateDirectRefundTx.length !== 0) {
      obj.intermediateDirectRefundTx = base64FromBytes2(message.intermediateDirectRefundTx);
    }
    if (message.intermediateDirectFromCpfpRefundTx.length !== 0) {
      obj.intermediateDirectFromCpfpRefundTx = base64FromBytes2(message.intermediateDirectFromCpfpRefundTx);
    }
    if (message.pendingKeyTweakPublicKey.length !== 0) {
      obj.pendingKeyTweakPublicKey = base64FromBytes2(message.pendingKeyTweakPublicKey);
    }
    return obj;
  },
  create(base) {
    return TransferLeaf.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseTransferLeaf();
    message.leaf = object.leaf !== void 0 && object.leaf !== null ? TreeNode.fromPartial(object.leaf) : void 0;
    message.secretCipher = object.secretCipher ?? new Uint8Array(0);
    message.signature = object.signature ?? new Uint8Array(0);
    message.intermediateRefundTx = object.intermediateRefundTx ?? new Uint8Array(0);
    message.intermediateDirectRefundTx = object.intermediateDirectRefundTx ?? new Uint8Array(0);
    message.intermediateDirectFromCpfpRefundTx = object.intermediateDirectFromCpfpRefundTx ?? new Uint8Array(0);
    message.pendingKeyTweakPublicKey = object.pendingKeyTweakPublicKey ?? new Uint8Array(0);
    return message;
  }
};
function createBaseTransferFilter() {
  return {
    participant: void 0,
    transferIds: [],
    limit: 0,
    offset: 0,
    types: [],
    network: 0,
    statuses: [],
    order: 0
  };
}
var TransferFilter = {
  encode(message, writer = new BinaryWriter4()) {
    switch (message.participant?.$case) {
      case "receiverIdentityPublicKey":
        writer.uint32(10).bytes(message.participant.receiverIdentityPublicKey);
        break;
      case "senderIdentityPublicKey":
        writer.uint32(18).bytes(message.participant.senderIdentityPublicKey);
        break;
      case "senderOrReceiverIdentityPublicKey":
        writer.uint32(482).bytes(message.participant.senderOrReceiverIdentityPublicKey);
        break;
    }
    for (const v of message.transferIds) {
      writer.uint32(26).string(v);
    }
    if (message.limit !== 0) {
      writer.uint32(320).int64(message.limit);
    }
    if (message.offset !== 0) {
      writer.uint32(400).int64(message.offset);
    }
    writer.uint32(562).fork();
    for (const v of message.types) {
      writer.int32(v);
    }
    writer.join();
    if (message.network !== 0) {
      writer.uint32(32).int32(message.network);
    }
    writer.uint32(642).fork();
    for (const v of message.statuses) {
      writer.int32(v);
    }
    writer.join();
    if (message.order !== 0) {
      writer.uint32(40).int32(message.order);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTransferFilter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.participant = { $case: "receiverIdentityPublicKey", receiverIdentityPublicKey: reader.bytes() };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.participant = { $case: "senderIdentityPublicKey", senderIdentityPublicKey: reader.bytes() };
          continue;
        }
        case 60: {
          if (tag !== 482) {
            break;
          }
          message.participant = {
            $case: "senderOrReceiverIdentityPublicKey",
            senderOrReceiverIdentityPublicKey: reader.bytes()
          };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.transferIds.push(reader.string());
          continue;
        }
        case 40: {
          if (tag !== 320) {
            break;
          }
          message.limit = longToNumber2(reader.int64());
          continue;
        }
        case 50: {
          if (tag !== 400) {
            break;
          }
          message.offset = longToNumber2(reader.int64());
          continue;
        }
        case 70: {
          if (tag === 560) {
            message.types.push(reader.int32());
            continue;
          }
          if (tag === 562) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.types.push(reader.int32());
            }
            continue;
          }
          break;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }
          message.network = reader.int32();
          continue;
        }
        case 80: {
          if (tag === 640) {
            message.statuses.push(reader.int32());
            continue;
          }
          if (tag === 642) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.statuses.push(reader.int32());
            }
            continue;
          }
          break;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }
          message.order = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      participant: isSet3(object.receiverIdentityPublicKey) ? {
        $case: "receiverIdentityPublicKey",
        receiverIdentityPublicKey: bytesFromBase642(object.receiverIdentityPublicKey)
      } : isSet3(object.senderIdentityPublicKey) ? { $case: "senderIdentityPublicKey", senderIdentityPublicKey: bytesFromBase642(object.senderIdentityPublicKey) } : isSet3(object.senderOrReceiverIdentityPublicKey) ? {
        $case: "senderOrReceiverIdentityPublicKey",
        senderOrReceiverIdentityPublicKey: bytesFromBase642(object.senderOrReceiverIdentityPublicKey)
      } : void 0,
      transferIds: globalThis.Array.isArray(object?.transferIds) ? object.transferIds.map((e) => globalThis.String(e)) : [],
      limit: isSet3(object.limit) ? globalThis.Number(object.limit) : 0,
      offset: isSet3(object.offset) ? globalThis.Number(object.offset) : 0,
      types: globalThis.Array.isArray(object?.types) ? object.types.map((e) => transferTypeFromJSON(e)) : [],
      network: isSet3(object.network) ? networkFromJSON(object.network) : 0,
      statuses: globalThis.Array.isArray(object?.statuses) ? object.statuses.map((e) => transferStatusFromJSON(e)) : [],
      order: isSet3(object.order) ? orderFromJSON(object.order) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.participant?.$case === "receiverIdentityPublicKey") {
      obj.receiverIdentityPublicKey = base64FromBytes2(message.participant.receiverIdentityPublicKey);
    } else if (message.participant?.$case === "senderIdentityPublicKey") {
      obj.senderIdentityPublicKey = base64FromBytes2(message.participant.senderIdentityPublicKey);
    } else if (message.participant?.$case === "senderOrReceiverIdentityPublicKey") {
      obj.senderOrReceiverIdentityPublicKey = base64FromBytes2(message.participant.senderOrReceiverIdentityPublicKey);
    }
    if (message.transferIds?.length) {
      obj.transferIds = message.transferIds;
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    if (message.types?.length) {
      obj.types = message.types.map((e) => transferTypeToJSON(e));
    }
    if (message.network !== 0) {
      obj.network = networkToJSON(message.network);
    }
    if (message.statuses?.length) {
      obj.statuses = message.statuses.map((e) => transferStatusToJSON(e));
    }
    if (message.order !== 0) {
      obj.order = orderToJSON(message.order);
    }
    return obj;
  },
  create(base) {
    return TransferFilter.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseTransferFilter();
    switch (object.participant?.$case) {
      case "receiverIdentityPublicKey": {
        if (object.participant?.receiverIdentityPublicKey !== void 0 && object.participant?.receiverIdentityPublicKey !== null) {
          message.participant = {
            $case: "receiverIdentityPublicKey",
            receiverIdentityPublicKey: object.participant.receiverIdentityPublicKey
          };
        }
        break;
      }
      case "senderIdentityPublicKey": {
        if (object.participant?.senderIdentityPublicKey !== void 0 && object.participant?.senderIdentityPublicKey !== null) {
          message.participant = {
            $case: "senderIdentityPublicKey",
            senderIdentityPublicKey: object.participant.senderIdentityPublicKey
          };
        }
        break;
      }
      case "senderOrReceiverIdentityPublicKey": {
        if (object.participant?.senderOrReceiverIdentityPublicKey !== void 0 && object.participant?.senderOrReceiverIdentityPublicKey !== null) {
          message.participant = {
            $case: "senderOrReceiverIdentityPublicKey",
            senderOrReceiverIdentityPublicKey: object.participant.senderOrReceiverIdentityPublicKey
          };
        }
        break;
      }
    }
    message.transferIds = object.transferIds?.map((e) => e) || [];
    message.limit = object.limit ?? 0;
    message.offset = object.offset ?? 0;
    message.types = object.types?.map((e) => e) || [];
    message.network = object.network ?? 0;
    message.statuses = object.statuses?.map((e) => e) || [];
    message.order = object.order ?? 0;
    return message;
  }
};
function createBaseQueryTransfersResponse() {
  return { transfers: [], offset: 0 };
}
var QueryTransfersResponse = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.transfers) {
      Transfer.encode(v, writer.uint32(10).fork()).join();
    }
    if (message.offset !== 0) {
      writer.uint32(16).int64(message.offset);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryTransfersResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.transfers.push(Transfer.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.offset = longToNumber2(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      transfers: globalThis.Array.isArray(object?.transfers) ? object.transfers.map((e) => Transfer.fromJSON(e)) : [],
      offset: isSet3(object.offset) ? globalThis.Number(object.offset) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.transfers?.length) {
      obj.transfers = message.transfers.map((e) => Transfer.toJSON(e));
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    return obj;
  },
  create(base) {
    return QueryTransfersResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryTransfersResponse();
    message.transfers = object.transfers?.map((e) => Transfer.fromPartial(e)) || [];
    message.offset = object.offset ?? 0;
    return message;
  }
};
function createBaseClaimLeafKeyTweak() {
  return { leafId: "", secretShareTweak: void 0, pubkeySharesTweak: {} };
}
var ClaimLeafKeyTweak = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.leafId !== "") {
      writer.uint32(10).string(message.leafId);
    }
    if (message.secretShareTweak !== void 0) {
      SecretShare.encode(message.secretShareTweak, writer.uint32(18).fork()).join();
    }
    Object.entries(message.pubkeySharesTweak).forEach(([key, value]) => {
      ClaimLeafKeyTweak_PubkeySharesTweakEntry.encode({ key, value }, writer.uint32(26).fork()).join();
    });
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseClaimLeafKeyTweak();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.leafId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.secretShareTweak = SecretShare.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          const entry3 = ClaimLeafKeyTweak_PubkeySharesTweakEntry.decode(reader, reader.uint32());
          if (entry3.value !== void 0) {
            message.pubkeySharesTweak[entry3.key] = entry3.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      leafId: isSet3(object.leafId) ? globalThis.String(object.leafId) : "",
      secretShareTweak: isSet3(object.secretShareTweak) ? SecretShare.fromJSON(object.secretShareTweak) : void 0,
      pubkeySharesTweak: isObject2(object.pubkeySharesTweak) ? Object.entries(object.pubkeySharesTweak).reduce((acc, [key, value]) => {
        acc[key] = bytesFromBase642(value);
        return acc;
      }, {}) : {}
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.leafId !== "") {
      obj.leafId = message.leafId;
    }
    if (message.secretShareTweak !== void 0) {
      obj.secretShareTweak = SecretShare.toJSON(message.secretShareTweak);
    }
    if (message.pubkeySharesTweak) {
      const entries = Object.entries(message.pubkeySharesTweak);
      if (entries.length > 0) {
        obj.pubkeySharesTweak = {};
        entries.forEach(([k, v]) => {
          obj.pubkeySharesTweak[k] = base64FromBytes2(v);
        });
      }
    }
    return obj;
  },
  create(base) {
    return ClaimLeafKeyTweak.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseClaimLeafKeyTweak();
    message.leafId = object.leafId ?? "";
    message.secretShareTweak = object.secretShareTweak !== void 0 && object.secretShareTweak !== null ? SecretShare.fromPartial(object.secretShareTweak) : void 0;
    message.pubkeySharesTweak = Object.entries(object.pubkeySharesTweak ?? {}).reduce(
      (acc, [key, value]) => {
        if (value !== void 0) {
          acc[key] = value;
        }
        return acc;
      },
      {}
    );
    return message;
  }
};
function createBaseClaimLeafKeyTweak_PubkeySharesTweakEntry() {
  return { key: "", value: new Uint8Array(0) };
}
var ClaimLeafKeyTweak_PubkeySharesTweakEntry = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value.length !== 0) {
      writer.uint32(18).bytes(message.value);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseClaimLeafKeyTweak_PubkeySharesTweakEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.value = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet3(object.key) ? globalThis.String(object.key) : "",
      value: isSet3(object.value) ? bytesFromBase642(object.value) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value.length !== 0) {
      obj.value = base64FromBytes2(message.value);
    }
    return obj;
  },
  create(base) {
    return ClaimLeafKeyTweak_PubkeySharesTweakEntry.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseClaimLeafKeyTweak_PubkeySharesTweakEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? new Uint8Array(0);
    return message;
  }
};
function createBaseClaimTransferTweakKeysRequest() {
  return { transferId: "", ownerIdentityPublicKey: new Uint8Array(0), leavesToReceive: [] };
}
var ClaimTransferTweakKeysRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.transferId !== "") {
      writer.uint32(10).string(message.transferId);
    }
    if (message.ownerIdentityPublicKey.length !== 0) {
      writer.uint32(18).bytes(message.ownerIdentityPublicKey);
    }
    for (const v of message.leavesToReceive) {
      ClaimLeafKeyTweak.encode(v, writer.uint32(26).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseClaimTransferTweakKeysRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.transferId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.ownerIdentityPublicKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.leavesToReceive.push(ClaimLeafKeyTweak.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      transferId: isSet3(object.transferId) ? globalThis.String(object.transferId) : "",
      ownerIdentityPublicKey: isSet3(object.ownerIdentityPublicKey) ? bytesFromBase642(object.ownerIdentityPublicKey) : new Uint8Array(0),
      leavesToReceive: globalThis.Array.isArray(object?.leavesToReceive) ? object.leavesToReceive.map((e) => ClaimLeafKeyTweak.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.transferId !== "") {
      obj.transferId = message.transferId;
    }
    if (message.ownerIdentityPublicKey.length !== 0) {
      obj.ownerIdentityPublicKey = base64FromBytes2(message.ownerIdentityPublicKey);
    }
    if (message.leavesToReceive?.length) {
      obj.leavesToReceive = message.leavesToReceive.map((e) => ClaimLeafKeyTweak.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return ClaimTransferTweakKeysRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseClaimTransferTweakKeysRequest();
    message.transferId = object.transferId ?? "";
    message.ownerIdentityPublicKey = object.ownerIdentityPublicKey ?? new Uint8Array(0);
    message.leavesToReceive = object.leavesToReceive?.map((e) => ClaimLeafKeyTweak.fromPartial(e)) || [];
    return message;
  }
};
function createBaseClaimTransferSignRefundsRequest() {
  return { transferId: "", ownerIdentityPublicKey: new Uint8Array(0), signingJobs: [] };
}
var ClaimTransferSignRefundsRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.transferId !== "") {
      writer.uint32(10).string(message.transferId);
    }
    if (message.ownerIdentityPublicKey.length !== 0) {
      writer.uint32(18).bytes(message.ownerIdentityPublicKey);
    }
    for (const v of message.signingJobs) {
      LeafRefundTxSigningJob.encode(v, writer.uint32(26).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseClaimTransferSignRefundsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.transferId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.ownerIdentityPublicKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.signingJobs.push(LeafRefundTxSigningJob.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      transferId: isSet3(object.transferId) ? globalThis.String(object.transferId) : "",
      ownerIdentityPublicKey: isSet3(object.ownerIdentityPublicKey) ? bytesFromBase642(object.ownerIdentityPublicKey) : new Uint8Array(0),
      signingJobs: globalThis.Array.isArray(object?.signingJobs) ? object.signingJobs.map((e) => LeafRefundTxSigningJob.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.transferId !== "") {
      obj.transferId = message.transferId;
    }
    if (message.ownerIdentityPublicKey.length !== 0) {
      obj.ownerIdentityPublicKey = base64FromBytes2(message.ownerIdentityPublicKey);
    }
    if (message.signingJobs?.length) {
      obj.signingJobs = message.signingJobs.map((e) => LeafRefundTxSigningJob.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return ClaimTransferSignRefundsRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseClaimTransferSignRefundsRequest();
    message.transferId = object.transferId ?? "";
    message.ownerIdentityPublicKey = object.ownerIdentityPublicKey ?? new Uint8Array(0);
    message.signingJobs = object.signingJobs?.map((e) => LeafRefundTxSigningJob.fromPartial(e)) || [];
    return message;
  }
};
function createBaseClaimTransferSignRefundsResponse() {
  return { signingResults: [] };
}
var ClaimTransferSignRefundsResponse = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.signingResults) {
      LeafRefundTxSigningResult.encode(v, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseClaimTransferSignRefundsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.signingResults.push(LeafRefundTxSigningResult.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      signingResults: globalThis.Array.isArray(object?.signingResults) ? object.signingResults.map((e) => LeafRefundTxSigningResult.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.signingResults?.length) {
      obj.signingResults = message.signingResults.map((e) => LeafRefundTxSigningResult.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return ClaimTransferSignRefundsResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseClaimTransferSignRefundsResponse();
    message.signingResults = object.signingResults?.map((e) => LeafRefundTxSigningResult.fromPartial(e)) || [];
    return message;
  }
};
function createBaseStorePreimageShareRequest() {
  return {
    paymentHash: new Uint8Array(0),
    preimageShare: void 0,
    threshold: 0,
    invoiceString: "",
    userIdentityPublicKey: new Uint8Array(0)
  };
}
var StorePreimageShareRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.paymentHash.length !== 0) {
      writer.uint32(10).bytes(message.paymentHash);
    }
    if (message.preimageShare !== void 0) {
      SecretShare.encode(message.preimageShare, writer.uint32(18).fork()).join();
    }
    if (message.threshold !== 0) {
      writer.uint32(24).uint32(message.threshold);
    }
    if (message.invoiceString !== "") {
      writer.uint32(34).string(message.invoiceString);
    }
    if (message.userIdentityPublicKey.length !== 0) {
      writer.uint32(42).bytes(message.userIdentityPublicKey);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseStorePreimageShareRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.paymentHash = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.preimageShare = SecretShare.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.threshold = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.invoiceString = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.userIdentityPublicKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      paymentHash: isSet3(object.paymentHash) ? bytesFromBase642(object.paymentHash) : new Uint8Array(0),
      preimageShare: isSet3(object.preimageShare) ? SecretShare.fromJSON(object.preimageShare) : void 0,
      threshold: isSet3(object.threshold) ? globalThis.Number(object.threshold) : 0,
      invoiceString: isSet3(object.invoiceString) ? globalThis.String(object.invoiceString) : "",
      userIdentityPublicKey: isSet3(object.userIdentityPublicKey) ? bytesFromBase642(object.userIdentityPublicKey) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.paymentHash.length !== 0) {
      obj.paymentHash = base64FromBytes2(message.paymentHash);
    }
    if (message.preimageShare !== void 0) {
      obj.preimageShare = SecretShare.toJSON(message.preimageShare);
    }
    if (message.threshold !== 0) {
      obj.threshold = Math.round(message.threshold);
    }
    if (message.invoiceString !== "") {
      obj.invoiceString = message.invoiceString;
    }
    if (message.userIdentityPublicKey.length !== 0) {
      obj.userIdentityPublicKey = base64FromBytes2(message.userIdentityPublicKey);
    }
    return obj;
  },
  create(base) {
    return StorePreimageShareRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseStorePreimageShareRequest();
    message.paymentHash = object.paymentHash ?? new Uint8Array(0);
    message.preimageShare = object.preimageShare !== void 0 && object.preimageShare !== null ? SecretShare.fromPartial(object.preimageShare) : void 0;
    message.threshold = object.threshold ?? 0;
    message.invoiceString = object.invoiceString ?? "";
    message.userIdentityPublicKey = object.userIdentityPublicKey ?? new Uint8Array(0);
    return message;
  }
};
function createBaseRequestedSigningCommitments() {
  return { signingNonceCommitments: {} };
}
var RequestedSigningCommitments = {
  encode(message, writer = new BinaryWriter4()) {
    Object.entries(message.signingNonceCommitments).forEach(([key, value]) => {
      RequestedSigningCommitments_SigningNonceCommitmentsEntry.encode(
        { key, value },
        writer.uint32(10).fork()
      ).join();
    });
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRequestedSigningCommitments();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          const entry1 = RequestedSigningCommitments_SigningNonceCommitmentsEntry.decode(reader, reader.uint32());
          if (entry1.value !== void 0) {
            message.signingNonceCommitments[entry1.key] = entry1.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      signingNonceCommitments: isObject2(object.signingNonceCommitments) ? Object.entries(object.signingNonceCommitments).reduce(
        (acc, [key, value]) => {
          acc[key] = SigningCommitment.fromJSON(value);
          return acc;
        },
        {}
      ) : {}
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.signingNonceCommitments) {
      const entries = Object.entries(message.signingNonceCommitments);
      if (entries.length > 0) {
        obj.signingNonceCommitments = {};
        entries.forEach(([k, v]) => {
          obj.signingNonceCommitments[k] = SigningCommitment.toJSON(v);
        });
      }
    }
    return obj;
  },
  create(base) {
    return RequestedSigningCommitments.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRequestedSigningCommitments();
    message.signingNonceCommitments = Object.entries(object.signingNonceCommitments ?? {}).reduce((acc, [key, value]) => {
      if (value !== void 0) {
        acc[key] = SigningCommitment.fromPartial(value);
      }
      return acc;
    }, {});
    return message;
  }
};
function createBaseRequestedSigningCommitments_SigningNonceCommitmentsEntry() {
  return { key: "", value: void 0 };
}
var RequestedSigningCommitments_SigningNonceCommitmentsEntry = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== void 0) {
      SigningCommitment.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRequestedSigningCommitments_SigningNonceCommitmentsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.value = SigningCommitment.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet3(object.key) ? globalThis.String(object.key) : "",
      value: isSet3(object.value) ? SigningCommitment.fromJSON(object.value) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== void 0) {
      obj.value = SigningCommitment.toJSON(message.value);
    }
    return obj;
  },
  create(base) {
    return RequestedSigningCommitments_SigningNonceCommitmentsEntry.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRequestedSigningCommitments_SigningNonceCommitmentsEntry();
    message.key = object.key ?? "";
    message.value = object.value !== void 0 && object.value !== null ? SigningCommitment.fromPartial(object.value) : void 0;
    return message;
  }
};
function createBaseGetSigningCommitmentsRequest() {
  return { nodeIds: [], count: 0 };
}
var GetSigningCommitmentsRequest = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.nodeIds) {
      writer.uint32(10).string(v);
    }
    if (message.count !== 0) {
      writer.uint32(16).uint32(message.count);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetSigningCommitmentsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.nodeIds.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.count = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      nodeIds: globalThis.Array.isArray(object?.nodeIds) ? object.nodeIds.map((e) => globalThis.String(e)) : [],
      count: isSet3(object.count) ? globalThis.Number(object.count) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.nodeIds?.length) {
      obj.nodeIds = message.nodeIds;
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    return obj;
  },
  create(base) {
    return GetSigningCommitmentsRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetSigningCommitmentsRequest();
    message.nodeIds = object.nodeIds?.map((e) => e) || [];
    message.count = object.count ?? 0;
    return message;
  }
};
function createBaseGetSigningCommitmentsResponse() {
  return { signingCommitments: [] };
}
var GetSigningCommitmentsResponse = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.signingCommitments) {
      RequestedSigningCommitments.encode(v, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetSigningCommitmentsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.signingCommitments.push(RequestedSigningCommitments.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      signingCommitments: globalThis.Array.isArray(object?.signingCommitments) ? object.signingCommitments.map((e) => RequestedSigningCommitments.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.signingCommitments?.length) {
      obj.signingCommitments = message.signingCommitments.map((e) => RequestedSigningCommitments.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return GetSigningCommitmentsResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetSigningCommitmentsResponse();
    message.signingCommitments = object.signingCommitments?.map((e) => RequestedSigningCommitments.fromPartial(e)) || [];
    return message;
  }
};
function createBaseSigningCommitments() {
  return { signingCommitments: {} };
}
var SigningCommitments = {
  encode(message, writer = new BinaryWriter4()) {
    Object.entries(message.signingCommitments).forEach(([key, value]) => {
      SigningCommitments_SigningCommitmentsEntry.encode({ key, value }, writer.uint32(10).fork()).join();
    });
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSigningCommitments();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          const entry1 = SigningCommitments_SigningCommitmentsEntry.decode(reader, reader.uint32());
          if (entry1.value !== void 0) {
            message.signingCommitments[entry1.key] = entry1.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      signingCommitments: isObject2(object.signingCommitments) ? Object.entries(object.signingCommitments).reduce(
        (acc, [key, value]) => {
          acc[key] = SigningCommitment.fromJSON(value);
          return acc;
        },
        {}
      ) : {}
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.signingCommitments) {
      const entries = Object.entries(message.signingCommitments);
      if (entries.length > 0) {
        obj.signingCommitments = {};
        entries.forEach(([k, v]) => {
          obj.signingCommitments[k] = SigningCommitment.toJSON(v);
        });
      }
    }
    return obj;
  },
  create(base) {
    return SigningCommitments.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSigningCommitments();
    message.signingCommitments = Object.entries(object.signingCommitments ?? {}).reduce((acc, [key, value]) => {
      if (value !== void 0) {
        acc[key] = SigningCommitment.fromPartial(value);
      }
      return acc;
    }, {});
    return message;
  }
};
function createBaseSigningCommitments_SigningCommitmentsEntry() {
  return { key: "", value: void 0 };
}
var SigningCommitments_SigningCommitmentsEntry = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== void 0) {
      SigningCommitment.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSigningCommitments_SigningCommitmentsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.value = SigningCommitment.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet3(object.key) ? globalThis.String(object.key) : "",
      value: isSet3(object.value) ? SigningCommitment.fromJSON(object.value) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== void 0) {
      obj.value = SigningCommitment.toJSON(message.value);
    }
    return obj;
  },
  create(base) {
    return SigningCommitments_SigningCommitmentsEntry.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSigningCommitments_SigningCommitmentsEntry();
    message.key = object.key ?? "";
    message.value = object.value !== void 0 && object.value !== null ? SigningCommitment.fromPartial(object.value) : void 0;
    return message;
  }
};
function createBaseUserSignedRefund() {
  return {
    nodeId: "",
    refundTx: new Uint8Array(0),
    userSignature: new Uint8Array(0),
    signingCommitments: void 0,
    userSignatureCommitment: void 0,
    network: 0
  };
}
var UserSignedRefund = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.nodeId !== "") {
      writer.uint32(10).string(message.nodeId);
    }
    if (message.refundTx.length !== 0) {
      writer.uint32(18).bytes(message.refundTx);
    }
    if (message.userSignature.length !== 0) {
      writer.uint32(26).bytes(message.userSignature);
    }
    if (message.signingCommitments !== void 0) {
      SigningCommitments.encode(message.signingCommitments, writer.uint32(34).fork()).join();
    }
    if (message.userSignatureCommitment !== void 0) {
      SigningCommitment.encode(message.userSignatureCommitment, writer.uint32(42).fork()).join();
    }
    if (message.network !== 0) {
      writer.uint32(48).int32(message.network);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUserSignedRefund();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.nodeId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.refundTx = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.userSignature = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.signingCommitments = SigningCommitments.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.userSignatureCommitment = SigningCommitment.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }
          message.network = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      nodeId: isSet3(object.nodeId) ? globalThis.String(object.nodeId) : "",
      refundTx: isSet3(object.refundTx) ? bytesFromBase642(object.refundTx) : new Uint8Array(0),
      userSignature: isSet3(object.userSignature) ? bytesFromBase642(object.userSignature) : new Uint8Array(0),
      signingCommitments: isSet3(object.signingCommitments) ? SigningCommitments.fromJSON(object.signingCommitments) : void 0,
      userSignatureCommitment: isSet3(object.userSignatureCommitment) ? SigningCommitment.fromJSON(object.userSignatureCommitment) : void 0,
      network: isSet3(object.network) ? networkFromJSON(object.network) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.nodeId !== "") {
      obj.nodeId = message.nodeId;
    }
    if (message.refundTx.length !== 0) {
      obj.refundTx = base64FromBytes2(message.refundTx);
    }
    if (message.userSignature.length !== 0) {
      obj.userSignature = base64FromBytes2(message.userSignature);
    }
    if (message.signingCommitments !== void 0) {
      obj.signingCommitments = SigningCommitments.toJSON(message.signingCommitments);
    }
    if (message.userSignatureCommitment !== void 0) {
      obj.userSignatureCommitment = SigningCommitment.toJSON(message.userSignatureCommitment);
    }
    if (message.network !== 0) {
      obj.network = networkToJSON(message.network);
    }
    return obj;
  },
  create(base) {
    return UserSignedRefund.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseUserSignedRefund();
    message.nodeId = object.nodeId ?? "";
    message.refundTx = object.refundTx ?? new Uint8Array(0);
    message.userSignature = object.userSignature ?? new Uint8Array(0);
    message.signingCommitments = object.signingCommitments !== void 0 && object.signingCommitments !== null ? SigningCommitments.fromPartial(object.signingCommitments) : void 0;
    message.userSignatureCommitment = object.userSignatureCommitment !== void 0 && object.userSignatureCommitment !== null ? SigningCommitment.fromPartial(object.userSignatureCommitment) : void 0;
    message.network = object.network ?? 0;
    return message;
  }
};
function createBaseInvoiceAmountProof() {
  return { bolt11Invoice: "" };
}
var InvoiceAmountProof = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.bolt11Invoice !== "") {
      writer.uint32(10).string(message.bolt11Invoice);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInvoiceAmountProof();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.bolt11Invoice = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { bolt11Invoice: isSet3(object.bolt11Invoice) ? globalThis.String(object.bolt11Invoice) : "" };
  },
  toJSON(message) {
    const obj = {};
    if (message.bolt11Invoice !== "") {
      obj.bolt11Invoice = message.bolt11Invoice;
    }
    return obj;
  },
  create(base) {
    return InvoiceAmountProof.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseInvoiceAmountProof();
    message.bolt11Invoice = object.bolt11Invoice ?? "";
    return message;
  }
};
function createBaseInvoiceAmount() {
  return { valueSats: 0, invoiceAmountProof: void 0 };
}
var InvoiceAmount = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.valueSats !== 0) {
      writer.uint32(8).uint64(message.valueSats);
    }
    if (message.invoiceAmountProof !== void 0) {
      InvoiceAmountProof.encode(message.invoiceAmountProof, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInvoiceAmount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.valueSats = longToNumber2(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.invoiceAmountProof = InvoiceAmountProof.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      valueSats: isSet3(object.valueSats) ? globalThis.Number(object.valueSats) : 0,
      invoiceAmountProof: isSet3(object.invoiceAmountProof) ? InvoiceAmountProof.fromJSON(object.invoiceAmountProof) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.valueSats !== 0) {
      obj.valueSats = Math.round(message.valueSats);
    }
    if (message.invoiceAmountProof !== void 0) {
      obj.invoiceAmountProof = InvoiceAmountProof.toJSON(message.invoiceAmountProof);
    }
    return obj;
  },
  create(base) {
    return InvoiceAmount.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseInvoiceAmount();
    message.valueSats = object.valueSats ?? 0;
    message.invoiceAmountProof = object.invoiceAmountProof !== void 0 && object.invoiceAmountProof !== null ? InvoiceAmountProof.fromPartial(object.invoiceAmountProof) : void 0;
    return message;
  }
};
function createBaseInitiatePreimageSwapRequest() {
  return {
    paymentHash: new Uint8Array(0),
    invoiceAmount: void 0,
    reason: 0,
    transfer: void 0,
    receiverIdentityPublicKey: new Uint8Array(0),
    feeSats: 0,
    transferRequest: void 0
  };
}
var InitiatePreimageSwapRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.paymentHash.length !== 0) {
      writer.uint32(10).bytes(message.paymentHash);
    }
    if (message.invoiceAmount !== void 0) {
      InvoiceAmount.encode(message.invoiceAmount, writer.uint32(18).fork()).join();
    }
    if (message.reason !== 0) {
      writer.uint32(24).int32(message.reason);
    }
    if (message.transfer !== void 0) {
      StartUserSignedTransferRequest.encode(message.transfer, writer.uint32(34).fork()).join();
    }
    if (message.receiverIdentityPublicKey.length !== 0) {
      writer.uint32(42).bytes(message.receiverIdentityPublicKey);
    }
    if (message.feeSats !== 0) {
      writer.uint32(48).uint64(message.feeSats);
    }
    if (message.transferRequest !== void 0) {
      StartTransferRequest.encode(message.transferRequest, writer.uint32(58).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInitiatePreimageSwapRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.paymentHash = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.invoiceAmount = InvoiceAmount.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.reason = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.transfer = StartUserSignedTransferRequest.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.receiverIdentityPublicKey = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }
          message.feeSats = longToNumber2(reader.uint64());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.transferRequest = StartTransferRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      paymentHash: isSet3(object.paymentHash) ? bytesFromBase642(object.paymentHash) : new Uint8Array(0),
      invoiceAmount: isSet3(object.invoiceAmount) ? InvoiceAmount.fromJSON(object.invoiceAmount) : void 0,
      reason: isSet3(object.reason) ? initiatePreimageSwapRequest_ReasonFromJSON(object.reason) : 0,
      transfer: isSet3(object.transfer) ? StartUserSignedTransferRequest.fromJSON(object.transfer) : void 0,
      receiverIdentityPublicKey: isSet3(object.receiverIdentityPublicKey) ? bytesFromBase642(object.receiverIdentityPublicKey) : new Uint8Array(0),
      feeSats: isSet3(object.feeSats) ? globalThis.Number(object.feeSats) : 0,
      transferRequest: isSet3(object.transferRequest) ? StartTransferRequest.fromJSON(object.transferRequest) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.paymentHash.length !== 0) {
      obj.paymentHash = base64FromBytes2(message.paymentHash);
    }
    if (message.invoiceAmount !== void 0) {
      obj.invoiceAmount = InvoiceAmount.toJSON(message.invoiceAmount);
    }
    if (message.reason !== 0) {
      obj.reason = initiatePreimageSwapRequest_ReasonToJSON(message.reason);
    }
    if (message.transfer !== void 0) {
      obj.transfer = StartUserSignedTransferRequest.toJSON(message.transfer);
    }
    if (message.receiverIdentityPublicKey.length !== 0) {
      obj.receiverIdentityPublicKey = base64FromBytes2(message.receiverIdentityPublicKey);
    }
    if (message.feeSats !== 0) {
      obj.feeSats = Math.round(message.feeSats);
    }
    if (message.transferRequest !== void 0) {
      obj.transferRequest = StartTransferRequest.toJSON(message.transferRequest);
    }
    return obj;
  },
  create(base) {
    return InitiatePreimageSwapRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseInitiatePreimageSwapRequest();
    message.paymentHash = object.paymentHash ?? new Uint8Array(0);
    message.invoiceAmount = object.invoiceAmount !== void 0 && object.invoiceAmount !== null ? InvoiceAmount.fromPartial(object.invoiceAmount) : void 0;
    message.reason = object.reason ?? 0;
    message.transfer = object.transfer !== void 0 && object.transfer !== null ? StartUserSignedTransferRequest.fromPartial(object.transfer) : void 0;
    message.receiverIdentityPublicKey = object.receiverIdentityPublicKey ?? new Uint8Array(0);
    message.feeSats = object.feeSats ?? 0;
    message.transferRequest = object.transferRequest !== void 0 && object.transferRequest !== null ? StartTransferRequest.fromPartial(object.transferRequest) : void 0;
    return message;
  }
};
function createBaseInitiatePreimageSwapResponse() {
  return { preimage: new Uint8Array(0), transfer: void 0 };
}
var InitiatePreimageSwapResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.preimage.length !== 0) {
      writer.uint32(10).bytes(message.preimage);
    }
    if (message.transfer !== void 0) {
      Transfer.encode(message.transfer, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInitiatePreimageSwapResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.preimage = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.transfer = Transfer.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      preimage: isSet3(object.preimage) ? bytesFromBase642(object.preimage) : new Uint8Array(0),
      transfer: isSet3(object.transfer) ? Transfer.fromJSON(object.transfer) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.preimage.length !== 0) {
      obj.preimage = base64FromBytes2(message.preimage);
    }
    if (message.transfer !== void 0) {
      obj.transfer = Transfer.toJSON(message.transfer);
    }
    return obj;
  },
  create(base) {
    return InitiatePreimageSwapResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseInitiatePreimageSwapResponse();
    message.preimage = object.preimage ?? new Uint8Array(0);
    message.transfer = object.transfer !== void 0 && object.transfer !== null ? Transfer.fromPartial(object.transfer) : void 0;
    return message;
  }
};
function createBaseOutPoint() {
  return { txid: new Uint8Array(0), vout: 0 };
}
var OutPoint = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.txid.length !== 0) {
      writer.uint32(10).bytes(message.txid);
    }
    if (message.vout !== 0) {
      writer.uint32(16).uint32(message.vout);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseOutPoint();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.txid = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.vout = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      txid: isSet3(object.txid) ? bytesFromBase642(object.txid) : new Uint8Array(0),
      vout: isSet3(object.vout) ? globalThis.Number(object.vout) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.txid.length !== 0) {
      obj.txid = base64FromBytes2(message.txid);
    }
    if (message.vout !== 0) {
      obj.vout = Math.round(message.vout);
    }
    return obj;
  },
  create(base) {
    return OutPoint.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseOutPoint();
    message.txid = object.txid ?? new Uint8Array(0);
    message.vout = object.vout ?? 0;
    return message;
  }
};
function createBaseCooperativeExitRequest() {
  return { transfer: void 0, exitId: "", exitTxid: new Uint8Array(0) };
}
var CooperativeExitRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.transfer !== void 0) {
      StartTransferRequest.encode(message.transfer, writer.uint32(10).fork()).join();
    }
    if (message.exitId !== "") {
      writer.uint32(18).string(message.exitId);
    }
    if (message.exitTxid.length !== 0) {
      writer.uint32(26).bytes(message.exitTxid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCooperativeExitRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.transfer = StartTransferRequest.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.exitId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.exitTxid = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      transfer: isSet3(object.transfer) ? StartTransferRequest.fromJSON(object.transfer) : void 0,
      exitId: isSet3(object.exitId) ? globalThis.String(object.exitId) : "",
      exitTxid: isSet3(object.exitTxid) ? bytesFromBase642(object.exitTxid) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.transfer !== void 0) {
      obj.transfer = StartTransferRequest.toJSON(message.transfer);
    }
    if (message.exitId !== "") {
      obj.exitId = message.exitId;
    }
    if (message.exitTxid.length !== 0) {
      obj.exitTxid = base64FromBytes2(message.exitTxid);
    }
    return obj;
  },
  create(base) {
    return CooperativeExitRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseCooperativeExitRequest();
    message.transfer = object.transfer !== void 0 && object.transfer !== null ? StartTransferRequest.fromPartial(object.transfer) : void 0;
    message.exitId = object.exitId ?? "";
    message.exitTxid = object.exitTxid ?? new Uint8Array(0);
    return message;
  }
};
function createBaseCooperativeExitResponse() {
  return { transfer: void 0, signingResults: [] };
}
var CooperativeExitResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.transfer !== void 0) {
      Transfer.encode(message.transfer, writer.uint32(10).fork()).join();
    }
    for (const v of message.signingResults) {
      LeafRefundTxSigningResult.encode(v, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCooperativeExitResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.transfer = Transfer.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.signingResults.push(LeafRefundTxSigningResult.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      transfer: isSet3(object.transfer) ? Transfer.fromJSON(object.transfer) : void 0,
      signingResults: globalThis.Array.isArray(object?.signingResults) ? object.signingResults.map((e) => LeafRefundTxSigningResult.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.transfer !== void 0) {
      obj.transfer = Transfer.toJSON(message.transfer);
    }
    if (message.signingResults?.length) {
      obj.signingResults = message.signingResults.map((e) => LeafRefundTxSigningResult.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return CooperativeExitResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseCooperativeExitResponse();
    message.transfer = object.transfer !== void 0 && object.transfer !== null ? Transfer.fromPartial(object.transfer) : void 0;
    message.signingResults = object.signingResults?.map((e) => LeafRefundTxSigningResult.fromPartial(e)) || [];
    return message;
  }
};
function createBaseCounterLeafSwapRequest() {
  return {
    transfer: void 0,
    swapId: "",
    adaptorPublicKey: new Uint8Array(0),
    directAdaptorPublicKey: new Uint8Array(0),
    directFromCpfpAdaptorPublicKey: new Uint8Array(0)
  };
}
var CounterLeafSwapRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.transfer !== void 0) {
      StartTransferRequest.encode(message.transfer, writer.uint32(10).fork()).join();
    }
    if (message.swapId !== "") {
      writer.uint32(18).string(message.swapId);
    }
    if (message.adaptorPublicKey.length !== 0) {
      writer.uint32(26).bytes(message.adaptorPublicKey);
    }
    if (message.directAdaptorPublicKey.length !== 0) {
      writer.uint32(34).bytes(message.directAdaptorPublicKey);
    }
    if (message.directFromCpfpAdaptorPublicKey.length !== 0) {
      writer.uint32(42).bytes(message.directFromCpfpAdaptorPublicKey);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCounterLeafSwapRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.transfer = StartTransferRequest.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.swapId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.adaptorPublicKey = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.directAdaptorPublicKey = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.directFromCpfpAdaptorPublicKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      transfer: isSet3(object.transfer) ? StartTransferRequest.fromJSON(object.transfer) : void 0,
      swapId: isSet3(object.swapId) ? globalThis.String(object.swapId) : "",
      adaptorPublicKey: isSet3(object.adaptorPublicKey) ? bytesFromBase642(object.adaptorPublicKey) : new Uint8Array(0),
      directAdaptorPublicKey: isSet3(object.directAdaptorPublicKey) ? bytesFromBase642(object.directAdaptorPublicKey) : new Uint8Array(0),
      directFromCpfpAdaptorPublicKey: isSet3(object.directFromCpfpAdaptorPublicKey) ? bytesFromBase642(object.directFromCpfpAdaptorPublicKey) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.transfer !== void 0) {
      obj.transfer = StartTransferRequest.toJSON(message.transfer);
    }
    if (message.swapId !== "") {
      obj.swapId = message.swapId;
    }
    if (message.adaptorPublicKey.length !== 0) {
      obj.adaptorPublicKey = base64FromBytes2(message.adaptorPublicKey);
    }
    if (message.directAdaptorPublicKey.length !== 0) {
      obj.directAdaptorPublicKey = base64FromBytes2(message.directAdaptorPublicKey);
    }
    if (message.directFromCpfpAdaptorPublicKey.length !== 0) {
      obj.directFromCpfpAdaptorPublicKey = base64FromBytes2(message.directFromCpfpAdaptorPublicKey);
    }
    return obj;
  },
  create(base) {
    return CounterLeafSwapRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseCounterLeafSwapRequest();
    message.transfer = object.transfer !== void 0 && object.transfer !== null ? StartTransferRequest.fromPartial(object.transfer) : void 0;
    message.swapId = object.swapId ?? "";
    message.adaptorPublicKey = object.adaptorPublicKey ?? new Uint8Array(0);
    message.directAdaptorPublicKey = object.directAdaptorPublicKey ?? new Uint8Array(0);
    message.directFromCpfpAdaptorPublicKey = object.directFromCpfpAdaptorPublicKey ?? new Uint8Array(0);
    return message;
  }
};
function createBaseCounterLeafSwapResponse() {
  return { transfer: void 0, signingResults: [] };
}
var CounterLeafSwapResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.transfer !== void 0) {
      Transfer.encode(message.transfer, writer.uint32(10).fork()).join();
    }
    for (const v of message.signingResults) {
      LeafRefundTxSigningResult.encode(v, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCounterLeafSwapResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.transfer = Transfer.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.signingResults.push(LeafRefundTxSigningResult.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      transfer: isSet3(object.transfer) ? Transfer.fromJSON(object.transfer) : void 0,
      signingResults: globalThis.Array.isArray(object?.signingResults) ? object.signingResults.map((e) => LeafRefundTxSigningResult.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.transfer !== void 0) {
      obj.transfer = Transfer.toJSON(message.transfer);
    }
    if (message.signingResults?.length) {
      obj.signingResults = message.signingResults.map((e) => LeafRefundTxSigningResult.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return CounterLeafSwapResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseCounterLeafSwapResponse();
    message.transfer = object.transfer !== void 0 && object.transfer !== null ? Transfer.fromPartial(object.transfer) : void 0;
    message.signingResults = object.signingResults?.map((e) => LeafRefundTxSigningResult.fromPartial(e)) || [];
    return message;
  }
};
function createBaseRefreshTimelockRequest() {
  return { leafId: "", ownerIdentityPublicKey: new Uint8Array(0), signingJobs: [] };
}
var RefreshTimelockRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.leafId !== "") {
      writer.uint32(10).string(message.leafId);
    }
    if (message.ownerIdentityPublicKey.length !== 0) {
      writer.uint32(18).bytes(message.ownerIdentityPublicKey);
    }
    for (const v of message.signingJobs) {
      SigningJob.encode(v, writer.uint32(26).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRefreshTimelockRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.leafId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.ownerIdentityPublicKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.signingJobs.push(SigningJob.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      leafId: isSet3(object.leafId) ? globalThis.String(object.leafId) : "",
      ownerIdentityPublicKey: isSet3(object.ownerIdentityPublicKey) ? bytesFromBase642(object.ownerIdentityPublicKey) : new Uint8Array(0),
      signingJobs: globalThis.Array.isArray(object?.signingJobs) ? object.signingJobs.map((e) => SigningJob.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.leafId !== "") {
      obj.leafId = message.leafId;
    }
    if (message.ownerIdentityPublicKey.length !== 0) {
      obj.ownerIdentityPublicKey = base64FromBytes2(message.ownerIdentityPublicKey);
    }
    if (message.signingJobs?.length) {
      obj.signingJobs = message.signingJobs.map((e) => SigningJob.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return RefreshTimelockRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRefreshTimelockRequest();
    message.leafId = object.leafId ?? "";
    message.ownerIdentityPublicKey = object.ownerIdentityPublicKey ?? new Uint8Array(0);
    message.signingJobs = object.signingJobs?.map((e) => SigningJob.fromPartial(e)) || [];
    return message;
  }
};
function createBaseRefreshTimelockSigningResult() {
  return { signingResult: void 0, verifyingKey: new Uint8Array(0) };
}
var RefreshTimelockSigningResult = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.signingResult !== void 0) {
      SigningResult2.encode(message.signingResult, writer.uint32(10).fork()).join();
    }
    if (message.verifyingKey.length !== 0) {
      writer.uint32(18).bytes(message.verifyingKey);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRefreshTimelockSigningResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.signingResult = SigningResult2.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.verifyingKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      signingResult: isSet3(object.signingResult) ? SigningResult2.fromJSON(object.signingResult) : void 0,
      verifyingKey: isSet3(object.verifyingKey) ? bytesFromBase642(object.verifyingKey) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.signingResult !== void 0) {
      obj.signingResult = SigningResult2.toJSON(message.signingResult);
    }
    if (message.verifyingKey.length !== 0) {
      obj.verifyingKey = base64FromBytes2(message.verifyingKey);
    }
    return obj;
  },
  create(base) {
    return RefreshTimelockSigningResult.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRefreshTimelockSigningResult();
    message.signingResult = object.signingResult !== void 0 && object.signingResult !== null ? SigningResult2.fromPartial(object.signingResult) : void 0;
    message.verifyingKey = object.verifyingKey ?? new Uint8Array(0);
    return message;
  }
};
function createBaseRefreshTimelockResponse() {
  return { signingResults: [] };
}
var RefreshTimelockResponse = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.signingResults) {
      RefreshTimelockSigningResult.encode(v, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRefreshTimelockResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.signingResults.push(RefreshTimelockSigningResult.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      signingResults: globalThis.Array.isArray(object?.signingResults) ? object.signingResults.map((e) => RefreshTimelockSigningResult.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.signingResults?.length) {
      obj.signingResults = message.signingResults.map((e) => RefreshTimelockSigningResult.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return RefreshTimelockResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRefreshTimelockResponse();
    message.signingResults = object.signingResults?.map((e) => RefreshTimelockSigningResult.fromPartial(e)) || [];
    return message;
  }
};
function createBaseExtendLeafRequest() {
  return {
    leafId: "",
    ownerIdentityPublicKey: new Uint8Array(0),
    nodeTxSigningJob: void 0,
    refundTxSigningJob: void 0,
    directNodeTxSigningJob: void 0,
    directRefundTxSigningJob: void 0,
    directFromCpfpRefundTxSigningJob: void 0
  };
}
var ExtendLeafRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.leafId !== "") {
      writer.uint32(10).string(message.leafId);
    }
    if (message.ownerIdentityPublicKey.length !== 0) {
      writer.uint32(18).bytes(message.ownerIdentityPublicKey);
    }
    if (message.nodeTxSigningJob !== void 0) {
      SigningJob.encode(message.nodeTxSigningJob, writer.uint32(26).fork()).join();
    }
    if (message.refundTxSigningJob !== void 0) {
      SigningJob.encode(message.refundTxSigningJob, writer.uint32(34).fork()).join();
    }
    if (message.directNodeTxSigningJob !== void 0) {
      SigningJob.encode(message.directNodeTxSigningJob, writer.uint32(42).fork()).join();
    }
    if (message.directRefundTxSigningJob !== void 0) {
      SigningJob.encode(message.directRefundTxSigningJob, writer.uint32(50).fork()).join();
    }
    if (message.directFromCpfpRefundTxSigningJob !== void 0) {
      SigningJob.encode(message.directFromCpfpRefundTxSigningJob, writer.uint32(58).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseExtendLeafRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.leafId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.ownerIdentityPublicKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.nodeTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.refundTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.directNodeTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.directRefundTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.directFromCpfpRefundTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      leafId: isSet3(object.leafId) ? globalThis.String(object.leafId) : "",
      ownerIdentityPublicKey: isSet3(object.ownerIdentityPublicKey) ? bytesFromBase642(object.ownerIdentityPublicKey) : new Uint8Array(0),
      nodeTxSigningJob: isSet3(object.nodeTxSigningJob) ? SigningJob.fromJSON(object.nodeTxSigningJob) : void 0,
      refundTxSigningJob: isSet3(object.refundTxSigningJob) ? SigningJob.fromJSON(object.refundTxSigningJob) : void 0,
      directNodeTxSigningJob: isSet3(object.directNodeTxSigningJob) ? SigningJob.fromJSON(object.directNodeTxSigningJob) : void 0,
      directRefundTxSigningJob: isSet3(object.directRefundTxSigningJob) ? SigningJob.fromJSON(object.directRefundTxSigningJob) : void 0,
      directFromCpfpRefundTxSigningJob: isSet3(object.directFromCpfpRefundTxSigningJob) ? SigningJob.fromJSON(object.directFromCpfpRefundTxSigningJob) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.leafId !== "") {
      obj.leafId = message.leafId;
    }
    if (message.ownerIdentityPublicKey.length !== 0) {
      obj.ownerIdentityPublicKey = base64FromBytes2(message.ownerIdentityPublicKey);
    }
    if (message.nodeTxSigningJob !== void 0) {
      obj.nodeTxSigningJob = SigningJob.toJSON(message.nodeTxSigningJob);
    }
    if (message.refundTxSigningJob !== void 0) {
      obj.refundTxSigningJob = SigningJob.toJSON(message.refundTxSigningJob);
    }
    if (message.directNodeTxSigningJob !== void 0) {
      obj.directNodeTxSigningJob = SigningJob.toJSON(message.directNodeTxSigningJob);
    }
    if (message.directRefundTxSigningJob !== void 0) {
      obj.directRefundTxSigningJob = SigningJob.toJSON(message.directRefundTxSigningJob);
    }
    if (message.directFromCpfpRefundTxSigningJob !== void 0) {
      obj.directFromCpfpRefundTxSigningJob = SigningJob.toJSON(message.directFromCpfpRefundTxSigningJob);
    }
    return obj;
  },
  create(base) {
    return ExtendLeafRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseExtendLeafRequest();
    message.leafId = object.leafId ?? "";
    message.ownerIdentityPublicKey = object.ownerIdentityPublicKey ?? new Uint8Array(0);
    message.nodeTxSigningJob = object.nodeTxSigningJob !== void 0 && object.nodeTxSigningJob !== null ? SigningJob.fromPartial(object.nodeTxSigningJob) : void 0;
    message.refundTxSigningJob = object.refundTxSigningJob !== void 0 && object.refundTxSigningJob !== null ? SigningJob.fromPartial(object.refundTxSigningJob) : void 0;
    message.directNodeTxSigningJob = object.directNodeTxSigningJob !== void 0 && object.directNodeTxSigningJob !== null ? SigningJob.fromPartial(object.directNodeTxSigningJob) : void 0;
    message.directRefundTxSigningJob = object.directRefundTxSigningJob !== void 0 && object.directRefundTxSigningJob !== null ? SigningJob.fromPartial(object.directRefundTxSigningJob) : void 0;
    message.directFromCpfpRefundTxSigningJob = object.directFromCpfpRefundTxSigningJob !== void 0 && object.directFromCpfpRefundTxSigningJob !== null ? SigningJob.fromPartial(object.directFromCpfpRefundTxSigningJob) : void 0;
    return message;
  }
};
function createBaseExtendLeafSigningResult() {
  return { signingResult: void 0, verifyingKey: new Uint8Array(0) };
}
var ExtendLeafSigningResult = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.signingResult !== void 0) {
      SigningResult2.encode(message.signingResult, writer.uint32(10).fork()).join();
    }
    if (message.verifyingKey.length !== 0) {
      writer.uint32(18).bytes(message.verifyingKey);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseExtendLeafSigningResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.signingResult = SigningResult2.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.verifyingKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      signingResult: isSet3(object.signingResult) ? SigningResult2.fromJSON(object.signingResult) : void 0,
      verifyingKey: isSet3(object.verifyingKey) ? bytesFromBase642(object.verifyingKey) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.signingResult !== void 0) {
      obj.signingResult = SigningResult2.toJSON(message.signingResult);
    }
    if (message.verifyingKey.length !== 0) {
      obj.verifyingKey = base64FromBytes2(message.verifyingKey);
    }
    return obj;
  },
  create(base) {
    return ExtendLeafSigningResult.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseExtendLeafSigningResult();
    message.signingResult = object.signingResult !== void 0 && object.signingResult !== null ? SigningResult2.fromPartial(object.signingResult) : void 0;
    message.verifyingKey = object.verifyingKey ?? new Uint8Array(0);
    return message;
  }
};
function createBaseExtendLeafResponse() {
  return {
    leafId: "",
    nodeTxSigningResult: void 0,
    refundTxSigningResult: void 0,
    directNodeTxSigningResult: void 0,
    directRefundTxSigningResult: void 0,
    directFromCpfpRefundTxSigningResult: void 0
  };
}
var ExtendLeafResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.leafId !== "") {
      writer.uint32(10).string(message.leafId);
    }
    if (message.nodeTxSigningResult !== void 0) {
      ExtendLeafSigningResult.encode(message.nodeTxSigningResult, writer.uint32(18).fork()).join();
    }
    if (message.refundTxSigningResult !== void 0) {
      ExtendLeafSigningResult.encode(message.refundTxSigningResult, writer.uint32(26).fork()).join();
    }
    if (message.directNodeTxSigningResult !== void 0) {
      ExtendLeafSigningResult.encode(message.directNodeTxSigningResult, writer.uint32(34).fork()).join();
    }
    if (message.directRefundTxSigningResult !== void 0) {
      ExtendLeafSigningResult.encode(message.directRefundTxSigningResult, writer.uint32(42).fork()).join();
    }
    if (message.directFromCpfpRefundTxSigningResult !== void 0) {
      ExtendLeafSigningResult.encode(message.directFromCpfpRefundTxSigningResult, writer.uint32(50).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseExtendLeafResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.leafId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.nodeTxSigningResult = ExtendLeafSigningResult.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.refundTxSigningResult = ExtendLeafSigningResult.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.directNodeTxSigningResult = ExtendLeafSigningResult.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.directRefundTxSigningResult = ExtendLeafSigningResult.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.directFromCpfpRefundTxSigningResult = ExtendLeafSigningResult.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      leafId: isSet3(object.leafId) ? globalThis.String(object.leafId) : "",
      nodeTxSigningResult: isSet3(object.nodeTxSigningResult) ? ExtendLeafSigningResult.fromJSON(object.nodeTxSigningResult) : void 0,
      refundTxSigningResult: isSet3(object.refundTxSigningResult) ? ExtendLeafSigningResult.fromJSON(object.refundTxSigningResult) : void 0,
      directNodeTxSigningResult: isSet3(object.directNodeTxSigningResult) ? ExtendLeafSigningResult.fromJSON(object.directNodeTxSigningResult) : void 0,
      directRefundTxSigningResult: isSet3(object.directRefundTxSigningResult) ? ExtendLeafSigningResult.fromJSON(object.directRefundTxSigningResult) : void 0,
      directFromCpfpRefundTxSigningResult: isSet3(object.directFromCpfpRefundTxSigningResult) ? ExtendLeafSigningResult.fromJSON(object.directFromCpfpRefundTxSigningResult) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.leafId !== "") {
      obj.leafId = message.leafId;
    }
    if (message.nodeTxSigningResult !== void 0) {
      obj.nodeTxSigningResult = ExtendLeafSigningResult.toJSON(message.nodeTxSigningResult);
    }
    if (message.refundTxSigningResult !== void 0) {
      obj.refundTxSigningResult = ExtendLeafSigningResult.toJSON(message.refundTxSigningResult);
    }
    if (message.directNodeTxSigningResult !== void 0) {
      obj.directNodeTxSigningResult = ExtendLeafSigningResult.toJSON(message.directNodeTxSigningResult);
    }
    if (message.directRefundTxSigningResult !== void 0) {
      obj.directRefundTxSigningResult = ExtendLeafSigningResult.toJSON(message.directRefundTxSigningResult);
    }
    if (message.directFromCpfpRefundTxSigningResult !== void 0) {
      obj.directFromCpfpRefundTxSigningResult = ExtendLeafSigningResult.toJSON(
        message.directFromCpfpRefundTxSigningResult
      );
    }
    return obj;
  },
  create(base) {
    return ExtendLeafResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseExtendLeafResponse();
    message.leafId = object.leafId ?? "";
    message.nodeTxSigningResult = object.nodeTxSigningResult !== void 0 && object.nodeTxSigningResult !== null ? ExtendLeafSigningResult.fromPartial(object.nodeTxSigningResult) : void 0;
    message.refundTxSigningResult = object.refundTxSigningResult !== void 0 && object.refundTxSigningResult !== null ? ExtendLeafSigningResult.fromPartial(object.refundTxSigningResult) : void 0;
    message.directNodeTxSigningResult = object.directNodeTxSigningResult !== void 0 && object.directNodeTxSigningResult !== null ? ExtendLeafSigningResult.fromPartial(object.directNodeTxSigningResult) : void 0;
    message.directRefundTxSigningResult = object.directRefundTxSigningResult !== void 0 && object.directRefundTxSigningResult !== null ? ExtendLeafSigningResult.fromPartial(object.directRefundTxSigningResult) : void 0;
    message.directFromCpfpRefundTxSigningResult = object.directFromCpfpRefundTxSigningResult !== void 0 && object.directFromCpfpRefundTxSigningResult !== null ? ExtendLeafSigningResult.fromPartial(object.directFromCpfpRefundTxSigningResult) : void 0;
    return message;
  }
};
function createBaseAddressRequestNode() {
  return { userPublicKey: new Uint8Array(0), children: [] };
}
var AddressRequestNode = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.userPublicKey.length !== 0) {
      writer.uint32(10).bytes(message.userPublicKey);
    }
    for (const v of message.children) {
      AddressRequestNode.encode(v, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseAddressRequestNode();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.userPublicKey = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.children.push(AddressRequestNode.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      userPublicKey: isSet3(object.userPublicKey) ? bytesFromBase642(object.userPublicKey) : new Uint8Array(0),
      children: globalThis.Array.isArray(object?.children) ? object.children.map((e) => AddressRequestNode.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.userPublicKey.length !== 0) {
      obj.userPublicKey = base64FromBytes2(message.userPublicKey);
    }
    if (message.children?.length) {
      obj.children = message.children.map((e) => AddressRequestNode.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return AddressRequestNode.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseAddressRequestNode();
    message.userPublicKey = object.userPublicKey ?? new Uint8Array(0);
    message.children = object.children?.map((e) => AddressRequestNode.fromPartial(e)) || [];
    return message;
  }
};
function createBasePrepareTreeAddressRequest() {
  return { source: void 0, node: void 0, userIdentityPublicKey: new Uint8Array(0) };
}
var PrepareTreeAddressRequest = {
  encode(message, writer = new BinaryWriter4()) {
    switch (message.source?.$case) {
      case "parentNodeOutput":
        NodeOutput.encode(message.source.parentNodeOutput, writer.uint32(10).fork()).join();
        break;
      case "onChainUtxo":
        UTXO.encode(message.source.onChainUtxo, writer.uint32(18).fork()).join();
        break;
    }
    if (message.node !== void 0) {
      AddressRequestNode.encode(message.node, writer.uint32(26).fork()).join();
    }
    if (message.userIdentityPublicKey.length !== 0) {
      writer.uint32(34).bytes(message.userIdentityPublicKey);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePrepareTreeAddressRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.source = { $case: "parentNodeOutput", parentNodeOutput: NodeOutput.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.source = { $case: "onChainUtxo", onChainUtxo: UTXO.decode(reader, reader.uint32()) };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.node = AddressRequestNode.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.userIdentityPublicKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      source: isSet3(object.parentNodeOutput) ? { $case: "parentNodeOutput", parentNodeOutput: NodeOutput.fromJSON(object.parentNodeOutput) } : isSet3(object.onChainUtxo) ? { $case: "onChainUtxo", onChainUtxo: UTXO.fromJSON(object.onChainUtxo) } : void 0,
      node: isSet3(object.node) ? AddressRequestNode.fromJSON(object.node) : void 0,
      userIdentityPublicKey: isSet3(object.userIdentityPublicKey) ? bytesFromBase642(object.userIdentityPublicKey) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.source?.$case === "parentNodeOutput") {
      obj.parentNodeOutput = NodeOutput.toJSON(message.source.parentNodeOutput);
    } else if (message.source?.$case === "onChainUtxo") {
      obj.onChainUtxo = UTXO.toJSON(message.source.onChainUtxo);
    }
    if (message.node !== void 0) {
      obj.node = AddressRequestNode.toJSON(message.node);
    }
    if (message.userIdentityPublicKey.length !== 0) {
      obj.userIdentityPublicKey = base64FromBytes2(message.userIdentityPublicKey);
    }
    return obj;
  },
  create(base) {
    return PrepareTreeAddressRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBasePrepareTreeAddressRequest();
    switch (object.source?.$case) {
      case "parentNodeOutput": {
        if (object.source?.parentNodeOutput !== void 0 && object.source?.parentNodeOutput !== null) {
          message.source = {
            $case: "parentNodeOutput",
            parentNodeOutput: NodeOutput.fromPartial(object.source.parentNodeOutput)
          };
        }
        break;
      }
      case "onChainUtxo": {
        if (object.source?.onChainUtxo !== void 0 && object.source?.onChainUtxo !== null) {
          message.source = { $case: "onChainUtxo", onChainUtxo: UTXO.fromPartial(object.source.onChainUtxo) };
        }
        break;
      }
    }
    message.node = object.node !== void 0 && object.node !== null ? AddressRequestNode.fromPartial(object.node) : void 0;
    message.userIdentityPublicKey = object.userIdentityPublicKey ?? new Uint8Array(0);
    return message;
  }
};
function createBaseAddressNode() {
  return { address: void 0, children: [] };
}
var AddressNode = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.address !== void 0) {
      Address.encode(message.address, writer.uint32(10).fork()).join();
    }
    for (const v of message.children) {
      AddressNode.encode(v, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseAddressNode();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.address = Address.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.children.push(AddressNode.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      address: isSet3(object.address) ? Address.fromJSON(object.address) : void 0,
      children: globalThis.Array.isArray(object?.children) ? object.children.map((e) => AddressNode.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.address !== void 0) {
      obj.address = Address.toJSON(message.address);
    }
    if (message.children?.length) {
      obj.children = message.children.map((e) => AddressNode.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return AddressNode.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseAddressNode();
    message.address = object.address !== void 0 && object.address !== null ? Address.fromPartial(object.address) : void 0;
    message.children = object.children?.map((e) => AddressNode.fromPartial(e)) || [];
    return message;
  }
};
function createBasePrepareTreeAddressResponse() {
  return { node: void 0 };
}
var PrepareTreeAddressResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.node !== void 0) {
      AddressNode.encode(message.node, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePrepareTreeAddressResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.node = AddressNode.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { node: isSet3(object.node) ? AddressNode.fromJSON(object.node) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.node !== void 0) {
      obj.node = AddressNode.toJSON(message.node);
    }
    return obj;
  },
  create(base) {
    return PrepareTreeAddressResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBasePrepareTreeAddressResponse();
    message.node = object.node !== void 0 && object.node !== null ? AddressNode.fromPartial(object.node) : void 0;
    return message;
  }
};
function createBaseCreationNode() {
  return {
    nodeTxSigningJob: void 0,
    refundTxSigningJob: void 0,
    children: [],
    directNodeTxSigningJob: void 0,
    directRefundTxSigningJob: void 0,
    directFromCpfpRefundTxSigningJob: void 0
  };
}
var CreationNode = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.nodeTxSigningJob !== void 0) {
      SigningJob.encode(message.nodeTxSigningJob, writer.uint32(10).fork()).join();
    }
    if (message.refundTxSigningJob !== void 0) {
      SigningJob.encode(message.refundTxSigningJob, writer.uint32(18).fork()).join();
    }
    for (const v of message.children) {
      CreationNode.encode(v, writer.uint32(26).fork()).join();
    }
    if (message.directNodeTxSigningJob !== void 0) {
      SigningJob.encode(message.directNodeTxSigningJob, writer.uint32(34).fork()).join();
    }
    if (message.directRefundTxSigningJob !== void 0) {
      SigningJob.encode(message.directRefundTxSigningJob, writer.uint32(42).fork()).join();
    }
    if (message.directFromCpfpRefundTxSigningJob !== void 0) {
      SigningJob.encode(message.directFromCpfpRefundTxSigningJob, writer.uint32(50).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCreationNode();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.nodeTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.refundTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.children.push(CreationNode.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.directNodeTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.directRefundTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.directFromCpfpRefundTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      nodeTxSigningJob: isSet3(object.nodeTxSigningJob) ? SigningJob.fromJSON(object.nodeTxSigningJob) : void 0,
      refundTxSigningJob: isSet3(object.refundTxSigningJob) ? SigningJob.fromJSON(object.refundTxSigningJob) : void 0,
      children: globalThis.Array.isArray(object?.children) ? object.children.map((e) => CreationNode.fromJSON(e)) : [],
      directNodeTxSigningJob: isSet3(object.directNodeTxSigningJob) ? SigningJob.fromJSON(object.directNodeTxSigningJob) : void 0,
      directRefundTxSigningJob: isSet3(object.directRefundTxSigningJob) ? SigningJob.fromJSON(object.directRefundTxSigningJob) : void 0,
      directFromCpfpRefundTxSigningJob: isSet3(object.directFromCpfpRefundTxSigningJob) ? SigningJob.fromJSON(object.directFromCpfpRefundTxSigningJob) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.nodeTxSigningJob !== void 0) {
      obj.nodeTxSigningJob = SigningJob.toJSON(message.nodeTxSigningJob);
    }
    if (message.refundTxSigningJob !== void 0) {
      obj.refundTxSigningJob = SigningJob.toJSON(message.refundTxSigningJob);
    }
    if (message.children?.length) {
      obj.children = message.children.map((e) => CreationNode.toJSON(e));
    }
    if (message.directNodeTxSigningJob !== void 0) {
      obj.directNodeTxSigningJob = SigningJob.toJSON(message.directNodeTxSigningJob);
    }
    if (message.directRefundTxSigningJob !== void 0) {
      obj.directRefundTxSigningJob = SigningJob.toJSON(message.directRefundTxSigningJob);
    }
    if (message.directFromCpfpRefundTxSigningJob !== void 0) {
      obj.directFromCpfpRefundTxSigningJob = SigningJob.toJSON(message.directFromCpfpRefundTxSigningJob);
    }
    return obj;
  },
  create(base) {
    return CreationNode.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseCreationNode();
    message.nodeTxSigningJob = object.nodeTxSigningJob !== void 0 && object.nodeTxSigningJob !== null ? SigningJob.fromPartial(object.nodeTxSigningJob) : void 0;
    message.refundTxSigningJob = object.refundTxSigningJob !== void 0 && object.refundTxSigningJob !== null ? SigningJob.fromPartial(object.refundTxSigningJob) : void 0;
    message.children = object.children?.map((e) => CreationNode.fromPartial(e)) || [];
    message.directNodeTxSigningJob = object.directNodeTxSigningJob !== void 0 && object.directNodeTxSigningJob !== null ? SigningJob.fromPartial(object.directNodeTxSigningJob) : void 0;
    message.directRefundTxSigningJob = object.directRefundTxSigningJob !== void 0 && object.directRefundTxSigningJob !== null ? SigningJob.fromPartial(object.directRefundTxSigningJob) : void 0;
    message.directFromCpfpRefundTxSigningJob = object.directFromCpfpRefundTxSigningJob !== void 0 && object.directFromCpfpRefundTxSigningJob !== null ? SigningJob.fromPartial(object.directFromCpfpRefundTxSigningJob) : void 0;
    return message;
  }
};
function createBaseCreateTreeRequest() {
  return { source: void 0, node: void 0, userIdentityPublicKey: new Uint8Array(0) };
}
var CreateTreeRequest = {
  encode(message, writer = new BinaryWriter4()) {
    switch (message.source?.$case) {
      case "parentNodeOutput":
        NodeOutput.encode(message.source.parentNodeOutput, writer.uint32(10).fork()).join();
        break;
      case "onChainUtxo":
        UTXO.encode(message.source.onChainUtxo, writer.uint32(18).fork()).join();
        break;
    }
    if (message.node !== void 0) {
      CreationNode.encode(message.node, writer.uint32(26).fork()).join();
    }
    if (message.userIdentityPublicKey.length !== 0) {
      writer.uint32(34).bytes(message.userIdentityPublicKey);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCreateTreeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.source = { $case: "parentNodeOutput", parentNodeOutput: NodeOutput.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.source = { $case: "onChainUtxo", onChainUtxo: UTXO.decode(reader, reader.uint32()) };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.node = CreationNode.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.userIdentityPublicKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      source: isSet3(object.parentNodeOutput) ? { $case: "parentNodeOutput", parentNodeOutput: NodeOutput.fromJSON(object.parentNodeOutput) } : isSet3(object.onChainUtxo) ? { $case: "onChainUtxo", onChainUtxo: UTXO.fromJSON(object.onChainUtxo) } : void 0,
      node: isSet3(object.node) ? CreationNode.fromJSON(object.node) : void 0,
      userIdentityPublicKey: isSet3(object.userIdentityPublicKey) ? bytesFromBase642(object.userIdentityPublicKey) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.source?.$case === "parentNodeOutput") {
      obj.parentNodeOutput = NodeOutput.toJSON(message.source.parentNodeOutput);
    } else if (message.source?.$case === "onChainUtxo") {
      obj.onChainUtxo = UTXO.toJSON(message.source.onChainUtxo);
    }
    if (message.node !== void 0) {
      obj.node = CreationNode.toJSON(message.node);
    }
    if (message.userIdentityPublicKey.length !== 0) {
      obj.userIdentityPublicKey = base64FromBytes2(message.userIdentityPublicKey);
    }
    return obj;
  },
  create(base) {
    return CreateTreeRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseCreateTreeRequest();
    switch (object.source?.$case) {
      case "parentNodeOutput": {
        if (object.source?.parentNodeOutput !== void 0 && object.source?.parentNodeOutput !== null) {
          message.source = {
            $case: "parentNodeOutput",
            parentNodeOutput: NodeOutput.fromPartial(object.source.parentNodeOutput)
          };
        }
        break;
      }
      case "onChainUtxo": {
        if (object.source?.onChainUtxo !== void 0 && object.source?.onChainUtxo !== null) {
          message.source = { $case: "onChainUtxo", onChainUtxo: UTXO.fromPartial(object.source.onChainUtxo) };
        }
        break;
      }
    }
    message.node = object.node !== void 0 && object.node !== null ? CreationNode.fromPartial(object.node) : void 0;
    message.userIdentityPublicKey = object.userIdentityPublicKey ?? new Uint8Array(0);
    return message;
  }
};
function createBaseCreationResponseNode() {
  return {
    nodeId: "",
    nodeTxSigningResult: void 0,
    refundTxSigningResult: void 0,
    children: [],
    directNodeTxSigningResult: void 0,
    directRefundTxSigningResult: void 0,
    directFromCpfpRefundTxSigningResult: void 0
  };
}
var CreationResponseNode = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.nodeId !== "") {
      writer.uint32(10).string(message.nodeId);
    }
    if (message.nodeTxSigningResult !== void 0) {
      SigningResult2.encode(message.nodeTxSigningResult, writer.uint32(18).fork()).join();
    }
    if (message.refundTxSigningResult !== void 0) {
      SigningResult2.encode(message.refundTxSigningResult, writer.uint32(26).fork()).join();
    }
    for (const v of message.children) {
      CreationResponseNode.encode(v, writer.uint32(34).fork()).join();
    }
    if (message.directNodeTxSigningResult !== void 0) {
      SigningResult2.encode(message.directNodeTxSigningResult, writer.uint32(42).fork()).join();
    }
    if (message.directRefundTxSigningResult !== void 0) {
      SigningResult2.encode(message.directRefundTxSigningResult, writer.uint32(50).fork()).join();
    }
    if (message.directFromCpfpRefundTxSigningResult !== void 0) {
      SigningResult2.encode(message.directFromCpfpRefundTxSigningResult, writer.uint32(58).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCreationResponseNode();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.nodeId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.nodeTxSigningResult = SigningResult2.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.refundTxSigningResult = SigningResult2.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.children.push(CreationResponseNode.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.directNodeTxSigningResult = SigningResult2.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.directRefundTxSigningResult = SigningResult2.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.directFromCpfpRefundTxSigningResult = SigningResult2.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      nodeId: isSet3(object.nodeId) ? globalThis.String(object.nodeId) : "",
      nodeTxSigningResult: isSet3(object.nodeTxSigningResult) ? SigningResult2.fromJSON(object.nodeTxSigningResult) : void 0,
      refundTxSigningResult: isSet3(object.refundTxSigningResult) ? SigningResult2.fromJSON(object.refundTxSigningResult) : void 0,
      children: globalThis.Array.isArray(object?.children) ? object.children.map((e) => CreationResponseNode.fromJSON(e)) : [],
      directNodeTxSigningResult: isSet3(object.directNodeTxSigningResult) ? SigningResult2.fromJSON(object.directNodeTxSigningResult) : void 0,
      directRefundTxSigningResult: isSet3(object.directRefundTxSigningResult) ? SigningResult2.fromJSON(object.directRefundTxSigningResult) : void 0,
      directFromCpfpRefundTxSigningResult: isSet3(object.directFromCpfpRefundTxSigningResult) ? SigningResult2.fromJSON(object.directFromCpfpRefundTxSigningResult) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.nodeId !== "") {
      obj.nodeId = message.nodeId;
    }
    if (message.nodeTxSigningResult !== void 0) {
      obj.nodeTxSigningResult = SigningResult2.toJSON(message.nodeTxSigningResult);
    }
    if (message.refundTxSigningResult !== void 0) {
      obj.refundTxSigningResult = SigningResult2.toJSON(message.refundTxSigningResult);
    }
    if (message.children?.length) {
      obj.children = message.children.map((e) => CreationResponseNode.toJSON(e));
    }
    if (message.directNodeTxSigningResult !== void 0) {
      obj.directNodeTxSigningResult = SigningResult2.toJSON(message.directNodeTxSigningResult);
    }
    if (message.directRefundTxSigningResult !== void 0) {
      obj.directRefundTxSigningResult = SigningResult2.toJSON(message.directRefundTxSigningResult);
    }
    if (message.directFromCpfpRefundTxSigningResult !== void 0) {
      obj.directFromCpfpRefundTxSigningResult = SigningResult2.toJSON(message.directFromCpfpRefundTxSigningResult);
    }
    return obj;
  },
  create(base) {
    return CreationResponseNode.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseCreationResponseNode();
    message.nodeId = object.nodeId ?? "";
    message.nodeTxSigningResult = object.nodeTxSigningResult !== void 0 && object.nodeTxSigningResult !== null ? SigningResult2.fromPartial(object.nodeTxSigningResult) : void 0;
    message.refundTxSigningResult = object.refundTxSigningResult !== void 0 && object.refundTxSigningResult !== null ? SigningResult2.fromPartial(object.refundTxSigningResult) : void 0;
    message.children = object.children?.map((e) => CreationResponseNode.fromPartial(e)) || [];
    message.directNodeTxSigningResult = object.directNodeTxSigningResult !== void 0 && object.directNodeTxSigningResult !== null ? SigningResult2.fromPartial(object.directNodeTxSigningResult) : void 0;
    message.directRefundTxSigningResult = object.directRefundTxSigningResult !== void 0 && object.directRefundTxSigningResult !== null ? SigningResult2.fromPartial(object.directRefundTxSigningResult) : void 0;
    message.directFromCpfpRefundTxSigningResult = object.directFromCpfpRefundTxSigningResult !== void 0 && object.directFromCpfpRefundTxSigningResult !== null ? SigningResult2.fromPartial(object.directFromCpfpRefundTxSigningResult) : void 0;
    return message;
  }
};
function createBaseCreateTreeResponse() {
  return { node: void 0 };
}
var CreateTreeResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.node !== void 0) {
      CreationResponseNode.encode(message.node, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCreateTreeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.node = CreationResponseNode.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { node: isSet3(object.node) ? CreationResponseNode.fromJSON(object.node) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.node !== void 0) {
      obj.node = CreationResponseNode.toJSON(message.node);
    }
    return obj;
  },
  create(base) {
    return CreateTreeResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseCreateTreeResponse();
    message.node = object.node !== void 0 && object.node !== null ? CreationResponseNode.fromPartial(object.node) : void 0;
    return message;
  }
};
function createBaseSigningOperatorInfo() {
  return { index: 0, identifier: "", publicKey: new Uint8Array(0), address: "" };
}
var SigningOperatorInfo = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.index !== 0) {
      writer.uint32(8).uint64(message.index);
    }
    if (message.identifier !== "") {
      writer.uint32(18).string(message.identifier);
    }
    if (message.publicKey.length !== 0) {
      writer.uint32(26).bytes(message.publicKey);
    }
    if (message.address !== "") {
      writer.uint32(34).string(message.address);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSigningOperatorInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.index = longToNumber2(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.identifier = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.publicKey = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.address = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      index: isSet3(object.index) ? globalThis.Number(object.index) : 0,
      identifier: isSet3(object.identifier) ? globalThis.String(object.identifier) : "",
      publicKey: isSet3(object.publicKey) ? bytesFromBase642(object.publicKey) : new Uint8Array(0),
      address: isSet3(object.address) ? globalThis.String(object.address) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.index !== 0) {
      obj.index = Math.round(message.index);
    }
    if (message.identifier !== "") {
      obj.identifier = message.identifier;
    }
    if (message.publicKey.length !== 0) {
      obj.publicKey = base64FromBytes2(message.publicKey);
    }
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },
  create(base) {
    return SigningOperatorInfo.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSigningOperatorInfo();
    message.index = object.index ?? 0;
    message.identifier = object.identifier ?? "";
    message.publicKey = object.publicKey ?? new Uint8Array(0);
    message.address = object.address ?? "";
    return message;
  }
};
function createBaseGetSigningOperatorListResponse() {
  return { signingOperators: {} };
}
var GetSigningOperatorListResponse = {
  encode(message, writer = new BinaryWriter4()) {
    Object.entries(message.signingOperators).forEach(([key, value]) => {
      GetSigningOperatorListResponse_SigningOperatorsEntry.encode({ key, value }, writer.uint32(10).fork()).join();
    });
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetSigningOperatorListResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          const entry1 = GetSigningOperatorListResponse_SigningOperatorsEntry.decode(reader, reader.uint32());
          if (entry1.value !== void 0) {
            message.signingOperators[entry1.key] = entry1.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      signingOperators: isObject2(object.signingOperators) ? Object.entries(object.signingOperators).reduce(
        (acc, [key, value]) => {
          acc[key] = SigningOperatorInfo.fromJSON(value);
          return acc;
        },
        {}
      ) : {}
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.signingOperators) {
      const entries = Object.entries(message.signingOperators);
      if (entries.length > 0) {
        obj.signingOperators = {};
        entries.forEach(([k, v]) => {
          obj.signingOperators[k] = SigningOperatorInfo.toJSON(v);
        });
      }
    }
    return obj;
  },
  create(base) {
    return GetSigningOperatorListResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetSigningOperatorListResponse();
    message.signingOperators = Object.entries(object.signingOperators ?? {}).reduce((acc, [key, value]) => {
      if (value !== void 0) {
        acc[key] = SigningOperatorInfo.fromPartial(value);
      }
      return acc;
    }, {});
    return message;
  }
};
function createBaseGetSigningOperatorListResponse_SigningOperatorsEntry() {
  return { key: "", value: void 0 };
}
var GetSigningOperatorListResponse_SigningOperatorsEntry = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== void 0) {
      SigningOperatorInfo.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetSigningOperatorListResponse_SigningOperatorsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.value = SigningOperatorInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet3(object.key) ? globalThis.String(object.key) : "",
      value: isSet3(object.value) ? SigningOperatorInfo.fromJSON(object.value) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== void 0) {
      obj.value = SigningOperatorInfo.toJSON(message.value);
    }
    return obj;
  },
  create(base) {
    return GetSigningOperatorListResponse_SigningOperatorsEntry.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetSigningOperatorListResponse_SigningOperatorsEntry();
    message.key = object.key ?? "";
    message.value = object.value !== void 0 && object.value !== null ? SigningOperatorInfo.fromPartial(object.value) : void 0;
    return message;
  }
};
function createBaseQueryUserSignedRefundsRequest() {
  return { paymentHash: new Uint8Array(0), identityPublicKey: new Uint8Array(0) };
}
var QueryUserSignedRefundsRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.paymentHash.length !== 0) {
      writer.uint32(10).bytes(message.paymentHash);
    }
    if (message.identityPublicKey.length !== 0) {
      writer.uint32(18).bytes(message.identityPublicKey);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryUserSignedRefundsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.paymentHash = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.identityPublicKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      paymentHash: isSet3(object.paymentHash) ? bytesFromBase642(object.paymentHash) : new Uint8Array(0),
      identityPublicKey: isSet3(object.identityPublicKey) ? bytesFromBase642(object.identityPublicKey) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.paymentHash.length !== 0) {
      obj.paymentHash = base64FromBytes2(message.paymentHash);
    }
    if (message.identityPublicKey.length !== 0) {
      obj.identityPublicKey = base64FromBytes2(message.identityPublicKey);
    }
    return obj;
  },
  create(base) {
    return QueryUserSignedRefundsRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryUserSignedRefundsRequest();
    message.paymentHash = object.paymentHash ?? new Uint8Array(0);
    message.identityPublicKey = object.identityPublicKey ?? new Uint8Array(0);
    return message;
  }
};
function createBaseQueryUserSignedRefundsResponse() {
  return { userSignedRefunds: [], transfer: void 0 };
}
var QueryUserSignedRefundsResponse = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.userSignedRefunds) {
      UserSignedRefund.encode(v, writer.uint32(10).fork()).join();
    }
    if (message.transfer !== void 0) {
      Transfer.encode(message.transfer, writer.uint32(26).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryUserSignedRefundsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.userSignedRefunds.push(UserSignedRefund.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.transfer = Transfer.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      userSignedRefunds: globalThis.Array.isArray(object?.userSignedRefunds) ? object.userSignedRefunds.map((e) => UserSignedRefund.fromJSON(e)) : [],
      transfer: isSet3(object.transfer) ? Transfer.fromJSON(object.transfer) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.userSignedRefunds?.length) {
      obj.userSignedRefunds = message.userSignedRefunds.map((e) => UserSignedRefund.toJSON(e));
    }
    if (message.transfer !== void 0) {
      obj.transfer = Transfer.toJSON(message.transfer);
    }
    return obj;
  },
  create(base) {
    return QueryUserSignedRefundsResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryUserSignedRefundsResponse();
    message.userSignedRefunds = object.userSignedRefunds?.map((e) => UserSignedRefund.fromPartial(e)) || [];
    message.transfer = object.transfer !== void 0 && object.transfer !== null ? Transfer.fromPartial(object.transfer) : void 0;
    return message;
  }
};
function createBasePreimageRequestWithTransfer() {
  return {
    paymentHash: new Uint8Array(0),
    receiverIdentityPubkey: new Uint8Array(0),
    status: 0,
    createdTime: void 0,
    transfer: void 0,
    preimage: void 0
  };
}
var PreimageRequestWithTransfer = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.paymentHash.length !== 0) {
      writer.uint32(10).bytes(message.paymentHash);
    }
    if (message.receiverIdentityPubkey.length !== 0) {
      writer.uint32(18).bytes(message.receiverIdentityPubkey);
    }
    if (message.status !== 0) {
      writer.uint32(24).int32(message.status);
    }
    if (message.createdTime !== void 0) {
      Timestamp.encode(toTimestamp(message.createdTime), writer.uint32(34).fork()).join();
    }
    if (message.transfer !== void 0) {
      Transfer.encode(message.transfer, writer.uint32(42).fork()).join();
    }
    if (message.preimage !== void 0) {
      writer.uint32(50).bytes(message.preimage);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePreimageRequestWithTransfer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.paymentHash = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.receiverIdentityPubkey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.status = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.createdTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.transfer = Transfer.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.preimage = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      paymentHash: isSet3(object.paymentHash) ? bytesFromBase642(object.paymentHash) : new Uint8Array(0),
      receiverIdentityPubkey: isSet3(object.receiverIdentityPubkey) ? bytesFromBase642(object.receiverIdentityPubkey) : new Uint8Array(0),
      status: isSet3(object.status) ? preimageRequestStatusFromJSON(object.status) : 0,
      createdTime: isSet3(object.createdTime) ? fromJsonTimestamp(object.createdTime) : void 0,
      transfer: isSet3(object.transfer) ? Transfer.fromJSON(object.transfer) : void 0,
      preimage: isSet3(object.preimage) ? bytesFromBase642(object.preimage) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.paymentHash.length !== 0) {
      obj.paymentHash = base64FromBytes2(message.paymentHash);
    }
    if (message.receiverIdentityPubkey.length !== 0) {
      obj.receiverIdentityPubkey = base64FromBytes2(message.receiverIdentityPubkey);
    }
    if (message.status !== 0) {
      obj.status = preimageRequestStatusToJSON(message.status);
    }
    if (message.createdTime !== void 0) {
      obj.createdTime = message.createdTime.toISOString();
    }
    if (message.transfer !== void 0) {
      obj.transfer = Transfer.toJSON(message.transfer);
    }
    if (message.preimage !== void 0) {
      obj.preimage = base64FromBytes2(message.preimage);
    }
    return obj;
  },
  create(base) {
    return PreimageRequestWithTransfer.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBasePreimageRequestWithTransfer();
    message.paymentHash = object.paymentHash ?? new Uint8Array(0);
    message.receiverIdentityPubkey = object.receiverIdentityPubkey ?? new Uint8Array(0);
    message.status = object.status ?? 0;
    message.createdTime = object.createdTime ?? void 0;
    message.transfer = object.transfer !== void 0 && object.transfer !== null ? Transfer.fromPartial(object.transfer) : void 0;
    message.preimage = object.preimage ?? void 0;
    return message;
  }
};
function createBaseQueryHtlcRequest() {
  return { paymentHashes: [], identityPublicKey: new Uint8Array(0), status: void 0, limit: 0, offset: 0 };
}
var QueryHtlcRequest = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.paymentHashes) {
      writer.uint32(10).bytes(v);
    }
    if (message.identityPublicKey.length !== 0) {
      writer.uint32(18).bytes(message.identityPublicKey);
    }
    if (message.status !== void 0) {
      writer.uint32(24).int32(message.status);
    }
    if (message.limit !== 0) {
      writer.uint32(32).int64(message.limit);
    }
    if (message.offset !== 0) {
      writer.uint32(40).int64(message.offset);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryHtlcRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.paymentHashes.push(reader.bytes());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.identityPublicKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.status = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }
          message.limit = longToNumber2(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }
          message.offset = longToNumber2(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      paymentHashes: globalThis.Array.isArray(object?.paymentHashes) ? object.paymentHashes.map((e) => bytesFromBase642(e)) : [],
      identityPublicKey: isSet3(object.identityPublicKey) ? bytesFromBase642(object.identityPublicKey) : new Uint8Array(0),
      status: isSet3(object.status) ? preimageRequestStatusFromJSON(object.status) : void 0,
      limit: isSet3(object.limit) ? globalThis.Number(object.limit) : 0,
      offset: isSet3(object.offset) ? globalThis.Number(object.offset) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.paymentHashes?.length) {
      obj.paymentHashes = message.paymentHashes.map((e) => base64FromBytes2(e));
    }
    if (message.identityPublicKey.length !== 0) {
      obj.identityPublicKey = base64FromBytes2(message.identityPublicKey);
    }
    if (message.status !== void 0) {
      obj.status = preimageRequestStatusToJSON(message.status);
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    return obj;
  },
  create(base) {
    return QueryHtlcRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryHtlcRequest();
    message.paymentHashes = object.paymentHashes?.map((e) => e) || [];
    message.identityPublicKey = object.identityPublicKey ?? new Uint8Array(0);
    message.status = object.status ?? void 0;
    message.limit = object.limit ?? 0;
    message.offset = object.offset ?? 0;
    return message;
  }
};
function createBaseQueryHtlcResponse() {
  return { preimageRequests: [], offset: 0 };
}
var QueryHtlcResponse = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.preimageRequests) {
      PreimageRequestWithTransfer.encode(v, writer.uint32(10).fork()).join();
    }
    if (message.offset !== 0) {
      writer.uint32(16).int64(message.offset);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryHtlcResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.preimageRequests.push(PreimageRequestWithTransfer.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.offset = longToNumber2(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      preimageRequests: globalThis.Array.isArray(object?.preimageRequests) ? object.preimageRequests.map((e) => PreimageRequestWithTransfer.fromJSON(e)) : [],
      offset: isSet3(object.offset) ? globalThis.Number(object.offset) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.preimageRequests?.length) {
      obj.preimageRequests = message.preimageRequests.map((e) => PreimageRequestWithTransfer.toJSON(e));
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    return obj;
  },
  create(base) {
    return QueryHtlcResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryHtlcResponse();
    message.preimageRequests = object.preimageRequests?.map((e) => PreimageRequestWithTransfer.fromPartial(e)) || [];
    message.offset = object.offset ?? 0;
    return message;
  }
};
function createBaseProvidePreimageRequest() {
  return { paymentHash: new Uint8Array(0), preimage: new Uint8Array(0), identityPublicKey: new Uint8Array(0) };
}
var ProvidePreimageRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.paymentHash.length !== 0) {
      writer.uint32(10).bytes(message.paymentHash);
    }
    if (message.preimage.length !== 0) {
      writer.uint32(18).bytes(message.preimage);
    }
    if (message.identityPublicKey.length !== 0) {
      writer.uint32(26).bytes(message.identityPublicKey);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseProvidePreimageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.paymentHash = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.preimage = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.identityPublicKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      paymentHash: isSet3(object.paymentHash) ? bytesFromBase642(object.paymentHash) : new Uint8Array(0),
      preimage: isSet3(object.preimage) ? bytesFromBase642(object.preimage) : new Uint8Array(0),
      identityPublicKey: isSet3(object.identityPublicKey) ? bytesFromBase642(object.identityPublicKey) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.paymentHash.length !== 0) {
      obj.paymentHash = base64FromBytes2(message.paymentHash);
    }
    if (message.preimage.length !== 0) {
      obj.preimage = base64FromBytes2(message.preimage);
    }
    if (message.identityPublicKey.length !== 0) {
      obj.identityPublicKey = base64FromBytes2(message.identityPublicKey);
    }
    return obj;
  },
  create(base) {
    return ProvidePreimageRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseProvidePreimageRequest();
    message.paymentHash = object.paymentHash ?? new Uint8Array(0);
    message.preimage = object.preimage ?? new Uint8Array(0);
    message.identityPublicKey = object.identityPublicKey ?? new Uint8Array(0);
    return message;
  }
};
function createBaseProvidePreimageResponse() {
  return { transfer: void 0 };
}
var ProvidePreimageResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.transfer !== void 0) {
      Transfer.encode(message.transfer, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseProvidePreimageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.transfer = Transfer.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { transfer: isSet3(object.transfer) ? Transfer.fromJSON(object.transfer) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.transfer !== void 0) {
      obj.transfer = Transfer.toJSON(message.transfer);
    }
    return obj;
  },
  create(base) {
    return ProvidePreimageResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseProvidePreimageResponse();
    message.transfer = object.transfer !== void 0 && object.transfer !== null ? Transfer.fromPartial(object.transfer) : void 0;
    return message;
  }
};
function createBaseReturnLightningPaymentRequest() {
  return { paymentHash: new Uint8Array(0), userIdentityPublicKey: new Uint8Array(0) };
}
var ReturnLightningPaymentRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.paymentHash.length !== 0) {
      writer.uint32(10).bytes(message.paymentHash);
    }
    if (message.userIdentityPublicKey.length !== 0) {
      writer.uint32(18).bytes(message.userIdentityPublicKey);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseReturnLightningPaymentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.paymentHash = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.userIdentityPublicKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      paymentHash: isSet3(object.paymentHash) ? bytesFromBase642(object.paymentHash) : new Uint8Array(0),
      userIdentityPublicKey: isSet3(object.userIdentityPublicKey) ? bytesFromBase642(object.userIdentityPublicKey) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.paymentHash.length !== 0) {
      obj.paymentHash = base64FromBytes2(message.paymentHash);
    }
    if (message.userIdentityPublicKey.length !== 0) {
      obj.userIdentityPublicKey = base64FromBytes2(message.userIdentityPublicKey);
    }
    return obj;
  },
  create(base) {
    return ReturnLightningPaymentRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseReturnLightningPaymentRequest();
    message.paymentHash = object.paymentHash ?? new Uint8Array(0);
    message.userIdentityPublicKey = object.userIdentityPublicKey ?? new Uint8Array(0);
    return message;
  }
};
function createBaseTreeNodeIds() {
  return { nodeIds: [] };
}
var TreeNodeIds = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.nodeIds) {
      writer.uint32(10).string(v);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTreeNodeIds();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.nodeIds.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      nodeIds: globalThis.Array.isArray(object?.nodeIds) ? object.nodeIds.map((e) => globalThis.String(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.nodeIds?.length) {
      obj.nodeIds = message.nodeIds;
    }
    return obj;
  },
  create(base) {
    return TreeNodeIds.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseTreeNodeIds();
    message.nodeIds = object.nodeIds?.map((e) => e) || [];
    return message;
  }
};
function createBaseQueryNodesRequest() {
  return { source: void 0, includeParents: false, limit: 0, offset: 0, network: 0, statuses: [] };
}
var QueryNodesRequest = {
  encode(message, writer = new BinaryWriter4()) {
    switch (message.source?.$case) {
      case "ownerIdentityPubkey":
        writer.uint32(10).bytes(message.source.ownerIdentityPubkey);
        break;
      case "nodeIds":
        TreeNodeIds.encode(message.source.nodeIds, writer.uint32(18).fork()).join();
        break;
    }
    if (message.includeParents !== false) {
      writer.uint32(24).bool(message.includeParents);
    }
    if (message.limit !== 0) {
      writer.uint32(32).int64(message.limit);
    }
    if (message.offset !== 0) {
      writer.uint32(40).int64(message.offset);
    }
    if (message.network !== 0) {
      writer.uint32(48).int32(message.network);
    }
    writer.uint32(58).fork();
    for (const v of message.statuses) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryNodesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.source = { $case: "ownerIdentityPubkey", ownerIdentityPubkey: reader.bytes() };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.source = { $case: "nodeIds", nodeIds: TreeNodeIds.decode(reader, reader.uint32()) };
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.includeParents = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }
          message.limit = longToNumber2(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }
          message.offset = longToNumber2(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }
          message.network = reader.int32();
          continue;
        }
        case 7: {
          if (tag === 56) {
            message.statuses.push(reader.int32());
            continue;
          }
          if (tag === 58) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.statuses.push(reader.int32());
            }
            continue;
          }
          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      source: isSet3(object.ownerIdentityPubkey) ? { $case: "ownerIdentityPubkey", ownerIdentityPubkey: bytesFromBase642(object.ownerIdentityPubkey) } : isSet3(object.nodeIds) ? { $case: "nodeIds", nodeIds: TreeNodeIds.fromJSON(object.nodeIds) } : void 0,
      includeParents: isSet3(object.includeParents) ? globalThis.Boolean(object.includeParents) : false,
      limit: isSet3(object.limit) ? globalThis.Number(object.limit) : 0,
      offset: isSet3(object.offset) ? globalThis.Number(object.offset) : 0,
      network: isSet3(object.network) ? networkFromJSON(object.network) : 0,
      statuses: globalThis.Array.isArray(object?.statuses) ? object.statuses.map((e) => treeNodeStatusFromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.source?.$case === "ownerIdentityPubkey") {
      obj.ownerIdentityPubkey = base64FromBytes2(message.source.ownerIdentityPubkey);
    } else if (message.source?.$case === "nodeIds") {
      obj.nodeIds = TreeNodeIds.toJSON(message.source.nodeIds);
    }
    if (message.includeParents !== false) {
      obj.includeParents = message.includeParents;
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    if (message.network !== 0) {
      obj.network = networkToJSON(message.network);
    }
    if (message.statuses?.length) {
      obj.statuses = message.statuses.map((e) => treeNodeStatusToJSON(e));
    }
    return obj;
  },
  create(base) {
    return QueryNodesRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryNodesRequest();
    switch (object.source?.$case) {
      case "ownerIdentityPubkey": {
        if (object.source?.ownerIdentityPubkey !== void 0 && object.source?.ownerIdentityPubkey !== null) {
          message.source = { $case: "ownerIdentityPubkey", ownerIdentityPubkey: object.source.ownerIdentityPubkey };
        }
        break;
      }
      case "nodeIds": {
        if (object.source?.nodeIds !== void 0 && object.source?.nodeIds !== null) {
          message.source = { $case: "nodeIds", nodeIds: TreeNodeIds.fromPartial(object.source.nodeIds) };
        }
        break;
      }
    }
    message.includeParents = object.includeParents ?? false;
    message.limit = object.limit ?? 0;
    message.offset = object.offset ?? 0;
    message.network = object.network ?? 0;
    message.statuses = object.statuses?.map((e) => e) || [];
    return message;
  }
};
function createBaseQueryNodesResponse() {
  return { nodes: {}, offset: 0 };
}
var QueryNodesResponse = {
  encode(message, writer = new BinaryWriter4()) {
    Object.entries(message.nodes).forEach(([key, value]) => {
      QueryNodesResponse_NodesEntry.encode({ key, value }, writer.uint32(10).fork()).join();
    });
    if (message.offset !== 0) {
      writer.uint32(16).int64(message.offset);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryNodesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          const entry1 = QueryNodesResponse_NodesEntry.decode(reader, reader.uint32());
          if (entry1.value !== void 0) {
            message.nodes[entry1.key] = entry1.value;
          }
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.offset = longToNumber2(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      nodes: isObject2(object.nodes) ? Object.entries(object.nodes).reduce((acc, [key, value]) => {
        acc[key] = TreeNode.fromJSON(value);
        return acc;
      }, {}) : {},
      offset: isSet3(object.offset) ? globalThis.Number(object.offset) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.nodes) {
      const entries = Object.entries(message.nodes);
      if (entries.length > 0) {
        obj.nodes = {};
        entries.forEach(([k, v]) => {
          obj.nodes[k] = TreeNode.toJSON(v);
        });
      }
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    return obj;
  },
  create(base) {
    return QueryNodesResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryNodesResponse();
    message.nodes = Object.entries(object.nodes ?? {}).reduce((acc, [key, value]) => {
      if (value !== void 0) {
        acc[key] = TreeNode.fromPartial(value);
      }
      return acc;
    }, {});
    message.offset = object.offset ?? 0;
    return message;
  }
};
function createBaseQueryNodesResponse_NodesEntry() {
  return { key: "", value: void 0 };
}
var QueryNodesResponse_NodesEntry = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== void 0) {
      TreeNode.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryNodesResponse_NodesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.value = TreeNode.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet3(object.key) ? globalThis.String(object.key) : "",
      value: isSet3(object.value) ? TreeNode.fromJSON(object.value) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== void 0) {
      obj.value = TreeNode.toJSON(message.value);
    }
    return obj;
  },
  create(base) {
    return QueryNodesResponse_NodesEntry.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryNodesResponse_NodesEntry();
    message.key = object.key ?? "";
    message.value = object.value !== void 0 && object.value !== null ? TreeNode.fromPartial(object.value) : void 0;
    return message;
  }
};
function createBaseCancelTransferRequest() {
  return { transferId: "", senderIdentityPublicKey: new Uint8Array(0) };
}
var CancelTransferRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.transferId !== "") {
      writer.uint32(10).string(message.transferId);
    }
    if (message.senderIdentityPublicKey.length !== 0) {
      writer.uint32(18).bytes(message.senderIdentityPublicKey);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCancelTransferRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.transferId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.senderIdentityPublicKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      transferId: isSet3(object.transferId) ? globalThis.String(object.transferId) : "",
      senderIdentityPublicKey: isSet3(object.senderIdentityPublicKey) ? bytesFromBase642(object.senderIdentityPublicKey) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.transferId !== "") {
      obj.transferId = message.transferId;
    }
    if (message.senderIdentityPublicKey.length !== 0) {
      obj.senderIdentityPublicKey = base64FromBytes2(message.senderIdentityPublicKey);
    }
    return obj;
  },
  create(base) {
    return CancelTransferRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseCancelTransferRequest();
    message.transferId = object.transferId ?? "";
    message.senderIdentityPublicKey = object.senderIdentityPublicKey ?? new Uint8Array(0);
    return message;
  }
};
function createBaseCancelTransferResponse() {
  return { transfer: void 0 };
}
var CancelTransferResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.transfer !== void 0) {
      Transfer.encode(message.transfer, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCancelTransferResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.transfer = Transfer.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { transfer: isSet3(object.transfer) ? Transfer.fromJSON(object.transfer) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.transfer !== void 0) {
      obj.transfer = Transfer.toJSON(message.transfer);
    }
    return obj;
  },
  create(base) {
    return CancelTransferResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseCancelTransferResponse();
    message.transfer = object.transfer !== void 0 && object.transfer !== null ? Transfer.fromPartial(object.transfer) : void 0;
    return message;
  }
};
function createBaseQueryUnusedDepositAddressesRequest() {
  return { identityPublicKey: new Uint8Array(0), network: 0, limit: 0, offset: 0 };
}
var QueryUnusedDepositAddressesRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.identityPublicKey.length !== 0) {
      writer.uint32(10).bytes(message.identityPublicKey);
    }
    if (message.network !== 0) {
      writer.uint32(16).int32(message.network);
    }
    if (message.limit !== 0) {
      writer.uint32(24).int64(message.limit);
    }
    if (message.offset !== 0) {
      writer.uint32(32).int64(message.offset);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryUnusedDepositAddressesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.identityPublicKey = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.network = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.limit = longToNumber2(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }
          message.offset = longToNumber2(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      identityPublicKey: isSet3(object.identityPublicKey) ? bytesFromBase642(object.identityPublicKey) : new Uint8Array(0),
      network: isSet3(object.network) ? networkFromJSON(object.network) : 0,
      limit: isSet3(object.limit) ? globalThis.Number(object.limit) : 0,
      offset: isSet3(object.offset) ? globalThis.Number(object.offset) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.identityPublicKey.length !== 0) {
      obj.identityPublicKey = base64FromBytes2(message.identityPublicKey);
    }
    if (message.network !== 0) {
      obj.network = networkToJSON(message.network);
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    return obj;
  },
  create(base) {
    return QueryUnusedDepositAddressesRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryUnusedDepositAddressesRequest();
    message.identityPublicKey = object.identityPublicKey ?? new Uint8Array(0);
    message.network = object.network ?? 0;
    message.limit = object.limit ?? 0;
    message.offset = object.offset ?? 0;
    return message;
  }
};
function createBaseQueryStaticDepositAddressesRequest() {
  return { identityPublicKey: new Uint8Array(0), network: 0, limit: 0, offset: 0, depositAddress: void 0 };
}
var QueryStaticDepositAddressesRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.identityPublicKey.length !== 0) {
      writer.uint32(10).bytes(message.identityPublicKey);
    }
    if (message.network !== 0) {
      writer.uint32(16).int32(message.network);
    }
    if (message.limit !== 0) {
      writer.uint32(32).int64(message.limit);
    }
    if (message.offset !== 0) {
      writer.uint32(40).int64(message.offset);
    }
    if (message.depositAddress !== void 0) {
      writer.uint32(50).string(message.depositAddress);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryStaticDepositAddressesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.identityPublicKey = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.network = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }
          message.limit = longToNumber2(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }
          message.offset = longToNumber2(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.depositAddress = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      identityPublicKey: isSet3(object.identityPublicKey) ? bytesFromBase642(object.identityPublicKey) : new Uint8Array(0),
      network: isSet3(object.network) ? networkFromJSON(object.network) : 0,
      limit: isSet3(object.limit) ? globalThis.Number(object.limit) : 0,
      offset: isSet3(object.offset) ? globalThis.Number(object.offset) : 0,
      depositAddress: isSet3(object.depositAddress) ? globalThis.String(object.depositAddress) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.identityPublicKey.length !== 0) {
      obj.identityPublicKey = base64FromBytes2(message.identityPublicKey);
    }
    if (message.network !== 0) {
      obj.network = networkToJSON(message.network);
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    if (message.depositAddress !== void 0) {
      obj.depositAddress = message.depositAddress;
    }
    return obj;
  },
  create(base) {
    return QueryStaticDepositAddressesRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryStaticDepositAddressesRequest();
    message.identityPublicKey = object.identityPublicKey ?? new Uint8Array(0);
    message.network = object.network ?? 0;
    message.limit = object.limit ?? 0;
    message.offset = object.offset ?? 0;
    message.depositAddress = object.depositAddress ?? void 0;
    return message;
  }
};
function createBaseDepositAddressQueryResult() {
  return {
    depositAddress: "",
    userSigningPublicKey: new Uint8Array(0),
    verifyingPublicKey: new Uint8Array(0),
    leafId: void 0,
    proofOfPossession: void 0
  };
}
var DepositAddressQueryResult = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.depositAddress !== "") {
      writer.uint32(10).string(message.depositAddress);
    }
    if (message.userSigningPublicKey.length !== 0) {
      writer.uint32(18).bytes(message.userSigningPublicKey);
    }
    if (message.verifyingPublicKey.length !== 0) {
      writer.uint32(26).bytes(message.verifyingPublicKey);
    }
    if (message.leafId !== void 0) {
      writer.uint32(34).string(message.leafId);
    }
    if (message.proofOfPossession !== void 0) {
      DepositAddressProof.encode(message.proofOfPossession, writer.uint32(42).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseDepositAddressQueryResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.depositAddress = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.userSigningPublicKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.verifyingPublicKey = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.leafId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.proofOfPossession = DepositAddressProof.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      depositAddress: isSet3(object.depositAddress) ? globalThis.String(object.depositAddress) : "",
      userSigningPublicKey: isSet3(object.userSigningPublicKey) ? bytesFromBase642(object.userSigningPublicKey) : new Uint8Array(0),
      verifyingPublicKey: isSet3(object.verifyingPublicKey) ? bytesFromBase642(object.verifyingPublicKey) : new Uint8Array(0),
      leafId: isSet3(object.leafId) ? globalThis.String(object.leafId) : void 0,
      proofOfPossession: isSet3(object.proofOfPossession) ? DepositAddressProof.fromJSON(object.proofOfPossession) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.depositAddress !== "") {
      obj.depositAddress = message.depositAddress;
    }
    if (message.userSigningPublicKey.length !== 0) {
      obj.userSigningPublicKey = base64FromBytes2(message.userSigningPublicKey);
    }
    if (message.verifyingPublicKey.length !== 0) {
      obj.verifyingPublicKey = base64FromBytes2(message.verifyingPublicKey);
    }
    if (message.leafId !== void 0) {
      obj.leafId = message.leafId;
    }
    if (message.proofOfPossession !== void 0) {
      obj.proofOfPossession = DepositAddressProof.toJSON(message.proofOfPossession);
    }
    return obj;
  },
  create(base) {
    return DepositAddressQueryResult.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseDepositAddressQueryResult();
    message.depositAddress = object.depositAddress ?? "";
    message.userSigningPublicKey = object.userSigningPublicKey ?? new Uint8Array(0);
    message.verifyingPublicKey = object.verifyingPublicKey ?? new Uint8Array(0);
    message.leafId = object.leafId ?? void 0;
    message.proofOfPossession = object.proofOfPossession !== void 0 && object.proofOfPossession !== null ? DepositAddressProof.fromPartial(object.proofOfPossession) : void 0;
    return message;
  }
};
function createBaseQueryUnusedDepositAddressesResponse() {
  return { depositAddresses: [], offset: 0 };
}
var QueryUnusedDepositAddressesResponse = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.depositAddresses) {
      DepositAddressQueryResult.encode(v, writer.uint32(10).fork()).join();
    }
    if (message.offset !== 0) {
      writer.uint32(16).int64(message.offset);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryUnusedDepositAddressesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.depositAddresses.push(DepositAddressQueryResult.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.offset = longToNumber2(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      depositAddresses: globalThis.Array.isArray(object?.depositAddresses) ? object.depositAddresses.map((e) => DepositAddressQueryResult.fromJSON(e)) : [],
      offset: isSet3(object.offset) ? globalThis.Number(object.offset) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.depositAddresses?.length) {
      obj.depositAddresses = message.depositAddresses.map((e) => DepositAddressQueryResult.toJSON(e));
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    return obj;
  },
  create(base) {
    return QueryUnusedDepositAddressesResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryUnusedDepositAddressesResponse();
    message.depositAddresses = object.depositAddresses?.map((e) => DepositAddressQueryResult.fromPartial(e)) || [];
    message.offset = object.offset ?? 0;
    return message;
  }
};
function createBaseQueryStaticDepositAddressesResponse() {
  return { depositAddresses: [] };
}
var QueryStaticDepositAddressesResponse = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.depositAddresses) {
      DepositAddressQueryResult.encode(v, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryStaticDepositAddressesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.depositAddresses.push(DepositAddressQueryResult.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      depositAddresses: globalThis.Array.isArray(object?.depositAddresses) ? object.depositAddresses.map((e) => DepositAddressQueryResult.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.depositAddresses?.length) {
      obj.depositAddresses = message.depositAddresses.map((e) => DepositAddressQueryResult.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return QueryStaticDepositAddressesResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryStaticDepositAddressesResponse();
    message.depositAddresses = object.depositAddresses?.map((e) => DepositAddressQueryResult.fromPartial(e)) || [];
    return message;
  }
};
function createBaseQueryBalanceRequest() {
  return { identityPublicKey: new Uint8Array(0), network: 0 };
}
var QueryBalanceRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.identityPublicKey.length !== 0) {
      writer.uint32(10).bytes(message.identityPublicKey);
    }
    if (message.network !== 0) {
      writer.uint32(16).int32(message.network);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryBalanceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.identityPublicKey = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.network = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      identityPublicKey: isSet3(object.identityPublicKey) ? bytesFromBase642(object.identityPublicKey) : new Uint8Array(0),
      network: isSet3(object.network) ? networkFromJSON(object.network) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.identityPublicKey.length !== 0) {
      obj.identityPublicKey = base64FromBytes2(message.identityPublicKey);
    }
    if (message.network !== 0) {
      obj.network = networkToJSON(message.network);
    }
    return obj;
  },
  create(base) {
    return QueryBalanceRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryBalanceRequest();
    message.identityPublicKey = object.identityPublicKey ?? new Uint8Array(0);
    message.network = object.network ?? 0;
    return message;
  }
};
function createBaseQueryBalanceResponse() {
  return { balance: 0, nodeBalances: {} };
}
var QueryBalanceResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.balance !== 0) {
      writer.uint32(8).uint64(message.balance);
    }
    Object.entries(message.nodeBalances).forEach(([key, value]) => {
      QueryBalanceResponse_NodeBalancesEntry.encode({ key, value }, writer.uint32(18).fork()).join();
    });
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryBalanceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.balance = longToNumber2(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          const entry2 = QueryBalanceResponse_NodeBalancesEntry.decode(reader, reader.uint32());
          if (entry2.value !== void 0) {
            message.nodeBalances[entry2.key] = entry2.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      balance: isSet3(object.balance) ? globalThis.Number(object.balance) : 0,
      nodeBalances: isObject2(object.nodeBalances) ? Object.entries(object.nodeBalances).reduce((acc, [key, value]) => {
        acc[key] = Number(value);
        return acc;
      }, {}) : {}
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.balance !== 0) {
      obj.balance = Math.round(message.balance);
    }
    if (message.nodeBalances) {
      const entries = Object.entries(message.nodeBalances);
      if (entries.length > 0) {
        obj.nodeBalances = {};
        entries.forEach(([k, v]) => {
          obj.nodeBalances[k] = Math.round(v);
        });
      }
    }
    return obj;
  },
  create(base) {
    return QueryBalanceResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryBalanceResponse();
    message.balance = object.balance ?? 0;
    message.nodeBalances = Object.entries(object.nodeBalances ?? {}).reduce(
      (acc, [key, value]) => {
        if (value !== void 0) {
          acc[key] = globalThis.Number(value);
        }
        return acc;
      },
      {}
    );
    return message;
  }
};
function createBaseQueryBalanceResponse_NodeBalancesEntry() {
  return { key: "", value: 0 };
}
var QueryBalanceResponse_NodeBalancesEntry = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== 0) {
      writer.uint32(16).uint64(message.value);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryBalanceResponse_NodeBalancesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.value = longToNumber2(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet3(object.key) ? globalThis.String(object.key) : "",
      value: isSet3(object.value) ? globalThis.Number(object.value) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== 0) {
      obj.value = Math.round(message.value);
    }
    return obj;
  },
  create(base) {
    return QueryBalanceResponse_NodeBalancesEntry.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryBalanceResponse_NodeBalancesEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? 0;
    return message;
  }
};
function createBaseSparkAddress() {
  return { identityPublicKey: new Uint8Array(0), sparkInvoiceFields: void 0, signature: void 0 };
}
var SparkAddress = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.identityPublicKey.length !== 0) {
      writer.uint32(10).bytes(message.identityPublicKey);
    }
    if (message.sparkInvoiceFields !== void 0) {
      SparkInvoiceFields.encode(message.sparkInvoiceFields, writer.uint32(18).fork()).join();
    }
    if (message.signature !== void 0) {
      writer.uint32(26).bytes(message.signature);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSparkAddress();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.identityPublicKey = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.sparkInvoiceFields = SparkInvoiceFields.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.signature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      identityPublicKey: isSet3(object.identityPublicKey) ? bytesFromBase642(object.identityPublicKey) : new Uint8Array(0),
      sparkInvoiceFields: isSet3(object.sparkInvoiceFields) ? SparkInvoiceFields.fromJSON(object.sparkInvoiceFields) : void 0,
      signature: isSet3(object.signature) ? bytesFromBase642(object.signature) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.identityPublicKey.length !== 0) {
      obj.identityPublicKey = base64FromBytes2(message.identityPublicKey);
    }
    if (message.sparkInvoiceFields !== void 0) {
      obj.sparkInvoiceFields = SparkInvoiceFields.toJSON(message.sparkInvoiceFields);
    }
    if (message.signature !== void 0) {
      obj.signature = base64FromBytes2(message.signature);
    }
    return obj;
  },
  create(base) {
    return SparkAddress.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSparkAddress();
    message.identityPublicKey = object.identityPublicKey ?? new Uint8Array(0);
    message.sparkInvoiceFields = object.sparkInvoiceFields !== void 0 && object.sparkInvoiceFields !== null ? SparkInvoiceFields.fromPartial(object.sparkInvoiceFields) : void 0;
    message.signature = object.signature ?? void 0;
    return message;
  }
};
function createBaseSparkInvoiceFields() {
  return {
    version: 0,
    id: new Uint8Array(0),
    paymentType: void 0,
    memo: void 0,
    senderPublicKey: void 0,
    expiryTime: void 0
  };
}
var SparkInvoiceFields = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.version !== 0) {
      writer.uint32(8).uint32(message.version);
    }
    if (message.id.length !== 0) {
      writer.uint32(18).bytes(message.id);
    }
    switch (message.paymentType?.$case) {
      case "tokensPayment":
        TokensPayment.encode(message.paymentType.tokensPayment, writer.uint32(26).fork()).join();
        break;
      case "satsPayment":
        SatsPayment.encode(message.paymentType.satsPayment, writer.uint32(34).fork()).join();
        break;
    }
    if (message.memo !== void 0) {
      writer.uint32(42).string(message.memo);
    }
    if (message.senderPublicKey !== void 0) {
      writer.uint32(50).bytes(message.senderPublicKey);
    }
    if (message.expiryTime !== void 0) {
      Timestamp.encode(toTimestamp(message.expiryTime), writer.uint32(58).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSparkInvoiceFields();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.version = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.id = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.paymentType = {
            $case: "tokensPayment",
            tokensPayment: TokensPayment.decode(reader, reader.uint32())
          };
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.paymentType = { $case: "satsPayment", satsPayment: SatsPayment.decode(reader, reader.uint32()) };
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.memo = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.senderPublicKey = reader.bytes();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.expiryTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      version: isSet3(object.version) ? globalThis.Number(object.version) : 0,
      id: isSet3(object.id) ? bytesFromBase642(object.id) : new Uint8Array(0),
      paymentType: isSet3(object.tokensPayment) ? { $case: "tokensPayment", tokensPayment: TokensPayment.fromJSON(object.tokensPayment) } : isSet3(object.satsPayment) ? { $case: "satsPayment", satsPayment: SatsPayment.fromJSON(object.satsPayment) } : void 0,
      memo: isSet3(object.memo) ? globalThis.String(object.memo) : void 0,
      senderPublicKey: isSet3(object.senderPublicKey) ? bytesFromBase642(object.senderPublicKey) : void 0,
      expiryTime: isSet3(object.expiryTime) ? fromJsonTimestamp(object.expiryTime) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    if (message.id.length !== 0) {
      obj.id = base64FromBytes2(message.id);
    }
    if (message.paymentType?.$case === "tokensPayment") {
      obj.tokensPayment = TokensPayment.toJSON(message.paymentType.tokensPayment);
    } else if (message.paymentType?.$case === "satsPayment") {
      obj.satsPayment = SatsPayment.toJSON(message.paymentType.satsPayment);
    }
    if (message.memo !== void 0) {
      obj.memo = message.memo;
    }
    if (message.senderPublicKey !== void 0) {
      obj.senderPublicKey = base64FromBytes2(message.senderPublicKey);
    }
    if (message.expiryTime !== void 0) {
      obj.expiryTime = message.expiryTime.toISOString();
    }
    return obj;
  },
  create(base) {
    return SparkInvoiceFields.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSparkInvoiceFields();
    message.version = object.version ?? 0;
    message.id = object.id ?? new Uint8Array(0);
    switch (object.paymentType?.$case) {
      case "tokensPayment": {
        if (object.paymentType?.tokensPayment !== void 0 && object.paymentType?.tokensPayment !== null) {
          message.paymentType = {
            $case: "tokensPayment",
            tokensPayment: TokensPayment.fromPartial(object.paymentType.tokensPayment)
          };
        }
        break;
      }
      case "satsPayment": {
        if (object.paymentType?.satsPayment !== void 0 && object.paymentType?.satsPayment !== null) {
          message.paymentType = {
            $case: "satsPayment",
            satsPayment: SatsPayment.fromPartial(object.paymentType.satsPayment)
          };
        }
        break;
      }
    }
    message.memo = object.memo ?? void 0;
    message.senderPublicKey = object.senderPublicKey ?? void 0;
    message.expiryTime = object.expiryTime ?? void 0;
    return message;
  }
};
function createBaseSatsPayment() {
  return { amount: void 0 };
}
var SatsPayment = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.amount !== void 0) {
      writer.uint32(8).uint64(message.amount);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSatsPayment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.amount = longToNumber2(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { amount: isSet3(object.amount) ? globalThis.Number(object.amount) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.amount !== void 0) {
      obj.amount = Math.round(message.amount);
    }
    return obj;
  },
  create(base) {
    return SatsPayment.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSatsPayment();
    message.amount = object.amount ?? void 0;
    return message;
  }
};
function createBaseTokensPayment() {
  return { tokenIdentifier: void 0, amount: void 0 };
}
var TokensPayment = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.tokenIdentifier !== void 0) {
      writer.uint32(10).bytes(message.tokenIdentifier);
    }
    if (message.amount !== void 0) {
      writer.uint32(18).bytes(message.amount);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTokensPayment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.tokenIdentifier = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.amount = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      tokenIdentifier: isSet3(object.tokenIdentifier) ? bytesFromBase642(object.tokenIdentifier) : void 0,
      amount: isSet3(object.amount) ? bytesFromBase642(object.amount) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.tokenIdentifier !== void 0) {
      obj.tokenIdentifier = base64FromBytes2(message.tokenIdentifier);
    }
    if (message.amount !== void 0) {
      obj.amount = base64FromBytes2(message.amount);
    }
    return obj;
  },
  create(base) {
    return TokensPayment.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseTokensPayment();
    message.tokenIdentifier = object.tokenIdentifier ?? void 0;
    message.amount = object.amount ?? void 0;
    return message;
  }
};
function createBaseInitiateStaticDepositUtxoRefundRequest() {
  return { onChainUtxo: void 0, refundTxSigningJob: void 0, userSignature: new Uint8Array(0) };
}
var InitiateStaticDepositUtxoRefundRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.onChainUtxo !== void 0) {
      UTXO.encode(message.onChainUtxo, writer.uint32(10).fork()).join();
    }
    if (message.refundTxSigningJob !== void 0) {
      SigningJob.encode(message.refundTxSigningJob, writer.uint32(26).fork()).join();
    }
    if (message.userSignature.length !== 0) {
      writer.uint32(34).bytes(message.userSignature);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInitiateStaticDepositUtxoRefundRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.onChainUtxo = UTXO.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.refundTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.userSignature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      onChainUtxo: isSet3(object.onChainUtxo) ? UTXO.fromJSON(object.onChainUtxo) : void 0,
      refundTxSigningJob: isSet3(object.refundTxSigningJob) ? SigningJob.fromJSON(object.refundTxSigningJob) : void 0,
      userSignature: isSet3(object.userSignature) ? bytesFromBase642(object.userSignature) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.onChainUtxo !== void 0) {
      obj.onChainUtxo = UTXO.toJSON(message.onChainUtxo);
    }
    if (message.refundTxSigningJob !== void 0) {
      obj.refundTxSigningJob = SigningJob.toJSON(message.refundTxSigningJob);
    }
    if (message.userSignature.length !== 0) {
      obj.userSignature = base64FromBytes2(message.userSignature);
    }
    return obj;
  },
  create(base) {
    return InitiateStaticDepositUtxoRefundRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseInitiateStaticDepositUtxoRefundRequest();
    message.onChainUtxo = object.onChainUtxo !== void 0 && object.onChainUtxo !== null ? UTXO.fromPartial(object.onChainUtxo) : void 0;
    message.refundTxSigningJob = object.refundTxSigningJob !== void 0 && object.refundTxSigningJob !== null ? SigningJob.fromPartial(object.refundTxSigningJob) : void 0;
    message.userSignature = object.userSignature ?? new Uint8Array(0);
    return message;
  }
};
function createBaseInitiateStaticDepositUtxoRefundResponse() {
  return { refundTxSigningResult: void 0, depositAddress: void 0 };
}
var InitiateStaticDepositUtxoRefundResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.refundTxSigningResult !== void 0) {
      SigningResult2.encode(message.refundTxSigningResult, writer.uint32(10).fork()).join();
    }
    if (message.depositAddress !== void 0) {
      DepositAddressQueryResult.encode(message.depositAddress, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInitiateStaticDepositUtxoRefundResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.refundTxSigningResult = SigningResult2.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.depositAddress = DepositAddressQueryResult.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      refundTxSigningResult: isSet3(object.refundTxSigningResult) ? SigningResult2.fromJSON(object.refundTxSigningResult) : void 0,
      depositAddress: isSet3(object.depositAddress) ? DepositAddressQueryResult.fromJSON(object.depositAddress) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.refundTxSigningResult !== void 0) {
      obj.refundTxSigningResult = SigningResult2.toJSON(message.refundTxSigningResult);
    }
    if (message.depositAddress !== void 0) {
      obj.depositAddress = DepositAddressQueryResult.toJSON(message.depositAddress);
    }
    return obj;
  },
  create(base) {
    return InitiateStaticDepositUtxoRefundResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseInitiateStaticDepositUtxoRefundResponse();
    message.refundTxSigningResult = object.refundTxSigningResult !== void 0 && object.refundTxSigningResult !== null ? SigningResult2.fromPartial(object.refundTxSigningResult) : void 0;
    message.depositAddress = object.depositAddress !== void 0 && object.depositAddress !== null ? DepositAddressQueryResult.fromPartial(object.depositAddress) : void 0;
    return message;
  }
};
function createBaseInitiateUtxoSwapRequest() {
  return {
    onChainUtxo: void 0,
    requestType: 0,
    amount: void 0,
    sspSignature: new Uint8Array(0),
    userSignature: new Uint8Array(0),
    transfer: void 0,
    spendTxSigningJob: void 0
  };
}
var InitiateUtxoSwapRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.onChainUtxo !== void 0) {
      UTXO.encode(message.onChainUtxo, writer.uint32(10).fork()).join();
    }
    if (message.requestType !== 0) {
      writer.uint32(16).int32(message.requestType);
    }
    switch (message.amount?.$case) {
      case "creditAmountSats":
        writer.uint32(24).uint64(message.amount.creditAmountSats);
        break;
      case "maxFeeSats":
        writer.uint32(32).uint64(message.amount.maxFeeSats);
        break;
    }
    if (message.sspSignature.length !== 0) {
      writer.uint32(42).bytes(message.sspSignature);
    }
    if (message.userSignature.length !== 0) {
      writer.uint32(50).bytes(message.userSignature);
    }
    if (message.transfer !== void 0) {
      StartTransferRequest.encode(message.transfer, writer.uint32(58).fork()).join();
    }
    if (message.spendTxSigningJob !== void 0) {
      SigningJob.encode(message.spendTxSigningJob, writer.uint32(66).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInitiateUtxoSwapRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.onChainUtxo = UTXO.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.requestType = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.amount = { $case: "creditAmountSats", creditAmountSats: longToNumber2(reader.uint64()) };
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }
          message.amount = { $case: "maxFeeSats", maxFeeSats: longToNumber2(reader.uint64()) };
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.sspSignature = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.userSignature = reader.bytes();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.transfer = StartTransferRequest.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }
          message.spendTxSigningJob = SigningJob.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      onChainUtxo: isSet3(object.onChainUtxo) ? UTXO.fromJSON(object.onChainUtxo) : void 0,
      requestType: isSet3(object.requestType) ? utxoSwapRequestTypeFromJSON(object.requestType) : 0,
      amount: isSet3(object.creditAmountSats) ? { $case: "creditAmountSats", creditAmountSats: globalThis.Number(object.creditAmountSats) } : isSet3(object.maxFeeSats) ? { $case: "maxFeeSats", maxFeeSats: globalThis.Number(object.maxFeeSats) } : void 0,
      sspSignature: isSet3(object.sspSignature) ? bytesFromBase642(object.sspSignature) : new Uint8Array(0),
      userSignature: isSet3(object.userSignature) ? bytesFromBase642(object.userSignature) : new Uint8Array(0),
      transfer: isSet3(object.transfer) ? StartTransferRequest.fromJSON(object.transfer) : void 0,
      spendTxSigningJob: isSet3(object.spendTxSigningJob) ? SigningJob.fromJSON(object.spendTxSigningJob) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.onChainUtxo !== void 0) {
      obj.onChainUtxo = UTXO.toJSON(message.onChainUtxo);
    }
    if (message.requestType !== 0) {
      obj.requestType = utxoSwapRequestTypeToJSON(message.requestType);
    }
    if (message.amount?.$case === "creditAmountSats") {
      obj.creditAmountSats = Math.round(message.amount.creditAmountSats);
    } else if (message.amount?.$case === "maxFeeSats") {
      obj.maxFeeSats = Math.round(message.amount.maxFeeSats);
    }
    if (message.sspSignature.length !== 0) {
      obj.sspSignature = base64FromBytes2(message.sspSignature);
    }
    if (message.userSignature.length !== 0) {
      obj.userSignature = base64FromBytes2(message.userSignature);
    }
    if (message.transfer !== void 0) {
      obj.transfer = StartTransferRequest.toJSON(message.transfer);
    }
    if (message.spendTxSigningJob !== void 0) {
      obj.spendTxSigningJob = SigningJob.toJSON(message.spendTxSigningJob);
    }
    return obj;
  },
  create(base) {
    return InitiateUtxoSwapRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseInitiateUtxoSwapRequest();
    message.onChainUtxo = object.onChainUtxo !== void 0 && object.onChainUtxo !== null ? UTXO.fromPartial(object.onChainUtxo) : void 0;
    message.requestType = object.requestType ?? 0;
    switch (object.amount?.$case) {
      case "creditAmountSats": {
        if (object.amount?.creditAmountSats !== void 0 && object.amount?.creditAmountSats !== null) {
          message.amount = { $case: "creditAmountSats", creditAmountSats: object.amount.creditAmountSats };
        }
        break;
      }
      case "maxFeeSats": {
        if (object.amount?.maxFeeSats !== void 0 && object.amount?.maxFeeSats !== null) {
          message.amount = { $case: "maxFeeSats", maxFeeSats: object.amount.maxFeeSats };
        }
        break;
      }
    }
    message.sspSignature = object.sspSignature ?? new Uint8Array(0);
    message.userSignature = object.userSignature ?? new Uint8Array(0);
    message.transfer = object.transfer !== void 0 && object.transfer !== null ? StartTransferRequest.fromPartial(object.transfer) : void 0;
    message.spendTxSigningJob = object.spendTxSigningJob !== void 0 && object.spendTxSigningJob !== null ? SigningJob.fromPartial(object.spendTxSigningJob) : void 0;
    return message;
  }
};
function createBaseInitiateUtxoSwapResponse() {
  return { spendTxSigningResult: void 0, transfer: void 0, depositAddress: void 0 };
}
var InitiateUtxoSwapResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.spendTxSigningResult !== void 0) {
      SigningResult2.encode(message.spendTxSigningResult, writer.uint32(10).fork()).join();
    }
    if (message.transfer !== void 0) {
      Transfer.encode(message.transfer, writer.uint32(18).fork()).join();
    }
    if (message.depositAddress !== void 0) {
      DepositAddressQueryResult.encode(message.depositAddress, writer.uint32(26).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInitiateUtxoSwapResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.spendTxSigningResult = SigningResult2.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.transfer = Transfer.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.depositAddress = DepositAddressQueryResult.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      spendTxSigningResult: isSet3(object.spendTxSigningResult) ? SigningResult2.fromJSON(object.spendTxSigningResult) : void 0,
      transfer: isSet3(object.transfer) ? Transfer.fromJSON(object.transfer) : void 0,
      depositAddress: isSet3(object.depositAddress) ? DepositAddressQueryResult.fromJSON(object.depositAddress) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.spendTxSigningResult !== void 0) {
      obj.spendTxSigningResult = SigningResult2.toJSON(message.spendTxSigningResult);
    }
    if (message.transfer !== void 0) {
      obj.transfer = Transfer.toJSON(message.transfer);
    }
    if (message.depositAddress !== void 0) {
      obj.depositAddress = DepositAddressQueryResult.toJSON(message.depositAddress);
    }
    return obj;
  },
  create(base) {
    return InitiateUtxoSwapResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseInitiateUtxoSwapResponse();
    message.spendTxSigningResult = object.spendTxSigningResult !== void 0 && object.spendTxSigningResult !== null ? SigningResult2.fromPartial(object.spendTxSigningResult) : void 0;
    message.transfer = object.transfer !== void 0 && object.transfer !== null ? Transfer.fromPartial(object.transfer) : void 0;
    message.depositAddress = object.depositAddress !== void 0 && object.depositAddress !== null ? DepositAddressQueryResult.fromPartial(object.depositAddress) : void 0;
    return message;
  }
};
function createBaseExitingTree() {
  return { treeId: "", userSigningCommitment: void 0, vin: 0 };
}
var ExitingTree = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.treeId !== "") {
      writer.uint32(10).string(message.treeId);
    }
    if (message.userSigningCommitment !== void 0) {
      SigningCommitment.encode(message.userSigningCommitment, writer.uint32(18).fork()).join();
    }
    if (message.vin !== 0) {
      writer.uint32(24).uint32(message.vin);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseExitingTree();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.treeId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.userSigningCommitment = SigningCommitment.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.vin = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      treeId: isSet3(object.treeId) ? globalThis.String(object.treeId) : "",
      userSigningCommitment: isSet3(object.userSigningCommitment) ? SigningCommitment.fromJSON(object.userSigningCommitment) : void 0,
      vin: isSet3(object.vin) ? globalThis.Number(object.vin) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.treeId !== "") {
      obj.treeId = message.treeId;
    }
    if (message.userSigningCommitment !== void 0) {
      obj.userSigningCommitment = SigningCommitment.toJSON(message.userSigningCommitment);
    }
    if (message.vin !== 0) {
      obj.vin = Math.round(message.vin);
    }
    return obj;
  },
  create(base) {
    return ExitingTree.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseExitingTree();
    message.treeId = object.treeId ?? "";
    message.userSigningCommitment = object.userSigningCommitment !== void 0 && object.userSigningCommitment !== null ? SigningCommitment.fromPartial(object.userSigningCommitment) : void 0;
    message.vin = object.vin ?? 0;
    return message;
  }
};
function createBaseExitSingleNodeTreeSigningResult() {
  return { treeId: "", signingResult: void 0, verifyingKey: new Uint8Array(0) };
}
var ExitSingleNodeTreeSigningResult = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.treeId !== "") {
      writer.uint32(10).string(message.treeId);
    }
    if (message.signingResult !== void 0) {
      SigningResult2.encode(message.signingResult, writer.uint32(18).fork()).join();
    }
    if (message.verifyingKey.length !== 0) {
      writer.uint32(26).bytes(message.verifyingKey);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseExitSingleNodeTreeSigningResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.treeId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.signingResult = SigningResult2.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.verifyingKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      treeId: isSet3(object.treeId) ? globalThis.String(object.treeId) : "",
      signingResult: isSet3(object.signingResult) ? SigningResult2.fromJSON(object.signingResult) : void 0,
      verifyingKey: isSet3(object.verifyingKey) ? bytesFromBase642(object.verifyingKey) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.treeId !== "") {
      obj.treeId = message.treeId;
    }
    if (message.signingResult !== void 0) {
      obj.signingResult = SigningResult2.toJSON(message.signingResult);
    }
    if (message.verifyingKey.length !== 0) {
      obj.verifyingKey = base64FromBytes2(message.verifyingKey);
    }
    return obj;
  },
  create(base) {
    return ExitSingleNodeTreeSigningResult.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseExitSingleNodeTreeSigningResult();
    message.treeId = object.treeId ?? "";
    message.signingResult = object.signingResult !== void 0 && object.signingResult !== null ? SigningResult2.fromPartial(object.signingResult) : void 0;
    message.verifyingKey = object.verifyingKey ?? new Uint8Array(0);
    return message;
  }
};
function createBaseBitcoinTransactionOutput() {
  return { value: 0, pkScript: new Uint8Array(0) };
}
var BitcoinTransactionOutput = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.value !== 0) {
      writer.uint32(8).int64(message.value);
    }
    if (message.pkScript.length !== 0) {
      writer.uint32(18).bytes(message.pkScript);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseBitcoinTransactionOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.value = longToNumber2(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.pkScript = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      value: isSet3(object.value) ? globalThis.Number(object.value) : 0,
      pkScript: isSet3(object.pkScript) ? bytesFromBase642(object.pkScript) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.value !== 0) {
      obj.value = Math.round(message.value);
    }
    if (message.pkScript.length !== 0) {
      obj.pkScript = base64FromBytes2(message.pkScript);
    }
    return obj;
  },
  create(base) {
    return BitcoinTransactionOutput.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseBitcoinTransactionOutput();
    message.value = object.value ?? 0;
    message.pkScript = object.pkScript ?? new Uint8Array(0);
    return message;
  }
};
function createBaseExitSingleNodeTreesRequest() {
  return { ownerIdentityPublicKey: new Uint8Array(0), exitingTrees: [], rawTx: new Uint8Array(0), previousOutputs: [] };
}
var ExitSingleNodeTreesRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.ownerIdentityPublicKey.length !== 0) {
      writer.uint32(10).bytes(message.ownerIdentityPublicKey);
    }
    for (const v of message.exitingTrees) {
      ExitingTree.encode(v, writer.uint32(18).fork()).join();
    }
    if (message.rawTx.length !== 0) {
      writer.uint32(26).bytes(message.rawTx);
    }
    for (const v of message.previousOutputs) {
      BitcoinTransactionOutput.encode(v, writer.uint32(34).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseExitSingleNodeTreesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.ownerIdentityPublicKey = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.exitingTrees.push(ExitingTree.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.rawTx = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.previousOutputs.push(BitcoinTransactionOutput.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      ownerIdentityPublicKey: isSet3(object.ownerIdentityPublicKey) ? bytesFromBase642(object.ownerIdentityPublicKey) : new Uint8Array(0),
      exitingTrees: globalThis.Array.isArray(object?.exitingTrees) ? object.exitingTrees.map((e) => ExitingTree.fromJSON(e)) : [],
      rawTx: isSet3(object.rawTx) ? bytesFromBase642(object.rawTx) : new Uint8Array(0),
      previousOutputs: globalThis.Array.isArray(object?.previousOutputs) ? object.previousOutputs.map((e) => BitcoinTransactionOutput.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.ownerIdentityPublicKey.length !== 0) {
      obj.ownerIdentityPublicKey = base64FromBytes2(message.ownerIdentityPublicKey);
    }
    if (message.exitingTrees?.length) {
      obj.exitingTrees = message.exitingTrees.map((e) => ExitingTree.toJSON(e));
    }
    if (message.rawTx.length !== 0) {
      obj.rawTx = base64FromBytes2(message.rawTx);
    }
    if (message.previousOutputs?.length) {
      obj.previousOutputs = message.previousOutputs.map((e) => BitcoinTransactionOutput.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return ExitSingleNodeTreesRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseExitSingleNodeTreesRequest();
    message.ownerIdentityPublicKey = object.ownerIdentityPublicKey ?? new Uint8Array(0);
    message.exitingTrees = object.exitingTrees?.map((e) => ExitingTree.fromPartial(e)) || [];
    message.rawTx = object.rawTx ?? new Uint8Array(0);
    message.previousOutputs = object.previousOutputs?.map((e) => BitcoinTransactionOutput.fromPartial(e)) || [];
    return message;
  }
};
function createBaseExitSingleNodeTreesResponse() {
  return { signingResults: [] };
}
var ExitSingleNodeTreesResponse = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.signingResults) {
      ExitSingleNodeTreeSigningResult.encode(v, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseExitSingleNodeTreesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.signingResults.push(ExitSingleNodeTreeSigningResult.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      signingResults: globalThis.Array.isArray(object?.signingResults) ? object.signingResults.map((e) => ExitSingleNodeTreeSigningResult.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.signingResults?.length) {
      obj.signingResults = message.signingResults.map((e) => ExitSingleNodeTreeSigningResult.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return ExitSingleNodeTreesResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseExitSingleNodeTreesResponse();
    message.signingResults = object.signingResults?.map((e) => ExitSingleNodeTreeSigningResult.fromPartial(e)) || [];
    return message;
  }
};
function createBaseQueryNodesDistributionRequest() {
  return { ownerIdentityPublicKey: new Uint8Array(0) };
}
var QueryNodesDistributionRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.ownerIdentityPublicKey.length !== 0) {
      writer.uint32(10).bytes(message.ownerIdentityPublicKey);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryNodesDistributionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.ownerIdentityPublicKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      ownerIdentityPublicKey: isSet3(object.ownerIdentityPublicKey) ? bytesFromBase642(object.ownerIdentityPublicKey) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.ownerIdentityPublicKey.length !== 0) {
      obj.ownerIdentityPublicKey = base64FromBytes2(message.ownerIdentityPublicKey);
    }
    return obj;
  },
  create(base) {
    return QueryNodesDistributionRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryNodesDistributionRequest();
    message.ownerIdentityPublicKey = object.ownerIdentityPublicKey ?? new Uint8Array(0);
    return message;
  }
};
function createBaseQueryNodesDistributionResponse() {
  return { nodeDistribution: {} };
}
var QueryNodesDistributionResponse = {
  encode(message, writer = new BinaryWriter4()) {
    Object.entries(message.nodeDistribution).forEach(([key, value]) => {
      QueryNodesDistributionResponse_NodeDistributionEntry.encode({ key, value }, writer.uint32(10).fork()).join();
    });
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryNodesDistributionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          const entry1 = QueryNodesDistributionResponse_NodeDistributionEntry.decode(reader, reader.uint32());
          if (entry1.value !== void 0) {
            message.nodeDistribution[entry1.key] = entry1.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      nodeDistribution: isObject2(object.nodeDistribution) ? Object.entries(object.nodeDistribution).reduce((acc, [key, value]) => {
        acc[globalThis.Number(key)] = Number(value);
        return acc;
      }, {}) : {}
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.nodeDistribution) {
      const entries = Object.entries(message.nodeDistribution);
      if (entries.length > 0) {
        obj.nodeDistribution = {};
        entries.forEach(([k, v]) => {
          obj.nodeDistribution[k] = Math.round(v);
        });
      }
    }
    return obj;
  },
  create(base) {
    return QueryNodesDistributionResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryNodesDistributionResponse();
    message.nodeDistribution = Object.entries(object.nodeDistribution ?? {}).reduce(
      (acc, [key, value]) => {
        if (value !== void 0) {
          acc[globalThis.Number(key)] = globalThis.Number(value);
        }
        return acc;
      },
      {}
    );
    return message;
  }
};
function createBaseQueryNodesDistributionResponse_NodeDistributionEntry() {
  return { key: 0, value: 0 };
}
var QueryNodesDistributionResponse_NodeDistributionEntry = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.key !== 0) {
      writer.uint32(8).uint64(message.key);
    }
    if (message.value !== 0) {
      writer.uint32(16).uint64(message.value);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryNodesDistributionResponse_NodeDistributionEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.key = longToNumber2(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.value = longToNumber2(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet3(object.key) ? globalThis.Number(object.key) : 0,
      value: isSet3(object.value) ? globalThis.Number(object.value) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.key !== 0) {
      obj.key = Math.round(message.key);
    }
    if (message.value !== 0) {
      obj.value = Math.round(message.value);
    }
    return obj;
  },
  create(base) {
    return QueryNodesDistributionResponse_NodeDistributionEntry.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryNodesDistributionResponse_NodeDistributionEntry();
    message.key = object.key ?? 0;
    message.value = object.value ?? 0;
    return message;
  }
};
function createBaseQueryNodesByValueRequest() {
  return { ownerIdentityPublicKey: new Uint8Array(0), value: 0, offset: 0, limit: 0 };
}
var QueryNodesByValueRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.ownerIdentityPublicKey.length !== 0) {
      writer.uint32(10).bytes(message.ownerIdentityPublicKey);
    }
    if (message.value !== 0) {
      writer.uint32(16).int64(message.value);
    }
    if (message.offset !== 0) {
      writer.uint32(24).int64(message.offset);
    }
    if (message.limit !== 0) {
      writer.uint32(32).int64(message.limit);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryNodesByValueRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.ownerIdentityPublicKey = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.value = longToNumber2(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.offset = longToNumber2(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }
          message.limit = longToNumber2(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      ownerIdentityPublicKey: isSet3(object.ownerIdentityPublicKey) ? bytesFromBase642(object.ownerIdentityPublicKey) : new Uint8Array(0),
      value: isSet3(object.value) ? globalThis.Number(object.value) : 0,
      offset: isSet3(object.offset) ? globalThis.Number(object.offset) : 0,
      limit: isSet3(object.limit) ? globalThis.Number(object.limit) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.ownerIdentityPublicKey.length !== 0) {
      obj.ownerIdentityPublicKey = base64FromBytes2(message.ownerIdentityPublicKey);
    }
    if (message.value !== 0) {
      obj.value = Math.round(message.value);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    return obj;
  },
  create(base) {
    return QueryNodesByValueRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryNodesByValueRequest();
    message.ownerIdentityPublicKey = object.ownerIdentityPublicKey ?? new Uint8Array(0);
    message.value = object.value ?? 0;
    message.offset = object.offset ?? 0;
    message.limit = object.limit ?? 0;
    return message;
  }
};
function createBaseQueryNodesByValueResponse() {
  return { nodes: {}, offset: 0 };
}
var QueryNodesByValueResponse = {
  encode(message, writer = new BinaryWriter4()) {
    Object.entries(message.nodes).forEach(([key, value]) => {
      QueryNodesByValueResponse_NodesEntry.encode({ key, value }, writer.uint32(10).fork()).join();
    });
    if (message.offset !== 0) {
      writer.uint32(16).int64(message.offset);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryNodesByValueResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          const entry1 = QueryNodesByValueResponse_NodesEntry.decode(reader, reader.uint32());
          if (entry1.value !== void 0) {
            message.nodes[entry1.key] = entry1.value;
          }
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.offset = longToNumber2(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      nodes: isObject2(object.nodes) ? Object.entries(object.nodes).reduce((acc, [key, value]) => {
        acc[key] = TreeNode.fromJSON(value);
        return acc;
      }, {}) : {},
      offset: isSet3(object.offset) ? globalThis.Number(object.offset) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.nodes) {
      const entries = Object.entries(message.nodes);
      if (entries.length > 0) {
        obj.nodes = {};
        entries.forEach(([k, v]) => {
          obj.nodes[k] = TreeNode.toJSON(v);
        });
      }
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    return obj;
  },
  create(base) {
    return QueryNodesByValueResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryNodesByValueResponse();
    message.nodes = Object.entries(object.nodes ?? {}).reduce((acc, [key, value]) => {
      if (value !== void 0) {
        acc[key] = TreeNode.fromPartial(value);
      }
      return acc;
    }, {});
    message.offset = object.offset ?? 0;
    return message;
  }
};
function createBaseQueryNodesByValueResponse_NodesEntry() {
  return { key: "", value: void 0 };
}
var QueryNodesByValueResponse_NodesEntry = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== void 0) {
      TreeNode.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryNodesByValueResponse_NodesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.value = TreeNode.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet3(object.key) ? globalThis.String(object.key) : "",
      value: isSet3(object.value) ? TreeNode.fromJSON(object.value) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== void 0) {
      obj.value = TreeNode.toJSON(message.value);
    }
    return obj;
  },
  create(base) {
    return QueryNodesByValueResponse_NodesEntry.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryNodesByValueResponse_NodesEntry();
    message.key = object.key ?? "";
    message.value = object.value !== void 0 && object.value !== null ? TreeNode.fromPartial(object.value) : void 0;
    return message;
  }
};
function createBaseGetUtxosForAddressRequest() {
  return { address: "", offset: 0, limit: 0, network: 0, excludeClaimed: false };
}
var GetUtxosForAddressRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.offset !== 0) {
      writer.uint32(16).uint64(message.offset);
    }
    if (message.limit !== 0) {
      writer.uint32(24).uint64(message.limit);
    }
    if (message.network !== 0) {
      writer.uint32(32).int32(message.network);
    }
    if (message.excludeClaimed !== false) {
      writer.uint32(40).bool(message.excludeClaimed);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetUtxosForAddressRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.address = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.offset = longToNumber2(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.limit = longToNumber2(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }
          message.network = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }
          message.excludeClaimed = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      address: isSet3(object.address) ? globalThis.String(object.address) : "",
      offset: isSet3(object.offset) ? globalThis.Number(object.offset) : 0,
      limit: isSet3(object.limit) ? globalThis.Number(object.limit) : 0,
      network: isSet3(object.network) ? networkFromJSON(object.network) : 0,
      excludeClaimed: isSet3(object.excludeClaimed) ? globalThis.Boolean(object.excludeClaimed) : false
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.network !== 0) {
      obj.network = networkToJSON(message.network);
    }
    if (message.excludeClaimed !== false) {
      obj.excludeClaimed = message.excludeClaimed;
    }
    return obj;
  },
  create(base) {
    return GetUtxosForAddressRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetUtxosForAddressRequest();
    message.address = object.address ?? "";
    message.offset = object.offset ?? 0;
    message.limit = object.limit ?? 0;
    message.network = object.network ?? 0;
    message.excludeClaimed = object.excludeClaimed ?? false;
    return message;
  }
};
function createBaseGetUtxosForAddressResponse() {
  return { utxos: [], offset: 0 };
}
var GetUtxosForAddressResponse = {
  encode(message, writer = new BinaryWriter4()) {
    for (const v of message.utxos) {
      UTXO.encode(v, writer.uint32(10).fork()).join();
    }
    if (message.offset !== 0) {
      writer.uint32(16).uint64(message.offset);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetUtxosForAddressResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.utxos.push(UTXO.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.offset = longToNumber2(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      utxos: globalThis.Array.isArray(object?.utxos) ? object.utxos.map((e) => UTXO.fromJSON(e)) : [],
      offset: isSet3(object.offset) ? globalThis.Number(object.offset) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.utxos?.length) {
      obj.utxos = message.utxos.map((e) => UTXO.toJSON(e));
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    return obj;
  },
  create(base) {
    return GetUtxosForAddressResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetUtxosForAddressResponse();
    message.utxos = object.utxos?.map((e) => UTXO.fromPartial(e)) || [];
    message.offset = object.offset ?? 0;
    return message;
  }
};
function createBaseQuerySparkInvoicesRequest() {
  return { limit: 0, offset: 0, invoice: [] };
}
var QuerySparkInvoicesRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.limit !== 0) {
      writer.uint32(8).int64(message.limit);
    }
    if (message.offset !== 0) {
      writer.uint32(16).int64(message.offset);
    }
    for (const v of message.invoice) {
      writer.uint32(26).string(v);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQuerySparkInvoicesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.limit = longToNumber2(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.offset = longToNumber2(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.invoice.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      limit: isSet3(object.limit) ? globalThis.Number(object.limit) : 0,
      offset: isSet3(object.offset) ? globalThis.Number(object.offset) : 0,
      invoice: globalThis.Array.isArray(object?.invoice) ? object.invoice.map((e) => globalThis.String(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    if (message.invoice?.length) {
      obj.invoice = message.invoice;
    }
    return obj;
  },
  create(base) {
    return QuerySparkInvoicesRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQuerySparkInvoicesRequest();
    message.limit = object.limit ?? 0;
    message.offset = object.offset ?? 0;
    message.invoice = object.invoice?.map((e) => e) || [];
    return message;
  }
};
function createBaseQuerySparkInvoicesResponse() {
  return { offset: 0, invoiceStatuses: [] };
}
var QuerySparkInvoicesResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.offset !== 0) {
      writer.uint32(8).int64(message.offset);
    }
    for (const v of message.invoiceStatuses) {
      InvoiceResponse.encode(v, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQuerySparkInvoicesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.offset = longToNumber2(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.invoiceStatuses.push(InvoiceResponse.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      offset: isSet3(object.offset) ? globalThis.Number(object.offset) : 0,
      invoiceStatuses: globalThis.Array.isArray(object?.invoiceStatuses) ? object.invoiceStatuses.map((e) => InvoiceResponse.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    if (message.invoiceStatuses?.length) {
      obj.invoiceStatuses = message.invoiceStatuses.map((e) => InvoiceResponse.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return QuerySparkInvoicesResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQuerySparkInvoicesResponse();
    message.offset = object.offset ?? 0;
    message.invoiceStatuses = object.invoiceStatuses?.map((e) => InvoiceResponse.fromPartial(e)) || [];
    return message;
  }
};
function createBaseInvoiceResponse() {
  return { invoice: "", status: 0, transferType: void 0 };
}
var InvoiceResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.invoice !== "") {
      writer.uint32(10).string(message.invoice);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    switch (message.transferType?.$case) {
      case "satsTransfer":
        SatsTransfer.encode(message.transferType.satsTransfer, writer.uint32(26).fork()).join();
        break;
      case "tokenTransfer":
        TokenTransfer.encode(message.transferType.tokenTransfer, writer.uint32(34).fork()).join();
        break;
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInvoiceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.invoice = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.status = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.transferType = { $case: "satsTransfer", satsTransfer: SatsTransfer.decode(reader, reader.uint32()) };
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.transferType = {
            $case: "tokenTransfer",
            tokenTransfer: TokenTransfer.decode(reader, reader.uint32())
          };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      invoice: isSet3(object.invoice) ? globalThis.String(object.invoice) : "",
      status: isSet3(object.status) ? invoiceStatusFromJSON(object.status) : 0,
      transferType: isSet3(object.satsTransfer) ? { $case: "satsTransfer", satsTransfer: SatsTransfer.fromJSON(object.satsTransfer) } : isSet3(object.tokenTransfer) ? { $case: "tokenTransfer", tokenTransfer: TokenTransfer.fromJSON(object.tokenTransfer) } : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.invoice !== "") {
      obj.invoice = message.invoice;
    }
    if (message.status !== 0) {
      obj.status = invoiceStatusToJSON(message.status);
    }
    if (message.transferType?.$case === "satsTransfer") {
      obj.satsTransfer = SatsTransfer.toJSON(message.transferType.satsTransfer);
    } else if (message.transferType?.$case === "tokenTransfer") {
      obj.tokenTransfer = TokenTransfer.toJSON(message.transferType.tokenTransfer);
    }
    return obj;
  },
  create(base) {
    return InvoiceResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseInvoiceResponse();
    message.invoice = object.invoice ?? "";
    message.status = object.status ?? 0;
    switch (object.transferType?.$case) {
      case "satsTransfer": {
        if (object.transferType?.satsTransfer !== void 0 && object.transferType?.satsTransfer !== null) {
          message.transferType = {
            $case: "satsTransfer",
            satsTransfer: SatsTransfer.fromPartial(object.transferType.satsTransfer)
          };
        }
        break;
      }
      case "tokenTransfer": {
        if (object.transferType?.tokenTransfer !== void 0 && object.transferType?.tokenTransfer !== null) {
          message.transferType = {
            $case: "tokenTransfer",
            tokenTransfer: TokenTransfer.fromPartial(object.transferType.tokenTransfer)
          };
        }
        break;
      }
    }
    return message;
  }
};
function createBaseSatsTransfer() {
  return { transferId: new Uint8Array(0) };
}
var SatsTransfer = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.transferId.length !== 0) {
      writer.uint32(10).bytes(message.transferId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSatsTransfer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.transferId = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { transferId: isSet3(object.transferId) ? bytesFromBase642(object.transferId) : new Uint8Array(0) };
  },
  toJSON(message) {
    const obj = {};
    if (message.transferId.length !== 0) {
      obj.transferId = base64FromBytes2(message.transferId);
    }
    return obj;
  },
  create(base) {
    return SatsTransfer.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSatsTransfer();
    message.transferId = object.transferId ?? new Uint8Array(0);
    return message;
  }
};
function createBaseTokenTransfer() {
  return { finalTokenTransactionHash: new Uint8Array(0) };
}
var TokenTransfer = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.finalTokenTransactionHash.length !== 0) {
      writer.uint32(10).bytes(message.finalTokenTransactionHash);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTokenTransfer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.finalTokenTransactionHash = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      finalTokenTransactionHash: isSet3(object.finalTokenTransactionHash) ? bytesFromBase642(object.finalTokenTransactionHash) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.finalTokenTransactionHash.length !== 0) {
      obj.finalTokenTransactionHash = base64FromBytes2(message.finalTokenTransactionHash);
    }
    return obj;
  },
  create(base) {
    return TokenTransfer.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseTokenTransfer();
    message.finalTokenTransactionHash = object.finalTokenTransactionHash ?? new Uint8Array(0);
    return message;
  }
};
function createBaseInitiateSwapPrimaryTransferRequest() {
  return { transfer: void 0, adaptorPublicKeys: void 0 };
}
var InitiateSwapPrimaryTransferRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.transfer !== void 0) {
      StartTransferRequest.encode(message.transfer, writer.uint32(10).fork()).join();
    }
    if (message.adaptorPublicKeys !== void 0) {
      AdaptorPublicKeyPackage.encode(message.adaptorPublicKeys, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInitiateSwapPrimaryTransferRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.transfer = StartTransferRequest.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.adaptorPublicKeys = AdaptorPublicKeyPackage.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      transfer: isSet3(object.transfer) ? StartTransferRequest.fromJSON(object.transfer) : void 0,
      adaptorPublicKeys: isSet3(object.adaptorPublicKeys) ? AdaptorPublicKeyPackage.fromJSON(object.adaptorPublicKeys) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.transfer !== void 0) {
      obj.transfer = StartTransferRequest.toJSON(message.transfer);
    }
    if (message.adaptorPublicKeys !== void 0) {
      obj.adaptorPublicKeys = AdaptorPublicKeyPackage.toJSON(message.adaptorPublicKeys);
    }
    return obj;
  },
  create(base) {
    return InitiateSwapPrimaryTransferRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseInitiateSwapPrimaryTransferRequest();
    message.transfer = object.transfer !== void 0 && object.transfer !== null ? StartTransferRequest.fromPartial(object.transfer) : void 0;
    message.adaptorPublicKeys = object.adaptorPublicKeys !== void 0 && object.adaptorPublicKeys !== null ? AdaptorPublicKeyPackage.fromPartial(object.adaptorPublicKeys) : void 0;
    return message;
  }
};
function createBaseInitiateSwapPrimaryTransferResponse() {
  return { transfer: void 0, signingResults: [] };
}
var InitiateSwapPrimaryTransferResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.transfer !== void 0) {
      Transfer.encode(message.transfer, writer.uint32(10).fork()).join();
    }
    for (const v of message.signingResults) {
      LeafRefundTxSigningResult.encode(v, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInitiateSwapPrimaryTransferResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.transfer = Transfer.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.signingResults.push(LeafRefundTxSigningResult.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      transfer: isSet3(object.transfer) ? Transfer.fromJSON(object.transfer) : void 0,
      signingResults: globalThis.Array.isArray(object?.signingResults) ? object.signingResults.map((e) => LeafRefundTxSigningResult.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.transfer !== void 0) {
      obj.transfer = Transfer.toJSON(message.transfer);
    }
    if (message.signingResults?.length) {
      obj.signingResults = message.signingResults.map((e) => LeafRefundTxSigningResult.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return InitiateSwapPrimaryTransferResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseInitiateSwapPrimaryTransferResponse();
    message.transfer = object.transfer !== void 0 && object.transfer !== null ? Transfer.fromPartial(object.transfer) : void 0;
    message.signingResults = object.signingResults?.map((e) => LeafRefundTxSigningResult.fromPartial(e)) || [];
    return message;
  }
};
function createBaseAdaptorPublicKeyPackage() {
  return {
    adaptorPublicKey: new Uint8Array(0),
    directAdaptorPublicKey: new Uint8Array(0),
    directFromCpfpAdaptorPublicKey: new Uint8Array(0)
  };
}
var AdaptorPublicKeyPackage = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.adaptorPublicKey.length !== 0) {
      writer.uint32(10).bytes(message.adaptorPublicKey);
    }
    if (message.directAdaptorPublicKey.length !== 0) {
      writer.uint32(18).bytes(message.directAdaptorPublicKey);
    }
    if (message.directFromCpfpAdaptorPublicKey.length !== 0) {
      writer.uint32(26).bytes(message.directFromCpfpAdaptorPublicKey);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseAdaptorPublicKeyPackage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.adaptorPublicKey = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.directAdaptorPublicKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.directFromCpfpAdaptorPublicKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      adaptorPublicKey: isSet3(object.adaptorPublicKey) ? bytesFromBase642(object.adaptorPublicKey) : new Uint8Array(0),
      directAdaptorPublicKey: isSet3(object.directAdaptorPublicKey) ? bytesFromBase642(object.directAdaptorPublicKey) : new Uint8Array(0),
      directFromCpfpAdaptorPublicKey: isSet3(object.directFromCpfpAdaptorPublicKey) ? bytesFromBase642(object.directFromCpfpAdaptorPublicKey) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.adaptorPublicKey.length !== 0) {
      obj.adaptorPublicKey = base64FromBytes2(message.adaptorPublicKey);
    }
    if (message.directAdaptorPublicKey.length !== 0) {
      obj.directAdaptorPublicKey = base64FromBytes2(message.directAdaptorPublicKey);
    }
    if (message.directFromCpfpAdaptorPublicKey.length !== 0) {
      obj.directFromCpfpAdaptorPublicKey = base64FromBytes2(message.directFromCpfpAdaptorPublicKey);
    }
    return obj;
  },
  create(base) {
    return AdaptorPublicKeyPackage.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseAdaptorPublicKeyPackage();
    message.adaptorPublicKey = object.adaptorPublicKey ?? new Uint8Array(0);
    message.directAdaptorPublicKey = object.directAdaptorPublicKey ?? new Uint8Array(0);
    message.directFromCpfpAdaptorPublicKey = object.directFromCpfpAdaptorPublicKey ?? new Uint8Array(0);
    return message;
  }
};
function createBaseWalletSetting() {
  return { ownerIdentityPublicKey: new Uint8Array(0), privateEnabled: false };
}
var WalletSetting = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.ownerIdentityPublicKey.length !== 0) {
      writer.uint32(10).bytes(message.ownerIdentityPublicKey);
    }
    if (message.privateEnabled !== false) {
      writer.uint32(16).bool(message.privateEnabled);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseWalletSetting();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.ownerIdentityPublicKey = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.privateEnabled = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      ownerIdentityPublicKey: isSet3(object.ownerIdentityPublicKey) ? bytesFromBase642(object.ownerIdentityPublicKey) : new Uint8Array(0),
      privateEnabled: isSet3(object.privateEnabled) ? globalThis.Boolean(object.privateEnabled) : false
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.ownerIdentityPublicKey.length !== 0) {
      obj.ownerIdentityPublicKey = base64FromBytes2(message.ownerIdentityPublicKey);
    }
    if (message.privateEnabled !== false) {
      obj.privateEnabled = message.privateEnabled;
    }
    return obj;
  },
  create(base) {
    return WalletSetting.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseWalletSetting();
    message.ownerIdentityPublicKey = object.ownerIdentityPublicKey ?? new Uint8Array(0);
    message.privateEnabled = object.privateEnabled ?? false;
    return message;
  }
};
function createBaseUpdateWalletSettingRequest() {
  return { privateEnabled: void 0 };
}
var UpdateWalletSettingRequest = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.privateEnabled !== void 0) {
      writer.uint32(8).bool(message.privateEnabled);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUpdateWalletSettingRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.privateEnabled = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { privateEnabled: isSet3(object.privateEnabled) ? globalThis.Boolean(object.privateEnabled) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.privateEnabled !== void 0) {
      obj.privateEnabled = message.privateEnabled;
    }
    return obj;
  },
  create(base) {
    return UpdateWalletSettingRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseUpdateWalletSettingRequest();
    message.privateEnabled = object.privateEnabled ?? void 0;
    return message;
  }
};
function createBaseUpdateWalletSettingResponse() {
  return { walletSetting: void 0 };
}
var UpdateWalletSettingResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.walletSetting !== void 0) {
      WalletSetting.encode(message.walletSetting, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUpdateWalletSettingResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.walletSetting = WalletSetting.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { walletSetting: isSet3(object.walletSetting) ? WalletSetting.fromJSON(object.walletSetting) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.walletSetting !== void 0) {
      obj.walletSetting = WalletSetting.toJSON(message.walletSetting);
    }
    return obj;
  },
  create(base) {
    return UpdateWalletSettingResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseUpdateWalletSettingResponse();
    message.walletSetting = object.walletSetting !== void 0 && object.walletSetting !== null ? WalletSetting.fromPartial(object.walletSetting) : void 0;
    return message;
  }
};
function createBaseQueryWalletSettingRequest() {
  return {};
}
var QueryWalletSettingRequest = {
  encode(_, writer = new BinaryWriter4()) {
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryWalletSettingRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(_) {
    return {};
  },
  toJSON(_) {
    const obj = {};
    return obj;
  },
  create(base) {
    return QueryWalletSettingRequest.fromPartial(base ?? {});
  },
  fromPartial(_) {
    const message = createBaseQueryWalletSettingRequest();
    return message;
  }
};
function createBaseQueryWalletSettingResponse() {
  return { walletSetting: void 0 };
}
var QueryWalletSettingResponse = {
  encode(message, writer = new BinaryWriter4()) {
    if (message.walletSetting !== void 0) {
      WalletSetting.encode(message.walletSetting, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader4 ? input : new BinaryReader4(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryWalletSettingResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.walletSetting = WalletSetting.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { walletSetting: isSet3(object.walletSetting) ? WalletSetting.fromJSON(object.walletSetting) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.walletSetting !== void 0) {
      obj.walletSetting = WalletSetting.toJSON(message.walletSetting);
    }
    return obj;
  },
  create(base) {
    return QueryWalletSettingResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseQueryWalletSettingResponse();
    message.walletSetting = object.walletSetting !== void 0 && object.walletSetting !== null ? WalletSetting.fromPartial(object.walletSetting) : void 0;
    return message;
  }
};
var SparkServiceDefinition = {
  name: "SparkService",
  fullName: "spark.SparkService",
  methods: {
    generate_deposit_address: {
      name: "generate_deposit_address",
      requestType: GenerateDepositAddressRequest,
      requestStream: false,
      responseType: GenerateDepositAddressResponse,
      responseStream: false,
      options: {}
    },
    /** Generates a new static deposit address of the user or returns the existing one for the specified network. */
    generate_static_deposit_address: {
      name: "generate_static_deposit_address",
      requestType: GenerateStaticDepositAddressRequest,
      requestStream: false,
      responseType: GenerateStaticDepositAddressResponse,
      responseStream: false,
      options: {}
    },
    start_deposit_tree_creation: {
      name: "start_deposit_tree_creation",
      requestType: StartDepositTreeCreationRequest,
      requestStream: false,
      responseType: StartDepositTreeCreationResponse,
      responseStream: false,
      options: {}
    },
    /**
     * This is deprecated, please use start_deposit_tree_creation instead.
     *
     * @deprecated
     */
    start_tree_creation: {
      name: "start_tree_creation",
      requestType: StartTreeCreationRequest,
      requestStream: false,
      responseType: StartTreeCreationResponse,
      responseStream: false,
      options: {}
    },
    finalize_node_signatures: {
      name: "finalize_node_signatures",
      requestType: FinalizeNodeSignaturesRequest,
      requestStream: false,
      responseType: FinalizeNodeSignaturesResponse,
      responseStream: false,
      options: {}
    },
    start_transfer: {
      name: "start_transfer",
      requestType: StartTransferRequest,
      requestStream: false,
      responseType: StartTransferResponse,
      responseStream: false,
      options: {}
    },
    /**
     * This is deprecated, please use finalize_transfer_with_transfer_package instead.
     *
     * @deprecated
     */
    finalize_transfer: {
      name: "finalize_transfer",
      requestType: FinalizeTransferRequest,
      requestStream: false,
      responseType: FinalizeTransferResponse,
      responseStream: false,
      options: {}
    },
    finalize_transfer_with_transfer_package: {
      name: "finalize_transfer_with_transfer_package",
      requestType: FinalizeTransferWithTransferPackageRequest,
      requestStream: false,
      responseType: FinalizeTransferResponse,
      responseStream: false,
      options: {}
    },
    cancel_transfer: {
      name: "cancel_transfer",
      requestType: CancelTransferRequest,
      requestStream: false,
      responseType: CancelTransferResponse,
      responseStream: false,
      options: {}
    },
    query_pending_transfers: {
      name: "query_pending_transfers",
      requestType: TransferFilter,
      requestStream: false,
      responseType: QueryTransfersResponse,
      responseStream: false,
      options: {}
    },
    query_all_transfers: {
      name: "query_all_transfers",
      requestType: TransferFilter,
      requestStream: false,
      responseType: QueryTransfersResponse,
      responseStream: false,
      options: {}
    },
    claim_transfer_tweak_keys: {
      name: "claim_transfer_tweak_keys",
      requestType: ClaimTransferTweakKeysRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {}
    },
    claim_transfer_sign_refunds: {
      name: "claim_transfer_sign_refunds",
      requestType: ClaimTransferSignRefundsRequest,
      requestStream: false,
      responseType: ClaimTransferSignRefundsResponse,
      responseStream: false,
      options: {}
    },
    store_preimage_share: {
      name: "store_preimage_share",
      requestType: StorePreimageShareRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {}
    },
    /**
     * Gets a specified number of signing commmitments for a set of nodes, which can be used as
     * part of a transfer package.
     */
    get_signing_commitments: {
      name: "get_signing_commitments",
      requestType: GetSigningCommitmentsRequest,
      requestStream: false,
      responseType: GetSigningCommitmentsResponse,
      responseStream: false,
      options: {}
    },
    cooperative_exit: {
      name: "cooperative_exit",
      requestType: CooperativeExitRequest,
      requestStream: false,
      responseType: CooperativeExitResponse,
      responseStream: false,
      options: {}
    },
    initiate_preimage_swap: {
      name: "initiate_preimage_swap",
      requestType: InitiatePreimageSwapRequest,
      requestStream: false,
      responseType: InitiatePreimageSwapResponse,
      responseStream: false,
      options: {}
    },
    provide_preimage: {
      name: "provide_preimage",
      requestType: ProvidePreimageRequest,
      requestStream: false,
      responseType: ProvidePreimageResponse,
      responseStream: false,
      options: {}
    },
    query_htlc: {
      name: "query_htlc",
      requestType: QueryHtlcRequest,
      requestStream: false,
      responseType: QueryHtlcResponse,
      responseStream: false,
      options: {}
    },
    /**
     * This is the exact same as start_transfer, but expresses to the SO
     * this transfer is specifically for a leaf swap.
     */
    start_leaf_swap: {
      name: "start_leaf_swap",
      requestType: StartTransferRequest,
      requestStream: false,
      responseType: StartTransferResponse,
      responseStream: false,
      options: {}
    },
    /**
     * This is deprecated, please use counter_leaf_swap instead.
     *
     * @deprecated
     */
    leaf_swap: {
      name: "leaf_swap",
      requestType: CounterLeafSwapRequest,
      requestStream: false,
      responseType: CounterLeafSwapResponse,
      responseStream: false,
      options: {}
    },
    /**
     * This is the exact same as start_leaf_swap, but signs with
     * an adaptor public key after a counterparty has begun the swap via start_leaf_swap.
     */
    counter_leaf_swap: {
      name: "counter_leaf_swap",
      requestType: CounterLeafSwapRequest,
      requestStream: false,
      responseType: CounterLeafSwapResponse,
      responseStream: false,
      options: {}
    },
    refresh_timelock: {
      name: "refresh_timelock",
      requestType: RefreshTimelockRequest,
      requestStream: false,
      responseType: RefreshTimelockResponse,
      responseStream: false,
      options: {}
    },
    extend_leaf: {
      name: "extend_leaf",
      requestType: ExtendLeafRequest,
      requestStream: false,
      responseType: ExtendLeafResponse,
      responseStream: false,
      options: {}
    },
    /**
     * Resets the timelocks for a leaf's transactions. Can be used to reset the
     * refund transaction timelock for a leaf (when the node transaction
     * timelock is still > 300) or reset the node and refund transaction
     * timelock. Returns an error if a leaf is not yet eligible to renew the
     * timelocks, see RenewLeafRequest for more details.
     */
    renew_leaf: {
      name: "renew_leaf",
      requestType: RenewLeafRequest,
      requestStream: false,
      responseType: RenewLeafResponse,
      responseStream: false,
      options: {}
    },
    get_signing_operator_list: {
      name: "get_signing_operator_list",
      requestType: Empty,
      requestStream: false,
      responseType: GetSigningOperatorListResponse,
      responseStream: false,
      options: {}
    },
    query_nodes: {
      name: "query_nodes",
      requestType: QueryNodesRequest,
      requestStream: false,
      responseType: QueryNodesResponse,
      responseStream: false,
      options: {}
    },
    query_nodes_distribution: {
      name: "query_nodes_distribution",
      requestType: QueryNodesDistributionRequest,
      requestStream: false,
      responseType: QueryNodesDistributionResponse,
      responseStream: false,
      options: {}
    },
    query_nodes_by_value: {
      name: "query_nodes_by_value",
      requestType: QueryNodesByValueRequest,
      requestStream: false,
      responseType: QueryNodesByValueResponse,
      responseStream: false,
      options: {}
    },
    query_balance: {
      name: "query_balance",
      requestType: QueryBalanceRequest,
      requestStream: false,
      responseType: QueryBalanceResponse,
      responseStream: false,
      options: {}
    },
    query_user_signed_refunds: {
      name: "query_user_signed_refunds",
      requestType: QueryUserSignedRefundsRequest,
      requestStream: false,
      responseType: QueryUserSignedRefundsResponse,
      responseStream: false,
      options: {}
    },
    /** Token RPCs */
    start_token_transaction: {
      name: "start_token_transaction",
      requestType: StartTokenTransactionRequest,
      requestStream: false,
      responseType: StartTokenTransactionResponse,
      responseStream: false,
      options: {}
    },
    sign_token_transaction: {
      name: "sign_token_transaction",
      requestType: SignTokenTransactionRequest,
      requestStream: false,
      responseType: SignTokenTransactionResponse,
      responseStream: false,
      options: {}
    },
    finalize_token_transaction: {
      name: "finalize_token_transaction",
      requestType: FinalizeTokenTransactionRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {}
    },
    freeze_tokens: {
      name: "freeze_tokens",
      requestType: FreezeTokensRequest,
      requestStream: false,
      responseType: FreezeTokensResponse,
      responseStream: false,
      options: {}
    },
    query_token_outputs: {
      name: "query_token_outputs",
      requestType: QueryTokenOutputsRequest,
      requestStream: false,
      responseType: QueryTokenOutputsResponse,
      responseStream: false,
      options: {}
    },
    query_token_transactions: {
      name: "query_token_transactions",
      requestType: QueryTokenTransactionsRequest,
      requestStream: false,
      responseType: QueryTokenTransactionsResponse,
      responseStream: false,
      options: {}
    },
    return_lightning_payment: {
      name: "return_lightning_payment",
      requestType: ReturnLightningPaymentRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {}
    },
    query_unused_deposit_addresses: {
      name: "query_unused_deposit_addresses",
      requestType: QueryUnusedDepositAddressesRequest,
      requestStream: false,
      responseType: QueryUnusedDepositAddressesResponse,
      responseStream: false,
      options: {}
    },
    query_static_deposit_addresses: {
      name: "query_static_deposit_addresses",
      requestType: QueryStaticDepositAddressesRequest,
      requestStream: false,
      responseType: QueryStaticDepositAddressesResponse,
      responseStream: false,
      options: {}
    },
    subscribe_to_events: {
      name: "subscribe_to_events",
      requestType: SubscribeToEventsRequest,
      requestStream: false,
      responseType: SubscribeToEventsResponse,
      responseStream: true,
      options: {}
    },
    /**
     * Signs the provided refund transaction which spends the UTXO from a static
     * deposit address. If successful, the UTXO will no longer be available to claim on
     * the Spark network, and the refund transaction must be broadcasted on L1 to claim
     * the funds. Returns an error if the UTXO has already been claimed.
     */
    initiate_static_deposit_utxo_refund: {
      name: "initiate_static_deposit_utxo_refund",
      requestType: InitiateStaticDepositUtxoRefundRequest,
      requestStream: false,
      responseType: InitiateStaticDepositUtxoRefundResponse,
      responseStream: false,
      options: {}
    },
    /**
     * DEPRECATED: This unified method is being split for better clarity and type safety
     *
     * For swap operations: Use spark_ssp_internal.initiate_static_deposit_utxo_swap()
     * For refund operations: Use initiate_static_deposit_utxo_refund()
     *
     * @deprecated
     */
    initiate_utxo_swap: {
      name: "initiate_utxo_swap",
      requestType: InitiateUtxoSwapRequest,
      requestStream: false,
      responseType: InitiateUtxoSwapResponse,
      responseStream: false,
      options: {}
    },
    exit_single_node_trees: {
      name: "exit_single_node_trees",
      requestType: ExitSingleNodeTreesRequest,
      requestStream: false,
      responseType: ExitSingleNodeTreesResponse,
      responseStream: false,
      options: {}
    },
    /**
     * The following endpoints enforce inclusion of Direct Transactions used
     * for unilateral exits
     */
    cooperative_exit_v2: {
      name: "cooperative_exit_v2",
      requestType: CooperativeExitRequest,
      requestStream: false,
      responseType: CooperativeExitResponse,
      responseStream: false,
      options: {}
    },
    extend_leaf_v2: {
      name: "extend_leaf_v2",
      requestType: ExtendLeafRequest,
      requestStream: false,
      responseType: ExtendLeafResponse,
      responseStream: false,
      options: {}
    },
    claim_transfer_sign_refunds_v2: {
      name: "claim_transfer_sign_refunds_v2",
      requestType: ClaimTransferSignRefundsRequest,
      requestStream: false,
      responseType: ClaimTransferSignRefundsResponse,
      responseStream: false,
      options: {}
    },
    finalize_node_signatures_v2: {
      name: "finalize_node_signatures_v2",
      requestType: FinalizeNodeSignaturesRequest,
      requestStream: false,
      responseType: FinalizeNodeSignaturesResponse,
      responseStream: false,
      options: {}
    },
    initiate_preimage_swap_v2: {
      name: "initiate_preimage_swap_v2",
      requestType: InitiatePreimageSwapRequest,
      requestStream: false,
      responseType: InitiatePreimageSwapResponse,
      responseStream: false,
      options: {}
    },
    initiate_preimage_swap_v3: {
      name: "initiate_preimage_swap_v3",
      requestType: InitiatePreimageSwapRequest,
      requestStream: false,
      responseType: InitiatePreimageSwapResponse,
      responseStream: false,
      options: {}
    },
    start_leaf_swap_v2: {
      name: "start_leaf_swap_v2",
      requestType: StartTransferRequest,
      requestStream: false,
      responseType: StartTransferResponse,
      responseStream: false,
      options: {}
    },
    counter_leaf_swap_v2: {
      name: "counter_leaf_swap_v2",
      requestType: CounterLeafSwapRequest,
      requestStream: false,
      responseType: CounterLeafSwapResponse,
      responseStream: false,
      options: {}
    },
    start_transfer_v2: {
      name: "start_transfer_v2",
      requestType: StartTransferRequest,
      requestStream: false,
      responseType: StartTransferResponse,
      responseStream: false,
      options: {}
    },
    refresh_timelock_v2: {
      name: "refresh_timelock_v2",
      requestType: RefreshTimelockRequest,
      requestStream: false,
      responseType: RefreshTimelockResponse,
      responseStream: false,
      options: {}
    },
    get_utxos_for_address: {
      name: "get_utxos_for_address",
      requestType: GetUtxosForAddressRequest,
      requestStream: false,
      responseType: GetUtxosForAddressResponse,
      responseStream: false,
      options: {}
    },
    query_spark_invoices: {
      name: "query_spark_invoices",
      requestType: QuerySparkInvoicesRequest,
      requestStream: false,
      responseType: QuerySparkInvoicesResponse,
      responseStream: false,
      options: {}
    },
    /**
     * Inititiates a primary transfer in a Swap V3 protocol. The sender submits the
     * transfer package, but the SOs will not tweak the keys at this stage of the flow.
     * It will be done later, when the SSP initiates a counter swap.
     */
    initiate_swap_primary_transfer: {
      name: "initiate_swap_primary_transfer",
      requestType: InitiateSwapPrimaryTransferRequest,
      requestStream: false,
      responseType: InitiateSwapPrimaryTransferResponse,
      responseStream: false,
      options: {}
    },
    update_wallet_setting: {
      name: "update_wallet_setting",
      requestType: UpdateWalletSettingRequest,
      requestStream: false,
      responseType: UpdateWalletSettingResponse,
      responseStream: false,
      options: {}
    },
    query_wallet_setting: {
      name: "query_wallet_setting",
      requestType: QueryWalletSettingRequest,
      requestStream: false,
      responseType: QueryWalletSettingResponse,
      responseStream: false,
      options: {}
    }
  }
};
function bytesFromBase642(b64) {
  if (globalThis.Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes2(arr) {
  if (globalThis.Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}
function toTimestamp(date) {
  const seconds = Math.trunc(date.getTime() / 1e3);
  const nanos = date.getTime() % 1e3 * 1e6;
  return { seconds, nanos };
}
function fromTimestamp(t) {
  let millis = (t.seconds || 0) * 1e3;
  millis += (t.nanos || 0) / 1e6;
  return new globalThis.Date(millis);
}
function fromJsonTimestamp(o) {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}
function longToNumber2(int64) {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}
function isObject2(value) {
  return typeof value === "object" && value !== null;
}
function isSet3(value) {
  return value !== null && value !== void 0;
}

export {
  common_exports,
  Empty,
  Timestamp,
  protobufPackage2 as protobufPackage,
  Network,
  networkFromJSON,
  networkToJSON,
  Direction,
  directionFromJSON,
  directionToJSON,
  TokenTransactionStatus,
  tokenTransactionStatusFromJSON,
  tokenTransactionStatusToJSON,
  TransferStatus,
  transferStatusFromJSON,
  transferStatusToJSON,
  TransferType,
  transferTypeFromJSON,
  transferTypeToJSON,
  Order,
  orderFromJSON,
  orderToJSON,
  PreimageRequestStatus,
  preimageRequestStatusFromJSON,
  preimageRequestStatusToJSON,
  UtxoSwapRequestType,
  utxoSwapRequestTypeFromJSON,
  utxoSwapRequestTypeToJSON,
  InvoiceStatus,
  invoiceStatusFromJSON,
  invoiceStatusToJSON,
  TreeNodeStatus,
  treeNodeStatusFromJSON,
  treeNodeStatusToJSON,
  InitiatePreimageSwapRequest_Reason,
  initiatePreimageSwapRequest_ReasonFromJSON,
  initiatePreimageSwapRequest_ReasonToJSON,
  SubscribeToEventsRequest,
  SubscribeToEventsResponse,
  ConnectedEvent,
  TransferEvent,
  DepositEvent,
  PageRequest,
  PageResponse,
  DepositAddressProof,
  DepositAddressProof_AddressSignaturesEntry,
  GenerateDepositAddressRequest,
  Address,
  GenerateDepositAddressResponse,
  GenerateStaticDepositAddressRequest,
  GenerateStaticDepositAddressResponse,
  UTXO,
  NodeOutput,
  SigningJob,
  SigningKeyshare,
  SigningKeyshare_PublicSharesEntry,
  SigningResult2 as SigningResult,
  SigningResult_PublicKeysEntry,
  SigningResult_SigningNonceCommitmentsEntry,
  SigningResult_SignatureSharesEntry,
  RenewLeafRequest,
  RenewNodeTimelockSigningJob,
  RenewRefundTimelockSigningJob,
  RenewNodeZeroTimelockSigningJob,
  RenewLeafResponse,
  RenewNodeTimelockResult,
  RenewRefundTimelockResult,
  RenewNodeZeroTimelockResult,
  NodeSignatureShares,
  NodeSignatures,
  StartTreeCreationRequest,
  StartTreeCreationResponse,
  StartDepositTreeCreationRequest,
  StartDepositTreeCreationResponse,
  TokenOutputToSpend,
  TokenTransferInput,
  TokenMintInput,
  TokenCreateInput,
  TokenOutput,
  TokenTransaction,
  SpentTokenOutputMetadata,
  TokenTransactionConfirmationMetadata,
  TokenTransactionWithStatus,
  SignatureWithIndex,
  TokenTransactionSignatures,
  StartTokenTransactionRequest,
  StartTokenTransactionResponse,
  OperatorSpecificTokenTransactionSignablePayload,
  OperatorSpecificOwnerSignature,
  SignTokenTransactionRequest,
  KeyshareWithIndex,
  SignTokenTransactionResponse,
  RevocationSecretWithIndex,
  FinalizeTokenTransactionRequest,
  FreezeTokensPayload,
  FreezeTokensRequest,
  FreezeTokensResponse,
  QueryTokenOutputsRequest,
  QueryTokenTransactionsRequest,
  QueryTokenTransactionsResponse,
  OutputWithPreviousTransactionData,
  QueryTokenOutputsResponse,
  TreeNode,
  FinalizeNodeSignaturesRequest,
  FinalizeNodeSignaturesResponse,
  SecretShare,
  SecretProof,
  LeafRefundTxSigningJob,
  UserSignedTxSigningJob,
  LeafRefundTxSigningResult,
  StartUserSignedTransferRequest,
  StartTransferRequest,
  StartTransferResponse,
  TransferPackage,
  TransferPackage_KeyTweakPackageEntry,
  SendLeafKeyTweaks,
  SendLeafKeyTweak,
  SendLeafKeyTweak_PubkeySharesTweakEntry,
  FinalizeTransferRequest,
  FinalizeTransferWithTransferPackageRequest,
  FinalizeTransferResponse,
  Transfer,
  TransferLeaf,
  TransferFilter,
  QueryTransfersResponse,
  ClaimLeafKeyTweak,
  ClaimLeafKeyTweak_PubkeySharesTweakEntry,
  ClaimTransferTweakKeysRequest,
  ClaimTransferSignRefundsRequest,
  ClaimTransferSignRefundsResponse,
  StorePreimageShareRequest,
  RequestedSigningCommitments,
  RequestedSigningCommitments_SigningNonceCommitmentsEntry,
  GetSigningCommitmentsRequest,
  GetSigningCommitmentsResponse,
  SigningCommitments,
  SigningCommitments_SigningCommitmentsEntry,
  UserSignedRefund,
  InvoiceAmountProof,
  InvoiceAmount,
  InitiatePreimageSwapRequest,
  InitiatePreimageSwapResponse,
  OutPoint,
  CooperativeExitRequest,
  CooperativeExitResponse,
  CounterLeafSwapRequest,
  CounterLeafSwapResponse,
  RefreshTimelockRequest,
  RefreshTimelockSigningResult,
  RefreshTimelockResponse,
  ExtendLeafRequest,
  ExtendLeafSigningResult,
  ExtendLeafResponse,
  AddressRequestNode,
  PrepareTreeAddressRequest,
  AddressNode,
  PrepareTreeAddressResponse,
  CreationNode,
  CreateTreeRequest,
  CreationResponseNode,
  CreateTreeResponse,
  SigningOperatorInfo,
  GetSigningOperatorListResponse,
  GetSigningOperatorListResponse_SigningOperatorsEntry,
  QueryUserSignedRefundsRequest,
  QueryUserSignedRefundsResponse,
  PreimageRequestWithTransfer,
  QueryHtlcRequest,
  QueryHtlcResponse,
  ProvidePreimageRequest,
  ProvidePreimageResponse,
  ReturnLightningPaymentRequest,
  TreeNodeIds,
  QueryNodesRequest,
  QueryNodesResponse,
  QueryNodesResponse_NodesEntry,
  CancelTransferRequest,
  CancelTransferResponse,
  QueryUnusedDepositAddressesRequest,
  QueryStaticDepositAddressesRequest,
  DepositAddressQueryResult,
  QueryUnusedDepositAddressesResponse,
  QueryStaticDepositAddressesResponse,
  QueryBalanceRequest,
  QueryBalanceResponse,
  QueryBalanceResponse_NodeBalancesEntry,
  SparkAddress,
  SparkInvoiceFields,
  SatsPayment,
  TokensPayment,
  InitiateStaticDepositUtxoRefundRequest,
  InitiateStaticDepositUtxoRefundResponse,
  InitiateUtxoSwapRequest,
  InitiateUtxoSwapResponse,
  ExitingTree,
  ExitSingleNodeTreeSigningResult,
  BitcoinTransactionOutput,
  ExitSingleNodeTreesRequest,
  ExitSingleNodeTreesResponse,
  QueryNodesDistributionRequest,
  QueryNodesDistributionResponse,
  QueryNodesDistributionResponse_NodeDistributionEntry,
  QueryNodesByValueRequest,
  QueryNodesByValueResponse,
  QueryNodesByValueResponse_NodesEntry,
  GetUtxosForAddressRequest,
  GetUtxosForAddressResponse,
  QuerySparkInvoicesRequest,
  QuerySparkInvoicesResponse,
  InvoiceResponse,
  SatsTransfer,
  TokenTransfer,
  InitiateSwapPrimaryTransferRequest,
  InitiateSwapPrimaryTransferResponse,
  AdaptorPublicKeyPackage,
  WalletSetting,
  UpdateWalletSettingRequest,
  UpdateWalletSettingResponse,
  QueryWalletSettingRequest,
  QueryWalletSettingResponse,
  SparkServiceDefinition,
  spark_exports
};
